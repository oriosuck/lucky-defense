# 인수인계 메모 (Claude용)

새 세션에서 이 프로젝트를 이어받았다면 이 문서부터 읽을 것. 실제 사양은
`길드레이드_연습게임_기획서_v4.md`/`길드레이드_연습게임_기술설계서_v3.md`가 최신 원본이다
(파일명에 버전이 없는 것/그보다 낮은 버전은 이전 버전이니 내용이 충돌하면 번호가 가장 큰
쪽을 따를 것). **주의**: 사용자가 같은 파일명(`_v4.md` 등)을 삭제 후 다시 업로드하는 방식으로
내용만 갱신하는 경우가 있었다(버전 번호는 그대로인데 내용이 달라짐) — 세션 시작 시 파일명만
보고 "이미 반영했다"고 판단하지 말고, 실제 내용을 이전에 반영한 버전과 diff해서 확인할 것.
여기는 "코드만 봐서는 안 보이는 것들" — 삽질 이력, 자산 매핑, 이 저장소 특유의
워크플로우 함정 — 을 정리한 것이다.

## 프로젝트 개요

서버/DB/로그인 없는 정적 SPA (Vite + Vanilla JS). 배포는 Vercel, `main` 브랜치를
추적해서 merge될 때마다 자동 재배포된다. 사용자는 개발자가 아니고, UI를
실제 게임 스크린샷/직접 만든 에셋과 비교하며 매우 꼼꼼하게 피드백을 준다.

## 이미지 없이 진행한 구조 리팩토링 (기획서_v4/기술설계서_v2 반영)

이 리팩토링에서 이미지 렌더링을 전부 걷어내고 등급별 단색 블록 + 이름 텍스트로
임시 표시하도록 바꿨다(`src/ui/components/heroPlaceholder.js`의 `heroPlaceholder()`/
`placeholderBlock()`). **`src/data/heroes.js`의 `image` 필드, `src/data/assets.js`의
모든 매핑(STAGE_LAYOUT 포함)은 삭제하지 않고 그대로 남겨뒀다** — 나중에 이미지를 다시
붙일 때는 `heroPlaceholder`/`placeholderBlock` 호출부만 `<img>`로 교체하면 된다.
`GameScreen.js`의 `sizeStageToFit()`(688:1508 비율 유지)은 배경 이미지가 없어도 구조를
그대로 살려뒀으니 이것도 되돌리지 말 것.

같은 리팩토링에서 신/구 기획서 차이를 반영해 로직도 같이 정리했다:
- **유물 시스템 제거**: 모든 유물이 항상 맥스(11레벨) 고정이라는 확정 사항에 따라
  `src/data/relics.js`(레벨별 가변 함수)를 삭제하고 `src/data/constants.js`(고정 상수)로
  대체했다. 영웅 선택 화면의 유물 레벨 select UI도 제거.
- **영웅 "보유" 개념 제거**: 모든 영웅이 기본 보유 상태라는 확정 사항에 따라
  `HeroSelectScreen`의 자물쇠/보유 토글을 없앴다. `ownedHeroes` → `heroSettings`로
  이름을 바꿨고(신화 등급의 즐겨찾기/불멸 체크만 저장), `GameState.heroSettings`도 동일하게
  개명했다.
- **이동불능 이벤트 재작성**: 예전 코드는 2/4/5/7/9라운드에 고정으로 발동했는데, 확정된
  기획서는 "게임당 정확히 2회(1~9라운드 중 1회 + 11~19라운드 중 1회), 즉시형/게이지형 랜덤"이다.
  `createGameState()`가 게임 시작 시 두 라운드를 미리 뽑아 `eventLog.immobilizeRounds`에
  저장해두고, `waveEvents.js`의 `handleImmobilizeEvent`가 이를 소비한다.
- **삭제 공격 버그 수정**: 예전 코드는 `gameType`과 무관하게 13/20라운드마다 무조건
  삭제 이벤트를 발동시켰다(삭제 없는 버전에서도 발동하던 버그). `onWaveStart()`에서
  `gameType === 'delete'`일 때만 이벤트를 생성하도록 고쳤다.
- **보스 레이드 창 신규 구현**: 10/20라운드에서 몬스터가 0마리가 된 시점부터 지연시간
  (기본 4초 + 마마/베인/로카 중 필드에 없는 영웅 1명당 +5초)을 세는
  `tickBossRaidWindow()`를 추가했다. 보스 자리 아래에 "레이드 창 열림!"/"몬스터 소탕 대기 중"
  배지로 표시.
- **몬스터 등장 방식 변경**: 초당 트리클 증가 방식에서 "매 라운드 시작 시 40마리 일괄 등장"
  (`MONSTER_PER_ROUND`)으로 바꿨다. 처치(초당 2마리, 필드에 영웅 1마리 이상일 때)는
  여전히 확정 수치가 없는 플레이스홀더라 그대로 유지.
- **라운드 종료 보상 공식 정리**: `applyRoundEndReward()`(기술설계서 4장 의사코드 그대로)로
  0→1라운드는 `FIRST_WAVE_SUBSIDY`(2284) 고정, 그 외에는 `보유골드×10% + 6010` +
  행운석 고정 +5.
- **신화 버튼 배지 버그 수정**: "현재 소환 가능한 영웅 개수"라는 기획 문구와 달리 예전
  코드는 `ownedHeroes`에 등록된 신화 개수를 표시했다. `synthesis.js`의
  `craftableMythicCount()`(재료가 필드에 다 모인 신화 수를 실시간 계산)로 교체했다.

이 다섯 가지 로직 변경은 이미지와 무관하게 신/구 기획서 차이 자체를 반영한 것이니,
이미지를 다시 붙이는 작업과 섞어서 되돌리지 않도록 주의할 것.

## 기획서/기술설계서 2차 갱신분 반영 (기획서_v4 재업로드 / 기술설계서_v3)

바로 위 리팩토링 직후, 사용자가 `길드레이드_연습게임_기획서_v4.md`를 삭제 후 내용이 달라진
버전으로 재업로드하고 `길드레이드_연습게임_기술설계서_v3.md`를 새로 추가했다. 반영한 차이:

- **일반 소환 확률 고정값 변경**: 일반50/희귀30/영웅15/전설5%(강화 시 전설 +1%p씩 가변) →
  **일반53.57/희귀33.24/영웅10.98/전설2.2%, 항상 고정**(강화 버튼의 확률 업그레이드는 연출만
  있고 실제 수치엔 반영 안 됨)으로 확정. `heroes.js`의 `NORMAL_SUMMON_RATES` 갱신, `summon.js`의
  `summonRateBonusPct` 가변 로직 제거하고 `rollNormalTier()`로 통합(인디 보물 발굴과 공유).
- **그랜드 마마 조건 재정의**: "임프 9(돌파 시 7)마리 소모" → **"9(7)마리가 동시에 필드에 존재"
  하면 승급 가능, 승급 시 필드의 임프는 전부 자동 소멸(수동 소모 없음)**으로 변경.
  `promotionHandlers.m_mama`에서 `impStock -= cost` 제거(존재 확인만 하고, 승급은 어차피
  `promoteInstance`가 개체를 통째로 새로 만들어서 impStock이 자연히 리셋됨).
- **오크 지도자 불멸 조건 신규 추가**: `m_orc_shaman`(오크주술사)에 불멸 조건이 원래 없었는데
  "저주 스킬 100회 발동, 7초마다 자동 1회"가 새로 확정되어 `IMMORTAL_CONDITIONS`에 추가
  (제네릭 시간 기반 엔진으로 그대로 처리됨, 별도 핸들러 불필요).
- **사신개구리변신(2차 변신) 신규 구현**: 사신 개구리(`i_death_frog`, 이미 불멸 등급)가 수동
  버튼으로 한 번 더 승급을 시도해 50% 확률로 "사신개구리변신"이 되거나(실패 시 원본도 함께
  소멸) 하는 새 메커니즘. 기존 "신화→불멸" 제네릭 엔진(IMMORTAL_CONDITIONS 기반)과는 다른
  "불멸→불멸" 구조라 `heroes.js`에 `SECOND_STAGE_IMMORTAL` 테이블을 별도로 두고
  `immortal.js`의 `attemptSecondStageEvolution()`으로 처리. 나중에 이런 2차 변신 영웅이
  더 추가되면 이 테이블에만 항목을 더하면 됨(코드 변경 불필요, 제네릭하게 작성해둠).
- **인디 "보물 발굴" 신규 구현(5-4)**: 30초마다 필드 임의의 칸에 보물이 등장
  (`state.indyTreasure`, `waveEvents.js`의 `tickIndyTreasure`), 인디를 그 칸으로 이동시키면
  선택 패널에 "발굴" 버튼이 활성화되고(`actions.js`의 `digTreasure`), 성공 시 일반 소환과
  동일한 확률표로 등급이 정해지며 기존 보유 보물보다 낮으면 교체되지 않는다(인디 개체당
  `indyTreasureTier` 1개만 보유).
- **보스 일반공격(디버프) 스케줄 신규 구현**: 이전 세션에서 누락했던 기능. 1~2 라운드 간격
  주사위로 반복 발동, 실질 효과 없이 대상 영웅 1명을 3초간 보라색으로만 표시
  (`.hero-token.debuffed`). **이동불능과는 완전히 별개의 독립 스케줄**로 구현했음 — 기술설계서
  v3의 `resolveBossAttackForRound` 의사코드는 이동불능이 이 디버프 스케줄에 종속되는 것처럼
  중첩해서 쓰여 있지만, 기획서 원문이 "이동불능(일반공격 스케줄과 별개, 게임당 정확히 2회
  강제 발생)"이라고 명시하므로 의사코드보다 기획서 프로즈를 우선했다. 혼동하지 말 것.
- **GameState 구조 정리**: `bossRaidWindow`, `bossAttackSchedule`(다음 디버프 발동 라운드 +
  이동불능 예정 라운드 2개)를 `eventLog` 밖 최상위 필드로 뺐다(기술설계서 v3의 데이터 모델
  그대로). `eventLog`에는 진행 중인 이벤트 상태(delete/immobilize/debuff)만 남음.

## ⚠️ 가장 중요한 워크플로우 함정: PR이 생각보다 빨리 merge된다

이 세션 동안 **세 번**, 커밋을 브랜치(`claude/game-system-architecture-kawhq6`)에
푸시한 뒤 다음 응답을 준비하는 사이에 사용자가 이미 그 PR을 merge해버린 상태를
발견했다. 이 브랜치에 그대로 새 커밋을 얹어 푸시하면 **어떤 열린 PR에도 속하지
않는 유령 커밋**이 되어 사용자 화면에 절대 반영되지 않는다("반영이 안 되어있어"
라는 피드백의 원인이었음).

**작업을 시작하기 전에 항상:**
```bash
git fetch origin main
git merge-base --is-ancestor origin/main HEAD && echo ok || echo "브랜치 새로 파야 함"
```
`ok`가 아니면:
```bash
git stash push -u   # 진행 중이던 변경사항이 있으면
git checkout -B claude/game-system-architecture-kawhq6 origin/main
git stash pop
```
그 다음 새로 커밋 → 푸시 → **새 PR**을 연다(머지된 PR은 후속 커밋을 추적하지
않으므로 PR 번호가 계속 올라간다 — 현재 #6까지 merge됨). 브랜치 이름 자체는
계속 재사용한다(작업 지시서에 고정된 이름).

## 이미지 자산 매핑 2차분 (게임화면_목업.html 기준 재구축, 최신)

이미지 없이 진행한 리팩토링(위 섹션) 이후, 사용자가 **목업 HTML**(레이아웃 참고용) +
**전체 이미지 zip**(영웅 89장 + UI 24장 + 배경)을 새로 줘서 실제 이미지를 전부
연결했다. **아래 내용이 이 프로젝트의 이미지 매핑에 대한 최신 원본이다** — 예전
zip(`zap.webp`/`birdraw.webp` 같은 영문 코드명, `public/heroes/extra/` 여분 파일 등)
관련 기록은 이번에 완전히 새 zip으로 교체되면서 전부 무효가 됐으니 참고하지 말 것
(`public/heroes/extra/`도 이번에 삭제함 - 미야옹/배트맨/로켓츄 변신 이미지였다는 게
이번에 밝혀져서 새 zip의 정식 파일로 교체됨).

### 영웅 초상화 (`public/heroes/*.webp|png`, 총 89장)

새 zip은 파일명이 **한글 영웅명 그대로**라(`영웅 이미지/1.일반/궁수.webp` 식으로
등급별 폴더 안에 있음) 매핑이 훨씬 쉬웠다. `heroes.js`에서 `HEROES` 배열을 덤프해서
한글 이름으로 자동 매칭하는 스크립트로 74종은 기계적으로 처리하고, 나머지는 수동
매핑했다:
- `독수리.webp` → `h_eagle_general`(독수리장군, zip 파일명이 축약형이었음)
- `타르v1/v2/v3.webp` → `m_tar`(기본상태=v1) + `TAR_STAGE_IMAGES`(1~3단계)
- `사신개구리변신전/변신후.webp` → `i_death_frog`(전) / `i_death_frog_evolved`(후,
  신규 2차 변신 - 아래 5-3 갱신분 참고)
- `에이스배트맨변신전/변신투수모드/변신타자모드.webp` → `i_ace_batman`(기본) +
  `i_ace_batman_pitcher`/`i_ace_batman_batter`(신규, `ACE_BATMAN_TRANSFORM_IMAGES`)
- `아이언미야옹변신1/2.webp` → `m_iron_meyaong_transform1`/`transform2`(신규,
  `IRON_MEYAONG_TRANSFORM_IMAGES`)
- `로켓츄변신.webp` → `m_rocketchu_transform`(신규, `ROCKETCHU_TRANSFORM_IMAGE`)
- `개구리 왕자.webp`/`개구리왕자변신.webp` → `m_frog_prince` / `FROG_TRANSFORM_IMAGES`
- `드레인.webp` → `DRAGON_DRAIN_IMAGE`(드래곤 승급 준비 시 필드 토큰용)

**주의**: `FROG_TRANSFORM_IMAGES`에서 `i_death_frog` 항목은 이번에 뺐다 - 예전엔
사신개구리의 "변신 모습" 보조 썸네일이었는데, 이제 `i_death_frog_evolved`가 진짜
불멸 개체(2차 변신)로 승격됐으니 그 역할이 필요 없어짐(`heroes.js`의
`SECOND_STAGE_IMMORTAL` 참고).

**여전히 못 채운 것**: 기획서 5-4의 "나아무"(초나의 소환 유닛) 이미지는 이번 zip에도
없었다. 텍스트("나아무 N회 소환" 진행도)로만 표시 중 - 나중에 파일 받으면 연결.

### UI 버튼/아이콘 (`public/ui/*.png`, `src/data/assets.js`의 `UI_IMAGES`)

목업 HTML에서 숫자 파일명(`2.png`~`25.png`)이라 용도를 육안으로 하나씩 확인해야
했는데, **이 1차 매핑에서 몬스터/임프를 서로 바꿔 넣는 실수가 있었다** (아래
"버그 리포트 라운드" 섹션에서 사용자가 정정한 표로 수정함 - 최종 정답은
`2`=임프, `4`=몬스터다. 이 문단은 처음 작업할 때의 판단 과정 기록이라 그대로
남겨두지만, 숫자 자체를 다시 참고할 땐 아래 정정 섹션을 우선할 것):
`3`=이동불능 아이콘(사슬), `5`=행운석, `6`=골드,
`7/8/9/10`=강화 팝업 4종(소환확률/전설~불멸/영웅/일반~희귀 - 이것도 아래
정정 섹션에서 실제 순서가 확정됐다), `13`=해골(룰렛 실패 전용, 몬스터 카운트
표시에는 쓰지 않음 - `11`번은 몬스터 카운트 바가 아니라 룰렛 실패 표시용
이미지였고 현재 미사용), `14`=상단 라운드/시간 배지 배경, `15/16`=배속
꺼짐/켜짐, `17`=재화 바(골드·행운석·인원 아이콘이 이미 그림 안에 그려져
있어서 숫자만 텍스트로 얹으면 됨), `18/19/20/21`=강화/소환/룰렛/신화
버튼(텍스트가 그림에 이미 포함돼 있어서 별도 라벨 오버레이 불필요),
`22/23/24`=전설/희귀/영웅 룰렛(원형 배지, 가격 칸 포함, 예전 zip과 **비율이 정확히
일치**해서 같은 원화 재사용으로 확인됨), `25`=보스(예전엔 `boss.png`가 없어서
플레이스홀더였는데 이번에 정식 파일이 생김). `12.png`(커튼 프레임, 예전
`roulette_popup_bg.webp`와 비율 일치)는 새 목업이 룰렛/강화 팝업을 플랫 배경 하단
시트로 바꾸면서 안 쓰게 됐다 - `UI_IMAGES.popupTopBarBg`에 매핑만 해두고 미사용.

**함정 재발**: `2.png`(몬스터)와 `3.png`(이동불능 아이콘) 두 장이 이번에도
배경이 완전 불투명 흰색이었다(RGB 모드, 알파 채널 자체가 없었음). 예전과 똑같이
**네 모서리에서 BFS flood-fill로 흰 배경만 제거**해서 고쳤다. 그림 안쪽 흰색
디테일(몬스터 눈동자, 사슬 하이라이트)은 안 건드림 - 이 프로젝트에서 흰 배경
이미지가 섞여 오는 건 일회성이 아니라 반복되는 패턴이니, 새 이미지 받을 때마다
`Image.open(f).convert('RGBA').getpixel((0,0))`으로 모서리 알파부터 확인할 것.

**강화 팝업 4칸의 실제 데이터 매핑 (구버전, 폐기됨)**: 처음엔 common만 실제
`enhanceHero`에 연결하고 rate는 "MAX 고정" 비활성 카드, epic/legend는 "준비 중"
잠금 카드로 지어냈었다. **사용자가 실제 게임 스크린샷을 첨부하며 명시적으로
거부했다**("네 마음대로 쳐적지 말고") - 4칸 전부 실제 레벨/비용이 있는 진짜
버튼이었다. 최종 구현은 아래 "버그 리포트 라운드" 섹션 참고
(`GameState.globalEnhance` 4트랙 전역 시스템).

### 배경 (`public/bg/background.jpg`, 688×1508)

새 zip의 배경도 크기가 688×1508로 예전과 동일 - `STAGE_LAYOUT` 좌표를 그대로 재사용
가능했다(재실측 불필요).

`src/data/assets.js`의 `STAGE_LAYOUT`이 실측 좌표(%). 핵심 교훈: 원본에
검은 테두리로 그려져 있던 사각형은 **보스 자리가 아니라 전장(4x6칸) 자리**였다
(6x4 칸 비율과 정확히 일치해서 뒤늦게 알아챔). 보스는 그 위 빈 하늘 공간에
배치. 좌우 굴(몬스터 스폰 위치)은 전장 박스 바로 위 모서리에 있어서, 몬스터
이동 애니메이션은 전장 박스 아래로 돌아나가는 ㄷ자 경로로 되어 있음
(`@keyframes monster-travel`, `main.css`).

## 버그 리포트 라운드 (PR #11 배포 후, 8개 문제 정정)

PR #11이 merge/배포된 뒤 사용자가 실제 라이브 화면 스크린샷과 실제 게임의 강화
팝업 스크린샷을 첨부해 8개 문제를 지적했다. 이 세션에서 8개 전부 수정하고
Playwright로 라이브 인터랙션까지 검증했다(정적 스크린샷만으로는 룰렛 스핀/실패
애니메이션처럼 타이밍에 의존하는 버그를 못 잡으므로, 실제로 룰렛을 여러 번 눌러서
`.spinning` 클래스와 `.roulette-fail-skull`이 뜨고 사라지는 것까지 확인함).

1. **몬스터/임프 이미지가 서로 바뀌어 있었음**: 위 "이미지 자산 매핑 2차분"
   섹션에서 1차로 `2`=몬스터/`4`=임프로 잘못 매핑했었다. 사용자가 준 정정표 기준
   **`4`=몬스터, `2`=임프**가 맞다. `assets.js`의 `UI_IMAGES.monsterIcon`/`impIcon`과
   `public/ui/monster.png`/`imp.png` 파일 내용을 서로 바꿔서 고침.
2. **이미지 비율/위치가 전체적으로 깨져 있었음**: 원인은 UI 소스 PNG들이
   1000×1000 캔버스에 그림이 한쪽으로 치우쳐 그려진 상태로 왔는데, CSS가
   `background-size:100% 100%`로 캔버스 전체를 박스에 늘려 넣어서 여백까지
   같이 늘어나 그림이 찌그러져 보인 것이었다. **모든 UI 이미지를 알파 채널
   bbox(+6px 여백)로 크롭**해서 재저장하고, `.top-badge`/`.resource-bar`의
   `aspect-ratio`를 크롭된 실제 치수에 맞게 갱신해서 고침. 새 이미지를 받을
   때마다 이 크롭 작업을 빼먹지 말 것 - 이 프로젝트에서 반복되는 함정이다.
3. **룰렛 배경이 몬스터 카운트 바 배경으로 잘못 쓰이고 있었음**: 1차 매핑
   때 `11.png`(해골+메달 이미지, 사실은 "룰렛 실패 표시 전용" 자산으로
   확인됨)를 `.monster-row`의 배경 이미지로 잘못 썼었다. 몬스터 카운트 바는
   배경 이미지 자체를 안 쓰는 게 맞아서(대응하는 전용 자산이 없음) 순수 CSS
   패널로 바꿨다. `11.png`는 현재 미사용.
4. **해골 아이콘이 룰렛 실패 외 다른 곳에도 쓰이고 있었음**: 미션(☰) 버튼이
   `skullIcon`을 아이콘으로 쓰고 있었다 - 텍스트 버튼("☰")으로 교체. 해골은
   `renderRoulettePopup`의 `ui.rouletteFailTier` 조건부 렌더링에서만 나오게 함.
5. **룰렛 스핀 모션 신규 구현**: `ui.spinningTier` 상태 + `.roulette-wheel-btn.spinning`
   CSS 클래스(`@keyframes roulette-spin`, 900ms 회전) + `setTimeout` 900ms 뒤에
   `getState()`로 **최신 상태를 다시 읽어서** `summonRoulette` 판정(클로저에 갇힌
   stale state로 판정하면 스핀 도는 동안 다른 조작을 했을 때 어긋남).
6. **신화 팝업 클릭이 잘 안 먹힘 / 전체적으로 더블클릭해야 눌리는 문제(공통 원인)**:
   게임 루프가 ~0.2초마다 `root.innerHTML=''`로 전체 DOM을 갈아엎는데, 그 타이밍이
   사용자의 mousedown~mouseup 사이에 걸리면 누르고 있던 버튼 노드 자체가
   통째로 교체돼서 클릭(mouseup)이 새 노드가 아닌 사라진 노드에서 발생해 씹혔다.
   `GameScreen.js`에 `pointerdown`~`pointerup` 사이엔 주기적 `update()`를 건너뛰는
   가드를 추가해서 해결(클릭 핸들러가 직접 부르는 `render()`는 영향 없음 - `pointerup`
   이후에만 실행되므로). **이 가드를 지우면 클릭 불량 버그가 재발한다.**
7. **강화 팝업이 실제 게임과 다르게 표시됨**: 위 "강화 팝업 4칸의 실제 데이터 매핑
   (구버전, 폐기됨)" 참고 - `common`만 진짜고 나머지 3칸은 지어낸 잠금/비활성
   카드였던 게 문제였다. `GameState.globalEnhance: {common, hero, legendary, rate}`
   (계정 전체에 적용되는, 선택 영웅과 무관한 별도 4트랙 시스템)을 신규 추가하고
   `actions.js`의 `upgradeGlobalEnhance()`로 처리. 비용은 `constants.js`의
   `GLOBAL_ENHANCE_COST`(common 🪙30, hero 🪙50, legendary 💧2, rate 🪙100 - 사용자가
   보낸 실제 스크린샷 수치 그대로). 레벨을 올려도 실질 게임 효과는 없다(소환 확률은
   기획서상 항상 고정) - 이건 의도된 설계이지 버그 아님, 실제 게임도 그렇게
   보임(연출용 진행 시스템).
8. **선택된 영웅 개체 강화 버튼(`enhanceHero`)이 화면에서 사라져 있었음**: 목업
   기준 재구축 때 누락됐던 것을 `renderSelectedPanel`에 다시 연결함(전역
   강화(7번)와는 별개의, 필드 개체별 `instance.enhanceLevel` 강화 - 불멸 승급
   조건 등에 쓰임).

## 몬스터 카운트 바 이미지 재정정 + 클릭 체감 속도 문제 (PR #12 배포 후)

PR #12 배포 후 사용자가 "몬스터 카운트 바 배경이 또 잘못됐다"며 실제 배경 이미지를
스크린샷으로 보내줬고("카운트 이미지는 이거야"), "클릭이 너무 느리다"고도 지적했다.

**몬스터 카운트 바 배경 재정정**: "버그 리포트 라운드"에서 `.monster-row`를 배경
이미지 없는 순수 CSS 패널로 바꿨었는데, 이게 또 틀렸다. 원인을 제대로 찾아보니:
`d0aea9d7-*.html`(사용자가 준 "게임화면_목업.html")을 다시 열어보면 `.monster-count`
요소는 **이미지가 전혀 아니고 순수 CSS 배경(`rgba(...)`) + 이모지(💀) + 텍스트
placeholder**였다 - 이 HTML에는 실제 이미지가 보스 하나(base64 인라인) 말고는 아예
없다. 즉 "몬스터 카운트 자리에 어떤 이미지가 들어가야 하는가"는 이 HTML만 봐서는
알 수 없고, **숫자 파일명 zip의 각 이미지를 직접 열어서 모양으로 대조하는 것 외엔
확인할 방법이 없다.** 사용자가 보낸 정정 스크린샷(위쪽 중앙에 탭이 튀어나온 명패
모양, 스켈레톤 없음)을 `mockup2/assets/ui 이미지/`의 숫자 파일들과 대조해보니
**12번**이 정확히 일치했다 - 이전엔 12번을 "구 룰렛 팝업 커튼 프레임(미사용)"으로
잘못 짐작했었다. `assets.js`에서 `popupTopBarBg` → `monsterCountBg`로 개명하고
`public/ui/monster_count_bg.png`(알파 bbox 크롭, 실제 알파 채널 있어서 흰배경
flood-fill 불필요했음)로 교체, `.monster-row`에 `aspect-ratio: 972/398`로 배경
이미지를 다시 적용했다. **교훈**: 목업 HTML의 각 자리는 레이아웃(위치/크기)만
신뢰할 수 있는 참고 자료고, 실제 아트 자산이 어떤 파일인지는 매번 숫자 파일을 직접
열어서 모양을 눈으로 대조해야 한다 - HTML의 class 이름이나 구조(이모지가 스켈레톤
모양이라고 해서 스켈레톤 있는 이미지를 찾는 식)로 추측하면 이번처럼 틀린다.

**"클릭이 느리다" 진단**: `src/logic/gameLoop.js`의 `tick()`이 내부적으로
`structuredClone(state)`를 9번 연쇄 호출하는데(각 단계가 불변 업데이트 패턴), 이걸
`main.js`의 게임 루프가 **매 requestAnimationFrame마다**(초당 최대 60회, 즉
클론 540회/초) 실행하고 있었다 - 정작 화면은 `RENDER_INTERVAL_SEC`(0.2초)마다만
갱신되니 그 사이의 tick 결과 대부분은 렌더링되지도 않고 버려지는 낭비였다. 필드에
영웅이 많이 쌓일수록 state가 깊어져 클론 비용도 커지므로, 게임을 오래 할수록
체감 랙이 심해지는 것과도 부합한다. **수정**: `tick()` 호출 자체를 렌더링과 같은
`RENDER_INTERVAL_SEC` 주기로 묶어서(누적된 deltaSec를 한 번에 넘김) 클론 횟수를
45회/초로 12배 줄였다 - 시뮬레이션 결과 자체는 deltaSec 누적이라 동일하다.

**주의(디버깅 과정에서 배운 것)**: 처음엔 Playwright로 소환 버튼을 반복 클릭하며
클릭→상태변경 왕복시간을 쟀는데, 정확히 9번째 클릭마다 매번 2~4초씩 튀는 걸
발견하고 진짜 프리징인 줄 알았다. CDP profiler로 그 구간을 떠보니 시간의 90%
이상이 `(idle)`이라 JS 실행 자체는 문제가 아니었고, `page.click()` 대신
`el.dispatchEvent(new MouseEvent('click'))`로 Playwright의 "엘리먼트가 안정적일
때까지 대기" 액셔너빌리티 체크를 건너뛰고 직접 디스패치했더니 모든 클릭이
5~10ms로 균일하게 나왔다. 즉 그 스파이크는 **매 0.2초마다 `root.innerHTML=''`로
버튼 노드를 통째로 교체하는 패턴이 Playwright의 "같은 엘리먼트가 두 프레임 연속
움직이지 않아야 통과"하는 폴링을 방해해서 생긴 테스트 하네스 아티팩트**였다 -
실제 사용자의 손가락 탭에는 이런 폴링이 없어서 재현되지 않는다. 이 프로젝트에서
클릭 관련 버그를 조사할 때 Playwright의 `page.click()` 왕복시간만으로 판단하지
말고, 이렇게 튀는 값이 나오면 `dispatchEvent` 직접 호출이나 CDP profiler로
JS 실행 시간인지 테스트 도구 자체의 대기시간인지부터 구분할 것.

## 목업 재대조 라운드 (한글 파일명 zip 재업로드, PR #13 배포 후) - 이미지 매핑 최신 원본

PR #13 배포 후 사용자가 `게임화면_목업.html`과 실제 배포 화면이 많이 다르다며, 이번엔
**숫자 대신 한글로 파일명을 다시 붙인 UI 이미지 zip**을 새로 줬다("몬스터 카운트
바.png", "룰렛 배경.png" 등). **이 라운드의 매핑이 이미지에 대한 최신 원본이다** -
위 "이미지 자산 매핑 2차분"/"버그 리포트 라운드"/"몬스터 카운트 바 이미지 재정정"
세 섹션의 숫자 기반 매핑(2.png~25.png 눈대중 대조)은 전부 이번 한글 파일명으로
대체되어 무효가 됐으니 참고하지 말 것. 매핑 자체는 `src/data/assets.js`의
`UI_IMAGES` 주석에 파일명 그대로 남겨뒀다.

**핵심 교훈: 목업 HTML의 구조(클래스명/자식 요소)가 실제 쓸 이미지를 알려주지
않는다.** `d0aea9d7-*.html`(게임화면_목업.html)을 base64까지 다 풀어서 다시 열어보니
`.monster-count`/`.top-badge`/`.stat-row` 전부 실제 이미지가 아니라 **순수 CSS
배경색 + 이모지(💀)로 그린 placeholder**였다(보스 하나만 base64 인라인 이미지).
즉 목업은 "레이아웃(위치/크기)"만 신뢰할 수 있는 자료고, 실제로 어떤 아트를 쓸지는
매번 사용자가 준 이미지 파일을 직접 열어서 판단해야 한다 - 클래스 구조나 이모지
모양으로 "이 이미지가 맞겠지"라고 추측하면 이번처럼 계속 틀린다.

1. **"룰렛 배경.png"(커튼 프레임 + 보석/인원/X 아이콘이 그림에 박힌 이미지)가
   상시 노출되는 top-badge 배경으로 잘못 쓰이고 있었음** (문제 4의 정체): 지난
   라운드에 이 이미지를 "상단 라운드/시간 배지 배경"으로 짐작해서 썼었는데, 실제로는
   목업 어디에도 안 쓰이는 자산이었다(목업의 `.top-badge`는 그냥 `background:
   var(--panel-brown)` 순수 CSS 박스, `.popup`도 전부 flat gradient). 사용자가 "팝업
   전용 요소가 항상 떠 있다"고 지적한 게 바로 이 이미지였다 - top-badge를 목업 그대로
   순수 CSS 박스로 되돌리고, 이 이미지는 현재 아무 데도 매핑하지 않는다(파일도
   `public/ui`에서 뺐음 - 필요해지면 zip에서 다시 꺼내 쓸 것).
2. **필드 그리드가 불투명 회색 박스로 보이던 원인**: `.field-grid`(그리드 컨테이너)와
   `.field-slot`(칸마다) 양쪽에 각각 `rgba` 반투명 배경이 걸려 있어서 겹겹이 쌓이며
   사실상 불투명해졌다 - 배경 원화에 이미 돌판 프레임이 그려져 있는데 그 위를
   덮어버린 것. 배경/패딩을 전부 빼고 칸 테두리선만 남겼다(목업의 `.field-grid .cell`도
   테두리만 있고 채우기 색이 없음). 스크린샷을 확대해보면 이제 돌판의 갈라진 금/이음새
   질감이 그대로 비쳐 보인다.
3. **영웅 토큰이 그리드 밖으로 삐져나오던 원인**: `.hero-token-image`가 `24px`/`17px`
   고정 픽셀이었다 - `.game-stage`는 뷰포트에 따라 크기가 변하는데(688:1508 비율
   유지하며 축소), 좁은 뷰포트에서 칸 자체가 24px보다 작아지면 고정 크기 아이콘이
   칸 테두리를 넘어갔다. `.hero-token`을 칸 크기에 대한 상대(%) 크기로 바꿔서
   뷰포트가 줄어들면 토큰도 같이 줄어들게 고쳤다.
4. **하단 재화 바(stat-row)/강화 버튼 레이아웃**: 목업의 px 좌표(375x812 기준)를 %로
   환산해서 위치를 다시 맞췄다. 강화 버튼은 특히 `height`를 %로 고정해서 실제 이미지
   비율과 안 맞아 letterbox되어 얇아 보였던 것 - `height` 대신 `aspect-ratio`(이미지
   실제 크기 그대로)를 쓰는 이 프로젝트의 기존 관행(top-badge, monster-row와 동일
   패턴)을 여기도 적용해서 고쳤다.
5. **보스 위치/크기**: 배경 원화에 그려진 게 아니라 위에 얹는 스프라이트라 목업의 px
   좌표를 %로 환산해서 다시 잡았다(`STAGE_LAYOUT.boss`). 높이는 고정값 대신
   `boss.png`의 실제 가로세로 비율로 계산해서 이미지가 눌리지 않게 했다. **field는
   배경 원화의 돌판 프레임과 정확히 겹쳐야 해서 목업 환산값 대신 기존 실측값을
   그대로 유지했다** - field/boss가 서로 다른 방식으로 좌표를 잡는 이유이니 헷갈리지
   말 것(field는 배경에 그려진 것과 맞추는 문제, boss는 그냥 배치 문제).
6. **룰렛 휠 이미지가 원형뿐 아니라 아래 가격 칸까지 이어붙은 세로로 긴 형태로
   바뀜**: 등급별로 실제 비율이 다 달라서(0.64~0.82) 공용 `aspect-ratio:1`을 강제하면
   안 되고, `GameScreen.js`의 `ROULETTE_TIERS`에 이미지별 실제 비율을 넣어서 버튼마다
   인라인 `aspect-ratio`로 정확히 맞췄다.
7. **"카운트 이미지.png"는 이번에도 매핑하지 않음**: 이번 zip에 다시 들어있었지만
   목업 어디에도 대응하는 자리가 없다(몬스터 카운트는 이제 더 명확한 이름의 "몬스터
   카운트 바.png"를 씀). 나중에 이 파일이 필요해지는 화면이 생기면 그때 다시 검토할 것.

## 상단 표시 5개 항목 정정 (PR #14 배포 후)

PR #14 배포 후 실제 플레이 스크린샷(WAVE 3, 8/110 표시)을 보내며 5개를 지적했다.

1. **WAVE 배지에서 "Lv.X" 제거**: `renderTopBadge`가 `WAVE ${wave} · Lv.${wave}`를
   라벨로 쓰고 있었는데, 웨이브 숫자를 그대로 복붙한 의미 없는 표시였다. `WAVE ${wave}`만
   남김. (몬스터 카운트 바 배경 자체는 이미 지난 라운드에 "몬스터 카운트 바.png"로
   정정되어 있었고 이번에도 스크린샷에서 정상 표시 확인함 - 재확인만 하고 코드 변경은
   없었음.)
2. **시간 표시를 `00:00` 형식으로**: `${Math.ceil(waveTimeLeft)}s` 같은 초 단위 텍스트
   대신 `formatClock()` 헬퍼로 `MM:SS`(2자리 패딩) 포맷으로 바꿈.
3. **재화 바 숫자가 아이콘과 겹쳐서 안 보이던 문제**: `resource_bar.png`("재화 및 맵
   카운트 바.png")에는 코인/행운석/인원 아이콘과 "/" 구분자가 전부 그림 안에 이미
   그려져 있는데, 텍스트 오버레이 위치(`left: 9%/37%/66%`)가 전부 해당 아이콘의
   "안쪽"이었다(실측: 코인 2.87~11.59%, 행운석 34.26~41.64%, 인원 63.59~71.38%, "/"
   80.82~84.82%) - 숫자가 아이콘 그림 위에 그대로 찍혀서 묻혀 보였다. 아이콘 bbox를
   흰 배경 대비 색상 스캔으로 다시 실측해서 각 아이콘 "다음" 빈 공간에 재배치했다.
   인원 칸은 그림에 이미 "/"가 있어서 `resource-pop`(직접 만든 "X/Y" 텍스트) 하나 대신
   `resource-pop-current`/`resource-pop-max` 두 개로 쪼개서 그림의 "/" 좌우에 각각
   배치했다(안 그러면 "0/30/"처럼 슬래시가 중복돼 보임).
4. **몬스터 카운트가 "라운드당 40마리, 시간에 따라 1마리씩 증가"가 아니라 필드 누적치
   최대 110에서 처치로 줄어드는 것처럼 보이던 문제**: `state.monsterCount`/`monsterMax`
   자체는 원래 기술설계서 기반의 "필드 누적 몬스터, 도달 시 패배" 판정용 내부 상태라
   그대로 뒀다(처치 시 감소하는 것도 의도된 동작 - `MONSTER_KILL_RATE_PER_SEC` 참고).
   대신 **화면에 보여주는 값은 완전히 별개로 새로 계산**했다: `roundMonsterSpawnProgress()`
   가 `(경과시간/라운드길이)*40`을 라운드 시작마다 0부터 다시 세서 `X/40`으로 표시한다
   (`MONSTER_PER_ROUND` 상수 재사용, 새 상태 필드 없이 `state.waveTimeLeft`에서
   역산해서 순수 계산값으로 처리 - 저장할 필요가 없었음). **패배 판정에 쓰이는 내부
   몬스터 누적치와 화면에 보여주는 라운드 진행도는 이제 서로 다른 값이니 헷갈리지
   말 것** - 나중에 패배 판정 자체를 바꾸라는 요청이 오면 `waveEvents.js`의
   `tickWave`/`onWaveStart` 안의 `monsterCount` 로직을 따로 봐야 한다.
5. **몬스터 이동 애니메이션이 캐릭터 칸을 침범하며 지나가던 문제**: 좌우 굴이
   전장 바로 위 모서리(y≈44.2%)에 있어서 `@keyframes monster-travel`의 시작/끝
   구간(전장 첫 줄 y범위 43.63~48.24%와 겹침)에서 몬스터가 전장 위를 그대로 가로질러
   보였다. 중간 구간(전장 아래 y:65%)은 원래도 안 겹쳤음 - 문제는 굴 근처 진입/이탈
   구간이었다. 굴 근처를 지나는 동안은 계속 `opacity:0`으로 숨겨두고, 전장 아래로
   완전히 내려온(또는 다시 올라가기 전) 구간에서만 보이도록 keyframe 퍼센트를
   조정했다(0~16%/84~100%는 숨김, 그 사이만 노출).

## 강화 레벨캡/글자 크기/룰렛 이미지 제거/몬스터 크기·동기화 (PR #15 배포 후)

PR #15 배포 후 강화 팝업 스크린샷을 보내며 4개를 지적했다.

1. **강화의 "소환 확률" 트랙에 11레벨 맥스 추가**: 이전엔 4트랙 전부 레벨 제한이
   없었는데, 소환 확률만 11레벨이 맥스라는 기획 확정 사항이 새로 나왔다.
   `constants.js`에 `GLOBAL_ENHANCE_MAX_LEVEL = { rate: 11 }`(트랙별 맥스, 나머지
   3트랙은 항목 자체가 없어 무제한 유지)를 추가하고 `upgradeGlobalEnhance()`에서
   맥스 도달 시 거부하도록 함. UI에서는 맥스에 도달하면 비용 칸이 "MAX"로 바뀌고
   버튼이 비활성화된다.
2. **재화 텍스트 크기 확대 + 몬스터 카운트 바는 반대로 축소**: `.resource-value`
   폰트를 `clamp(7px,2.1vw,11px)`→`clamp(11px,3.4vw,16px)`로 키웠다.
   `.monster-row`는 반대로 **바 자체를 줄이고**(`width:56%`→`42%`) **글자는
   키웠다**(`clamp(9px,2.6vw,12px)`→`clamp(13px,3.8vw,17px)`) - 두 요구가 바와
   글자에 각각 반대 방향으로 걸려 있었으니 헷갈리지 말 것(바는 작게, 안의 숫자는
   크게).
3. **룰렛 휠 이미지 완전 제거, CSS로 직접 그린 원으로 교체**: 등급별 이미지 파일마다
   실제 크기/비율이 달라서(0.64~0.82) 화면에서 들쭉날쭉해 보이는 문제가 근본적으로
   해결이 안 됐다 - 사용자가 아예 이미지를 걷어내고 목업(`게임화면_목업.html`의
   `.roulette-wheel`)처럼 CSS로 원(배경색+테두리색)을 그리고 원 가운데에 등급명
   텍스트(`TIER_LABEL`, 희귀/영웅/전설)를 넣으라고 지시했다. `roulette_rare.png`
   등 3개 이미지 파일은 더 이상 참조하지 않는다(파일 자체는 안 지웠음 - 코드에서
   안 씀). 색상은 목업 그대로 blue(#2f6fb0)/purple(#5b3f8f)/gold(#c98a1f) 유지.
4. **몬스터 아이콘 50% 축소 + "카운트되는 수"와 "장식용 몬스터 스폰 수" 동기화**:
   `.stage-monster` width를 7%→3.5%로 줄임. 스폰 동기화는 예전에 800ms 고정
   간격으로 따로 돌던 걸(`MONSTER_SPAWN_INTERVAL_MS`) 없애고,
   `roundMonsterSpawnProgress(state)`(카운트 바에 표시되는 그 값)를 목표치로 삼아
   장식용 몬스터를 그 값에 맞춰 정확히 1:1로 스폰하도록 `updateMonsterAnimation`을
   다시 짰다 - 카운트가 1 늘 때마다 장식용 몬스터도 정확히 1마리 새로 등장한다.
   **주의**: 이동 애니메이션(`MONSTER_TRAVEL_MS=2600ms`) 특성상 오래된 몬스터는
   화면을 빠져나가며 사라지므로 "현재 화면에 동시에 떠 있는 개수"가 카운트 숫자와
   정확히 일치하지는 않는다(카운트는 "이번 라운드에 지금까지 등장한 누적 수",
   화면에 떠 있는 건 그중 최근 2.6초 안에 스폰된 것만) - "누적 스폰 수 = 카운트
   숫자"라는 의미로 맞춘 것이니, 나중에 "동시에 정확히 N마리가 항상 보여야 한다"는
   요청이 오면 이 부분을 다시 봐야 한다.

## 판매 칸 위/아래 버튼 + 스택 정리 + 조합 순서 + 룰렛 타이밍 (PR #16 배포 후)

PR #16 배포 후 몬스터 카운트 바 스크린샷과 실제 게임의 "판매/합성" UI 스크린샷을
보내며 2개 UI 항목 + "운영에서의 문제점들"이라는 이름으로 3개 게임 로직 버그를
지적했다.

1. **몬스터 카운트 바 텍스트 중앙정렬**: `padding-left:20%` + `justify-content:center`
   조합이 "해골 메달을 제외한 나머지 영역의 중앙"을 어림잡아 맞추는 방식이라 실제로는
   살짝 메달 쪽으로 치우쳐 보였다. `monster_count_bg.png`를 다시 실측(해골 메달
   0.9~20%, 나머지 어두운 바 20.5~98.5%)해서 `.monster-count-text`를 그 바 구간에만
   딱 맞춰 `position:absolute`로 잘라내고 그 안에서 `text-align`(flex
   justify-content:center)하도록 바꿨다 - 앞으로 이 이미지가 바뀌면 이 실측값도
   다시 재야 한다.
2. **판매/합성을 팝업이 아니라 칸 위/아래 버튼으로 (1차, 이후 "몬스터 카운트/이동경로
   재수정" 섹션에서 renderSelectedPanel 자체가 통째로 없어지면서 최종 형태로
   대체됨 - 아래 참고)**: 기존엔 `renderSelectedPanel`(화면 상단에 뜨는 큰 카드)의
   버튼 목록에 판매/합성이 다른 액션(이동/강화/승급 시도 등)과 나란히 있었는데,
   사용자가 실제 게임 스크린샷을 보내며 이 두 개만 빼서 **선택된 칸 바로
   위(판매)/아래(합성)에 붙는 작은 버튼**으로 옮기라고 지시했다. `GameScreen.js`에
   `fieldCellRect(row,col)`(STAGE_LAYOUT.field를 FIELD_ROWS/FIELD_COLS로 나눠 칸
   하나의 %좌표를 계산)과 `renderCellQuickActions(state)`를 새로 추가해서 선택된
   칸의 좌표 위/아래에 절대좌표로 버튼을 띄운다(이동 모드 중엔 숨김 - 다른 칸
   클릭을 방해하면 안 되므로). 판매 버튼엔 실제 지급될 재화 아이콘+수량이 같이
   뜬다(`synthesis.js`의 `sellPreview()`). 판매 보상 수치도 이번에 확정 수치로
   교체:
   **일반 마리당 30코인, 희귀 1행운석, 영웅 2행운석, 전설 4행운석**(예전엔 일반
   10코인/전설 3행운석짜리 플레이스홀더였음).
3. **같은 영웅이 여러 칸에 조각나 있으면 자동으로 한 칸에 모으기**: 예) 4마리가
   3+1로 나뉘어 있을 때 3마리 칸에서 1마리를 팔면(2마리 남음) 다른 칸의 1마리가
   자동으로 그 칸에 합쳐져야 한다는 요구. `synthesis.js`에
   `consolidateHeroStacks(state, heroId)`를 새로 추가했다 - 해당 영웅이 있는 칸들을
   현재 개체 수 내림차순으로 정렬해서(많이 남은 칸이 우선순위를 가짐) 전부 걷어낸
   뒤 그 순서대로 다시 3개씩 채워 넣는 방식. `sellHero()` 판매 직후에 호출한다.
   신화/불멸(항상 칸당 1마리, 스택 없음)은 대상에서 제외.
4. **신화 조합 시 재료 소모 순서 버그**: "재료를 먼저 없앤 다음 자리를 확보해서
   소환하는 방식"이어야 하는데(필드가 30/30으로 꽉 차 있어도 재료를 없애면 빈 칸이
   생기는 경우 조합 가능해야 함), 기존 `craftMythic()`은 **반대 순서**였다 -
   `findAutoPlaceSlot()`으로 빈 칸부터 확인하고 나서 재료를 소모했기 때문에, 필드가
   꽉 찬 상태에서는 재료를 없애면 자리가 남는 상황인데도 무조건 "필드 꽉 참"으로
   막혔다. 재료 소모(+위 3번 스택 정리)를 먼저 하고 그 다음에 빈 칸을 확인하도록
   순서를 바꿨다. 실패 시(그래도 자리가 없으면) 원본 `state`를 그대로 반환하므로
   재료 소모도 함께 롤백된다(부작용 없음) - 이 트랜잭션 패턴 자체는 기존 코드
   컨벤션을 그대로 따른 것.
5. **룰렛 스핀 1초 + 해골은 한 번만, 0.5초만**: `ROULETTE_SPIN_MS`를 900→1000으로,
   `ROULETTE_FAIL_FLASH_MS`를 900→500으로 바꿨다. "해골이 여러 번 보여서 어지럽다"는
   지적의 실제 원인은 게임 루프가 그 짧은 flash 구간 동안에도 몇 번씩 전체 DOM을
   다시 그리는 구조(`main.js`)라, 해골 엘리먼트에 걸려있던 `roulette-fail-pop`
   scale+opacity 팝인 애니메이션이 매번 새로 생성되는 엘리먼트마다 처음부터 다시
   재생되면서 깜빡이는 것처럼 보였던 것 - 애니메이션 자체를 없애고 정적으로만
   보여주게 했다(전체 DOM 재생성 구조에서 CSS 애니메이션을 쓰면 항상 이 문제가
   재발할 수 있다는 점을 기억할 것 - 이 프로젝트에서 반복 등장하는 함정).

## 몬스터 카운트 개념 재정정 + 이동경로/장식 강화 (PR #17 배포 후)

PR #17 배포 후 두 번에 걸쳐 총 7개를 지적했다(두 번째는 첫 메시지 처리 중간에
추가로 들어온 메시지).

1. **몬스터 카운트 최대치는 110, 라운드당 40마리와는 별개 개념** - 이전 라운드에서
   "1부터 40까지 늘어나는 개념"이라는 말을 "표시 최대값이 40"이라고 잘못 이해해서
   `roundMonsterSpawnProgress()`(0→40, 라운드마다 리셋)라는 별도 함수를 만들어
   보여줬었는데, **완전히 잘못 짚은 것이었다**. 실제로는 `MONSTER_MAX=110`이 화면에
   표시할 진짜 최대값이고, 40은 "라운드마다 그 누적치에 트리클로 더해지는 증가분"일
   뿐이었다. `roundMonsterSpawnProgress()`를 통째로 삭제하고 화면 표시를
   `state.monsterCount / state.monsterMax`로 되돌렸다 - 대신 "1부터 40까지
   늘어나는(줄어들지 않는) 개념"과 "몬스터가 시간이 되면 한마리씩 나온다"는 요구는
   `onWaveStart()`가 라운드 시작 시 40을 통째로 즉시 더하던 것을 없애고,
   `tickWave()`에서 라운드 경과시간에 비례해 트리클로(한 마리씩) `monsterCount`에
   더하는 방식으로 반영했다(`state.roundMonsterSpawnedSoFar`로 라운드별 진행도
   추적, 라운드 시작마다 0으로 리셋). 처치 시 감소하는 기존 로직과 110 도달 시
   패배하는 기존 로직은 그대로 유지 - 그래야 게임이 라운드 3만에 강제로 지지 않는다
   (40×3>110이라 처치 감소가 없으면 몇 라운드 안에 무조건 짐).
2. **화면에 보이는 몬스터 스프라이트 수 = 카운트 숫자**: 예전엔 몬스터 스프라이트가
   경로를 한 바퀴(`MONSTER_TRAVEL_MS`=2600ms) 돌면 사라지는 1회성 애니메이션이라
   카운트가 몇이든 화면엔 항상 비슷한 수(약 4마리)만 떠 있었다. `ui.monsters` 풀을
   `Math.floor(state.monsterCount)` 크기에 정확히 맞추도록 바꿨다(카운트가 늘면
   스프라이트 추가, 줄면 제거) - 각 스프라이트는 `animation-iteration-count:infinite`
   로 경로를 무한 반복하고, 다 같은 타이밍에 움직이지 않도록 `delayMs`(0~2600ms
   랜덤)로 시작 시점을 흩뿌린다. 카운트가 110에 가까워질수록 필드 주위가 몬스터로
   빽빽해 보이는 게 오히려 "위험하다"는 느낌을 잘 살려준다.
3. **몬스터 이동 경로가 전장을 가로지르는 문제(재발)**: "버그 리포트 라운드"에서
   한 번 고쳤다고 생각했는데, 사용자가 참고 이미지(빨간 경로: 오른쪽 굴에서 전장
   **바깥쪽** 오른쪽 변을 타고 내려와서 바깥쪽 아래 변을 가로지르고 왼쪽으로 이어짐)
   를 보내면서 재지적했다. 원인을 다시 보니: 예전 수정은 굴 근처 진입/이탈 구간을
   투명하게 "순간이동"시켜 안 보이게 가리는 임시방편이었지 실제 경로 자체가 전장
   바깥을 도는 게 아니었다 - 그래서 보이는 구간(중간의 수평 이동)만 자연스러웠고
   "빙 둘러 가는" 느낌이 안 났다. 이번엔 진짜로 경로를 다시 그렸다: 굴에서 나오자마자
   전장 왼쪽 바깥(x:7%, 전장 left=12.5%보다 왼쪽)으로 빠져서 아래로 내려가고, 전장
   아래(top:65%, 전장 bottom=62.07%보다 아래)를 가로질러 반대쪽 바깥(x:93%, 전장
   right=86.92%보다 오른쪽)으로 갔다가 위로 올라간다 - 경로 전체가 항상 전장 박스
   바깥에 있어서 이제 투명 처리 구간이 거의 필요 없다(굴 바로 옆 아주 짧은 구간만
   유지, opacity 전환도 x가 전장 경계를 넘은 뒤에만 일어나도록 퍼센트를 세밀하게
   맞춤). `@keyframes monster-travel`(main.css) 참고.
4. **룰렛 스핀 시간을 0.28초로**: `ROULETTE_SPIN_MS`/CSS 애니메이션 시간을 정확히
   280ms로 맞췄다(이전엔 1초).
5. **일시정지/나가기 버튼이 찌그러져 보이던 문제**: `width:11%; aspect-ratio:1;`을
   썼는데도 실제로는 16x24px처럼 세로로 길쭉하게 렌더링됐다. 원인은 CSS 스펙 함정 -
   `.stage-controls`가 `position:absolute`에 `right`/`top`만 있고 명시적
   `height`가 없어서(콘텐츠 기준 auto), 자식의 `height:%`는 물론이고
   `aspect-ratio`로 계산되는 암묵적 높이조차 버튼 안 이모지 글자의 최소 높이에
   밀려서 무시되는 경우가 있었다(컨테이닝 블록 높이가 확정값이 아니면 자식의
   퍼센트 높이는 스펙상 auto로 처리됨). width/height를 전부 `vw` 단위(항상 확정값)
   + `aspect-ratio:1`로 바꿔서 컨테이너의 auto-sizing과 무관하게 진짜 원이 되도록
   고쳤다 - **`position:absolute`인데 반대쪽 좌표(width/height)가 없는 컨테이너
   밑에서 자식에게 %height나 aspect-ratio를 쓸 때는 이 함정을 기억할 것.**
6. **좌우 굴에 보라색 소용돌이 효과 추가**: 몬스터가 나오는 자리라는 걸 시각적으로
   분명히 하기 위해 두 굴 위치에 항상(게임 진행 중) 보라색 방사형 글로우 + 회전하는
   conic-gradient 무늬를 겹친 `.hole-vortex`를 추가했다(전용 이미지 에셋이 없어서
   CSS로만 구현).
7. **라운드 종료 5초 전 카운트다운 배지**: 왼쪽 굴 위에 `waveTimeLeft<=5`일 때만
   시계 아이콘+숫자 배지(`.hole-countdown`)가 뜬다. 오른쪽 굴에는 안 띄움(사용자가
   "왼쪽에 원 하나"라고 명시적으로 왼쪽만 지목함).

## 굴 위치 재실측 + 이동속도 4배 + 선택 패널 완전 폐지 (PR #18 배포 후)

PR #18 배포 후 실제 스크린샷 2장을 보내며 3개를 지적했다.

1. **좌우 굴 위치가 실제로는 틀렸었음**: "몬스터 카운트 개념 재정정" 섹션에서 굴
   좌표를 `{x:14.97/85.03, y:44.23/44.3}`(전장 박스 바로 위 모서리)로 썼었는데,
   이건 원래부터 배경 원화의 실제 톱니바퀴 장식(청동색 허브가 있는 기계 바퀴
   그림)과 안 맞는 값이었다 - 우연히 "전장 박스 모서리 근처"였을 뿐 실제 그림
   요소를 가리키고 있지 않았다. 배경 이미지에 2% 간격 격자선을 그려 넣어
   재실측했더니 실제 톱니바퀴 허브 중심은 `{x:8/92, y:40.5}`였다(기존 값과
   6~7%p 차이) - **이 좌표가 지금까지 한 번도 정확히 실측된 적이 없었다는 뜻이라
   나중에 배경 이미지가 또 바뀌면 반드시 격자선 오버레이로 다시 재야 한다**(이
   섹션에 쓴 방법: 이미지에 라벨 붙인 grid line을 그려서 크롭 후 눈으로 읽기 -
   색상 매칭(bronze 계열 픽셀 스캔)은 갑옷 장식판 등 비슷한 색의 다른 요소까지
   같이 잡혀서 부정확했다). 새 굴 좌표는 전장 박스 바깥(x/y 둘 다)이라 아래 2번과
   맞물려 몬스터 이동 경로 전체를 가릴 필요가 없어졌다.
2. **몬스터 이동 애니메이션 4배 느리게**: `MONSTER_TRAVEL_MS`를 2600→10400ms로.
3. **선택 패널(`renderSelectedPanel`)을 완전히 없앰**: 지난 라운드에 판매/합성만
   칸 버튼으로 옮기고 이동/강화/승급시도 등 나머지는 여전히 화면 상단에 크게 뜨는
   `.selected-panel`(반투명 어두운 카드) 안에 남겨뒀었는데, 사용자가 "아직
   반영 안 됐다"며 재지적했다 - 그 큰 카드 자체가 여전히 "팝업"으로 읽혔던 것.
   `renderSelectedPanel` 함수를 통째로 삭제하고, 그 안에 있던 모든 버튼(이동,
   강화, 돌파, 승급 시도, 판매(+6행운석, 기가채드), 동족포식, 발굴, 2차 변신 시도,
   채드에게 먹이기)과 상태 텍스트(불멸 진행도, 마마 임프 수, 인디 보물, m_tar
   단계 등)를 전부 `renderCellQuickActions()`로 흡수했다 - 이동/강화/판매(+닫기
   버튼)는 칸 위쪽에, 합성과 나머지 영웅별 특수 액션은 칸 아래쪽에 각각 세로로
   쌓이는 작은 pill 버튼 묶음(`.cell-quick-stack-above`/`-below`)으로 표시한다.
   상태 텍스트는 큰 카드 대신 작은 배지(`.cell-status-badge`) 한 줄로 축약.
   **이제 칸을 클릭해도 화면을 덮는 큰 패널이 전혀 뜨지 않는다** - 선택 해제는
   ✕ 버튼(above 스택 맨 위) 또는 빈 칸 클릭으로. CSS는 `.cell-quick-btn`을
   `position:absolute`(개별 좌표 지정) 대신 `.cell-quick-stack`(칸 좌표로
   position:absolute) 안에서 `flex-direction:column`으로 자연스럽게 쌓이도록
   구조를 바꿨다 - 새로 버튼을 추가할 땐 `above`/`below` 배열에 push만 하면 알아서
   쌓인다.

## 칸 버튼 액션 범위 과잉 판단 정정 (PR #19 배포 후)

바로 위 라운드에서 `renderSelectedPanel`을 없애고 칸 버튼으로 흡수하면서, "이동"과
"강화"를 등급 무관하게 모든 영웅에게 항상 뜨는 버튼으로 만들어뒀었다. 사용자가
명시적으로 정정: **"일반~전설은 판매, 합성 외에 다른 기능은 없어. 신화~불멸만
특수 액션이 있을 뿐이야. 신화불멸은 판매할 수 없고 채드가 있을 때만 판매 가능하고.
강화는 캐릭터 클릭했을 때 나오면 안돼."** — 이건 내가 기획서에 없는 내용을 혼자
판단해서 끼워 넣은 것이었다.

1. **일반~전설 칸 버튼을 판매(+3마리 모이면 합성)만 남기고 이동/강화 버튼 삭제**.
2. **신화/불멸은 일반 판매(`sellHero`) 대상에서 완전히 제외**하고, `above` 스택의
   판매 버튼 자리에 조건부로 "채드 먹이기(+5💧)" 버튼이 온다 - 필드에 `m_chad`가
   있을 때만 노출, 없으면 그 자리는 비워둔다(신화/불멸을 처분할 방법이 아예 없어짐 -
   기획 의도대로). 단, 기가채드(`i_giga_chad`)는 이미 전용 판매 버튼(+6행운석)이
   있으므로 채드 먹이기 대상에서 제외.
3. **`feedMythicToChad`(`synthesis.js`)가 신화 등급만 받아주던 것을 불멸 등급도
   받도록 확장**(사용자 지적: "신화불멸은... 채드가 있을 때만 판매 가능" - 신화
   *와* 불멸 둘 다). 단 기가채드 승급 조건 문구(`heroes.js`의
   `note:'채드 보유 중 신화 영웅 판매(먹이기) 5회 누적'`)가 "신화 영웅"으로 못박혀
   있어서, 먹인 대상이 불멸 등급이면 행운석 보상은 주되 `chad.instance.progress`는
   증가시키지 않는다(신화를 먹였을 때만 카운트).
4. **이동/강화를 완전히 없애지는 않음 - 특정 영웅 전용 진행 조건으로만 남김**.
   `IMMORTAL_CONDITIONS`(`heroes.js`)를 보면 `m_bane`(탑 베인)의 불멸 조건
   `eventType:'move'`(이동 132회), `m_iron_meyaong`(아이언미야옹)의 조건
   `eventType:'enhance'`(강화 10회)가 실제로 `moveHero()`/`enhanceHero()` 호출에
   의존한다 - 이걸 완전히 지우면 그 두 영웅은 영원히 불멸 승급이 불가능해진다.
   그래서 "모든 영웅에게 뜨는 범용 버튼"이 아니라 `heroDef.immortalCondition?.eventType`
   이 `'move'`/`'enhance'`인 **그 신화 영웅을 선택했을 때만** `below` 스택에 뜨는
   전용 버튼으로 바꿨다(`immortalCondition`은 승급 전 신화 단계 heroDef에만 채워져
   있고 승급 후 불멸 heroDef는 `null`이라, 승급하고 나면 이 버튼도 자연히 사라진다).
   사용자의 "강화는 캐릭터 클릭했을 때 나오면 안돼"는 이 범용 노출을 가리킨
   것으로 해석해서, 게임 메커니즘상 필요한 이 두 특수 케이스까지 지우지는 않았다 -
   나중에 다시 문제 삼으면 이 두 케이스도 다른 방식(자동 카운트 등)으로 바꿔야 함.

## 몬스터 미표시/굴 위치/즉시소환 아이콘/조합 버그/드래그 이동/판매 UX (PR #20 배포 후)

PR #20 배포 후 실제 플레이 스크린샷(WAVE 3, 0/110, 필드에 영웅 25마리)을 보내며 7개를
지적했다.

1. **몬스터가 아예 화면에 안 보이던 버그**: `MONSTER_KILL_RATE_PER_SEC`(필드에 영웅이
   있으면 초당 처치되는 몬스터 수)가 2였는데, 일반 라운드(30초)의 트리클 스폰 속도는
   40/30≈1.33/초 - 처치 속도가 스폰 속도보다 항상 빨라서 `state.monsterCount`가 거의
   항상 0에 수렴했다. 장식용 몬스터 스프라이트 풀도 `Math.floor(monsterCount)`에
   연동돼 있어서(PR#17) 몬스터가 화면에 뜰 일 자체가 없었다 - 시뮬레이션으로 직접
   확인함(`tickWave`를 반복 호출해보니 값이 0~0.6 사이에서만 왔다갔다함). 값을 0.5로
   낮췄다 - 일반 라운드는 스폰이 처치를 앞질러 몬스터가 실제로 쌓여 보이고, 스폰이
   훨씬 느린 보스 라운드(150초, 40/150≈0.27/초)에서는 여전히 처치가 스폰을 앞질러
   0까지 떨어지므로 보스 레이드 창이 정상적으로 열린다(20라운드 전체 시뮬레이션으로
   110을 넘지 않고 클리어 가능함을 확인).
2. **좌우 굴 위치가 또 틀렸었음**: "굴 위치 재실측" 라운드(PR#18)에서 격자선으로 한 번
   재실측했는데도 여전히 어긋나 있었다. 1% 간격 격자 + 크로스헤어 오버레이를 왼쪽/
   오른쪽 각각에 씌워서 어두운 구멍의 정중앙을 다시 정밀 대조했더니, 기존 값
   `{8,92}/{40.5,40.5}`이 실제 구멍 중심에서 약 1~1.5%p씩 벗어나 있었다(왼쪽은 완전
   대칭도 아니었음 - 오른쪽 굴이 왼쪽보다 살짝 아래쪽에 있음). `leftHole:{7.3,41.4}`,
   `rightHole:{93.4,42.0}`으로 갱신. **CSS의 `@keyframes monster-travel`(main.css)에도
   굴 좌표가 하드코딩돼 있다는 걸 잊지 말 것** - JS `STAGE_LAYOUT`을 고쳐도 CSS
   keyframe은 자동으로 안 따라오므로 항상 같이 수정해야 한다(이번에 실제로 두 곳 다
   갱신함).
3. **왼쪽 아이콘 목록이 "필드에 있는 신화"를 보여주고 있었음**: `craftedShowcaseHeroIds()`가
   필드에 이미 배치된 신화/불멸 인스턴스를 스캔해서 보여주고 있었는데, 사용자가
   "조합이 가능한 영웅이 떠야 한다"고 기획서 근거로 정정했다. 마침 신화 팝업에 이미
   같은 개념(`craftMaterialsReady` - 재료가 필드에 다 갖춰졌는지)이 구현돼 있어서
   그대로 재사용: `heroesByTier('mythic')` 중 `unlockedInstantSummons`에 있거나(이미
   한 번 조합해서 무료 재소환이 풀린 것) `craftMaterialsReady`가 true인(지금 바로
   조합 가능한) 것만 최대 5개, 즐겨찾기 우선으로 표시하도록 바꿨다. 클릭 시 동작도
   나눴다 - 이미 풀린 것은 기존처럼 무료 즉시소환(`instantSummonFavorite`), 아직 안
   풀렸지만 재료가 갖춰진 것은 실제 조합(`craftMythic`, 재료 소모)을 바로 실행한다.
4. **"닌자를 눌러 조합해도 닌자가 아니라 다른 신화가 뜬다"는 리포트**: `craftMythic()`
   자체는 인자로 받은 `mythicHeroId`를 그대로 배치하는 결정론적 함수라(랜덤 없음)
   코드상 버그는 없었다 - 실제 원인은 3번과 같은 버그였다. 하단 시트 팝업(`.game-popup`)
   이 화면 전체를 덮지 않아서 왼쪽 위 즉시소환 아이콘이 팝업이 열려 있는 동안에도
   계속 보이는데, 그 아이콘 목록이 "필드에 있는 신화"(3번 버그) 기준이라 닌자를
   조합한 직후에도 필드에 이미 있던 *다른* 즐겨찾기 신화만 아이콘에 뜨고 있었던
   것 - 사용자 입장에서는 "닌자를 조합했는데 다른 신화가 나타났다"로 보였다. 3번을
   고치면서 같이 해결됨(별도 수정 불필요, 원인 규명만 하고 문서화).
5. **이동을 버튼이 아니라 드래그 방식으로 변경**: 기존엔 (탑 베인 한정) "이동" 버튼을
   누르면 `moveMode`에 들어가 다른 칸을 클릭해야 이동이 확정되는 2단계 클릭 방식이었다.
   `GameScreen.js`에 `pointerdown`(칸에서 시작 - 시작 칸을 `dragState`에 기록) →
   `pointermove`(8px 이상 움직이면 드래그로 판정, `document.elementFromPoint`로 현재
   올라가 있는 칸을 찾아 `.drag-hover` 클래스로 표시) → `pointerup`(움직임이 없었으면
   기존처럼 탭-선택, 있었으면 `moveHero()` 호출)로 재구현했다. `ui.moveMode`/
   `move-target` 관련 코드와 탑 베인 전용 "이동" 버튼은 전부 제거 - 드래그가 모든
   영웅에 대해 동작하고 `moveHero()`를 그대로 호출하므로 탑 베인의 불멸 진행도
   카운트도 똑같이 잘 쌓인다. **함정**: `<img>` 태그는 기본적으로 브라우저 네이티브
   드래그(HTML5 Drag and Drop)가 켜져 있어서, 영웅 토큰 이미지를 마우스다운한 채
   움직이면 첫 픽셀만에 네이티브 드래그가 가로채 버려서 이후 `pointermove`가 전혀
   안 들어오는 버그가 있었다(Playwright로 재현: 드래그가 항상 시작 칸에서 멈춘
   것처럼 보였음 - `elementFromPoint`나 좌표 계산은 멀쩡했는데 애초에 두 번째
   `pointermove`부터 안 들어옴). `heroImage()`에서 생성하는 `<img>`에 `img.draggable
   = false`를 직접 DOM 프로퍼티로 설정하고(`el()` 헬퍼는 `value===false`인 attr를
   생략하는 구조라 `draggable:false`를 그냥 넘기면 안 먹힘), `.field-slot`에
   `ondragstart:e=>e.preventDefault()`와 CSS `-webkit-user-drag:none`을 안전망으로
   추가해서 고쳤다.
6. **판매 버튼에 예상 금액 미리보기 제거**: 칸 위 판매 버튼에서 `sellPreview()`로
   계산한 "+30" 같은 보상 미리보기 텍스트를 없애고 "판매" 텍스트만 남겼다(사용자
   요청 - 클릭 전에 금액을 안 보여줘도 된다). `sellPreview` import는 GameScreen.js에서
   제거(더 안 쓰임, 함수 자체는 `synthesis.js`에 그대로 남겨둠 - 혹시 다른 곳에서
   필요해질 수 있어서).
7. **일반 등급 판매 금액을 120코인으로 변경**: `synthesis.js`의
   `SELL_GOLD_BY_TIER.normal`을 30 → 120으로 수정(사용자가 지정한 확정 수치). 희귀/
   영웅/전설 행운석 보상은 그대로.

## 몬스터가 제자리에서 깜빡이던 버그 (PR #21 배포 후)

PR #21 배포 후 "몬스터가 왼쪽 굴에서 나와 모서리를 돌아 오른쪽 굴로 들어가야 하는데
제자리에서 좌우로 왔다갔다 하다가 중간에서 튀어나온다"는 리포트를 받았다.

**원인**: `updateMonsterAnimation()`이 몬스터 스폰 시점에 `delayMs`(0~MONSTER_TRAVEL_MS
사이 랜덤값)를 한 번 뽑아서 고정으로 저장해두고, 매 렌더마다 `animation-delay:
-${delayMs}ms`를 그대로 다시 적어 넣고 있었다. 그런데 게임 루프가 0.2초마다
`root.innerHTML=''`로 전체 DOM을 다시 그리기 때문에(main.js `RENDER_INTERVAL_SEC`),
`.stage-monster` `<div>`는 매번 새로 생성되는 노드다 - CSS 애니메이션은 엘리먼트가
생성된 시점부터 실제 경과 시간을 기준으로 재생되므로, 고정된 `delayMs`를 계속 같은
값으로 넣으면 새로 생성된 노드는 항상 "그 지점"에서부터 다시 시작해서 딱 0.2초만
전진하고 다음 렌더에서 또 "그 지점"으로 리셋되기를 반복한다 - 즉 10.4초짜리 전체
경로 중 0.2초(약 1.9%)짜리 아주 짧은 구간만 반복 재생되는 꼴이라 제자리에서
깜빡이는 것처럼 보였다. `MONSTER_TRAVEL_MS`를 2600→10400ms로 4배 늦춘 이전 라운드
(PR#18)에서 이 문제가 상대적으로 훨씬 두드러지게 됐다(리셋 구간 비율이 4배 커짐).

**수정**: 몬스터마다 고정 `delayMs` 대신 실제 스폰 시각(`spawnTime =
performance.now() - stagger`)을 저장해두고, 렌더마다
`phaseMs = (performance.now() - spawnTime) % MONSTER_TRAVEL_MS`로 "지금 이 순간
실제로 몇 ms째 재생 중이어야 하는지"를 다시 계산해서 `animation-delay`에 넣는다.
이렇게 하면 노드가 매번 재생성되어도 각 순간의 phase가 실제 경과 시간과 정확히
맞아떨어져서 애니메이션이 끊기지 않고 이어진다. Playwright로 1~2초 간격을 두고
`animation-delay` 값을 여러 번 샘플링해서 정확히 그 간격만큼 증가하는지(리셋되지
않는지) 확인해 검증했다. **교훈**: 이 프로젝트는 전체 DOM을 주기적으로 다시 그리는
구조라, `animation-delay`/`animation-play-state`처럼 "엘리먼트 생성 시점"에 값이
고정되는 CSS 속성을 쓸 때는 항상 고정값이 아니라 실제 경과 시간 기반으로 매 렌더
다시 계산해서 넣어야 한다 - 그렇지 않으면 애니메이션이 매번 리셋된다(해골
깜빡임 버그도 같은 계열 원인이었다 - "버그 리포트 라운드" 섹션의 5번 참고).

## 기본 게임타입/마마 임프 실체화/조합 스택 정리/불멸 승급 아이콘화/이동불능 강제 (PR #22 배포 후)

PR #22 배포 후 실제 플레이 스크린샷(WAVE 4, 신화 팝업 중 룰렛 실패 화면)을 보내며
7개를 지적했다.

1. **시작 화면 기본 게임타입을 "삭제 있는 버전"으로**: `HeroSelectScreen.js`의
   `createLocalState().gameType`이 `null`(아무것도 선택 안 된 상태)이었던 것을
   `'delete'`로 바꿨다.
2. **마마 임프를 실제 필드 토큰으로 + 속도 2.5배 느리게**: 예전엔 `instance.impStock`
   숫자만 올라가고 화면엔 상태 배지 텍스트로만 보였는데, 사용자가 "임프도 캐릭터처럼
   캐릭터 보드에 꺼내야 한다"고 지적했다. `heroes.js`에 `IMP_HERO_ID='x_imp'`
   (tier:'imp', HEROES 배열엔 안 넣고 HEROES_BY_ID에만 직접 등록 - 소환/선택 화면에
   노출 안 되면서 기존 필드 렌더링 경로는 그대로 탄다)를 추가하고, `tickMamaImps()`가
   매 틱마다 `findAutoPlaceSlot`으로 빈 칸을 찾아 실제 `x_imp` 인스턴스를 배치하도록
   바꿨다(자리가 없으면 그 틱은 건너뛰고 impStock도 안 늘림 - 실제 존재량과
   어긋나면 안 되므로). 무한정 쌓이지 않게 `MAX_IMP_STOCK=9`(승급에 필요한 최댓값)
   에서 생성을 멈춘다. 승급 조건 판정(`promotionHandlers.m_mama`)도 이제 필드에서
   전부 자동 제거하도록 명시적으로 구현(예전엔 "promoteInstance가 개체를 통째로 새로
   만들어서 impStock이 자연히 리셋된다"고 문서화했었는데, 그건 impStock이 숫자일
   때만 맞는 얘기였고 실제 필드 토큰이 된 지금은 명시적으로 지워야 한다). 속도는
   `IMMORTAL_MAMA`의 세 간격(3/2/5초)을 전부 2.5배(7.5/5/12.5초)로 늦췄다.
3. **조합(3마리 랜덤 합성) 결과도 판매/신화조합처럼 자동 스택 정리**: `synthesize()`가
   결과 영웅을 그 칸에 그대로 두기만 하고 다른 칸에 이미 같은 영웅이 있어도 안
   합쳐주고 있었다(사용자 예시: 산적이 조합으로 또 나왔는데 다른 칸의 산적 1마리와
   안 합쳐짐). `sellHero`/`craftMythic`과 같은 패턴으로 `consolidateHeroStacks`를
   조합 직후에도 호출하도록 추가.
4. **불멸 승급을 칸 아래 버튼이 아니라 신화처럼 왼쪽 아이콘으로**: "불멸이 먼저,
   신화가 밑으로"라는 사용자 지정 규칙에 따라 `renderCellQuickActions`의 "승급
   시도" 버튼을 삭제하고, `renderFavoriteBar`(좌측 즉시소환 바)가 조합 가능한 신화
   목록 앞에 "승급 가능한 불멸 후보"를 먼저 붙이도록 재구성했다(`favoriteBarItems()`).
   승급 준비 여부는 인스턴스별 판정이라(같은 heroId라도 개체마다 progress가 다름)
   `immortal.js`에 새로 `isImmortalPromotionReady(state, instanceId)`를 추가했다 -
   **`checkImmortalPromotion`을 그냥 재사용하면 안 된다는 게 함정이었다**: 그 함수는
   호출 즉시 실제 승급/소모 부작용을 일으키므로(사신개구리 승천 실패 시 소멸,
   마마 임프 소모 등), "지금 눌러도 되는 상태인지"만 미리보기하려면 각 핸들러의
   판정 로직만 읽기 전용으로 따로 다시 구현해야 했다(7개 특수 핸들러 + 나머지는
   제네릭 진행도 폴백). 보라색 테두리(`.favorite-icon-promote`)로 신화 조합 아이콘과
   시각적으로 구분.
5. **좌측 즉시소환/승급 아이콘 원이 너무 큼**: `.favorite-bar`의 `width`를 16%→10%로
   축소(아이콘은 그 안에서 `width:100%`라 자동으로 같이 줄어듦).
6. **신화 팝업 등급 그리드 스크롤이 안 먹힘**: 이 프로젝트의 반복 패턴대로,
   게임 루프가 0.2초마다 `root.innerHTML=''`로 전체 DOM을 다시 그리면서
   `.mythic-grid`(`overflow-y:auto`)도 매번 새 엘리먼트로 재생성돼 `scrollTop`이
   계속 0으로 리셋되고 있었다 - 사용자가 내려도 0.2초 뒤 바로 맨 위로 튕기니
   "스크롤이 안 된다"로 보였다. `render()`에서 재생성 직전 `.mythic-grid`의
   `scrollTop`을 저장해뒀다가 재생성 직후 새 노드에 그대로 복원하도록 고쳤다.
7. **이동불능(속박) 상태에서도 드래그로 캐릭터를 옮길 수 있던 버그**: `isImmobilized()`
   가 지금까지 순수 시각 효과(사슬 아이콘 + 테두리)에만 쓰이고 실제 이동을 막는 덴
   전혀 관여하지 않고 있었다. 드래그 시작 시점(`pointerdown`)에 해당 칸이 이동불능
   상태인지 확인해서 `dragState.immobilized`에 기록해두고, 드롭 시점(`endDrag`)에
   `moved && immobilized`면 `moveHero()` 호출을 건너뛰도록 막았다 - 탭으로 정보를
   보는 선택 동작 자체는 막지 않고, 실제 칸 이동만 차단한다.

## 대규모 정정 라운드 - 18개 항목 (PR #23 배포 후)

PR #23 배포 후 실제 플레이 스크린샷 1장과 참고 이미지(입체적으로 겹쳐 보이는 캐릭터
배치)를 첨부하며 한 번에 18개를 지적했다. 항목이 많아 로직 단위 테스트로 최대한
검증하고(전부 실제 플레이로 재현하긴 시간상 무리), Playwright는 리스크가 큰 것
위주로 확인했다.

1. **미션 성공 시 행운석이 안 들어오던 버그**: `missions.js`의 `MISSIONS` 전부
   `reward: null`("보상 미정")이었다 - 애초에 지급 코드 자체가 없었다. 확정 수치가
   없어서 행운석 5(플레이스홀더)로 우선 채우고, `checkMissions()`가 완료로 "막
   넘어가는 순간"에만 지급하도록 `state.claimedMissionIds`(중복 지급 방지)를
   추가했다. 추가로 "강화 시작"(강화 버튼 2회) 미션의 `counterKey: enhanceCount`가
   특정 영웅 전용의 희귀한 `enhanceHero()`에서만 올라가고 있어서(전역 강화 팝업
   `upgradeGlobalEnhance()`에서는 안 올라감) 실질적으로 거의 못 깨는 미션이었다 -
   사용자 힌트("강화 버튼 아무거나 2번")대로 `upgradeGlobalEnhance()`에도
   `enhanceCount += 1`을 추가.
2. **필드가 꽉 찼는데도 룰렛이 돌아가며 재화가 소모되던 버그(13번과 중복 리포트)**:
   `summonRoulette()`가 일반 소환과 달리 필드 꽉 참 체크가 아예 없었다(결과를
   `pendingPlacementQueue`에 쌓아두고 나중에 자리 나면 배치하는 방식이라 애초에
   막을 필요가 없다고 설계됐던 듯). 일반 소환과 동일하게 `fieldOccupantCount >=
   fieldMaxCapacity`면 재화 소모 전에 막도록 추가하고, UI(`onRouletteWheelClick`)
   에서도 스핀 애니메이션 시작 전에 미리 막았다(그래야 결과 처리 시점이 아니라
   클릭 즉시 막힌다).
3. **캐릭터가 칸 안에 눕혀 넣은 것처럼 보이던 문제 → 입체적으로 겹쳐 보이게**:
   `.field-slot`이 `overflow:hidden`이라 칸보다 큰 이미지를 못 담는 구조적 한계가
   있어서, 캐릭터 렌더링을 필드 그리드 안(자식)이 아니라 칸 좌표를 기준으로 절대
   배치되는 별도 오버레이 레이어(`renderHeroTokenLayer`, `.stage-hero-layer`)로
   완전히 분리했다. 칸 높이의 1.75배로 세로로 키우고 `object-position:bottom
   center`로 발밑을 칸 하단(바닥선)에 맞추고, z-index를 그리드 행(row) 번호로
   매겨서 아래쪽(화면 앞) 행이 위쪽(화면 뒤) 행을 가리게 했다. 발밑에는 반투명
   타원 그림자(`.stage-hero-shadow`)도 추가. **함정**: 이 레이어를 추가하고 나니
   드래그 이동/탭 선택이 전혀 안 먹혔다 - 새 레이어가 `.field-slot` 위에 시각적으로
   겹쳐 그려지면서 포인터 이벤트를 가로채, `e.target.closest('.field-slot')`가
   null이 되어버렸기 때문(예전엔 캐릭터가 `.field-slot`의 자식이라 문제가 없었음).
   `.stage-hero-layer`에 `pointer-events:none`을 걸어서 이벤트가 아래 칸으로
   투과되게 고쳤다 - **레이어를 분리할 때는 항상 pointer-events를 같이 점검할 것.**
4. **탑 베인 궁극기 이펙트 신규 추가**: 데미지 계산은 범위 밖이라 실질 효과는 없지만,
   이동 `moveToUltimateRatio`(11)회마다 "궁 1회 사용"으로 환산되는 시점에
   `recordImmortalEvent()`가 `instance.ultimateFlashAt` 타임스탬프를 남기고,
   1.2초간 글로우+확산 링 애니메이션을 보여준다. **몬스터 애니메이션과 같은
   함정을 피하려고**(0.2초마다 전체 DOM을 다시 그리는 구조에서 `infinite`
   애니메이션에 고정 delay를 주면 매번 처음부터 재생돼 멈춘 것처럼 보임) 실제 경과
   시간 기준으로 `--ring-delay`를 매 렌더 다시 계산해서 넣었다.
5. **채드 판매 흐름 전면 재설계 + 기가채드 조건 재확인**: 예전엔 신화/불멸 영웅을
   선택하면 자동으로 "채드 먹이기" 버튼이 떴는데, 사용자가 정확한 순서를 지정했다 -
   **채드를 선택했을 때 그 밑에 "판매하기" 버튼이 있고, 그걸 눌러야 필드의 다른
   신화/불멸(채드/기가채드 본인 제외) 머리 위에 초록 화살표가 뜨고, 그 화살표를
   눌러야 비로소 판매(먹이기)가 실행된다.** `ui.chadSellMode` 토글 +
   `renderChadArrowLayer()`(화살표 전용 오버레이)로 구현, 기존 자동 노출 버튼은
   제거. 기가채드 승급 조건도 "신화 판매 5회 누적"이 아니라 **"판매할 때마다
   확률적으로 능력치 2%p 상승, 총 10% 도달 시 승급"**이라고 재확인해서
   `IMMORTAL_CONDITIONS.m_chad`의 `target`을 5(횟수)에서 10(%)으로, `extra`에
   `procChance`(0.5, 미확정 플레이스홀더)/`procAmount`(2)를 추가했다.
6. **13/20라운드 삭제 공격 모션 신규 구현**: 로직(`handleDeleteEvent`)은 이미 행/열
   하나를 골라 9초(filling 단계) 뒤 삭제하는 구조가 있었지만, 화면엔 대상 칸마다
   작은 ❌ 아이콘만 떴었다. 대상 라인 전체를 감싸는 네모 테두리(`.delete-line-box`)
   + 왼쪽(행)/위쪽(열)에서부터 차오르는 반투명 붉은 게이지(`.delete-line-gauge`)로
   교체 - 진행률은 `DELETE_START_AT_TIME_LEFT`/`DELETE_TRIGGER_AT_TIME_LEFT`(둘 다
   waveEvents.js에서 export)로 계산.
7. **조합 스택 정리 방향이 반대로 되어 있던 버그(재수정)**: 저번 라운드에서
   "조합 결과를 기존 자리로 옮겨야 한다"고 고쳤다고 생각했는데, `consolidateHeroStacks`
   (개수 내림차순 정렬, 동점이면 배열 순서=칸 왼쪽 위부터)에 그대로 위임하다 보니
   방금 조합된 칸과 기존 칸이 개수가 같으면(둘 다 1마리) 배열에서 먼저 나오는 칸이
   이겨서 여전히 "새 자리"가 기준이 되는 경우가 있었다. `synthesize()`에서
   **다른 칸에 이미 그 영웅이 있으면 그 칸을 직접 찾아 새 개체를 그 칸에 push하고**,
   합성이 일어난 칸은 그대로 비우도록 명시적으로 고쳤다(제네릭 함수에 위임하지 않음).
8. **신화 팝업(즉시소환) 왼쪽 아이콘 위치가 너무 위**: `.favorite-bar`의 `top`을
   8%→17%로(칸 2개 높이만큼) 내렸다.
9. **칸 선택 기준을 개체 하나에서 칸 전체로**: 3마리가 쌓인 칸을 다른 칸으로 옮기려면
   3번 드래그해야 하던 문제. `ui.selectedInstanceId`(개체 ID)를
   `ui.selectedSlot`(`{row,col}`)으로 바꾸고, `moveHero()`의 시그니처를
   `(state, instanceId, toRow, toCol)`에서 `(state, fromRow, fromCol, toRow, toCol)`
   로 바꿔서 대상 칸이 비어 있으면 스택 전체를 한 번에 옮기도록 했다.
10. **전설~불멸 강화 시 마마 임프 생성 속도 2배**: `tickMamaImps()`에서
    `state.globalEnhance.legendary > 0`이면 간격을 절반으로.
11. **불멸 조건 충족 시 칸 아래 돌파/승급 버튼 대신 왼쪽 빨간 원**: `isImmortalPromotionReady()`
    가 true면 마마의 "돌파" 버튼도 숨기도록(승급 시도 버튼은 저번 라운드에 이미
    제거됨) 추가 가드. 왼쪽 즉시소환 바의 "승급 가능!" 아이콘이 지금(신화) 그림을
    보여주고 있던 것도 버그였음(사용자: "빨간 테두리 원 안에 불멸 그림이 있어야
    하는데 지금은 보라색 원에 신화 그림") - `heroDef.immortalCondition.id`로
    승급될 불멸 heroDef를 찾아 그 그림을 쓰고, 테두리색도 보라(#a855f7)에서
    빨강(#e53935)으로 변경.
12. **영웅 숫자에 따른 몬스터 제거 속도 재조정**: 고정 0.5/초(필드에 영웅 1마리
    이상이면)였던 걸 `MONSTER_KILL_RATE_PER_HERO_PER_SEC`(영웅 1마리당 초당
    처치량)로 바꿔서 영웅이 없으면 처치가 0이 되고, 많을수록 빨라지게 했다.
    "10라운드(150초 보스 라운드)가 2분24초(144초) 안에 0마리가 될 수 있어야
    한다"는 조건에 맞춰, 라운드별 영웅 수를 1→30(약 5~6라운드 만에 필드 참)으로
    가정해 시뮬레이션한 결과 0.04/초/마리로 맞추면 약 116초 만에 0에 도달함을
    확인(영웅 5마리만 있어도 20라운드까지 패배하지 않음도 함께 확인).
13. (2번과 동일 - 함께 수정)
14. **룰렛 성공 시 뽑힌 영웅 그림 표시**: 실패하면 해골이 뜨듯, 성공하면
    `ui.rouletteSuccessHero`에 결과 heroId를 담아 0.5초간 해당 영웅 그림을
    보여준다(실패 해골과 동일한 정적 표시 패턴 - 깜빡임 방지 이유는 기존 주석 참고).
15. **선택 상태가 판매 등 버튼 클릭 후 사라지던 문제**: 9번에서 선택을 칸 기준
    (`ui.selectedSlot`)으로 바꾸면서, 판매/채드먹이기/2차변신 등 액션 버튼 안에
    있던 `ui.selectedInstanceId = null` 명시적 리셋을 전부 제거했다 - 이제 그
    칸에 뭔가 남아있는 한 선택이 자연히 유지되고, 칸이 완전히 비면(마지막 1마리를
    팔았거나 등) `selectedInstance()`가 스스로 감지해서 자연스럽게 선택 해제된다
    (명시적 초기화가 아니라 "칸이 비었다"는 사실에서 파생되는 부수 효과).
16. **칸 선택 후 다른 곳을 눌러도 해제가 안 되던 문제**: 루트 `pointerdown`
    핸들러에 분기를 추가 - 클릭 대상이 `.field-slot`도, 그 칸의 액션 버튼
    (`.cell-quick-actions`)도, 채드 화살표(`.chad-arrow-layer`)도 아니면
    `ui.selectedSlot`을 비운다.
17. **캐릭터끼리 교차 이동(자리 맞바꿈) 불가 → 가능하게**: `moveHero()`를 슬롯
    기준으로 다시 짜면서(9번과 같은 리팩터) 대상 칸에 다른 영웅이 있으면 두 칸의
    내용을 통째로 스왑하도록 구현. Playwright로 실제 두 칸의 내용물(영웅 이름)이
    정확히 맞바뀌는지 확인.
18. **인디 보물 위치가 안 보이고 같은 자리에서 연속 발굴이 가능하던 버그**: 위치는
    이미 필드 임의의 칸에 랜덤 등장하고 있었지만(기획서대로), 화면 표시가 칸 구석의
    작은 💰 이모지 하나뿐이라 눈에 안 띄었다 - 칸 전체가 노란색으로 펄스 글로우
    (`.treasure-mark`, radial-gradient + opacity 애니메이션)하도록 바꿨다.
    "연속 발굴 가능" 버그는 `digTreasure()`가 발굴 성공 후 `indyTreasure.slot`을
    안 비워서 같은 자리에서 타이머가 돌 때까지 계속 재발굴이 가능했던 것 - 발굴
    성공 시 슬롯을 비우고 타이머도 `INDY_TREASURE_INTERVAL_SEC`로 다시 채워서,
    발굴 시점 기준으로 정확히 쿨타임 뒤에 새 위치가 등장하게 했다.

## 스택 선택 표시/캐릭터 고정 크기/버튼 확대 (PR #24 배포 후)

PR #24 배포 후 실제 스크린샷과 참고 이미지(흰 테두리로 묶인 임프 무리, 확대된
판매/합성 버튼)를 보내며 3개를 지적했다.

1. **칸 선택 시 쌓인 개체마다 따로 테두리가 그려지던 문제**: 3마리가 쌓인 칸을
   선택하면 각 캐릭터 이미지마다 개별 outline이 그려지고 있었다(`.stage-hero-token.
   selected .stage-hero-image`). 참고 이미지처럼 **몇 마리든 무리 전체를 감싸는
   흰 테두리 하나**로 바꿨다 - 개체별 `.selected` 클래스를 없애고, 칸이 선택됐을 때
   `renderHeroTokenLayer`가 토큰 무리의 가로 범위(clusterWidth)를 계산해서 그
   범위만큼의 `.stage-hero-selection-halo`를 무리 뒤(z-index가 토큰보다 1 낮음)에
   한 번만 그린다.
2. **캐릭터 크기가 1마리/3마리 칸마다 달라지던 문제**: 예전엔 `subWidth = 칸 너비 /
   마리 수`로 계산해서 3마리가 쌓이면 각 캐릭터가 눈에 띄게 작아졌다. 사용자 지정대로
   **크기를 마리 수와 무관하게 항상 고정**시켰다(`HERO_TOKEN_WIDTH_RATIO = 0.7`,
   칸 너비의 70% - 기존 100% 대비 30% 축소). 여러 마리는 폭을 나누는 대신 고정 폭
   토큰을 `HERO_TOKEN_STACK_STEP_RATIO`(토큰 폭의 42%)만큼씩 겹치며 칸 중앙 기준
   좌우로 살짝 흩어지게 배치한다 - "겹쳐져도 된다"는 사용자 지침대로 폭이 좁은 칸
   안에서도 여러 마리가 자연스럽게 겹쳐 보인다. Playwright로 필드의 모든
   `.stage-hero-token` width 값을 수집해 단일 값으로만 나오는지 확인해 검증했다.
3. **판매/합성 버튼 크기 확대**: `.cell-quick-sell`/`.cell-quick-synth`에
   `padding:6px 16px; font-size:15px; border-radius:10px; border-width:3px`를
   추가해 다른 `.cell-quick-btn`보다 눈에 띄게 크게 만들었다(참고 이미지 그대로).

## 임프 크기/마마 상태 배지/3마리 삼각 대열 (PR #25 배포 후)

PR #25 배포 후 마마를 선택한 스크린샷과 참고 이미지(삼각형으로 겹쳐진 3마리 캐릭터)를
보내며 3개를 지적했다.

1. **임프 크기를 절반으로 축소**: `renderHeroTokenLayer`에 `IMP_TOKEN_SCALE = 0.5`를
   추가해서, 칸의 첫 occupant가 `IMP_HERO_ID`(마마 전용 소환체)면 `tokenWidth`/
   `tokenHeight`에 이 배율을 곱해 다른 캐릭터의 절반 크기로 그리게 했다(칸에 임프만
   쌓이므로 첫 occupant 기준 판정으로 충분).
2. **마마 선택 시 뜨는 텍스트 배지 전부 제거**: "불멸 0 · 임프: 2/9" 같은 배지가
   `renderCellQuickActions`에서 자동으로 붙고 있었는데, 임프가 이미 필드에 실제
   캐릭터로 보이는 지금은 중복 정보라 `instance.heroId !== 'm_mama'`일 때만
   `cell-status-badge`를 붙이도록 가드를 추가했다(마마 외 다른 영웅의 상태 배지는
   그대로 유지 - 타르 단계 등은 여전히 필요).
3. **3마리 스택을 일렬이 아니라 삼각형 대열로**: `stackOffsets(n)` 헬퍼를 추가해
   n===3일 때 뒤 2마리(좌우로 벌어짐) + 앞 1마리(중앙, 아래로) 배치를 반환하게 했다 -
   DOM에 뒤→앞 순서로 그리기만 하면 같은 칸 안에서는 z-index 없이도 나중에 그려지는
   앞쪽 캐릭터가 자연히 위에 겹쳐 보인다(칸 간 z-index는 기존처럼 행 번호 기반 유지,
   칸 안에서는 DOM 순서로만 앞/뒤를 가른다 - 별도 스택 컨텍스트 충돌 위험이 없어서
   가장 간단한 방법이었다). **추가로 요청받은 "3마리가 차면 흰 테두리(일반~영웅
   등급만)"는 선택 여부와 무관하게 항상 켜지는 것으로 해석**해서, 기존 선택 시에만
   뜨던 `.stage-hero-selection-halo`를 `selected || readyToCombine`(3마리 +
   일반/희귀/영웅 등급) 조건으로 확장했다 - "합성 가능" 상태를 상시 안내하는
   용도로도 겸하게 된다.

## 즐겨찾기 별표/공격 모션/0라운드 연출/프리셋 UI/인디 보물 조건/베인 궁 랜덤화/삼각 대열 재수정 (PR #26 배포 후)

PR #26 배포 후 스크린샷 4장(왼쪽 즐겨찾기 바, 돈주머니 마커, 넓게 퍼진 삼각 대열+사각
테두리, 조밀하게 뭉친 기계 캐릭터 3마리 참고 이미지)을 보내며 7개를 지적했다. 특히
7번은 바로 이전 PR(#26)에서 구현한 "3마리 삼각 대열 + 상시 흰 테두리"를 정면으로
되돌리는 정정이었다("삼각형 대열을 다 띄워서 하지 말고... 붙여놓으라고. 외곽선을
따라 흰색 테두리 쓰랬지 누가 네모로 하랬냐").

1. **왼쪽 즉시소환/승급 아이콘에도 즐겨찾기 별표 표시**: `renderFavoriteBar`가
   `state.heroSettings`에서 즐겨찾기된 heroId 집합을 만들어 아이콘마다
   `.favorite-icon-star`(★, 우상단 절대배치)를 조건부로 붙였다.
2. **캐릭터 "깜빡임"을 공격 모션(스쿼시-스트레치)으로 교체**: 원인은
   `.stage-hero-token`에 걸려 있던 `animation: token-pop 0.12s ease-out`였다 - 게임
   루프가 0.2초마다 전체 DOM을 다시 그리는 구조라(main.js) 캐릭터 `<div>`가 매번
   재생성되면서 이 팝인 애니메이션이 매번 처음부터 다시 재생돼 깜빡이는 것처럼
   보였다(몬스터 이동/궁 링과 같은 계열의 함정, 이번엔 그 반대로 "0.12s짜리 1회성
   애니메이션을 매번 재생시키는" 형태로 나타났다). `token-pop`을 완전히 제거하고
   `.stage-hero-image`에 `transform-origin:bottom center` +
   `animation: stage-hero-attack 900ms ease-in-out infinite`(scaleY로 위아래
   스쿼시-스트레치)를 상시 재생시키되, `animation-delay`는 `hashPhase(instanceId,
   900)`(개체별 고정 위상 - 매번 같은 값)와 `Date.now()`(실제 경과 시간)를 조합해
   매 렌더 다시 계산해서 인라인 style로 넣는다 - 이렇게 하면 DOM이 재생성돼도
   애니메이션이 끊기지 않고 이어진다(몬스터 이동/궁 링에서 이미 검증된 패턴을
   재사용). Playwright로 `animation-delay` 값을 1초 간격으로 두 번 샘플링해서
   정확히 그만큼 증가하는지(멈추거나 리셋되지 않는지) 확인해 검증했다.
3. **0라운드부터 카운트다운/보라색 굴 이펙트가 나와야 함**: `renderHoleEffects`의
   웨이브 가드가 `state.wave < 1`이라 라운드 0(게임 시작 직후, 첫 웨이브 시작 전)에는
   `.hole-vortex`(보라색 소용돌이)도 `.hole-countdown`(카운트다운 배지)도 아예 안
   그려지고 있었다 - `state.wave < 0`으로 바꿔서(웨이브 번호가 음수일 수 없으니
   사실상 항상 통과) 0라운드부터 표시되게 했다. 카운트다운 배지 자체의 조건
   (`waveTimeLeft<=5`)은 이미 있어서 추가 로직 없이 게이트만 풀면 됐다.
4. **시작 화면 프리셋 UI 재점검**: `presetStore.js`에 `getDefaultPresetId()`/
   `setDefaultPreset(id)`(localStorage에 프리셋 id 하나만 별도 저장, 그 프리셋이
   삭제되면 `deletePreset`이 자동으로 기본값도 같이 해제)를 추가했다.
   `HeroSelectScreen`은 화면을 열 때 기본값 프리셋이 있으면 자동으로 그 설정을
   불러온 상태로 시작하고, 프리셋 바에 "기본값으로 설정/해제" 버튼을 추가했다(선택된
   프리셋이면 드롭다운 항목 앞에 `★`를 붙여 표시). **함정**: `<select>`도 다른
   컴포넌트처럼 렌더마다 `root.innerHTML=''`로 통째로 재생성되기 때문에, "지금
   드롭다운에서 뭘 골랐는지"를 DOM(`loadSelect.value`)에서 읽으면 매번 재생성 직후
   `''`(첫 옵션)로 초기화돼 있어서 기본값/삭제 버튼이 실제로는 항상 비활성 상태로
   보이는 문제가 있었다 - `presetSelection` 변수로 선택 상태를 별도로 기억해두고,
   `<option>`에 `selected: p.id === presetSelection`을 명시해서 재생성 후에도
   드롭다운이 이전 선택을 유지하도록 고쳤다. 프리셋 바 자체도 `.options` 섹션보다
   위(시작 버튼 바로 아래)로 옮기고, 패딩/폰트/버튼 크기를 키우고 강조 테두리를
   추가했다(사용자 지적 - 너무 작고 맨 밑에 있었음).
5. **인디가 필드에 없는데도 보물(돈주머니) 마커가 뜨던 버그**: `tickIndyTreasure`가
   인디 소환 여부와 무관하게 항상 타이머를 돌리고 있었다 - 필드에 `m_indy` 인스턴스가
   있는지(`state.field.some(...)`) 확인해서, 없으면 타이머를 멈추고 남아있던 마커도
   지우도록 고쳤다(인디가 다시 나타나면 그 시점부터 다시 30초 타이머가 새로 시작).
6. **베인 궁 이펙트 지속시간 3초 + 발동 조건을 왕복 15~20회 랜덤으로**:
   `ULTIMATE_FLASH_MS`를 1200→3000으로. `IMMORTAL_CONDITIONS.m_bane`의 발동 조건은
   기존에 이동 11회 고정 비율(`moveToUltimateRatio`)로 나눠떨어질 때마다 터지는
   방식이었는데, `extra.ultimateThresholdMin/Max`(15/20)로 바꾸고
   `immortal.js`의 `recordImmortalEvent`가 개체별로 `instance.nextUltimateAt`(다음
   임계치)을 저장해뒀다가 progress가 그 값을 넘길 때마다 궁 이펙트를 띄우고 다시
   15~20 중 랜덤으로 다음 임계치를 새로 뽑도록 했다(고정 비율이 아니라 매번 다시
   랜덤 - 사용자 지정). 불멸 승급 자체에 쓰이는 `target:132`는 별개 수치라 그대로
   유지했다(궁 이펙트는 순수 연출용 서브 카운터).
7. **삼각 대열을 조밀하게 좁히고, 사각 테두리 대신 실루엣을 따라가는 외곽선으로,
   캐릭터가 칸 밖으로 나가지 않게**: 바로 이전 라운드(PR #26)에서 구현한
   `HERO_TOKEN_HEIGHT_RATIO=1.75`(칸 위로 삐져나옴), 넓은 삼각 오프셋(뒤 ±0.5/앞
   0.22), 무리 전체를 감싸는 사각 `<div>`(`.stage-hero-selection-halo`) 세 가지를
   전부 사용자가 명시적으로 거부했다("누가 네모로 하랬냐", "칸 안에 무조건
   들어가있어야 해"). `HERO_TOKEN_HEIGHT_RATIO`를 0.8(칸 높이 이내), `WIDTH_RATIO`를
   0.44로 낮추고, `stackOffsets(3)`의 오프셋을 뒤 `±0.3/-0.2`, 앞 `0/0`(칸 바닥선
   그대로, 더 내밀지 않음)으로 좁혔다 - 이 조합이면 항상 칸 안에 완전히 들어간다는
   걸 좌표 계산으로 미리 검증했다(토큰 폭의 최대 오프셋+반폭 합이 칸 반폭 0.5보다
   항상 작음). 사각 halo `<div>` 생성 로직은 삭제하고, 대신 선택/합성가능
   상태(`outlined` 플래그)일 때 각 캐릭터 `<img>`에 흰색 `drop-shadow`를 8방향으로
   얇게 겹쳐 쌓는 `outlineFilter()`를 적용했다 - `drop-shadow`는 알파 채널이 있는
   픽셀 가장자리에만 번지므로 사각 박스가 아니라 캐릭터 실루엣을 따라가는 외곽선처럼
   보인다. **여기서 CSS filter가 클래스 여러 개를 동시에 걸어도 합쳐지지 않고
   마지막 것만 적용되는 문제**(디버프 틴트/궁 발광/선택 외곽선이 동시에 필요할 수
   있음)를 같이 해결해야 해서, `heroImage()`에 `style` 파라미터를 새로 추가하고
   `renderHeroTokenLayer`가 상태별 filter 문자열을 전부 합성해서 인라인 style로
   넣는 방식으로 바꿨다(디버프/궁 이펙트의 outline·클래스는 filter가 아닌 부분만
   CSS에 남겨둠 - `.debuffed`의 보라 `outline` 테두리, `.ultimate-flash::after`의
   링 확산 애니메이션). Playwright로 필드에 여러 영웅을 소환한 뒤 토큰 좌표가 항상
   칸 좌표 범위 안에 있는지, 선택 시 `filter`에 흰 `drop-shadow`가 여러 개 붙는지
   확인해 검증했다.

## 디버프 칸 단위/불멸 조건 숨김/즐겨찾기 정렬/불멸 펫 가정/라운드1 골드 버그/외곽선 축소 (PR #27 배포 후)

PR #27 배포 후 8개를 지적했다(스크린샷은 일시정지/나가기 버튼 근접 크롭과 흰 테두리가
너무 커진 필드 스크린샷 2장).

1. **"공격 당했을 때 당하는 시간"을 20초로 통일**: 이동불능(즉시형 `IMMOBILIZE_INSTANT_SEC`,
   게이지형 `IMMOBILIZE_GAUGE_LOCK_SEC`)과 디버프(`DEBUFF_MARK_SEC`)가 각각
   5/10/3초로 제각각이었다 - 전부 기획서에 정확한 수치가 없는 플레이스홀더였는데,
   사용자가 "일단 20초로 통일해두자"고 지정해서 세 값 다 20으로 맞췄다. 게이지형의
   "차오르는 시간"(`IMMOBILIZE_GAUGE_FILL_SEC`, 5초)은 당하는 시간이 아니라 발동 전
   예열 시간이라 별개로 그대로 뒀다.
2. **모든 신화 캐릭터의 불멸 조건 진행도 표시 제거**: 예전엔 "마마만" 상태 배지를
   완전히 숨겼었는데(임프가 필드에 실제 캐릭터로 보여서 중복 정보라는 이유), 사용자가
   "마마뿐만이 아니라 다른것들도 다 하지마"라고 확장 지적했다. `renderCellQuickActions`
   에서 `statusParts.push('불멸 N/target')` 줄 자체를 삭제해서 어떤 신화를 클릭해도
   불멸 진행도 숫자가 안 뜨게 했다(타르 단계 라벨이나 인디 보유 보물 같은, "불멸
   조건"이 아닌 다른 상태 정보는 그대로 유지 - 마마만 예외적으로 배지 전체를 숨기는
   기존 로직도 유지).
3. **왼쪽 즐겨찾기 즉시소환 바는 검증해보니 이미 정상 동작(정렬 로직 자체는 버그
   아니었음), 신화 팝업 그리드에 즐겨찾기 정렬이 아예 빠져 있던 게 진짜 문제**:
   `favoriteBarItems()`의 즐겨찾기 우선 정렬(`Number(favoriteIds.has(b))-
   Number(favoriteIds.has(a))`)을 Playwright로 실제 소환→필드 배치까지 재현해서
   검증했더니 정확히 즐겨찾기 항목이 앞으로 오는 걸 확인했다(코드상 로직도 이미
   맞았음). 반면 `renderMythicGrid`/`renderImmortalGrid`(신화 버튼을 눌렀을 때 뜨는
   전체 등급 그리드)는 `heroesByTier()` 원본 순서 그대로라 즐겨찾기가 전혀 반영되지
   않고 있었다 - 사용자가 "왼쪽에 즐겨찾기된 게 맨 위로 안 올라간다"고 한 게 아마
   이쪽이었을 것으로 보고 `sortFavoriteFirst()` 헬퍼를 추가해 두 그리드 모두
   즐겨찾기 우선 정렬을 적용했다(불멸 그리드는 `baseHeroId` 기준으로 원본 신화의
   즐겨찾기 여부를 본다). **만약 이걸로도 해결이 안 됐다면 즉시소환 바 쪽에 진짜
   다른 문제(승급 후보가 항상 조합 후보보다 우선하는 기존 규칙과의 혼동 등)가 있을
   수 있으니 다시 스크린샷으로 확인이 필요하다.**
4. **시작 화면에서 "불멸 펫 보유" 선택 제거**: 항상 보유한 것으로 가정하기로
   확정되어(사용자 지정) 라디오 버튼 UI를 없애고 `local.immortalPet`을 `true`로
   고정했다(프리셋을 불러와도 이 값은 더 이상 덮어쓰지 않음 - 예전 프리셋에 false가
   저장돼 있었어도 무시). 실제 게임 로직에서 이 값을 소비하는 곳이 없어서(현재는
   `GameState.immortalPet`에 저장만 되는 상태) 안전하게 상수화할 수 있었다.
5. **디버프가 칸이 아니라 개체 하나에만 걸리던 버그**: `handleImmobilizeEvent`와
   별개인 보스 일반공격 디버프가 `state.field.flatMap(occupants)`에서 무작위
   개체(instance) 하나를 뽑아 그 `instanceId`만 표시하고 있었다 - 한 칸에 3마리가
   쌓여 있어도 그 중 1마리만 보라색으로 물들었다. 대상을 개체가 아니라 **칸**
   (`{row, col}`)으로 뽑도록 바꾸고(`occupiedSlots`에서 랜덤 선택),
   `GameScreen.js`의 `debuffed` 판정도 `instanceId` 일치가 아니라 `slot.row/col`
   일치로 바꿨다 - 이제 그 칸에 몇 마리가 있든 전부 같이 물든다. Playwright로 한
   칸에 동일 영웅 3마리를 쌓아두고(그 칸이 필드의 유일한 점유 칸이 되도록 만들어서
   랜덤 타겟팅과 무관하게 검증 가능하게 함) 디버프가 발동하는 순간까지 기다려
   3마리 전부 `.debuffed` 클래스가 붙는지 확인했다.
6. **일시정지/나가기 버튼을 더 붙임**: 이미 같은 `.stage-controls` 컨테이너 안의
   flex 형제였지만(`gap:2vw`), 사용자가 "떨어져 보인다"고 지적해서 `gap`을 `0.8vw`로
   좁혔다. 시간 박스(`.top-badge`)는 왼쪽에 별도로 떠 있고 이 변경은 두 버튼을
   서로 더 가깝게(오른쪽으로 몰리는 방향과 무관하게 둘 사이 간격만) 좁히는
   거라 시간 박스 침범과는 무관하다 - Playwright로 좌표를 재서 시간 박스와 버튼
   묶음 사이에 여전히 여백이 있는지 확인했다.
7. **0→1라운드 시작 골드가 2284가 아니라 그보다 많이 들어오던 버그**: `applyRoundEndReward`
   의 최초 지원금 지급 로직이 `state.gold += FIRST_WAVE_SUBSIDY`로 **더하기**였다 -
   시작 골드(`STARTING_GOLD=250`) 위에 2284를 얹어서 2534가 되고 있었다. "고정값"이라는
   기획 문구(CLAUDE.md 자체 기록에도 "고정"이라고 적어뒀었음)를 오독한 것 - 0라운드에
   아무것도 안 썼다면 1라운드 시작 금액이 정확히 2284"여야" 한다는 사용자 확인에 맞춰
   `state.gold = FIRST_WAVE_SUBSIDY`(대입)로 고쳤다. Playwright로 실제 게임을
   시작해서 배속으로 라운드 1 진입 시점까지 돌린 뒤 골드 표시가 정확히 2284인지
   확인해 검증했다.
8. **흰색 선택 외곽선이 너무 커서 가시성이 떨어지던 버그**: 원인은 CSS
   `filter: drop-shadow(A) drop-shadow(B) ...`가 각 항을 원본이 아니라 "그 앞까지
   누적된 결과물"에 다시 적용하는 체이닝 방식이라는 걸 놓친 것이었다 - 8방향
   drop-shadow를 겹쳐 쌓으면 오프셋들이 서로 누적되어 실제 렌더 크기가
   `OUTLINE_OFFSET_PX`(2px) 하나보다 몇 배 크게 부풀어 보였다(사용자 참고
   스크린샷의 캐릭터를 거의 다 덮는 흰 얼룩). `OUTLINE_OFFSET_PX`를 0.6px로 줄이고
   방향도 8개(대각선 포함)에서 4개(상하좌우)로 줄여서 누적되는 항 자체를 줄였다 -
   결과적으로 실루엣을 얇게 따라가는 작은 테두리로 돌아왔다. **교훈**: 여러 개의
   `drop-shadow()`를 체이닝할 때는 "오프셋들이 원본 기준으로 병렬 적용된다"고
   가정하면 안 된다 - 실제로는 직렬(순차) 누적이라 오프셋 개수와 크기가 늘수록
   기하급수적으로 부풀어 보일 수 있다.

## 3마리 한정 외곽선/캐릭터 3배 확대+중앙 배치/판매 보너스/골드 정정 재확인 (PR #28 배포 후)

PR #28 배포 후 7개를 지적했다.

1. **흰색 외곽선이 1~2마리일 때도(선택 시) 뜨던 버그**: 직전 라운드에 `outlined =
   selected || readyToCombine`으로 짜뒀었는데, 사용자가 "흰색 테두리는 3마리 있을
   때만 하라 했다"고 명시적으로 정정했다 - 선택 여부(`selected`)는 외곽선 판정에서
   완전히 빼고 `outlined = readyToCombine`(3마리 + 일반/희귀/영웅 등급)만 남겼다.
   이제 칸을 선택해도 3마리가 안 차 있으면 외곽선이 안 뜬다.
2. **캐릭터 크기 3배 확대**: `HERO_TOKEN_HEIGHT_RATIO`(0.8→2.4), `HERO_TOKEN_WIDTH_RATIO`
   (0.44→1.32) 둘 다 3배로. 몇 라운드 전에 확정했던 "캐릭터는 칸 안에 완전히
   들어가야 한다"는 제약이 이번 요청으로 뒤집힌 것이니 헷갈리지 말 것 - 이제는
   캐릭터가 칸 위아래로 크게 넘치는 게 정상이다.
3. **배치 기준을 칸 바닥선에서 칸 정중앙으로**: `baseTop`을 `rect.top + rect.height -
   tokenHeight`(바닥선 정렬)에서 `rect.top + rect.height/2 - tokenHeight/2`(정중앙
   정렬)로 바꿨다. 캐릭터 이미지 자체의 `object-position`/`transform-origin`도
   `bottom center`에서 `center center`로 맞췄다. **발밑 그림자(`.stage-hero-shadow`)가
   덩달아 문제였다** - 예전엔 "토큰 박스 하단 = 칸 바닥선"이라는 전제로 박스 하단
   기준 고정 %(`bottom:3%`)였는데, 이제 박스가 훨씬 커지고 중앙 정렬이라 박스 하단이
   칸 바닥선보다 한참 아래로 밀려났다 - 그대로 뒀으면 그림자가 캐릭터와 동떨어진
   곳에 둥둥 떠 보였을 것. `shadowBottomPct = ((HEIGHT_RATIO*scale - 1) / (2*HEIGHT_RATIO*scale))
   * 100`로 "칸 바닥선이 지금 박스 안에서 하단 기준 몇 %에 오는지"를 역산해서 매
   렌더 인라인 style로 넣는 방식으로 바꿨다(마마 임프처럼 `sizeScale`이 다른 경우도
   같은 공식으로 자동 대응됨).
4. **일반~전설 판매 공통 보너스 신규 추가**: 판매(`sellHero`) 시 6% 확률로 행운석 1개
   추가 지급(`SELL_BONUS_LUCKSTONE_CHANCE`, `synthesis.js`) - 등급 불문 공통 시스템
   (사용자 지정 사항, 기존 등급별 보상표와는 별개로 항상 굴림).
5. **간헐적으로 게임이 튕기는 문제 - 원인 미상, 보류**: 사용자가 "왜 그런지 모르겠다"며
   재현 조건 없이 리포트했다. 이 세션에서는 원인 규명/수정을 진행하지 않았고 기억만
   해두기로 했다 - 나중에 재현 스텝이나 스크린샷/에러 로그를 받으면 그때 조사할 것
   (`main.js`의 게임 루프, `structuredClone` 깊은 복제 비용, 또는 브라우저 메모리
   누수 등이 후보로 의심되지만 아직 근거 없음).
6. **이동불능 게이지형(빨간 원)이 5초 기다린 뒤 공격하는지 재확인 요청 - 이미 정확히
   그렇게 구현돼 있었음**: `handleImmobilizeEvent`의 `filling`(빨간 원이 `conic-gradient`로
   `IMMOBILIZE_GAUGE_FILL_SEC`=5초 동안 차오름) → `active`(실제 이동불능 잠금 시작,
   "공격") 전이 로직과 CSS(`--fill-sec`가 JS에서 동적으로 5초로 주입됨)를 다시 읽고
   확인한 결과 사용자가 설명한 동작과 정확히 일치했다 - 코드 변경 없음(질문/확인
   차원의 요청이었던 것으로 보임).
7. **외곽선이 지난 라운드 수정(0.6px/4방향)으로 아예 안 보이게 된 문제**:
   `OUTLINE_OFFSET_PX`를 0.6→1.5로 다시 키웠다(방향 수는 4개 유지) - Playwright로
   스크린샷을 찍어 눈으로 직접 확인하면서 "너무 크지도 안 보이지도 않는" 지점을
   찾았다. **교훈**: 이 값은 배율(`filter` 체이닝 특성상 방향 수×오프셋이 비선형적으로
   결과에 영향을 줌)과 캐릭터 렌더 크기(이번 라운드에 3배로 커짐) 양쪽에 다 민감하니,
   앞으로 토큰 크기나 방향 수를 바꿀 때마다 스크린샷으로 재검증할 것 - 숫자만 보고
   가늠하면 계속 너무 크거나 안 보이거나를 오갈 수 있다.

## 캐릭터 크기 3배가 과했다는 정정 - 발끝 기준 배치로 재설계 (PR #29 배포 후)

PR #29 배포 후 스크린샷 1장을 보내며 "3배는 너무 크다"고 정정했다 - 직전 라운드에
"캐릭터를 3배 키우고 칸 정중앙(박스 전체)에 배치하라"고 했던 걸 이번엔 "적당한
크기로, 그리고 박스 전체가 아니라 캐릭터의 맨 밑선(발)을 기준으로 칸 중앙에
정렬하라"고 구체적으로 다시 지정했다.

**배치 기준 변경**: `baseTop`을 "박스 전체가 칸 중앙에 오도록"(`rect.top +
rect.height/2 - tokenHeight/2`)에서 "박스 하단(발끝)이 칸 중앙에 오도록"(`rect.top
+ rect.height/2 - tokenHeight`)로 바꿨다 - 박스를 칸 중앙에서 위로 끌어올리는
방식이라 몸통/머리는 칸 위로 넘치고 발은 칸 안(정중앙)에 남는다.
`object-position`/`transform-origin`도 `center center`에서 다시 `bottom center`로
되돌렸다(이미지 발밑이 박스 하단=발끝 위치에 오도록). 발밑 그림자도 "박스 하단이
곧 발끝"이라는 전제가 다시 성립하니 매 렌더 계산하던 `shadowBottomPct` 로직을
지우고 고정 `bottom:-2%` CSS로 되돌렸다 - 토큰 크기 배율(sizeScale)이 달라져도
박스 하단은 항상 발끝이라 재계산이 필요 없어졌다.

**크기 산출 방식**: "3마리 들어갔을 때 맨 밑선들이 한 칸 안에 다 들어올 만한
크기"라는 조건을 역산했다. `stackOffsets(3)`의 좌우 오프셋이 `±0.3*tokenWidth`이고
캐릭터 폭이 `tokenWidth`이므로, 3마리의 가로 전체 스팬은
`2*(0.3+0.5)*tokenWidth = 1.6*tokenWidth` - 칸 너비(1.0) 안에 들어오려면
`tokenWidth <= 0.625`. 여유를 두고 `HERO_TOKEN_WIDTH_RATIO = 0.55`(스팬 0.88, 칸
폭의 88%)로 잡고, 높이는 예전 비율(0.8/0.44≈1.82)을 유지해서
`HERO_TOKEN_HEIGHT_RATIO = 1.0`으로 뒀다(정확히 칸 높이 1배). Playwright로 3마리
스택의 각 토큰 박스 좌우 경계가 전부 칸의 가로 범위 안에 들어오는지 좌표로
검증했다. **교훈**: "칸 안에 들어가야 한다"/"3배 키워라"/"발끝 기준 중앙 정렬,
3마리 발이 칸에 들어올 크기" - 캐릭터 크기/배치 요구사항이 세 번 연속 바뀌었다.
매번 새 요구를 이전 요구의 부분 수정으로 오해하지 말고, 사용자가 준 구체적 조건
(이번엔 "발끝 기준", "3마리 발이 한 칸에")을 수식으로 정확히 옮겨서 역산할 것 -
감으로 숫자를 고르면 계속 너무 크거나 작기를 반복하게 된다.

## 3마리 삼각 대열 그룹 중앙정렬/디버프 6칸 동시 발동 (PR #30 배포 후)

PR #30 배포 후 2개를 지적했다.

1. **3마리 삼각 대열이 무리 전체가 아니라 앞 1마리 기준으로 중앙정렬되던 문제**:
   직전 라운드에 "발끝 기준으로 칸 중앙 정렬"을 도입하면서, `baseTop`을 앞 1마리
   (`dy:0`)의 발끝이 칸 정중앙에 오도록만 계산했다 - 뒤 2마리(`dy:-0.2`)는 그
   앞 캐릭터를 기준으로 얹히는 것이라 결과적으로 무리 전체(뒤 캐릭터 머리~앞
   캐릭터 발끝)가 칸 중앙보다 위로 치우쳐 보였다. 사용자가 "3마리째가 되면 밑에
   있는 한 마리 기준이 아니라 전체를 중앙정렬해달라"고 정정했다. `stackOffsets(3)`
   기준으로 무리 전체 높이가 `1.2*tokenHeight`(뒤 top ~ 앞 bottom)이므로, 그 중심이
   칸 중앙에 오게 하려면 기존 "앞 1마리 발끝 정렬" 공식보다 `baseTop`을
   `0.4*tokenHeight`만큼 위로 올려야 한다는 걸 역산했다 - `n===3`일 때만 이 보정을
   적용하고 1~2마리는 기존 발끝 정렬 그대로 둔다(사용자가 3마리 케이스만 콕 집어
   지적했으므로). Playwright로 무리의 bounding box 중심 Y좌표와 칸 중앙 Y좌표
   차이가 1px 이내인지 확인해 검증했다.
2. **디버프도 이동불능(게이지형)처럼 한 번에 6칸씩 발동하도록 변경**: 기존엔
   점유된 칸 중 하나만 무작위로 골라 디버프를 걸었는데, 사용자가 "보라색도 한
   6칸씩 한번에 걸어"라고 요청했다 - 이동불능 게이지형이 이미 `pickRandomSlots(state,
   6)`으로 6칸을 뽑는 것과 동일한 패턴을 재사용해서, 점유된 칸 중에서만 뽑는
   `pickRandomOccupiedSlots(state, 6)`을 새로 만들었다(빈 칸을 뽑아봐야 디버프
   틴트를 보여줄 캐릭터가 없어 의미가 없으므로). `eventLog.debuffEvent`의 스키마도
   `{row, col}` 단일 값에서 `{slots: [{row,col}, ...]}` 배열로 바꿨고, `GameScreen.js`의
   `debuffed` 판정도 그 배열 안에 현재 칸이 포함되는지로 바꿨다. Playwright로
   8개 점유 칸(그 중 하나는 3마리 스택) 중 디버프가 발동했을 때 물든 개체 수가
   8개(6칸 중 3마리 스택 1개 + 나머지 단일 칸 5개 = 3+5=8)로 나와서, 정확히 6칸이
   대상이 됐음을 개체 수 역산으로 확인했다.

## 삼각 대열 재정렬/모바일 풀스크린 안 되던 버그/더블탭 확대 방지 (PR #31 배포 후)

PR #31 배포 후 스크린샷 1장과 함께 3개를 지적했다.

1. **3마리 삼각 대열 정렬을 "무리 전체 중앙정렬"에서 "뒤 2마리 밑선 정렬 + 그
   아래 1마리 추가"로 재정정**: 직전 라운드에 무리(뒤 2마리+앞 1마리) 전체의
   bounding box 중심을 칸 중앙에 맞추도록 `baseTop`을 보정했었는데, 사용자가
   실제 화면을 보고 "정렬이 흐트러져 보인다"며 더 단순한 규칙으로 바꿔달라고
   했다 - "위에 있는 2마리는 밑선을 가운데에 맞추고 그 아래에 한 마리를 추가하는
   걸로 가자." `baseTop`을 다시 마리 수 무관 공통 공식(`rect.top + rect.height/2 -
   tokenHeight`, 발끝이 칸 중앙)으로 되돌리고, `stackOffsets(3)`만 바꿨다 - 뒤
   2마리는 `dy:0`(다른 마리 수와 동일한 발끝 기준선), 앞 1마리만 `dy:0.2`로 그
   아래에 걸리게 했다. 직전 라운드의 "무리 전체 중앙정렬" 공식(`0.4*tokenHeight`
   보정)은 완전히 폐기됐다 - 대신 뒤 2마리의 발끝이 칸 중앙에 오는 게 새 기준이다.
   Playwright로 뒤 2마리의 바닥 좌표가 서로 같고(정렬됨) 칸 중앙과 일치하는지,
   앞 1마리가 그보다 아래에 있는지 확인해 검증했다.
2. **모바일에서 게임 화면이 풀스크린이 안 되던 버그**: `#app`에 `padding:12px`가
   사방으로 걸려 있어서 게임 화면(`.game-screen`)이 뷰포트 전체를 못 채우고
   사방 12px씩 줄어들어 있었다 - 시작 화면(`HeroSelectScreen`)에는 이 여백이
   필요하지만 게임 화면은 완전히 풀스크린이어야 한다. `#app`의 패딩을 제거하고
   `.hero-select-screen` 클래스 자체에 패딩을 옮겨서 시작 화면 여백은 그대로
   유지하면서 게임 화면만 진짜 풀스크린이 되게 했다. `.game-screen`의
   `height: calc(100dvh - 24px)`도 뺄 패딩이 없어졌으니 `height: 100dvh`로
   단순화했다. Playwright로 `.game-screen`의 bounding box가 정확히 뷰포트
   전체(390×844, x=0,y=0)를 채우는지 확인해 검증했다.
3. **더블탭하면 브라우저가 확대되던 문제**: 뷰포트 meta에 `maximum-scale=1.0,
   user-scalable=no`를 추가해서 페이지 자체의 확대/축소를 막고, `body`에
   `touch-action: manipulation`도 CSS 레벨의 이중 방어로 추가했다(뷰포트 meta만
   믿지 않고 제스처 자체도 막음 - 브라우저별 구현 차이에 대한 안전장치).
   `.field-slot`에 이미 걸려 있던 `touch-action:none`(드래그 이동용)은 그보다
   더 좁은 범위의 예외라 충돌 없이 공존한다.

## 즐겨찾기 무료 재소환 폐지/영웅 이미지 bbox 크롭/디버프 테두리 제거 (PR #32 배포 후)

PR #32 배포 후 3개를 지적했다.

1. **신화 조합 시 재료가 안 없어지던 버그 - 원인은 "즐겨찾기 무료 재소환" 기능
   자체였음**: `craftMythic()`(신화 팝업/왼쪽 바의 "조합 가능" 아이콘)은 실제로는
   항상 재료를 정상적으로 소모하고 있었다(Playwright로 직접 재현해서 확인함) -
   문제는 즐겨찾기로 등록한 신화를 한 번 조합하면 `unlockedInstantSummons`에
   등록되고, 그 뒤로는 왼쪽 바 아이콘이 "즉시 소환!"으로 바뀌어 재료 없이 무한
   재소환되는 예전 기능(`instantSummonFavorite()`, 기획서 문구 "조합 완료 시
   좌측 최상단에 소환 버튼 노출"을 그렇게 해석해서 몇 세션 전에 구현했었음)이
   원인이었다. 사용자가 "신화를 소환하는 방식은 재료를 먼저 없애고 신화를
   추가하는 것"이라고 예외 없이 명확히 정정해서, 이 무료 재소환 예외를 완전히
   없앴다 - `unlockedInstantSummons`(gameState 필드)와 `instantSummonFavorite()`
   (synthesis.js)를 통째로 삭제하고, 왼쪽 바의 "조합 가능" 아이콘은 항상
   `craftMythic()`만 호출하도록 통일했다. **예전 기획서 해석을 뒤집는 변경이니
   나중에 "즉시소환 버튼이 없어졌다"는 지적이 다시 나와도 이 결정을 기억할 것 -
   사용자가 실제 게임 기준으로 명시적으로 정정한 사항이다.**
2. **캐릭터 발밑 그림자가 공중에 떠 보이던 버그 - 원인은 영웅 이미지 파일 자체의
   내부 여백(패딩)**: `.stage-hero-shadow`는 토큰 박스 하단(발끝 기준선)에 이미
   붙어 있었는데도 그림자가 캐릭터와 떨어져 보였다 - 원인을 찾아보니 영웅
   초상화 PNG/WEBP 89장 중 상당수가 알파 채널 bbox 기준으로 **캔버스 하단에
   1.6~23%의 빈 여백**을 가지고 있었다(`Image.open(f).getbbox()`로 실측).
   `object-fit:contain + object-position:bottom`은 이미지 "캔버스"의 바닥을
   박스 바닥에 맞추는 것이지 캐릭터의 실제 발끝을 맞추는 게 아니라서, 여백이 큰
   이미지일수록 실제 발끝이 박스 바닥보다 한참 위에 그려지고 그 아래 빈 공간에
   그림자만 놓이는 꼴이 됐다. UI 이미지에서 이미 여러 번 겪었던 것과 같은 함정
   (CLAUDE.md의 "이미지 자산 매핑"/"버그 리포트 라운드" 섹션 참고 - "이 프로젝트에서
   흰 배경/여백 있는 이미지가 섞여 오는 건 반복되는 패턴") - 영웅 이미지에도 같은
   처리가 안 돼 있었을 뿐이었다. `public/heroes/*.webp|png` 89장 전부를 알파 bbox +
   2% 여백으로 다시 크롭해서 재저장했다(파이썬 스크립트로 일괄 처리, 79장이
   실제로 잘려나감 - 나머지 10장은 이미 여백이 없어서 no-op). **앞으로 새 영웅
   이미지를 받으면 이 크롭 작업을 항상 먼저 할 것 - 이 프로젝트에서 이미지 여백은
   예외가 아니라 기본값으로 취급해야 한다.**
3. **디버프(보라색) 표시에서 사각 테두리 제거**: `.stage-hero-token.debuffed
   .stage-hero-image { outline: 2px solid #a259ff; }` 규칙을 삭제했다(사용자
   지적 - "네모박스 하지 말고 그냥 색상만 보라색으로"). 색조 변경(sepia+hue-rotate+
   saturate)은 이미 GameScreen.js가 인라인 filter로 합성해서 넣고 있었으니 그것만
   남기고 테두리만 뺐다 - `debuffed` 클래스 자체도 이제 CSS에서 아무 데도 안 쓰여서
   토큰 div에 붙이던 것도 같이 정리했다(필터 판정용 `debuffed` boolean 변수는
   그대로 유지, DOM class만 제거).

## 신화/불멸 2.5배 확대+발끝 기준선/모바일 letterbox 제거 (PR #33 배포 후)

PR #33 배포 후 실제 모바일(카카오톡 인앱 브라우저) 스크린샷과 함께 2개를 지적했다.

1. **신화(+불멸) 캐릭터를 2.5배 키우고, 발끝을 일반 3마리 스택의 "앞 1마리" 기준선에
   맞춤**: 신화/불멸은 항상 칸당 1마리라(스택 없음) 일반~영웅에 적용한 "3마리가
   한 칸 폭 안에 들어와야 한다"는 폭 제약이 아예 해당되지 않는다 - 사용자가 이 점을
   근거로 "신화는 2.5배 키워도 괜찮다"고 확인했고, 뒤이어 "불멸도 신화랑 크기
   똑같으니 참고"라고 덧붙였다. `MYTHIC_TOKEN_SCALE = 2.5`를 추가하고
   `HEROES_BY_ID[...].tier`가 `mythic`/`immortal`이면 이 배율을 쓰도록
   `renderHeroTokenLayer`를 확장했다. 발끝 기준선은 "일반 3마리 스택일 때 아래에
   있는 애(앞 1마리) 발선"을 지목했다 - `stackOffsets(3)`의 앞 1마리는
   `dy:0.2`이므로 그 발끝 Y좌표는 `칸 정중앙 + 0.2*(기본 1배 칸 높이)`가 된다(수식:
   `baseTop_normal + 1.2*tokenHeight_normal`을 전개하면 `rect.top + rect.height*(0.5
   + 0.2*HERO_TOKEN_HEIGHT_RATIO)`). 신화/불멸의 `baseTop`도 이 계산식으로 맞추고
   본인 크기(2.5배)만큼 위로 끌어올렸다. Playwright로 재료를 모아 실제 신화를
   조합한 뒤, 같은 칸 크기 기준으로 신화의 발끝 Y좌표와 별도 테스트의 일반 3마리
   스택 앞 캐릭터 발끝 Y좌표가 소수점까지 정확히 일치하는지 확인해 검증했다(칸을
   공유하는 두 그룹을 한 화면에 같이 두면 겹쳐서 판별이 안 되니, 각각 필드에
   하나씩만 있는 상태로 따로 테스트해서 비교했다).
2. **모바일에서 화면 상하로 검은 여백(letterbox)이 남던 문제**: `sizeStageToFit()`이
   지금까지 "contain"(전체 배경이 다 보이도록, 뷰포트 비율이 688:1508과 안 맞으면
   남는 쪽에 여백)이었는데, 카카오톡 인앱 브라우저처럼 자체 주소창/상태바가
   뷰포트 높이를 많이 잡아먹는 환경에서 이 여백이 크게 두드러졌다(사용자가 실제
   스크린샷으로 확인시켜줌 - 위아래 검은 띠). "저 거먼거 없이 다 확대해서"라는
   요청에 맞춰 "cover"(꽉 채우고 안 맞는 쪽은 넘치는 만큼 잘림) 방식으로
   뒤집었다 - 가로세로 비율 비교 분기의 큰/작은 쪽 배정만 서로 바꾸면 됐다.
   `.game-stage-wrap`에 이미 걸려 있던 `overflow:hidden`이 넘치는 부분을 그대로
   가려준다. Playwright로 STAGE_RATIO(0.456)와 크게 다른 뷰포트(390×700, 비율
   0.557)에서 스테이지가 wrap 전체를 폭/높이 모두 채우는지 확인해 검증했다.
   **트레이드오프**: 뷰포트 비율이 원본과 많이 다르면 배경 그림의 위/아래(또는
   좌/우)가 잘려나갈 수 있다 - 이번엔 사용자가 명시적으로 "여백 없이 꽉 채워달라"고
   요청했으니 의도된 선택이다. 나중에 "화면 위쪽 요소가 안 보인다"는 지적이 오면
   이 트레이드오프 때문일 수 있다는 걸 기억할 것.

## 미션 10종 확정 보상/체크박스 UI + 신화 크기 재조정 (PR #34 배포 후)

PR #34 배포 후 미션 시스템 전체를 재작성하고 신화/불멸 크기를 다시 줄였다.

**미션 10종 확정**: 기존엔 3개 미션(강화 2회/판매 5회/전설 룰렛 실패 5회)만 있었고
전부 행운석 5 플레이스홀더 보상이었다. 사용자가 10개 미션의 이름/조건/보상을 전부
직접 지정해서 `src/data/missions.js`를 통째로 다시 썼다:
1. 대장장이 미션(강화 2회) - 🪙100 💎3
2/3/4/5. 일반/희귀/영웅/전설 등급 컬렉터(그 등급 영웅을 필드에 전부 동시 보유) - 🪙100 💎3
6. 운빨좋은날 - 🪙100 💎3
7. 행운석 수집가(행운석 10개 보유) - 🪙50 💎3
8. 영웅 판매(5회) - 🪙100 💎3
9. 날아간 손모가지(전설 룰렛 실패 5회) - 💎4
10. 3전설(필드에 전설 3마리 동시 보유) - 💎5

`src/logic/missions.js`의 `checkMissions()`를 `checkType`별로 판정하도록 재작성했다
(예전엔 `counter` 하나뿐이었음):
- `'counter'`: `state.counters[counterKey]`를 target과 비교(기존과 동일)
- `'resource'`: `state[resourceKey]`(골드/행운석 등 보유량)를 target과 비교 - "행운석
  수집가"는 누적 획득이 아니라 "지금 10개를 들고 있는지"로 판정한다(써버리면 다시
  false가 될 수 있지만, 한 번이라도 10개를 채운 순간 `claimedMissionIds`에 등록돼서
  완료 상태 자체는 영구히 유지된다).
- `'tier-collect'`: `heroesByTier(tier)`의 모든 heroId가 필드에 1마리 이상씩 동시에
  있는지(등급 컬렉터 4종)
- `'field-tier-count'`: 해당 등급 영웅이 필드에 총 몇 마리 있는지(3전설)

새 카운터 `counters.normalSummonLegendaryCount`(룰렛이 아니라 **일반 소환**으로
전설을 뽑은 횟수, `summonNormal()`에서 증가)를 "운빨좋은날" 판정에 쓴다. **주의**:
이 발동 조건은 기획서에 정확히 명시돼 있지 않아서 "룰렛 없이 일반 소환만으로 전설이
나오는 흔치 않은 순간"으로 임의 해석한 것이다 - 나중에 사용자가 다른 조건("운빨좋은날"이
실제로는 다른 의미)을 말하면 `mission_lucky_day`의 `counterKey`만 바꾸면 된다.

**완료는 영구적(1회성)**: `claimedMissionIds`에 한 번 들어간 미션은 이후 조건이
깨져도(등급 컬렉터를 채웠다가 하나를 팔아버리는 등) `completed: true`가 계속
유지된다 - `checkMissions()`가 매번 라이브 조건을 재계산하지 않고
`claimedMissionIds.includes(id)`를 최종 `completed` 값으로 쓰기 때문(사용자 지정 -
"미션은 1번씩만 가능").

**미션 팝업 UI**: 체크박스(☐ 미완료 / ☑ 완료, 유니코드 글리프)를 각 항목 앞에
붙이고, 완료된 미션은 이름/설명에 `text-decoration: line-through`를 건다(사용자
지정 - "다된거는 취소선으로 그어줘... 체크박스로 표시해줘"). 보상(골드/행운석
아이콘+수치)도 항목 오른쪽에 같이 보여준다. 이 김에 아무 데서도 안 쓰이던 죽은
CSS(`.mythic-list`, `.missing-note` - 예전 미션 팝업 구현의 잔재로 보임)도 같이
정리했다.

**신화/불멸 크기 재조정**: 직전 라운드(PR #33)에 "3마리 발끝 기준선"에 맞춰 2.5배로
키웠는데 사용자가 다시 "너무 크다"며 50%만 줄여달라고 해서 `MYTHIC_TOKEN_SCALE`을
2.5 → 1.25로 낮췄다(발끝 기준선 계산식 자체는 그대로 재사용 - 배율만 바뀜).

## cover-fit 되돌림+전체화면 버튼/미션 완료 토스트/크기 위계 재조정 (PR #35 배포 후)

PR #35 배포 후 카카오톡 인앱 브라우저 스크린샷 2장과 함께 4개를 지적했다.

1. **모바일 "cover" 여백 제거가 오히려 상단 UI를 잘라먹던 문제**: 직전 라운드에
   검은 여백을 없애려고 `sizeStageToFit()`을 "cover"(꽉 채우고 넘치는 부분 자르기)
   로 바꿨는데, 실제 카카오톡 인앱 브라우저(iOS)에서 확인해보니 상단 배지가
   위쪽 절반이 잘려나가는 부작용이 있었다(사용자 스크린샷으로 확인). "안되면
   크기 그냥 원래대로 해야 할 거 같아"라는 지시대로 `sizeStageToFit()`을
   "contain"(예전 방식, 안 맞는 쪽엔 검은 여백)으로 되돌렸다.
2. **동영상처럼 버튼으로 전체화면 전환**: 사용자가 대안으로 제시한 아이디어 -
   `renderStageControls`에 전체화면 토글 버튼(⛶)을 추가했다. 브라우저 Fullscreen
   API(`document.documentElement.requestFullscreen()`, webkit 접두사 폴백 포함)를
   쓰되, `document.fullscreenEnabled`(또는 webkit 버전)로 지원 여부를 먼저 확인해서
   **지원 안 하는 브라우저에서는 버튼 자체를 아예 안 띄운다** - iOS 사파리 계열
   인앱 브라우저(카카오톡 포함) 다수가 `<video>` 외 임의 엘리먼트의 전체화면을
   지원하지 않는 걸로 알려져 있어서, 눌러도 반응 없는 죽은 버튼을 두지 않기
   위함이다. 지원되는 환경(데스크톱 브라우저, 최신 Android Chrome 등)에서는 정상
   동작한다.
3. **미션 완료 알림(우측 슬라이드인 토스트) 신설**: 사용자가 "미션 완료되면
   우측에서 팝업 띄워주면서 미션 성공 알림 주는거 설정했는지 재확인"이라고
   물어봤는데, 실제로는 구현이 안 돼 있었다(미션 팝업을 직접 열어야만 완료
   여부를 알 수 있었음) - 이번에 새로 만들었다. `checkMissions()`가 미션이 새로
   완료되는 순간마다 `missionToastQueue`에 `{missionId, timer}`를 쌓고,
   `tickMissionToast()`가 큐 맨 앞 항목의 타이머를 깎다가 0이 되면 다음 항목으로
   넘긴다(여러 미션이 한 틱에 동시 완료될 수 있어서 큐 구조로 처리 - 예:
   전설 4종을 다 모으는 순간 "전설 컬렉터"와 "3전설"이 동시에 조건을 채울 수
   있음). 화면 우측 상단에 3초짜리 슬라이드인→대기→슬라이드아웃 애니메이션으로
   표시한다 - 이 애니메이션도 몬스터 이동/공격 모션과 같은 이유로(0.2초마다 DOM
   재생성) 고정 delay를 쓰면 안 되고, `missionToastQueue[0].timer`를 거꾸로
   계산한 경과 시간 기준 `animation-delay`를 매 렌더 다시 넣는 방식으로 구현했다.
4. **신화/불멸이 일반~전설보다 작아 보이던 문제**: 직전 라운드(1.25배)에도 여전히
   "반대로 보인다"는 지적을 받았다 - 격차를 더 벌리기로 했다. 일반~전설 기준
   비율(`HERO_TOKEN_WIDTH_RATIO`/`HEIGHT_RATIO`)을 20% 줄이고(0.55/1.0 →
   0.44/0.8), 신화/불멸 배율은 "기존 절대 크기 대비 20% 증가"가 되도록 역산해서
   `MYTHIC_TOKEN_SCALE`을 1.25 → 1.875로 올렸다(계산: 기존 신화 절대 크기
   0.55×1.25=0.6875, 그 120%는 0.825, 새 기준 비율 0.44로 나누면 0.825/0.44=1.875).
   결과적으로 신화/불멸이 일반~전설 대비 1.875배(예전 1.25배보다 격차가 훨씬 큼)가
   됐다. Playwright로 신화 토큰 너비를 일반/희귀/영웅 토큰과 나란히 재서 정확히
   1.875배인지 확인해 검증했다.

## 운빨좋은날 미션 보류/신화 크기 10% 추가 축소 (PR #36 배포 후)

PR #36 배포 후 2개 지시를 받았다.

1. **"운빨좋은날" 미션 제외**: 정확한 발동 조건을 사용자가 나중에 다시 확인해주기로
   해서(사용자 지정 - "일단 제외해줘. 내가 나중에 정확하게 미션 다시 보고
   알려줄게"), `data/missions.js`의 `MISSIONS` 배열에서 해당 항목을 뺐다(현재 9종).
   이전에 "일반 소환으로 전설 획득"으로 임의 해석해뒀던 것도 같이 무효가 된 것 -
   나중에 정확한 조건을 받으면 새 항목으로 다시 추가하면 된다. 카운터
   (`counters.normalSummonLegendaryCount`, `summon.js`에서 증가)는 그대로 남겨뒀다 -
   미션 정의만 없앤 거라 나중에 그 조건으로 확정되면 바로 재사용 가능.
2. **신화/불멸 크기 10% 추가 축소**: `MYTHIC_TOKEN_SCALE`을 1.875 → 1.6875로
   낮췄다(1.875 × 0.9). 일반~전설 기준 비율(`HERO_TOKEN_WIDTH_RATIO`/
   `HEIGHT_RATIO`)은 그대로 두고 신화/불멸 배율만 줄였다 - 상대적 크기 위계
   (신화/불멸이 일반~전설보다 크다는 관계)는 그대로 유지된다.

## 인디 보물 발굴 UX 개선 - 발굴 대기시간/외곽선 색/상시 게이지/완료 플래시 (PR #37 배포 후)

PR #36 배포 후 사용자가 "이제 마지막이야"라며 인디(`m_indy`) "보물 발굴" 기능에
5가지를 지시했다(발굴 지시 중간에 메시지가 끊겼다가 "이때 캐릭터가 이미 있는
장소에도 보물이 있을 수 있는거 참고하고"라는 추가 조건이 이어졌다).

1. **발굴 버튼을 눌러도 즉시 결과가 안 나오고 2초 대기**: `state.indyTreasure`에
   `digging: {instanceId, timer}|null` 필드를 추가했다. `actions.js`의
   `digTreasure()`는 이제 즉시 등급을 굴리지 않고 `digging` 상태만 세팅하고,
   `waveEvents.js`에 새로 추가한 `tickIndyDig()`(게임 루프에 `tickIndyTreasure` 바로
   다음 순서로 연결, `gameLoop.js`)가 `INDY_DIG_DURATION_SEC`(2초) 뒤에 실제 등급
   판정(`rollNormalTier()`, 기존 보유 등급보다 낮으면 교체 안 함)을 확정한다. 발굴
   중엔 `tickIndyTreasure()`가 쿨타임 자체를 멈춘다(안 그러면 발굴 결과가 나오기
   전에 자연 만료로 보물 위치가 다른 칸으로 옮겨가버릴 수 있음). 칸 버튼도 발굴
   중엔 "발굴 중..."으로 바뀌고 비활성화된다(`GameScreen.js`의
   `renderCellQuickActions`).
2. **보물 등급에 따라 인디 본인의 외곽선 색이 바뀜**: 기존 `outlineFilter()`(3마리
   스택 합성 가능 표시에 쓰던, drop-shadow 4방향을 겹쳐 실루엣을 따라가는 흰색
   외곽선 헬퍼)가 흰색으로 하드코딩돼 있던 것을 `color` 인자를 받도록 일반화했다.
   `INDY_TREASURE_OUTLINE_COLOR`(일반/희귀/영웅/전설 - main.css의 `--tier-color`
   팔레트와 맞춤)를 새로 두고, `renderHeroTokenLayer`의 필터 합성 목록에 인디
   전용 항목으로 추가했다(`occ.heroId === 'm_indy'`일 때만, `occ.indyTreasureTier`
   기준).
3. **인디 쿨타임(30초) 게이지를 인디 바로 밑에 항상 표시**: 칸 선택 여부와 무관하게
   항상 보여야 한다는 게 사용자 지정 사항("이건 클릭 안 해도 보여지게") - 칸에
   `m_indy`가 있으면 `renderHeroTokenLayer`가 매 슬롯 루프 끝에서
   `renderIndyTreasureGauge(state, rect)`를 호출해 칸 좌표 바로 아래에 얇은 바를
   그린다. 채우는 비율은 `1 - indyTreasure.timer / INDY_TREASURE_INTERVAL_SEC`로
   계산(선택 상태와 무관하게 항상 최신 state 기준).
4. **게이지가 다 차면(새 보물 등장 시점) "완료" 텍스트를 한 번만 플래시**:
   `indyTreasure.completedAt`(게이지가 다 차서 `tickIndyTreasure()`가 새 슬롯을
   뽑은 시각, `Date.now()`)을 새로 추가하고, `renderIndyTreasureGauge`가
   `Date.now() - completedAt < INDY_GAUGE_FLASH_MS`(1.5초)인 동안만 "완료" 텍스트를
   그린다 - 몬스터 이동/궁 링 이펙트와 같은 이유로(0.2초마다 DOM이 재생성되는 구조)
   고정 타이머 대신 실제 경과 시간 기준으로 매 렌더 다시 판정해야 자연스럽게
   나타났다 사라진다.
5. **"보물이 이미 다른 캐릭터가 있는 칸에도 등장할 수 있다"는 사용자의 추가
   확인을 반영해 기존 `.treasure-mark`(칸 전체 노란 글로우) 가시성 버그를 같이
   고쳤다**: `.field-grid`(먼저 그려짐)와 `.stage-hero-layer`(나중에 그려짐,
   z-index 미지정) 둘 다 명시적 z-index가 없어서 기본 DOM 순서상 캐릭터 토큰이
   글로우 위에 그려지고 있었다 - 3마리가 쌓인 칸처럼 캐릭터가 칸을 거의 다 채우면
   글로우가 거의 안 보일 수 있는 상태였다(인디 본인이 이미 발굴한 칸에 있을 땐
   문제 없었지만, 아직 인디가 도착 안 한 다른 점유 칸에 보물이 뜬 경우 눈에 안
   띌 수 있었음). `.treasure-mark`의 `z-index`를 1 → 10(캐릭터 토큰 z-index 최대치
   2+행번호=5보다 확실히 높게)으로 올려서 항상 캐릭터 위에 뜨게 했다 -
   `pointer-events:none`이라 클릭/드래그는 그대로 아래 칸으로 투과된다. **참고**:
   "인디를 클릭했을 때 빛나야 한다"는 요청 자체는 이미 만족돼 있었다 -
   `.treasure-mark`는 원래부터 인디 선택 여부와 무관하게 보물이 존재하는 칸이면
   항상 켜져 있는 무조건부 글로우라(`isTreasureSlot()` 체크만), "클릭했을 때"도
   당연히 포함된다. 이번에 진짜로 고친 건 그 글로우의 z-index 가시성 문제였다.

**검증 방법**: `window.__debug`(getState/setState 훅)를 `main.js`에 임시로 추가해
Playwright에서 필드 상태를 직접 주입한 뒤(인디를 특정 칸에 배치, `indyTreasure`를
원하는 timer/slot/completedAt으로 세팅) 스크린샷과 DOM 계산값으로 5가지를 모두
확인했다 - 발굴 클릭 직후 `digging.timer`가 2초에서 실시간으로 줄어드는 것, 2.3초
뒤 `digging`이 null로 정리되고 `indyTreasureTier`가 갱신(또는 유지)되는 것, 등급별
`outlineFilter` 색상이 실제로 다르게 합성되는 것, 게이지 `width`가 timer에 비례하는
것, `completedAt` 직후엔 "완료" 텍스트가 있다가 1.5초 뒤엔 사라지는 것, 3마리
스택 위에 뜬 보물 글로우의 계산된 z-index가 10으로 실제 hero-layer보다 위에서
페인트되는 것까지 전부 확인했다. 검증이 끝난 뒤 `window.__debug` 훅은
`main.js`에서 다시 제거했다(디버그 전용, 커밋에 남기지 않음).

## 더블탭 확대 재발/드래그 중 요소 선택/디버프 개체 추적/속박 사슬 확대/불멸 밤바 속도/전체화면 제거/미션 토스트 위치 (PR #38 배포 후)

PR #37 배포 후 7개를 지적했다.

1. **더블탭 확대가 여전히 재현됨(재발)**: PR #31에서 뷰포트 meta
   (`maximum-scale=1.0, user-scalable=no`) + `body { touch-action: manipulation; }`로
   고쳤다고 기록했었는데 실제로는 여전히 발생했다. `touch-action`을 `body` 하나에만
   걸어두는 방식은, 이 프로젝트처럼 캐릭터 토큰/버튼이 전부 `position:absolute`인
   별도 레이어로 분리된 구조에서 자손 요소가 조상의 값을 제대로 물려받지 못하는
   경우가 있다(특히 iOS 계열 웹뷰) - `* { touch-action: manipulation; }`로 모든
   요소에 직접 걸어서 확실히 적용되게 했다. `.field-slot`의 기존
   `touch-action: none`(드래그 이동용)은 클래스 선택자가 우선순위가 높아 그대로
   유지된다.
2. **드래그 중 다른 요소(텍스트 등)가 브라우저 기본 선택으로 하이라이트되던 문제**:
   위와 같은 이유로 `user-select: none`도 `body`가 아니라 `*`(전체 요소)에 직접
   걸었다(사용자 지적 - "드래그했을때 이거 html이라 다른요소 선택되는데"). 프리셋
   이름 `<input>`은 조상의 `user-select`와 무관하게 폼 컨트롤 내부 텍스트는 브라우저가
   항상 별도로 선택 가능하게 처리하므로 영향 없다.
3. **디버프가 칸이 아니라 개체를 따라가야 함**: 예전엔(PR #30) "디버프도 이동불능처럼
   6칸을 한 번에" 요구를 반영하면서 `debuffEvent`를 `{slots:[{row,col},...]}`(좌표
   기준)로 저장했는데, 이번에 사용자가 "그 칸에 있던 캐릭터들한테 디버프가 걸린거라
   그 친구들 옮기면 보라색이 따라가야해"라고 정정했다. `waveEvents.js`의
   `pickRandomOccupiedSlots` → `pickDebuffTargetInstanceIds`로 바꿔서 발동 시점에
   대상 칸들의 occupant instanceId를 미리 뽑아 `debuffEvent.instanceIds`(좌표가 아니라
   개체 ID 배열)로 저장한다. `GameScreen.js`의 `debuffed` 판정도 `slot.row/col` 일치
   대신 `debuffEv.instanceIds.includes(occ.instanceId)`로 바꿨다 - 이제 디버프 걸린
   캐릭터를 드래그로 다른 칸에 옮겨도 보라색 틴트가 그 개체를 따라간다(칸에 새로
   들어온 다른 개체는 대상이 아니었으므로 자연히 물들지 않음).
4. **이동불능(속박) 사슬을 칸 구석의 작은 아이콘이 아니라 캐릭터 앞을 덮는 큰
   아이콘으로**: 사용자가 참고 이미지(캐릭터 크기만큼 큰 사슬 X가 캐릭터 얼굴
   앞에 겹쳐 보이는 모습)를 첨부해 요청했다. 기존 `.immobilize-mark`(12×12px,
   `.field-slot` 구석에 고정)를 없애고, `renderHeroTokenLayer`가 캐릭터 토큰과 같은
   좌표계(`cellCenterX`/`baseTop`/`tokenWidth`/`tokenHeight`)로 `.stage-immobilize-mark`를
   그리도록 옮겼다 - 캐릭터 토큰 크기의 1.6배(가로)/1.15배(세로)로 살짝 더 키워서
   3마리 스택도 덮고, z-index를 캐릭터 토큰(2~5)보다 훨씬 높은 20+행번호로 둬서
   "캐릭터 앞"에 겹쳐 보이게 했다. `.field-slot.immobilize-active`의 빨간 테두리는
   보조 표시로 그대로 유지.
5. **밤바(`m_bamba`)의 불멸 조건(스택 30) 누적 속도가 너무 빠름**: `heroes.js`의
   `IMMORTAL_CONDITIONS.m_bamba.tickIntervalSec`가 `[1, 5]`(평균 3초마다 1스택,
   30스택까지 평균 90초)였는데 사용자가 "5~15초 랜덤으로 스택쌓기 갈게"라고 확정
   수치를 지정해서 `[5, 15]`로 늦췄다(평균 10초마다 1스택, 평균 300초) - 이 값은
   `immortal.js`의 제네릭 `hybrid` 진행 엔진이 그대로 소비하므로 데이터 수정만으로
   충분했다(코드 변경 불필요).
6. **전체화면 버튼 제거**: PR #36에서 모바일 검은 여백(letterbox) 우회용으로 추가했던
   `⛶` 버튼(Fullscreen API)을 사용자 요청으로 완전히 제거했다(`renderStageControls`).
   letterbox 자체는 "contain" 방식(원본 비율 유지)의 의도된 트레이드오프로 그대로
   남는다 - 다시 문제 삼으면 그때 다른 대안을 검토해야 한다.
7. **미션 완료 토스트 위치를 우측 상단에서 우측 하단(미션 버튼 바로 위)으로**:
   `.mission-toast`의 `top:8%`를 `bottom:30%`로 바꿔서 `.stage-side-controls`
   (미션 버튼, `top:70.7%`) 바로 위에 뜨도록 옮겼다(사용자 지정 - "미션 성공했을 때
   우측 하단 즈음에 해줘(미션 그 클릭하는거 버튼 위에)"). 좌우 슬라이드인
   애니메이션 자체는 그대로 유지.

**검증 방법**: 이전 라운드와 동일하게 `window.__debug` 훅을 `main.js`에 임시로
추가해 Playwright로 필드 상태를 직접 주입한 뒤 검증하고, 검증이 끝나면 훅을 다시
제거했다(디버그 전용, 커밋에 남기지 않음) - 이동불능 사슬이 캐릭터 토큰보다 크고
z-index가 위인 것, 디버프가 칸을 비우고 다른 칸으로 이동시켜도 여전히 같은
instanceId에 걸려 있는 것, 미션 토스트의 bounding box가 미션 버튼 바로 위(겹치지
않고 인접)에 오는 것, `getComputedStyle`로 `touch-action:manipulation`/
`user-select:none`이 전역에 실제로 적용된 것, 전체화면 버튼(⛶)이 컨트롤 목록에서
사라진 것까지 전부 스크린샷과 DOM 계산값으로 확인했다.

## 합성 크래시 버그 수정 + 룰렛 팝업 중 필드 조작 허용 (PR #38 배포 후)

PR #38 배포 후 "합성 누르면 튕기고 게임이 꺼진다"는 크래시 리포트와 "룰렛 팝업이
떠있을 때에도 필드 캐릭터를 조작할 수 있게 해달라"는 요청 2개를 받았다.

1. **합성(3마리→상위 등급) 크래시 원인 규명 + 수정**: `synthesize()`가 조합 결과와
   같은 영웅이 이미 다른 칸에 있으면 그 칸(`existingHolder`)에 새 개체를 바로
   `push`한 뒤 `consolidateHeroStacks()`를 불러 정리하는 구조였다 - 그런데
   `existingHolder`가 이미 3마리로 꽉 찬 상태였다면 push 직후 그 칸은 **4마리**가
   된다. `consolidateHeroStacks()`는 "해당 영웅을 보유한 칸(holders)이 1개 이하면
   이미 정리된 것"으로 보고 조기 반환하는데, 이 경우 holders가 정확히 1개(그
   4마리짜리 칸)라서 재분배 없이 그대로 넘어가버렸다 - 4마리짜리 칸이 필드에 남는
   버그. 다음 렌더에서 `GameScreen.js`의 `stackOffsets(n)`이 n=3 이상이면 항상
   좌표 3개짜리 배열만 반환하는데, `slot.occupants.forEach((occ, i) => ...)`가
   4번째(`i=3`) 개체를 그리려다 `positions[3]`이 `undefined`라서
   `const { centerX, top } = positions[i]` 구조분해 대입이 그대로 던져서 렌더가
   터졌다(사용자가 "팅기고 게임이 꺼진다"고 표현한 게 바로 이 크래시 - 렌더 도중
   예외가 나면 `main.js`의 `requestAnimationFrame` 루프가 다음 프레임 예약 코드까지
   못 가고 조용히 멈춰버려서 게임이 먹통이 된 것처럼 보인다). 두 군데를 같이
   고쳤다: (1) `synthesize()`가 다른 칸에 바로 push하는 대신 방금 비운 원래 칸에
   새 개체를 넣고 `consolidateHeroStacks()`에게 병합을 전부 맡기도록 순서를
   바꿔서 애초에 4마리 오버플로우가 생기지 않게 했고, (2) `consolidateHeroStacks()`
   자체도 방어적으로 고쳐서 "holders가 1개뿐이어도 그 칸의 개체 수가 3을 넘으면"
   재분배를 계속 진행하고, 기존 holder들을 3마리씩 다 채우고도 남는 개체가 있으면
   빈 칸을 새로 찾아 옮기도록 했다(필드가 꽉 차서 옮길 빈 칸이 없으면 그 시점에서
   멈춘다 - 이 경우까지는 호출부에서 사전에 자리를 확인해야 하는 영역이라 그대로
   둠). Playwright로 정확히 이 크래시 시나리오(합성 결과가 나올 영웅이 이미 다른
   칸에 3마리 꽉 차 있는 상태에서 합성 버튼 클릭, `Math.random`을 임시로 고정해
   합성 결과를 결정론적으로 만듦)를 재현해서 크래시 없이 3+1로 정상 분배되는지,
   `pageerror` 이벤트가 안 뜨는지, 1초 뒤에도 게임 화면이 계속 렌더링되는지까지
   확인했다.
2. **룰렛 팝업이 떠 있어도 필드 캐릭터 조작 가능하게**: 루트 `pointerdown`
   핸들러에 `if (ui.popup) return;`이 있어서 팝업이 하나라도 열려 있으면 필드 칸
   탭/드래그 자체가 아예 시작되지 않았다. 룰렛(그리고 강화) 팝업은 전체화면을
   덮는 모달이 아니라 하단 시트(`.game-popup`)라 필드 위쪽이 여전히 보이는데도
   조작이 막혀 있었던 것 - `if (ui.popup && ui.popup !== 'roulette') return;`으로
   룰렛일 때만 예외를 뒀다(사용자가 룰렛만 콕 집어 요청했으므로 신화/강화/미션
   팝업은 기존대로 필드 조작을 막는 채로 남겨뒀다 - 강화도 하단 시트라 같은
   논리가 적용될 법하지만, 요청받지 않은 범위까지 넓히지 않았다. 나중에 강화
   팝업도 같은 요청이 오면 조건에 `'enhance'`만 추가하면 됨). Playwright로
   룰렛 팝업을 연 상태에서 필드 캐릭터를 탭해 선택(판매 버튼 노출)까지 되는지,
   반대로 신화 팝업이 열려 있을 때는 여전히 선택이 안 되는지(네거티브 컨트롤)
   둘 다 확인했다.

**검증 방법**: 이전 라운드들과 동일하게 `window.__debug` 훅(이번엔 결정론적 합성
결과를 위해 `heroesByTier`도 같이 노출)을 `main.js`에 임시로 추가해 Playwright로
검증하고 끝난 뒤 다시 제거했다(디버그 전용, 커밋에 남기지 않음).

## 속박 사슬 축소/팝업 외부 클릭 시 자동 닫힘/사신개구리 2단 변신 재확인 (PR #38 배포 후)

PR #38 배포 후 3개를 지적했다(4번째 항목은 메시지가 빈 채로 끊겨서 이번 라운드에는
포함하지 않았다 - 다음에 이어서 알려주기로 함).

1. **이동불능(속박) 사슬 크기 축소**: 직전 라운드에 칸 구석 아이콘에서 캐릭터를
   덮는 큰 아이콘으로 바꾸면서 `tokenWidth*1.6`/`tokenHeight*1.15`로 캐릭터보다
   상당히 크게 잡았었는데, 사용자가 "너무 크다"고 정정했다. `1.05`/`0.9`로 낮춰서
   캐릭터 크기에 훨씬 가깝게(살짝 크게 덮는 정도로만) 줄였다.
2. **모든 팝업이 팝업 외부를 클릭하면 자동으로 닫히게**: 미션 팝업(`.popup-overlay`)은
   이미 어두운 배경 클릭 시 닫히는 자체 로직이 있었지만, 룰렛/강화(`.game-popup`)와
   신화(`.mythic-popup`)는 ✕ 버튼을 눌러야만 닫혔다. 루트 `pointerdown` 핸들러
   맨 앞에 "팝업이 열려 있는데 클릭 대상이 그 팝업 안(`.game-popup`/`.mythic-popup`/
   `.popup-overlay`)이 아니면 즉시 닫는다"는 로직을 추가했다 - 룰렛이 열려 있을 때는
   필드 조작이 예외적으로 허용되므로(이전 라운드 요청) 필드 칸/액션을 누르는 것도
   "안쪽"으로 취급해 닫히지 않게 예외를 뒀다. **함정**: 이 로직을 `click` 이벤트에
   걸면 안 된다 - 예를 들어 룰렛이 열린 상태에서 신화 버튼을 누르면 그 버튼의
   `onclick`(신화 팝업 열기)이 먼저 실행되며 `render()`가 전체 DOM을 갈아엎는데,
   `click` 이벤트는 버블링 경로가 dispatch 시점에 고정되어 있어서 내 최상위
   리스너까지는 도달하지만 그 시점엔 `e.target`(원래 버튼)이 이미 DOM에서
   제거된 고아 노드라 `.closest()`가 부모 체인을 못 타고 즉시 null을 반환해 "팝업
   밖 클릭"으로 잘못 판정되고, 방금 연 신화 팝업이 바로 다시 닫혀버리는 깜빡임
   버그가 생긴다. `pointerdown`은 이 제스처에서 DOM을 변경하는 유일한 첫 핸들러라
   `e.target`이 아직 온전한 시점에 판정이 끝나므로 이 문제가 없다 - 팝업
   토글/전환 클릭을 다룰 땐 항상 `pointerdown`(제스처의 가장 이른 이벤트)에서
   판정할 것, `click`처럼 DOM 변경 이후에 도달할 수 있는 이벤트에 걸지 말 것.
3. **사신개구리 2단계 변신 메커니즘 재확인 - 이미 정확히 구현돼 있었음**: 사용자가
   "개구리왕자가 승급 버튼을 누르면 35% 확률로 사신개구리(불멸)가 되고, 그 다음
   수동으로 버튼을 한 번 더 눌러서 50% 확률로 또 변신 가능하다"고 설명했는데,
   `heroes.js`의 `IMMORTAL_CONDITIONS.m_frog_prince`(`successRate: 0.35`,
   `immortal.js`의 `promotionHandlers.m_frog_prince`가 실패 시 개체 소멸까지 처리)와
   `SECOND_STAGE_IMMORTAL.i_death_frog`(`successRate: 0.5`,
   `attemptSecondStageEvolution()`이 수동 버튼 호출로 처리)를 다시 읽고 확인한 결과
   설명과 정확히 일치했다 - 코드 변경 없음(확인 차원의 요청으로 판단, CLAUDE.md에
   재확인 기록만 남김).

**검증 방법**: 이전 라운드와 동일하게 `window.__debug` 훅을 `main.js`에 임시로
추가해 Playwright로 검증했다 - 사슬 크기가 캐릭터 토큰과 거의 같은 비율(1.05/0.9배)로
나오는지, 룰렛 팝업이 필드 바깥(상단 배지)을 탭하면 닫히지만 필드 칸을 탭하면 계속
열려 있으면서 캐릭터도 같이 선택되는지, 신화 팝업도 바깥 탭으로 닫히는지, 룰렛이
열린 상태에서 신화 버튼을 한 번만 탭해도(더블클릭 없이) 정상적으로 룰렛이 닫히고
신화가 열리는지(위 함정에서 설명한 깜빡임 버그가 없는지), 미션 팝업의 기존
배경-클릭-닫힘 동작이 그대로 유지되는지까지 실제 `pointerdown`→`pointerup`→`click`
전체 시퀀스를 디스패치해서 확인했다. 검증이 끝난 뒤 `window.__debug` 훅은
`main.js`에서 다시 제거했다(디버그 전용, 커밋에 남기지 않음).

## 합성 방향 재역전 버그 수정 + 미션 13종 확정 (PR #39 배포 후)

PR #39 배포 후 "합성했을 때 기존에 있던 캐릭터 자리로 가는 게 아니라 다른 자리에
있던 애가 합성 자리로 오는거"라는 리포트와 미션 13종 전체 확정 목록을 받았다.

1. **합성 결과 이동 방향이 다시 반대로 동작하던 버그**: "합성 크래시 버그 수정"
   라운드(PR #38)에서 크래시(existingHolder가 이미 3마리 꽉 찬 칸에 결과물을 바로
   push해서 4마리로 오버플로우되던 문제)를 고치면서, `synthesize()`가 "새 개체를
   다른 칸(existingHolder)에 직접 push"하던 방식을 "새 개체를 방금 비운 이 칸(slot)에
   먼저 넣고 consolidateHeroStacks에게 병합을 맡기는" 방식으로 바꿨었다. 그런데
   `consolidateHeroStacks`는 개수가 많은 홀더를 우선 채우는 정렬 로직이라, 이 순서
   변경 때문에 "기존 자리(홀더)가 원래 개체들을 그대로 유지하고, 새로 만들어진
   개체만 방금 비운 칸으로 이동"하는 게 아니라 결과적으로 사용자가 보기엔 여러
   재배치가 뒤섞여 보이는 문제가 생겼다 - 사용자가 "3전설" 훨씬 이전
   라운드("대규모 정정 라운드", 이 문서 위쪽)에서 이미 한 번 명시적으로 정정했던
   "새 개체가 기존 자리로 가야 한다(제네릭 함수에 위임하지 말고 existingHolder에
   직접 push)"는 방향을 이번 크래시 수정이 다시 뒤집어버린 것이었다. **수정**:
   `synthesize()`를 다시 "existingHolder를 찾아 거기에 직접 push, 없으면 방금
   비운 slot에 push" 방식으로 되돌렸다 - 크래시 자체는 `consolidateHeroStacks`
   쪽에 이미 해둔 방어 코드("holder가 1개뿐이어도 3마리를 넘으면 재분배하고,
   필요하면 빈 칸을 추가로 찾아 옮긴다")가 여전히 유효해서, 방향을 되돌려도
   크래시가 재발하지 않는다 - 애초에 크래시를 막는 데 필요했던 건 방향 자체가
   아니라 `consolidateHeroStacks`의 오버플로우 처리였다. Playwright로 (1) 기존
   자리가 이미 3마리 꽉 찬 상태에서 합성 → 기존 자리는 원래 개체 3마리를 그대로
   유지하고 새 개체만 별도 빈 칸으로 가는지(크래시 재현 시나리오 + 방향 확인),
   (2) 기존 자리에 1마리만 있던 상태에서 합성 → 새 개체가 그 기존 자리로 합류하는지
   둘 다 확인했다.
2. **미션 13종 전체 재확정**: 기존 9종(운빨좋은날 제외 후 상태)을 사용자가 준 13개
   목록으로 통째로 교체했다. 보상 수치를 보면 모든 미션에 공통으로 행운석 3개가
   깔려 있고, 등급/자원 계열 미션은 그 위에 골드를 얹고, 소환/룰렛 계열 특수
   미션 중 일부는 골드 대신 행운석을 더 얹는 패턴이다(예: "영웅 컬렉터"는
   행운석 1개 추가라 총 4, "3전설"은 2개 추가라 총 5 - 사용자가 준 "행운석 1
   행운석 3"처럼 두 숫자를 나란히 쓴 표기는 "추가분 + 공통 3"의 합으로 해석했다.
   과거 9종에서 이미 💎4/💎5로 확정돼 있던 두 미션(날아간 손모가지, 3전설)과
   정확히 일치해서 이 해석에 확신을 가질 수 있었다). 새로 추가된 소환/룰렛 계열
   미션 5개는 대응하는 카운터가 아예 없어서 새로 만들었다:
   - `counters.normalSummonHeroCount`(`summon.js`의 `summonNormal()`에서 영웅 등급이
     나올 때마다 증가) - "영웅 소환가"(3번)
   - 기존 `normalSummonLegendaryCount`("소환에서 전설 등급 등장" - 예전에 "운빨좋은날"
     용으로 만들어뒀다가 미확정으로 미션에서 빠져 있던 걸 이번에 정확한 이름/target(1회)로
     재사용)
   - `counters.rouletteAttemptCount`(`summonRoulette()`에서 재화가 실제로 깎이는
     매 시도마다 증가, 등급/성공 무관) - "룰렛 애호가"(20번)
   - `counters.rouletteFailCount`(등급 무관 전체 룰렛 실패 시 증가, 기존
     `legendaryRouletteFailCount`은 전설 한정이라 별도로 새로 만듦) - "룰렛 불운아"(10번)
   - `counters.legendaryRouletteSuccessCount`(전설 룰렛 성공 시 증가) - "전설의 손맛"(3번)
   구 미션 중 "전설 등급 컬렉터"(모든 전설 4종을 필드에 동시 보유)는 새 13종 목록에
   없어서 뺐다 - "3전설"(필드에 전설 3마리, 등급 무관 field-tier-count)만 남는다.
   **검증 함정**: 룰렛의 실제 성공/실패 판정은 클릭 즉시가 아니라 `ROULETTE_SPIN_MS`
   (280ms) 뒤 `setTimeout` 콜백 안에서 `getState()`로 최신 상태를 다시 읽어
   처리된다(스핀 애니메이션용 - CLAUDE.md의 "룰렛 스핀 모션" 섹션 참고) - 그래서
   Playwright에서 `Math.random`을 강제로 고정해 결과를 결정론적으로 만들 때 클릭
   직후 바로 원래 `Math.random`으로 복구하면 그 setTimeout 콜백이 실행되기 전에
   이미 원래대로 돌아가 있어서 고정한 값이 적용되지 않는다 - 최소 280ms(스핀
   시간)만큼 기다린 뒤에 복구해야 한다. Playwright로 일반 소환(영웅/전설 등급을
   강제로 뽑기 위해 `Math.random` 값을 확률표 구간에 맞춰 고정)과 룰렛(성공/실패
   모두, 위 타이밍 함정을 피해서)을 각각 실행해 5개 신규 카운터가 정확히
   1씩 증가하는지, 미션 팝업이 13개 항목을 전부 올바른 진행도/보상으로 표시하는지
   확인했다.

**검증 방법**: 이전 라운드들과 동일하게 `window.__debug` 훅(이번에도 결정론적
합성/소환 결과를 위해 `heroesByTier` 노출)을 `main.js`에 임시로 추가해 Playwright로
검증하고 끝난 뒤 다시 제거했다(디버그 전용, 커밋에 남기지 않음).

## 모바일 페이지 스크롤/바운스 고정 + 선택 캐릭터 원형 표시 + 빈 칸 탭 시 즉시 해제 (PR #40 배포 후)

PR #40 배포 후 3개를 지적했다. 첫 번째 요청("이거 스크롤도 안되게 고정해줘")은
메시지가 중간에 끊겨서 무엇을 고정해달라는 건지 애매했는데, 재질문으로 "게임
화면 전체(모바일 페이지 스크롤/바운스)"를 가리킨 것임을 확인했다.

1. **모바일 페이지 스크롤/바운스 고정**: 게임 화면(`.game-screen`)이 이미
   `100dvh`로 뷰포트를 꽉 채우고 내부는 전부 절대좌표 오버레이라 그 자체는
   스크롤이 필요 없는데도, `html`/`body` 레벨에서 막아두지 않으면 iOS/Android
   브라우저가 주소창 접힘 등으로 생기는 여분 공간을 당겨서 페이지를 튕기거나
   스크롤시킨다. `html, body`에 `height:100%; overflow:hidden;
   overscroll-behavior:none;`을 걸고 `body`는 `position:fixed; inset:0;`으로
   완전히 고정했다. **주의**: 시작 화면(`.hero-select-screen`)은 영웅 카드가
   89종이라 세로로 뷰포트보다 훨씬 길어서(테스트 결과 `scrollHeight`
   2528px vs `clientHeight` 844px) 자체 스크롤이 반드시 있어야 한다 - body를
   통째로 잠그면서 이 화면까지 스크롤 불가능해지는 걸 막기 위해 `#app`에
   `height:100%; overflow:hidden;`을 주고 `.hero-select-screen`에만
   `overflow-y:auto`를 열어줬다(게임 화면은 자체 스크롤이 필요 없으니 그대로
   overflow:hidden 상속). Playwright로 게임 화면 진입 후 `body`가 여전히
   `position:fixed`인지, 시작 화면에서는 `.hero-select-screen`의
   `scrollHeight > clientHeight`(실제로 스크롤이 필요한 상태)이면서
   `overflow-y:auto`인지 둘 다 확인했다.
2. **선택된 캐릭터 표시용 연한 원형 테두리 신규 추가**: "어떤 캐릭을 선택했는지
   잘 보이게 그 인근으로 원을 테두리만 하나 연하게 그려줘(버튼이랑 이어지게)"라는
   요청 - `renderCellQuickActions`에 `.cell-select-ring`(채우기 없이 흰색
   반투명 테두리만, `border-radius:50%`)을 추가했다. 위/아래 버튼 스택
   (`.cell-quick-stack-above`/`-below`)과 똑같이 칸 좌표(`rect`)를 기준으로
   위치를 잡되, 링의 세로 크기를 칸 높이의 1.7배로 살짝 넘치게 잡아서 버튼
   스택과 자연스럽게 맞닿아 보이게 했다(정확히 딱 맞추는 대신 넉넉하게 겹치는
   쪽을 택함 - 사용자 지정 "이어지게"라는 표현에 맞춰 링이 버튼에서 살짝 멀어져
   보이는 것보다 겹치는 쪽이 안전). Playwright로 링의 bounding box와 위쪽 버튼
   스택의 bounding box가 실제로 인접/겹치는지 좌표로 확인했다.
3. **빈 칸을 탭해도 선택이 바로 해제되지 않던 버그**: "캐릭터 한번 누르고 나서
   다른곳 누르면 바로 선택 해제되어야 하는데 지금 바로 해제가 안돼" - 원인은
   루트 `pointerdown` 핸들러에서 탭한 칸이 `.field-slot`과 매치되면(빈 칸이든
   찬 칸이든) "칸이 아닌 곳을 눌렀을 때 해제"하는 분기(`if (!cellEl) {...}`)를
   아예 안 타고, 그 아래에서 `if (!slot || slot.occupants.length === 0) return;`
   로 빈 칸이면 그대로 조용히 리턴해버리던 것이었다 - `dragState`가 전혀
   안 만들어지니 `pointerup`의 `endDrag()`도 `if (!dragState) return;`에서
   막혀서, 원래 빈 칸 탭 시 해제를 담당하도록 이미 짜여 있던 `onSlotClick()`
   (`slot.occupants.length ? {row,col} : null`)까지 아예 도달하지 못했다 -
   그래서 캐릭터가 선택된 상태에서 빈 칸을 눌러도 필드 그리드 밖(상단 배지 등
   `.field-slot`이 아닌 영역)을 눌러야만 해제가 됐다. `if (!slot) return;`
   으로만 가드를 좁히고 빈 칸이어도 `dragState`를 정상적으로 세워서
   `endDrag()`→`onSlotClick()` 경로를 타게 했다 - 빈 칸에서 시작하는 드래그
   이동은 `moveHero()` 자체가 출발 칸이 비어 있으면 안전하게 실패 처리하므로
   부작용이 없다. Playwright로 캐릭터를 선택한 뒤 다른 빈 칸을 탭 한 번만으로
   즉시 해제되는지(판매 버튼 등 액션 UI가 그 즉시 사라지는지) 확인했다.

**검증 방법**: 이전 라운드들과 동일하게 `window.__debug` 훅을 `main.js`에 임시로
추가해 Playwright로 검증하고 끝난 뒤 다시 제거했다(디버그 전용, 커밋에 남기지 않음).

## 더블탭 확대 3차 재발 - JS 레벨 방어선 추가 (PR #41 배포 후)

PR #41 배포 후 "이거 여전히 더블탭하면 화면이 커져(일단 태블릿으로 테스트함)"라는
리포트를 받았다. 더블탭 확대는 이 세션에서만 벌써 세 번째 재발이다:
1차(PR #31) 뷰포트 meta `user-scalable=no` + `body { touch-action: manipulation; }`,
2차(PR #38) `touch-action`을 `body` 하나가 아니라 `*`(전체 요소)로 확장 - 둘 다
CSS/meta 레벨 대응이었는데, 이번엔 태블릿에서 재현된다는 리포트를 받고서야
"CSS만으로는 기기/브라우저별로 완전히 막히지 않을 수 있다"고 판단해 처음으로
**JS 레벨 방어선**을 추가했다. `user-scalable=no`는 iOS가 접근성 이유로 여러
버전째 무시하고 있다는 게 잘 알려진 사실이고, `touch-action:manipulation`도
스펙상으로는 더블탭-확대 제스처를 막아야 하지만 태블릿 기기/브라우저 조합에
따라 완전히 막히지 않는 경우가 실제로 있다 - 이번에 CSS 두 번의 시도로도
안 되는 걸 확인했으니, 세 번째 CSS 튜닝을 또 시도하는 대신 다른 계층(JS)의
해법으로 넘어갔다. `main.js`에 연속된 `touchend` 사이 간격이 300ms 이내면(더블탭
판정) 그 두 번째 `touchend`에 `e.preventDefault()`를 호출하는 고전적인
더블탭-확대 방지 스크립트를 추가했다(`{ passive: false }` 필수 - 없으면
`preventDefault`가 무시된다). `document`에 한 번만 등록해서 화면 전환(시작
화면 ↔ 게임 화면)과 무관하게 항상 적용된다. **주의**: 이 `preventDefault`는
그 터치의 "확대할지" 판정만 취소할 뿐 각 터치 자체의 `click` 이벤트는 정상적으로
그대로 발생하므로, 버튼을 빠르게 두 번 눌러 두 번의 독립된 동작으로 처리되던
기존 동작(예: 소환 버튼 연타)에는 영향이 없다 - Playwright로 300ms 이내 연속
`touchend`의 두 번째가 `defaultPrevented:true`가 되는 것과, 버튼에 실제
`click` 이벤트 2번을 연속 발생시켰을 때 두 번 다 정상적으로 등록되는 것(연타가
씹히지 않는 것) 둘 다 확인했다 - 다만 헤드리스 Chromium은 실제 모바일 브라우저의
네이티브 더블탭-확대 제스처 자체를 재현하지는 않으므로, 이 검증은 JS 로직이
설계한 대로 동작함을 확인한 것이지 실기기에서 확대가 실제로 사라졌는지까지
보장하지는 않는다 - 실기기(특히 이번에 리포트된 태블릿) 재확인이 필요하면
사용자에게 다시 요청할 것.

## 전반적 성능 점검 - window 리스너 누수 + 백그라운드 복귀 시 몰아치기 방지 (PR #42 배포 후)

"게임이 좀 버벅거리고 멈추고 그렇게 좀 원활하지 않다"는 리포트를 받고 코드 전체를
점검했다. 새 버그를 만드는 대신 실제로 확인 가능한 두 가지 구조적 문제를 찾아
고쳤다 - 둘 다 "한 판만 짧게 해서는 잘 안 보이다가 오래/여러 번 플레이할수록
누적되어 체감되는" 종류의 문제라 이번처럼 여러 라운드째 플레이해온 세션에서
비로소 두드러졌을 가능성이 크다.

1. **`GameScreen`이 등록하는 `window` 리스너 4개(resize/pointermove/pointerup/
   pointercancel)가 게임을 나가도 해제되지 않던 누수**: `root.addEventListener(...)`
   (칸 드래그용 pointerdown 등)는 `root`가 DOM에서 떨어져 나가면 참조가 없어져
   자연히 가비지 컬렉션되지만, `window.addEventListener(...)`로 등록한 건 다르다 -
   `window`는 페이지가 살아있는 한 계속 남아있어서 거기 등록된 리스너(와 그
   클로저가 붙잡고 있는 `ui`/`dragState` 등 그 GameScreen 인스턴스 전체)도 계속
   살아있게 된다. 사용자가 게임을 나갔다가(🚪 버튼이나 승리/패배 화면의 "홈으로")
   다시 시작하는 매 판(`mountGame`이 다시 호출되는 시점)마다 이 4개가 계속
   쌓였다 - 여러 판을 하고 나면 겹겹이 쌓인 pointermove 리스너들이 드래그할 때마다
   전부 실행되는 식으로 체감 랙("버벅거리고 멈추고")의 원인이 될 수 있다.
   `GameScreen()`이 반환하는 객체에 `destroy()`를 추가해 이 4개를 명시적으로
   해제하도록 하고, `main.js`의 `onExit`을 `cancelAnimationFrame(rafId);
   screen.destroy(); mountSelect();`로 감쌌다. Playwright로 시작→종료를 4회
   반복하면서 `window.addEventListener`/`removeEventListener`를 몽키패치해 4개
   이벤트 타입의 순증감을 매 사이클 확인했다 - resize/pointermove/pointercancel은
   원래도 매 사이클 add=remove로 정확히 0이었고, pointerup만 1차 사이클 이후
   계속 +1로 나왔는데, add/remove 이벤트 로그를 직접 찍어보니 그건 Playwright
   자체가 내부적으로 붙이는 클릭 시뮬레이션용 리스너(`this._hitTarge...`로 시작하는
   난독화된 코드 - 우리 앱 코드가 아님)였다 - 실제 앱 코드의 4개 리스너는 매
   사이클 정확히 add 4개/remove 4개로 완벽히 대칭이었다.
2. **탭을 백그라운드로 뒀다가 돌아오면 한꺼번에 몰아쳐서 처리되던 문제**: `main.js`의
   `requestAnimationFrame` 루프가 `now - lastTime`(직전 프레임과의 실제 경과시간)을
   그대로 `tick()`에 넘기고 있었는데, 브라우저는 탭이 안 보이는 동안 `rAF` 콜백
   자체를 멈춰뒀다가 탭이 다시 보이는 순간 재개한다 - 그 사이 몇 분/몇 시간이
   지났든 복귀 직후 첫 프레임의 `now - lastTime`이 그 값 그대로 거대해진다. 이걸
   그대로 시뮬레이션에 반영하면 웨이브 타이머가 한 번에 몇 라운드씩 건너뛰거나
   몬스터 수가 순간적으로 급변하는 등 상태가 한꺼번에 왕창 튀어서, 다시 들어왔을
   때 게임이 "멈췄다가 버벅거리며 몰아치는" 것처럼 보일 수 있다(이동/모바일에서
   다른 앱 전환, 화면 잠금 등으로 자주 발생할 수 있는 시나리오라 이 사용자의
   "버벅거리고 멈추고" 리포트의 유력한 원인 후보로 판단). 프레임당 반영하는
   실제 경과시간에 `MAX_FRAME_DELTA_SEC = 1`(1초) 상한을 둬서, 오래 자리를
   비웠다 돌아와도 그 시간만큼 밀린 진행을 몰아서 처리하지 않고 그냥 잠깐
   멈춰있던 것처럼 자연스럽게 이어지도록 클램프했다 - 게임 루프에서 흔히 쓰이는
   표준적인 "delta time 클램프" 패턴이다.

**그 밖에 점검했지만 문제없음을 확인한 것들** (참고용으로 기록):
- `synthesize()`가 3마리 초과(오버플로우) 상태를 만들 수 있는 유일한 경로였던 것을
  PR #38/#40에서 이미 고쳤고, 이번에 `.occupants.push(` 전체 호출부를 다시
  훑어서 `findAutoPlaceSlot`/`placeInstanceAtSlot`(항상 3마리 이하로 캡) 경로를
  벗어나는 다른 직접 push가 더 없는지 확인했다 - 없음.
- `immortal.js`의 시간 기반 누적 `while (t.elapsed >= t.nextTick)` 루프들이
  `tickIntervalSec`이 0이 되면 무한루프에 빠질 수 있는 구조인지 데이터
  (`heroes.js`)를 전수 확인했다 - 모든 값이 1 이상이라 위험 없음(백그라운드
  복귀로 `deltaSec`가 커져도 반복 횟수만 늘어날 뿐 유한하게 끝남).

**검증 방법**: 리스너 누수는 위에 설명한 대로 실제 시작/종료 4회 반복 +
add/remove 이벤트 로그 대조로 확인했다. 백그라운드 복귀 클램프는 순수 산수
클램프(`Math.min(x, 1)`)라 코드 정확성 확인만 하고 별도 런타임 시뮬레이션은
생략했다(헤드리스 브라우저에서 `requestAnimationFrame` 타이밍을 몇 분 단위로
믿을 만하게 조작하는 게 번거롭고, 로직 자체가 단순해 오류 여지가 적다고 판단).

## 전설 등급 캐릭터 크기 50% 확대 (PR #42 배포 후)

PR #42 배포 후 "전설들 크기 50%만 더 키워줘. 다른 캐릭터들에 비해 너무 작아"라는
지적을 받았다. 프로그램상으로는 일반~전설 등급이 전부 같은
`HERO_TOKEN_WIDTH_RATIO`/`HEIGHT_RATIO`를 공유해서 크기가 동일했는데(다른
등급별로 별도 배율을 주는 건 임프(0.5배)/신화·불멸(1.6875배)뿐이었다), 실제로는
전설 등급 원화 89장 중 일부가 캔버스 안에서 인물이 차지하는 비중이 다른 등급보다
작아서(예: 전신 포즈라 알파 bbox 크롭 후에도 여백이 상대적으로 많이 남는 경우)
`object-fit:contain`이 그 bbox 기준으로만 박스에 맞추다 보니 같은 크기 박스
안에서도 작아 보이는 것으로 보인다 - 이미지 파일 자체를 다시 손보는 대신,
`LEGENDARY_TOKEN_SCALE = 1.5`를 신설해 임프/신화 배율과 같은 방식(`sizeScale`)
으로 전설 등급 칸에만 적용했다. **주의**: 전설은 신화/불멸과 달리 최대 3마리까지
스택이 가능한데(합성 대상에서만 제외될 뿐 스택 자체는 됨), 1.5배로 커진 3마리
스택은 `stackOffsets(3)`의 오프셋이 커진 tokenWidth 기준으로 그대로 비례 확대돼서
가로 폭이 칸 경계를 살짝 넘칠 수 있다 - 신화/불멸(1.6875배, 항상 1마리)도 이미 칸
밖으로 넘치는 게 허용된 상태라 같은 맥락으로 판단해 별도 처리 없이 그대로 뒀다.
Playwright로 일반/희귀/영웅/전설을 한 화면에 나란히 배치해 실측한 토큰
width/height 비율이 정확히 1.5배인지 확인했다.

## 더블탭 확대 JS 방어선이 빠른 연타를 막던 회귀 - 제거 (PR #42 배포 후)

PR #42 배포 후 "태블릿으로 빨리 소환버튼을 누르는게 안돼", 뒤이어 "휴대폰도
안돼"라는 리포트를 받았다 - 바로 직전 라운드에서 태블릿 더블탭 확대를 막으려고
추가한 JS 레벨 방어선(`main.js`, 연속 `touchend` 간격이 300ms 이내면
`preventDefault()`)이 원인이었다.

**원인**: `touchend`에 `preventDefault()`를 호출하면 그 터치의 확대 판정만 막히는
게 아니라, 브라우저가 그 터치로부터 합성해서 발생시키는 호환용 마우스
이벤트(mousedown/mouseup/**click**) 자체가 전부 취소된다 - 즉 연속으로 빠르게
두 번째 탭부터는 버튼의 `click` 핸들러(소환 등)가 아예 안 불린다. 지난 라운드에
"preventDefault는 확대 판정만 막을 뿐 click 이벤트는 그대로 발생한다"고 적어뒀던
건 **틀린 판단이었다** - Playwright로 검증할 때 실제 터치 대신
`dispatchEvent(new MouseEvent('click', ...))`로 직접 click 이벤트를 만들어
쐈었는데, 이 방식은 브라우저의 실제 touch→click 합성 파이프라인을 아예 거치지
않으므로 이 문제를 걸러내지 못했다(검증 방법 자체의 허점).

**수정**: 그 JS 방어선(`document.addEventListener('touchend', ...)` 블록)을
통째로 제거하고 CSS `touch-action: manipulation`(`main.css`의 `*` 규칙, 이미
있음)만 남겼다 - 더블탭 확대가 일부 기기에서 완전히 안 막힐 수 있다는 단점은
남지만, "빠른 연타가 아예 안 먹힌다"는 훨씬 치명적인 회귀(이 게임은 소환 버튼
연타가 핵심 조작 패턴이다)보다는 훨씬 나은 트레이드오프다. **교훈**: 더블탭
확대를 막으려고 `touchend`/`touchstart`에서 `preventDefault()`를 거는 JS
트릭은 "의도한 더블탭(두 번째 탭도 하나의 독립된 클릭으로 처리되길 원함)"과
"확대하려는 더블탭"을 타이밍만으로는 구분할 수 없다 - 같은 위치를 빠르게 두 번
누르는 제스처 자체가 둘 다 동일하기 때문에, 순수 JS 레벨 필터로는 이 프로젝트처럼
버튼 연타가 핵심 조작인 게임에 안전하게 적용할 방법이 사실상 없다. 이 문제는
CSS 레벨(touch-action, viewport meta)에서 해결하는 게 맞고, 그걸로 부족한
기기가 있다면 감수해야 하는 한계로 남겨두는 편이 낫다 - 다시 JS 레벨 더블탭
방지를 시도하지 말 것.

**검증 방법**: Playwright의 `page.touchscreen.tap()`(실제 브라우저의
touch→click 합성 파이프라인을 그대로 타는 진짜 터치 시뮬레이션 - 이전 라운드의
`dispatchEvent(new MouseEvent(...))` 방식과 달리 이번 회귀를 정확히 재현/검증할
수 있었다)으로 소환 버튼을 100ms 간격으로 5번 연타해서, 수정 전 코드에서는
5번 중 1번만 소환되고(필드 개체 수 1, 골드 -20) 수정 후에는 5번 모두 정상
소환되는 것(필드 개체 수 5, 골드 -120)을 `git stash`로 코드를 오갔다 실측
대조해 확인했다.

## 진짜 원인 발견 - 네이티브 disabled 버튼이 더블탭 확대를 유발 (PR #43 배포 후)

PR #43(JS 더블탭 방지 제거) 배포 후, 사용자가 직접 원인을 특정해서 알려줬다 -
"활성화된 버튼을 누를 때는 괜찮은데 비활성화 됐을 때 버튼을 누르면 더블탭으로
인식하는 거였어." 이 세션에서 더블탭 확대를 세 번이나 쫓아다니면서도(뷰포트
meta, body/전체 요소 touch-action, JS 레벨 방지) 결국 못 잡았던 이유가 여기
있었다 - 원인이 "터치 처리 로직"이 아니라 **"비활성(disabled) 버튼은애초에
`touch-action`의 보호를 못 받는다"**는, CSS 레벨로는 손댈 수 없는 지점에 있었다.

**원인**: `el()` 헬퍼(`components/dom.js`)가 `disabled: true`를 네이티브 HTML
`disabled` 속성(`setAttribute('disabled', '')`)으로 그대로 넘기고 있었다.
브라우저는 disabled 폼 컨트롤(버튼 등)을 상호작용 파이프라인에서 제외하는데,
이때 `touch-action: manipulation`도 같이 적용이 안 되는 것으로 보인다 - 그
버튼 위에서의 탭은 "이 요소가 처리하는 제스처"가 아니게 되어 브라우저 기본
제스처 인식(더블탭 확대 포함)으로 그대로 넘어간다. 활성 버튼은 정상적으로
`touch-action`의 보호를 받아 문제가 없었기 때문에, "버튼 자체는 멀쩡한데
가끔 확대된다"는 재현이 어려운 증상으로 나타났던 것 - 지금까지의 모든 CSS/JS
레벨 대응이 "활성 상태의 상호작용"만 가정하고 있어서 이 케이스를 못 잡았다.

**수정**: `el()`이 `disabled: true`를 네이티브 속성으로 넘기지 않도록 바꿨다 -
대신 `is-disabled` 클래스로 시각적 비활성 표시(불투명도 등 기존
`:disabled` CSS와 동일한 효과)만 하고, 클릭 핸들러 자체를 렌더 시점에
`disabled` 여부로 감싸서 눌러도 무시되게 했다(`node.addEventListener(type,
(e) => { if (disabled) return; value(e); })`) - 접근성을 위해
`aria-disabled="true"`도 같이 붙인다. 버튼은 끝까지 "정상적인 상호작용
요소"로 남아있으므로 `touch-action`이 항상 적용된다. `main.css`의 `X:disabled`
셀렉터 7개를 전부 `X.is-disabled`로 바꿨다(`.btn`, `.favorite-icon`,
`.cell-quick-btn`, `.side-btn`/`.summon-btn-wrap`, `.roulette-wheel-btn`,
`.enhance-col`, `.mythic-summon-btn`). 모든 `disabled:` 호출부(9곳)가 이미
`class` prop을 `disabled` prop보다 앞에 쓰고 있어서(`el()`이 prop을 객체
순서대로 처리) `node.className = value`가 먼저 실행된 뒤 `classList.add`가
따라붙는 순서가 보장되어 있었다 - 별도 수정 없이 그대로 호환됐다.
**교훈**: 더블탭 확대처럼 산발적으로 재현되는 터치 버그는 "항상 이 조건에서만
재현된다"는 사용자의 정확한 관찰이 나올 때까지는 CSS/JS 레벨의 일반적인 대응만
반복하며 헛수고할 수 있다 - 이번처럼 원인이 상태(disabled 여부)에 따라
갈리는 경우, 재현 조건을 좁혀주는 사용자 관찰이 결정적이었다.

**검증 방법**: Playwright `page.touchscreen.tap()`(실제 터치 시뮬레이션)으로
확인했다 - 골드를 0으로 만들어 소환 버튼을 비활성 상태로 만든 뒤, 그 버튼에
네이티브 `disabled` 속성이 없고(`btn.disabled === false`) `is-disabled`
클래스와 `aria-disabled="true"`만 붙어 있는지, 그 상태로 3번 연속 탭해도
필드에 아무것도 소환되지 않는지(클릭이 정상적으로 무시되는지) 확인했다. 이어서
골드를 복구해 버튼이 다시 활성화됐을 때 `is-disabled`/`aria-disabled`가
정상적으로 사라지고, 5번 연속 빠른 탭이 전부 정상 소환되는지(이 리팩터가
PR #43에서 고친 "빠른 연타" 자체를 다시 깨뜨리지 않았는지)도 함께 확인했다.

## 배경(비클릭 영역) 더블탭 확대 - 버튼/팝업 제외한 JS 방어선 재도입 (PR #44 배포 후)

PR #44(비활성 버튼 disabled 속성 제거) 배포 후 "배경 누를때도 확대되는데 이건
못고쳐?"라는 질문을 받았다. 재확인해보니 "배경"은 게임 화면 밖 여백이 아니라
**게임 화면 안의 빈 공간**(보스 위, 전장의 빈 칸, 하늘 부분 등 클릭 핸들러가
없는 영역)이었다.

**분석**: 이전에 모든 `touchend`에 `preventDefault()`를 거는 JS 방어선을
추가했다가 통째로 제거한 적이 있다(PR #43) - 그게 브라우저의 click 합성을
취소시켜서 버튼 연타가 막히는 회귀를 냈기 때문이다. 하지만 이번에 다시 보니
그 회귀는 **버튼(click 이벤트 의존)에서만** 발생하는 문제였다 - 이 프로젝트의
필드 칸 선택/드래그는 애초에 click이 아니라 `pointerdown`/`pointerup`을 직접
쓰고(`root`/`window`에 직접 리스너 등록), pointerup은 touchend보다 먼저 끝나는
별개의 이벤트라 나중에 touchend에 preventDefault를 걸어도 이미 끝난
pointerdown/pointerup 처리에는 영향을 주지 않는다. 즉 "배경"(보스/빈 칸/하늘 등
click 핸들러가 없는 영역)은 애초에 이 JS 방어선을 걸어도 안전했는데, 지난
라운드에 문제 소재를 좁히지 않고 전체를 통째로 제거해버린 것이었다.

**수정**: `main.js`에 더블탭 방지 JS를 다시 추가하되, 이번엔 `e.target.closest('button,
.popup-overlay')`로 버튼과(팝업 배경 클릭으로 닫는 `.popup-overlay`도 onclick을
쓰므로 함께) 제외했다 - 그 외의 모든 영역(필드 배경, 빈 칸, 보스, 하늘 등)에서만
같은 지점(`target` 동일)을 300ms 이내에 두 번 건드리면 더블탭으로 보고 확대만
막는다. `target` 동일 조건을 추가한 것도 이전 버전(순수 타이밍만 보던 버전)보다
개선된 점 - 서로 다른 두 곳을 빠르게 연달아 누르는 정상적인 조작까지 걸리지
않는다.

**검증 방법**: Playwright로 4가지를 확인했다 - (1) `page.touchscreen.tap()`
(실제 터치)으로 소환 버튼을 5번 연타해도 5번 다 정상 소환되는지(PR #43에서
고친 회귀가 재발하지 않는지), (2) `.game-stage`(배경)에 합성
`TouchEvent('touchend')`를 300ms 이내 두 번 쏘면 두 번째가
`defaultPrevented:true`가 되고 300ms 이후 세 번째는 아닌지, (3) 버튼에 같은
방식으로 두 번 빠르게 쏴도 둘 다 `defaultPrevented:false`(제외 로직이 실제로
작동)인지, (4) 빈 필드 칸을 실제 터치로 탭해도 정상 동작하는지(pointer 기반
로직이 영향받지 않는지) - 전부 기대대로 확인했다.

## 배트맨 기능 완성 + 로카 진행도 표시 + 몬스터 처치속도/마마 임프 카운트 재조정 (PR #45 배포 후)

PR #45 배포 후 새 기능 요청을 받았다: "배트맨 기능이 빠져있다"는 지적에 먼저 말로
설명을 요구받아(코딩 전에) 코드를 분석해서 설명하고 확인받은 뒤 구현했고, 이어서
몬스터 처치속도/마마 임프 카운트 수정 요청 + 대화 도중 "배트 강화 비용" 구간표가
추가로 도착했다. 로카 불멸 조건 점검 요청도 같이 받았다.

1. **배트맨 강화 버튼이 아예 안 뜨던 버그(신규 발견)**: 필드 칸의 "강화" 버튼은
   `heroDef.immortalCondition?.eventType === 'enhance'`인 영웅(아이언미야옹)에게만
   뜨도록 좁혀져 있었다(PR #19 - "강화는 캐릭터 클릭했을 때 나오면 안돼"라는 지적을
   반영하면서 진짜 필요한 두 특수 케이스만 남긴 것). 그런데 배트맨의 불멸 조건
   (`IMMORTAL_CONDITIONS.m_batman`)은 `eventType:'ultimateAttempt'`라 이 조건에 안
   걸렸다 - 강화 레벨이 10 이상이어야 승급 판정이 시작되는데(`extra.minEnhance:10`),
   강화 버튼 자체가 없어서 레벨을 10까지 올릴 방법이 없었다 - 즉 배트맨은 영원히
   불멸 승급이 불가능한 상태였다. 조건을 `eventType === 'enhance' ||
   immortalCondition.extra?.minEnhance != null`로 넓혀서 배트맨도 강화 버튼이
   뜨게 했다(`GameScreen.js`). 승급 로직 자체(`immortal.js`의 `m_batman` 틱 핸들러 -
   10강부터 5초마다 `1% + 2.5%×(레벨-10)` 확률로 즉시 승급)는 이미 정상 구현돼
   있었다 - 버튼만 고치면 자연히 작동한다는 걸 실제로 강화 12회 반복 호출 +
   `tickImmortalProgress`/`checkImmortalPromotion` 반복 호출 시뮬레이션으로 확인했다
   (2000초 시뮬레이션 내에서 실제로 `i_ace_batman`으로 승급되는 것까지 검증함).
2. **배트맨 전용 강화 골드 비용 구간표(사용자 추가 지정)**: 대화 도중 "배트 강화
   비용은 배트 레벨 0→1~5강: 30코인, 6~10강: 60, 10~15강: 120"이라는 수치가
   도착했다 - 다른 영웅은 전부 고정 30코인인데 배트맨만 예외. "5강마다 2배"
   패턴으로 일반화해서(1~5:30, 6~10:60, 11~15:120 - 정확히 이 세 구간과 일치)
   명시 안 된 그 이상 구간(16~20강 240...)에도 같은 패턴을 이어가도록
   `actions.js`에 `nextEnhanceGoldCost(heroId, currentLevel)`를 추가했다
   (`ENHANCE_GOLD_COST × 2^floor((다음레벨-1)/5)`). 이 비용표는 승급 전
   `m_batman`에만 적용된다 - 승급 후(`i_ace_batman`)는 `enhanceLevel`이 0으로
   리셋되고(`promoteInstance`가 매번 `createHeroInstance`로 개체를 통째로 새로
   만들기 때문) 더 강화할 이유(승급 조건은 이미 끝났고 데미지 계산도 범위 밖)가
   없어서 강화 버튼 자체를 승급 후엔 노출하지 않는다(강화 버튼 조건은 여전히
   `heroDef.immortalCondition` 유무로 걸리는데 승급 후 heroDef엔 그 필드가 없음).
3. **에이스 배트맨(불멸) "모드 변신" 신규 구현**: `assets.js`의
   `ACE_BATMAN_TRANSFORM_IMAGES`(투수모드/타자모드)가 이미지 매핑만 해두고
   몇 세션째 아무 로직에도 연결이 안 된 채 방치돼 있었다(사용자가 지적한
   "빠진 기능"의 나머지 절반). `instance.batmanMode`(null|'pitcher'|'batter')
   필드를 추가하고 `actions.js`의 `cycleBatmanMode()`가 매 클릭마다 그 세
   상태를 순환시킨다(데미지 계산 범위 밖이라 실질 효과 없는 연출용 전환 - 이
   프로젝트의 다른 변신들과 같은 패턴). `heroVisual.js`의 `resolveHeroImage()`에
   `i_ace_batman` + `batmanMode` 있을 때 해당 이미지를 쓰는 분기를 추가했다.
   칸 아래 버튼 스택에 `i_ace_batman`일 때만 "변신 (현재: N모드)" 버튼이 뜨고,
   누를 때마다 기본→투수→타자→기본으로 순환한다. Playwright로 버튼을 3번 눌러
   라벨과 실제 `<img src>`가 매번 올바르게 바뀌는지(타자/투수 이미지 파일명이
   실제로 포함되는지) 확인했다.
4. **몬스터 처치 속도 재계산 - "10라운드 시작 4초 후 전멸" 조건**: 예전엔 "10라운드가
   144초 안에 0마리가 될 수 있어야 한다"(`MONSTER_KILL_RATE_PER_HERO_PER_SEC=0.04`)
   였는데, 사용자가 훨씬 강한 조건으로 재지정했다 - "10라운드 시작하고 4초가
   지났을 때 모든 몬스터가 다 사라지는 수준". 얼핏 "110마리를 4초 안에 죽여야
   하나" 싶지만, 처치가 매 라운드 내내 스폰을 갉아먹는 구조라 k값을 올리면
   10라운드 시작 시점의 누적치 자체가 이미 훨씬 줄어드는 효과가 있다 - 필드가
   5~6라운드 만에 꽉 찬다(30마리)는 기존 확립된 가정으로 실제 `tickWave()` 함수를
   Playwright 안에서 반복 호출(라운드 1~9를 진짜로 시뮬레이션)해 이분탐색으로
   구했다. `k=0.06`(예전 0.04에서 1.5배)이면 9라운드가 끝날 때 이미 몬스터가
   0에 수렴하고, 10라운드 진입 직후(0.02~3초 사이) 즉시 전멸하는 걸 실제 코드로
   확인했다 - "110마리를 4초 만에" 식의 극단적인 k가 필요하지 않았다(그런
   극단값은 반대로 정상 라운드의 몬스터 누적을 거의 0으로 만들어서 "몬스터가
   화면에 쌓여 보이는" 기존 연출 의도를 깨버렸을 것 - 시뮬레이션으로 이 트레이드
   오프를 미리 확인하고 고른 값이다). **주의**: 이 계산은 "필드가 5~6라운드
   만에 30마리로 찬다"는 이 프로젝트에서 반복 사용된 가정에 의존한다 - 훨씬 적은
   영웅으로 느리게 플레이하는 경우까지 4초 조건을 보장하지는 않는다(그런 극단적
   시나리오는 사실상 10라운드 전에 몬스터 누적치가 110에 도달해 먼저 패배할
   가능성이 높다).
5. **마마 임프가 캐릭터 카운트(인원 30 상한)에 잡히던 버그 수정**: `fieldOccupantCount()`
   (인원 X/30 표시, 소환/룰렛/조합의 "필드 꽉 참" 판정에 전부 재사용되는 공용
   함수)가 임프(`x_imp`)까지 그대로 합산하고 있었다 - 마마가 임프를 계속 만들어
   내면 진짜 영웅을 소환할 자리가 임프한테 다 뺏기는 버그였다(사용자 - "지금은
   캐릭터 카운트에 들어가서 30에 금방 차버려"). `fieldOccupantCount()`에서
   `IMP_HERO_ID` 개체를 필터링해서 빼도록 고쳤다(칸을 물리적으로 차지하는
   것 자체는 그대로 유지 - 진짜로 필드에 빈 칸이 하나도 없으면 임프도 못 놓이는
   게 맞다, 여기서 빼는 건 "인원수 상한 30" 판정에서만). **여기서 파생된 추가
   버그**: `findAutoPlaceSlot()`의 캡 체크(`fieldOccupantCount(state) >=
   fieldMaxCapacity`)가 임프 배치에도 그대로 적용되고 있어서, 진짜 영웅이 정확히
   30마리로 꽉 찬 상태에서는(카운트 제외 여부와 무관하게) 임프가 물리적으로
   빈 칸이 있어도 하나도 못 나오는 상태가 남아있었다 - "임프는 캐릭터 카운트에
   안 들어간다"는 의도라면 이 캡 체크 자체도 임프에는 적용하면 안 되는 게
   일관적이라, `heroId !== IMP_HERO_ID`일 때만 캡을 확인하도록 가드를 추가로
   넓혔다. Playwright로 실제 영웅 30마리(칸당 3마리 스택 활용)로 필드를 채운 뒤
   `fieldOccupantCount`가 정확히 30인지, 그 상태에서 `findAutoPlaceSlot`으로
   임프 3마리를 추가로 더 배치할 수 있는지(물리적 빈 칸 존재), 배치 후에도
   `fieldOccupantCount`가 여전히 30(임프 제외)으로 유지되는지 확인했다.
6. **로카 불멸 조건 - 재확인 결과 이미 정상 동작, 진행도 표시만 신규 추가**:
   `IMMORTAL_CONDITIONS.m_roka`(시간 기반, 10초마다 1~5 누적, 목표 160)는
   개체별 특수 핸들러가 필요 없는 순수 제네릭 케이스라 `immortal.js`의
   `applyGenericTick`/`isEligible`/`isImmortalPromotionReady` 기본 분기를 그대로
   타고 있었다 - 코드를 다시 읽고 실제로 `tickImmortalProgress`를 반복 호출해
   `progress`가 정상적으로 쌓이는 것까지 확인했지만 버그는 없었다(코드 변경
   없음). 사용자가 추가로 요청한 "장전된거 캐릭터 위에 적어주면 더 좋고"는 신규
   기능으로 구현 - `renderHeroTokenLayer`(`GameScreen.js`)에서 `m_roka` 개체마다
   `.roka-charge-badge`("장전 N/160")를 캐릭터 토큰 바로 위에 항상(칸 선택 여부와
   무관, 인디 발굴 게이지와 같은 패턴) 띄운다. **주의**: PR #27에서 "마마뿐만
   아니라 다른 신화들도 진행도 표시하지 말라"고 명시적으로 정정했던 것과 정면으로
   반대되는 요청이다 - 로카만 예외로 다시 켠 것이니, 나중에 로카 배지가 안
   보인다는 리포트가 오면 이 예외를 잊지 말 것(다른 신화들은 여전히 진행도 숨김
   상태 그대로).

**검증 방법**: 이전 라운드들과 동일하게 `window.__debug` 훅(이번엔 `tickWave`,
`enhanceHero`, `nextEnhanceGoldCost`, `cycleBatmanMode`, `tickImmortalProgress`,
`checkImmortalPromotion`, `fieldOccupantCount`, `findAutoPlaceSlot` 등 이번 라운드
검증에 필요한 함수를 전부 노출)을 `main.js`에 임시로 추가해 Playwright로
검증하고 끝난 뒤 다시 제거했다(디버그 전용, 커밋에 남기지 않음 - 제거 후 재빌드한
JS 번들 해시가 디버그 훅 추가 전 최초 빌드와 정확히 일치하는 것까지 확인해 잔여
코드가 없음을 재차 확인). 로직 검증은 실제 게임 코드 함수(`tickWave` 등)를 그대로
호출하는 방식으로 했다 - 초반에 몬스터 처치 시뮬레이션을 자체 재구현한
스크립트로 먼저 돌렸다가, 나중에 실제 코드로 다시 검증하는 과정에서 테스트
스크립트 자체의 버그(필드 24칸에 영웅을 칸당 1마리씩만 채워서 최대 24마리밖에
못 채우던 헬퍼 함수 버그 - 실제로는 칸당 3마리까지 스택 가능)를 발견해 고쳤다 -
자체 재구현 시뮬레이션은 방향성을 잡는 데는 유용했지만 실제 코드 검증을
대체하지 못한다는 걸 다시 확인한 사례.

## 로카 배지 정정 - 누적 진행도가 아니라 "매번 굴린 장전량" (PR #46 배포 전)

바로 위 라운드에서 만든 로카 배지("장전 N/160", 누적 진행도)를 사용자가 즉시
정정했다 - "장전 진행도가 아니라, 10초엿나 그 몇초동안 한번씩 1~5씩 장전을
하잖아 그 숫자를 적어달라고." 즉 목표까지 남은 누적치가 아니라 **매 틱마다
실제로 몇을 굴렸는지(1~5 중 하나)** 그 자체를 보여달라는 요청이었다 - 완전히
다른 정보라 UI 문구만 바꾸는 게 아니라 그 값을 아예 어디에도 저장하고 있지
않아서(제네릭 틱 핸들러 `applyGenericTick`은 굴린 값을 바로 `progress`에 더하고
버림) 새로 기록해야 했다.

`immortal.js`의 `tickOverrides`에 `m_roka` 전용 핸들러를 추가했다(기존
`applyGenericTick`과 로직은 동일 - 10초마다 1~5 누적 - 이지만 매번 굴린 값을
`instance.lastChargeAmount`/`instance.lastChargeAt`에도 같이 남긴다). 배지
텍스트를 `장전 ${progress}/${target}`에서 `장전 +${lastChargeAmount}`로
바꿨다(첫 틱이 돌기 전에는 `lastChargeAmount`가 아직 없으니 "장전 대기중" 표시).
`progress` 자체는 여전히 그대로 누적되고 있어서(불멸 승급 판정용 - 이건 여전히
필요) 승급 로직에는 영향 없다 - 화면에 보여주는 값만 "무엇을 위한 숫자인지"를
바꾼 것.

Playwright로 실제 `tickImmortalProgress`를 1초씩 200번 반복 호출해(틱 간격
10초보다 짧은 간격이라 한 번의 호출에서 최대 1번만 장전이 발생하는 걸 이용해
progress 증가분 = 그 순간의 lastChargeAmount라는 걸 교차검증) 20번 안팎의 장전이
발생하고 매번 1~5 사이 값이 기록되며 배지에도 그 값이 정확히 반영되는지
확인했다. **교훈**: "진행도 배지"류 요청은 숫자 하나만 바꾸면 되는 경우도 있지만,
이번처럼 요구하는 게 아예 다른 값(누적치 vs 개별 이벤트 값)일 수 있다 - 사용자가
"그게 아니라"로 정정하면 표시 텍스트만 고치지 말고 애초에 그 값 자체가 상태에
저장되고 있는지부터 다시 확인할 것.

## 마마 승급 조건이 마마 마리수에 비례해 늘어나던 버그 + 불멸 종류별 1마리 제한 신규 규칙

사용자가 두 가지를 지적했다: (1) "마마는 필드에 몇마리가 있건 간에 임프 9마리가
있으면 불멸 변신이 가능해(돌파면 7마리). 지금 2마리 마마 뽑으면 12마리에 변신이
가능하게 된다" - 승급 조건이 전역 임프 수가 아니라 마마 개체 각각이 "자기가
만든" 임프 수를 세고 있었던 버그. (2) "필드에는 무조건 불멸이 종류별로 1마리씩만
있어야 해 - 마마 2개를 뽑아서 2마리 전부를 마마로 만드는 게 아니라, 1마리만
불멸 만들고 나머지는 일반으로 무조건 남는거야" - 이건 마마 한정이 아니라 모든
불멸에 적용되는 일반 규칙으로 요구한 것.

1. **마마 승급 조건을 전역 임프 수 기준으로 정정**: `instance.impStock`(마마
   개체별로 "자기가 직접 생성에 성공한" 임프 수만 세는 카운터)를 완전히 제거하고,
   `countHeroOnField(state, IMP_HERO_ID).count`(필드에 실존하는 임프 개수를 그때
   그때 세는 함수, 이미 `synthesis.js`에 있던 걸 재사용)로 대체했다 -
   `promotionHandlers.m_mama`, `isImmortalPromotionReady`의 `m_mama` 분기,
   `tickMamaImps`(생성 정지 조건도 전역 9마리 기준으로) 세 곳 전부. 마마가
   여러 마리면 각자의 생성 타이머가 같은 전역 풀에 기여하므로(생성 *속도*는
   여전히 마마 마리수에 비례해 빨라짐 - 이건 사용자가 문제삼지 않은 부분이라
   그대로 둠), 정확히 9마리(또는 돌파 시 7마리)에서 멈추고 그 이상은 안 쌓인다.
   Playwright로 마마 2마리 + 임프를 정확히 9마리로 채운 상태에서 두 마마 모두
   동시에 "승급 가능"으로 표시되는지(마리수와 무관하게 9마리 하나의 기준),
   `tickMamaImps`를 실제로 400초 시뮬레이션했을 때 마마가 몇 마리든 총 임프가
   9를 넘지 않는지 확인했다.
2. **불멸은 종류별로 필드에 최대 1마리 - 신규 공통 규칙**: `immortal.js`의
   `checkImmortalPromotion()`과 `isImmortalPromotionReady()` 양쪽에 "승급하려는
   불멸 heroId(`cond.id`)와 같은 개체가 이미 필드에 있으면 무조건 거부"하는
   가드를 추가했다 - 마마 전용이 아니라 모든 신화→불멸 승급에 적용되는 제네릭
   체크다(어떤 신화든 필드에 2마리 이상 있을 수 있는 경우라면 전부 해당). 두
   마마가 동시에 "승급 가능!"으로 뜨더라도, 플레이어가 그 중 하나를 클릭해서
   실제로 승급시키면(임프 전역 풀도 그 순간 0으로 리셋됨 - 1번 수정과 자연히
   맞물림) 남은 마마는 화면에서 바로 "승급 가능" 아이콘이 사라지고, 이후 다시
   임프를 9마리까지 채워도(이미 같은 종류의 불멸이 필드에 있으므로) 계속 승급이
   막힌다 - "1마리만 불멸이 되고 나머지는 일반으로 무조건 남는다"는 사용자
   요구사항을 그대로 만족한다. `checkImmortalPromotion`에도 같은 가드를 넣어둔
   이유는 UI(`isImmortalPromotionReady`로 미리 걸러진 아이콘)를 거치지 않고
   직접 호출되는 경로에 대한 최종 방어선이 필요해서다. Playwright로 (a) 한
   마마를 승급시킨 뒤 남은 마마가 정상적인 신화 `m_mama`로 그대로 남아있는지,
   필드에 `i_grand_mama`가 정확히 1마리만 있는지, (b) 이미 그랜드 마마가 있는
   상태에서 다른 마마의 임프를 9마리로 채워도 승급 후보로 뜨지 않고 직접
   `checkImmortalPromotion`을 호출해도 `immortal-already-exists` 사유로 거부되는지,
   (c) 단일 마마의 정상 승급 흐름(회귀 확인)까지 전부 확인했다.
3. **`GameScreen.js`의 죽은 코드 정리**: `extraStatusText()`에 남아있던
   `instance.impStock` 참조 분기(호출부에서 `instance.heroId !== 'm_mama'`
   가드로 애초에 마마는 절대 이 함수를 안 타서 실행될 일이 없던 죽은 코드)를
   삭제했다 - impStock 필드 자체가 이제 존재하지 않으니 그대로 뒀다면 나중에
   헷갈릴 수 있었다.

## 배트맨 강화에서 행운석 비용 제거

사용자 지적: "배트 강화 비용에 행운석은 안들어가. 빼." 다른 영웅은 전부 강화 시
골드+행운석(`ENHANCE_LUCKSTONE_COST`, 1개) 둘 다 소모되는데, 배트맨은 골드
구간표(1~5강 30/6~10강 60/11~15강 120...)만 적용되고 행운석은 아예 안 들어야
한다는 정정이다. `actions.js`에 `nextEnhanceLuckstoneCost(heroId)`를 추가해서
`m_batman`이면 0, 그 외엔 기존처럼 `ENHANCE_LUCKSTONE_COST`(1)를 반환하도록 하고
`enhanceHero()`가 이 값을 실제 차감/부족 판정에 쓰도록 고쳤다. `GameScreen.js`의
강화 버튼도 같은 함수로 라벨을 만들어서, 행운석 비용이 0이면 "강화 (30G)"처럼
💎 표기 자체를 아예 안 보여주고(다른 영웅은 여전히 "강화 (30G 1💎)"), 비활성화
판정에서도 행운석 부족 여부를 더는 따지지 않는다. Playwright로 행운석을 0으로
만든 상태에서 배트맨은 정상적으로 강화되고(행운석 그대로 0 유지, 골드만 차감)
일반 영웅(`h_hunter`)은 자원 부족으로 막히는 것, 버튼 라벨에 💎가 안 뜨고
행운석 0인 상태에서도 버튼이 비활성화되지 않는 것까지 확인했다.

## 배트맨 강화 성공확률 추가 + 다른 영웅 강화 비용 완전 제거

바로 이전 라운드에서 개인 강화(엔핸스) 비용을 손보고 나니, 사용자가 두 가지를
한 번에 정정했다: (1) "강화 확률을 안알려줬네. 0→10은 100% 10→11 83, 11→12 69,
12→13 55, 13→14 41, 14→15 28%" - 배트맨 강화가 그동안 항상 성공한다고
가정했었는데, 실제로는 10강부터 확률적으로 실패할 수 있는 거였다. (2) "다른
영웅들은 무슨 골드랑 행운석이야? 그런거 없는데" - 직전 커밋에서 "배트맨만
행운석이 빠지고 다른 영웅(아이언미야옹 등)은 여전히 30G+1💎가 든다"고 구현했던
게 애초에 잘못된 가정이었음이 드러났다 - **개인 강화(enhanceHero)에 비용이
있는 건 배트맨 하나뿐**이고, 나머지 영웅(강화 "횟수"가 불멸 조건인 아이언미야옹
등)은 원래부터 완전 무료였다. 30G/1💎이라는 값 자체가 이 기능을 처음 만들 때의
추측성 플레이스홀더였을 뿐 한 번도 확정된 적이 없었다.

1. **다른 영웅 강화를 완전 무료로**: `actions.js`의 `nextEnhanceGoldCost()`를
   `heroId !== 'm_batman'`이면 0을 반환하도록 바꾸고(기존엔 `ENHANCE_GOLD_COST`
   고정값), `nextEnhanceLuckstoneCost()`도 아예 배트맨 여부와 무관하게 항상
   0을 반환하도록 단순화했다(배트맨도 행운석은 안 쓰므로 - 직전 라운드 결정
   그대로 유지). `ENHANCE_GOLD_COST`/`ENHANCE_LUCKSTONE_COST` export 상수
   자체를 지우고 배트맨 전용 내부 상수 `BATMAN_ENHANCE_GOLD_BASE`(30)로
   대체했다 - 다른 곳에서 이 상수를 참조하는 데가 없어서 완전히 걷어낼 수
   있었다(회귀 확인용으로 grep까지 다시 훑었음).
2. **배트맨 전용 강화 성공확률 신규 구현**: `BATMAN_ENHANCE_SUCCESS_RATE = {11:
   0.83, 12:0.69, 13:0.55, 14:0.41, 15:0.28}`(다음 레벨 기준 - 10강까지는
   무조건 100%). `enhanceHero()`가 비용을 먼저 차감한 뒤(시도 자체는 항상
   비용이 들고, **실패해도 그 비용은 그대로 소모된다** - 사용자가 실패 시
   페널티를 명시하지 않아서 "시도 비용은 늘 소모, 실패해도 레벨/개체에 추가
   페널티는 없음"이라는 가장 무난한 강화 게임 관례로 가정해뒀다, 나중에
   다르다는 지적이 오면 이 가정부터 재검토할 것) 성공 여부를 굴려서, 실패하면
   `enhanceLevel`을 그대로 두고 `{success:true, leveledUp:false}`를 반환한다
   (호출부의 `apply()`는 `success`만 보고 그대로 반영하므로 실패해도 골드
   차감은 정상적으로 화면에 반영됨). **15강을 넘는 구간(16강+)은 데이터가 없어
   마지막 값(28%)을 그대로 유지하는 플레이스홀더**로 남겨뒀다 - 명시된 구간이
   강화 비용표의 마지막 구간(11~15강, 120G)과 정확히 겹치는 걸 보면 이쯤이
   설계상 실질적 한계일 가능성이 높지만, 확정된 근거는 아니니 나중에 더 높은
   강화를 실제로 시도하는 상황이 나오면 재확인이 필요하다. `GameScreen.js`의
   강화 버튼에도 `nextEnhanceSuccessRate()`로 성공률을 같이 보여준다(100%일
   땐 표시 생략) - "2차 변신(성공 50%)" 버튼과 같은 표기 패턴을 재사용했다.
3. **버튼 라벨을 비용 유무에 따라 동적으로 구성**: 골드/행운석 비용이 둘 다
   0이면(배트맨 외 전부) 괄호 자체를 안 붙이고 그냥 "강화"만 보여준다 - 이전엔
   무조건 `(${goldCost}G ...)` 형태로 "0G"가 찍히는 어색한 표시가 될 뻔했다.
   Playwright로 (a) 성공확률표 전 구간(10강 이전 100%, 11~15강 표 그대로,
   16강+는 28% 유지) 순수 함수 검증, (b) 다른 영웅(h_hunter)이 골드/행운석
   0인 상태에서도 무료로 항상 성공하는지, (c) 배트맨 10→11강 시도를 2000회
   반복해 실측 성공률이 83% ±3%p 오차 안에 드는지(통계적 검증 - 결정론적으로
   확인 불가능한 확률값이라 대량 시행으로 근사), (d) `Math.random`을 강제로
   고정해 실패를 100% 재현시켜서 그 상황에도 골드는 그대로 빠지고 레벨은 안
   오르는지, (e) UI에서 아이언미야옹 버튼은 "강화"만, 배트맨 버튼은
   "강화 (120G 성공 83%)" 식으로 표시되고 💎는 안 보이는지까지 전부 확인했다.

## 아이언미야옹 3단계 진행 재구현 (기획서 재확인 + 사용자 지정 수치)

직전 라운드에서 "다른 영웅은 강화가 무료"라고 단정하고 아이언미야옹도 그 일반
규칙에 묶어버렸는데, 사용자가 강하게 정정했다 - "아이언 미야옹은 기획서에도
적어놨다... 첫번째 변신때 5행운석, 두번째 변신때 10행운석, 그리고 기술
강화할때 행운석 1개씩 사용하면서 확률적으로 기술 강화가 되고 10이 되면은 불멸
되는거다." 확인해보니(NFD로 정규화된 파일명이라 처음엔 grep/파이썬 glob이
전부 매치를 못 했다 - `unicodedata.normalize('NFC', f)`로 정규화해야 검색됨)
`길드레이드_연습게임_기획서_v4.md`/`길드레이드_연습게임_기술설계서_v3.md`
어디에도 이 변신 비용(5/10행운석)은 실제로 적혀있지 않았다 - 문서 5-3절엔
"아이엠 미야옹 | 아이언미야옹 기술 강화 10강 | 강화 버튼으로 해당 개체 10강
달성 시 승급"이라고만 있고 비용/변신 단계 얘기가 없다. 문서에 없는 내용이라도
사용자가 직접 알려준 수치가 최신/우선이므로, 사용자에게 사과하고 빠진 수치
(기술 강화 성공확률 - 문서에도 사용자 말에도 없었음)만 추가로 확인받아
(**10% 고정**) 그대로 구현했다. **교훈**: "기획서에 적혀있다"는 사용자 말을
곧이곧대로 믿지 말고 실제로 다시 열어서 확인은 하되, 확인 결과 문서에 없더라도
사용자가 직접 준 수치를 무시하면 안 된다 - 문서가 최신이 아닐 수도 있고,
사용자가 다른 자료(자신의 메모 등)를 보고 있을 수도 있다. 애초에 "다른
영웅들은 골드/행운석이 없다"는 지난 라운드의 판단이 아이언미야옹에까지
과잉 일반화된 게 이번 사태의 근본 원인 - 개별 영웅 지적을 받으면 그 영웅
하나만 고치고 다른 영웅으로 일반화하기 전에 각 영웅의 실제 기획을 다시
확인했어야 했다.

**구현**: `heroes.js`의 `IMMORTAL_CONDITIONS.m_iron_meyaong`에
`extra:{transform1LuckstoneCost:5, transform2LuckstoneCost:10,
enhanceLuckstoneCost:1, enhanceSuccessRate:0.1}`을 추가하고 `eventType`을
`'enhance'`(예전 제네릭 강화 버튼과 충돌하던 이름)에서 `'ironMeyaongEnhance'`로
바꿨다. `actions.js`에 전용 함수 `advanceIronMeyaong(state, instanceId)`를
새로 만들었다 - `instance.meyaongTransformStage`(0/1/2)를 보고 지금 해야 할
액션을 자동으로 판단한다: 0이면 1차 변신(5💎, stage→1), 1이면 2차 변신(10💎,
stage→2), 2가 되고부터는 "기술 강화" 시도(1💎/회, 10% 확률로만 성공 - 성공해야
`progress`가 +1, 실패해도 소모한 행운석은 배트맨 강화 실패와 같은 관례로
돌려주지 않음). `progress`가 목표(10)에 도달하면 기존 제네릭 승급 판정
(`checkImmortalPromotion`)이 그대로 동작해서 `i_im_meyaong`으로 승급된다 -
새 필드(`meyaongTransformStage`)만 추가했을 뿐 승급 판정 자체는 그대로
재사용했다. `GameScreen.js`는 예전에 있던 제네릭 "강화" 버튼 조건에서
`eventType==='enhance'` 분기를 완전히 제거하고(이제 이 이벤트타입을 쓰는
영웅이 없음 - `enhanceHero()`는 배트맨 전용으로 정리) `instance.heroId ===
'm_iron_meyaong'` 전용 블록을 새로 만들어 stage에 따라 라벨/비용이 바뀌는
버튼 하나를 보여준다("1차 변신 (5💎)" → "2차 변신 (10💎)" → "기술 강화
(1💎 성공 10%)"). 승급 조건을 다 채우면(왼쪽 즉시소환 바에 "승급 가능!") 이
버튼은 마마의 "돌파" 버튼과 같은 패턴으로 숨긴다. `heroVisual.js`의
`resolveHeroImage()`에도 `meyaongTransformStage`에 따라
`IRON_MEYAONG_TRANSFORM_IMAGES[stage-1]`를 보여주는 분기를 추가해서, 변신할
때마다 실제로 이미지도 바뀐다(예전엔 이 이미지 파일들이 매핑만 되고 아무
로직에도 연결이 안 된 채 방치돼 있었다 - 배트맨 변신모드와 같은 계열의
문제였음).

**검증 방법**: 이전 라운드들과 동일하게 `window.__debug` 훅을 임시로 추가해
Playwright로 검증했다 - (1) 1차/2차 변신 각각 행운석 부족 시 실패, 정확한
비용(5/10)으로 성공 시 stage가 오르고 행운석이 정확히 소모되는지, (2) 기술
강화를 3000회 반복 시행해서 실측 성공률이 10% ±2%p 오차 안에 드는지(통계적
검증), (3) `Math.random`을 강제로 고정해 실패/성공을 각각 100% 재현시켜서
실패해도 행운석은 소모되고 progress는 그대로인지, 성공하면 progress가
정확히 +1 되고 10에서 승급 후보로 뜨는지, (4) `checkImmortalPromotion`으로
실제 `i_im_meyaong` 승급까지, (5) UI에서 단계별 버튼 라벨이 정확한지, 1차/2차
변신 버튼을 실제로 클릭했을 때 필드 위 이미지가 `transform1`/`transform2`
파일로 실제로 바뀌는지까지 전부 확인했다.

## 로카 탄약 시스템 + 즐겨찾기 바 승급 아이콘 중복 제거 + 마마 임프 생성 속도 단순화

세 가지를 한 번에 받았다.

1. **로카 배지를 "장전 +N/대기중" 문구에서 순수 숫자만으로**: 사용자가 "로카 위에
   장전 대기중 이런거 쓸데없는 말 다 빼... 숫자만 적으라고"라고 정정 - 텍스트를
   깎는 것에서 그치지 않고, 아예 로카에게 "탄약(ammo)" 개념을 새로 도입해야 하는
   추가 요청이 이어졌다: "장전이 되었으면 0.5초당 1발씩 없애. 그리고 다시
   쿨타임 차면 장전해." `immortal.js`의 `m_roka` 틱 핸들러를 재작성 - 기존
   10초마다 1~5 장전 로직은 그대로 두되(승급용 `progress`는 계속 누적), 이제
   그 굴린 값을 `instance.ammo`에도 더한다(재장전은 누적 - 기존 탄약 위에
   얹힘). 탄약이 1발 이상이면 별도의 0.5초 간격 타이머(`ROKA_FIRE_INTERVAL_SEC`)로
   1발씩 깎아나가고, 0이 되면 다음 장전 틱까지 그대로 대기한다(발사 타이머
   자체도 리셋해서 다음 장전 직후 0.5초부터 다시 세게 함). `GameScreen.js`의
   배지는 이제 `${occ.ammo ?? 0}` 숫자 하나만 표시 - 0이어도 그냥 "0"이고
   "대기중" 같은 문구는 아예 없다.
2. **로카 배지 위치를 머리 바로 위로**: 기존엔 토큰 박스의 top(box top)을 그대로
   좌표로 썼는데, 신화 등급은 박스가 실제 캐릭터 그림보다 훨씬 커서(object-fit:
   contain의 여백 - 박스 비율과 실제 인물 이미지 비율이 안 맞으면 위쪽에 빈
   공간이 생김) 배지가 머리에서 한참 떨어져 보였다(사용자가 스크린샷 첨부 -
   "너무 멀어"). 순수 좌표 계산만으로는 실제 그림이 박스 안 어디에 있는지 알
   수 없어서(이미지마다 비율이 다름), Playwright 스크린샷으로 직접 눈으로
   보면서 `ROKA_BADGE_TOP_RATIO`(토큰 박스 top에서 얼마나 아래로 내릴지 -
   0.62 → 0.92 → 0.86 순서로 조정, 최종 0.86 = 박스 높이의 86% 지점)를 튜닝했다.
   **교훈**: 신화/불멸처럼 박스가 실제 그림보다 큰 등급에서 "머리 위"처럼 그림
   내용물 기준 좌표가 필요할 땐 박스 좌표만으로 계산하지 말고 반드시 스크린샷
   으로 실측할 것 - 순수 수식(박스 aspect ratio 비교 등)으로는 실제 여백이 어디
   생기는지 예측이 빗나갔다(이번에도 처음 계산은 틀렸음).
3. **즐겨찾기 바 승급 아이콘 중복 제거**: 마마를 2마리 뽑아서 둘 다 승급 조건을
   채우면 왼쪽 즉시소환 바에 "승급 가능!" 아이콘이 2개 뜨고 있었다 - 사용자
   지적: "불멸 왼쪽에 띄울 때 하나만 띄워라... 필드에는 1마리 정상 적용되어도
   왼쪽에도 적용되어야지"(필드에는 이미 불멸 종류별 최대 1마리 규칙이 있는데
   왼쪽 아이콘 목록은 그 규칙과 무관하게 개체(instanceId) 단위로 나열하고
   있었다). `favoriteBarItems()`가 `promoteHeroIds` Set으로 heroId 중복을
   걸러서 먼저 찾은 개체 하나만 대표로 보여주도록 고쳤다 - 필드 순회 순서상
   먼저 발견되는 개체가 대표가 된다(어느 걸 눌러도 결과는 사실상 같음 - 마마는
   공유 임프 풀 기준이라 하나가 승급하면 나머지도 다시 조건을 채워야 함).
4. **마마 임프 생성 속도를 1~10초 랜덤으로 단순화**: 기존엔 돌파/라운드8 이후/
   전설강화 여부에 따라 3단계 고정값(7.5/5/12.5초, 강화 시 절반)이 복잡하게
   얽혀 있었는데, 사용자가 "너무 빨라서... 1~10초 사이로 랜덤 적용하자"고
   전면 단순화를 요청했다. `heroes.js`의 `IMMORTAL_CONDITIONS.m_mama.extra.
   impIntervalSec`를 고정 구조에서 `[1, 10]` 배열 하나로 바꾸고,
   `immortal.js`의 `tickMamaImps()`도 돌파/라운드/강화 분기를 전부 제거하고
   매 장전마다 `rollValue([1,10])`으로 다음 간격을 다시 뽑는 방식으로 재작성
   했다(다른 시간 기반 불멸 조건들과 같은 rollValue 패턴 재사용).

**검증 방법**: 이전 라운드들과 동일하게 `window.__debug` 훅을 임시로 추가해
Playwright로 검증했다 - 로카는 실제 `tickImmortalProgress`를 0.1초 간격으로
30초간 반복 호출해서 10초 근처에 처음 장전되고 그 후 0.5초마다 정확히 1씩
줄어들다 0에서 멈추는지, 배지가 숫자만(문구 없이) 나오는지, 실제 DOM 좌표로
배지가 토큰 박스 하단 25% 이내(머리 근처)에 오는지 확인했다. 마마는
`tickMamaImps`를 0.05초 간격으로 반복 호출해 임프가 실제로 생성되는 시점
간격들을 측정해서 전부 1~10초 사이이고 매번 다른 값(변동성)인지 확인했고,
즐겨찾기 바는 마마 2마리를 각각 임프 9마리 조건을 채운 상태로 만들어(로직상
둘 다 `isImmortalPromotionReady`가 true) 실제 렌더된 `.favorite-icon-promote`
개수가 정확히 1개인지 DOM으로 확인했다.

## 배트맨 강화 실패 시 초기화 + 머리 위 배지 확장(배트맨/밤바) + 베인 쿨타임 게이지 + 배트맨 모드 좌우 버튼화 + 채드 화살표 애니메이션 제거

한 번에 여러 요청을 받았다.

1. **배트맨 강화 실패 시 0강으로 초기화**: 사용자 지정 - "배트 강화 실패하면
   0으로 돌아가야해". `enhanceHero()`에서 `leveledUp`이 false인 분기에
   `found.instance.heroId === 'm_batman'`이면 `enhanceLevel = 0`으로 리셋하는
   한 줄을 추가했다(10강 미만은 성공확률 100%라 애초에 이 분기를 안 타므로
   영향 없음 - 11강 이상 시도에서 실패할 때만 발동).
2. **머리 위 숫자 배지를 배트맨/밤바까지 확장**: 로카 전용이던 `roka-charge-badge`
   클래스와 `ROKA_BADGE_TOP_RATIO` 상수를 `hero-head-badge`/`HEAD_BADGE_TOP_RATIO`로
   개명해서 재사용 가능하게 만들고, 배트맨(`m_batman`, 현재 강화 레벨)과
   밤바(`m_bamba`, 불멸 조건 진행도/스택 수)에도 같은 자리에 숫자만 띄우도록
   추가했다(사용자 지정 - "몇강인지는 배트 머리 위에 로카처럼", "밤바 강화
   수치도 로카처럼 머리에 적어줘"). 승급 후(에이스 배트맨)는 enhanceLevel이
   0으로 리셋되고 의미가 없어서 배지가 안 뜬다(m_batman일 때만 조건부).
3. **탑 베인 궁극기 쿨타임 게이지 신규 추가**: 사용자 요청 - "베인 구를때 쿨타임
   차는거 밑에 바로 보여주면 좋겠어"(궁극기 임계치를 향해 채워지는 걸 인디
   발굴 게이지처럼 캐릭터 밑에 항상 보여달라는 뜻으로 해석). 문제는 기존
   `nextUltimateAt`(다음 궁 발동 임계치)만 저장돼 있어서 "지금 이 구간이 어디서
   시작했는지"를 몰라 채움 비율을 계산할 수 없었다 - `immortal.js`의
   `recordImmortalEvent`에 `instance.ultimateWindowStart`(이번 구간 시작 시점의
   누적 이동수)를 새로 기록하도록 추가했다(최초 설정 시/매 궁 발동 재설정 시
   둘 다). `GameScreen.js`의 `renderBaneUltimateGauge()`가
   `(progress-start)/(next-start)`로 채움 비율을 계산해서 인디 게이지와 같은
   시각 스타일(`indy-treasure-gauge` 클래스 재사용)로 그린다 - 선택 여부와
   무관하게 항상 표시.
4. **에이스 배트맨 모드 선택을 순환 버튼에서 좌우 버튼으로 전면 교체**: 기존
   "변신 (현재: N모드)" 한 개 버튼을 누를 때마다 기본→투수→타자→기본으로
   순환하던 방식을 사용자가 거부했다 - "배트 불멸 소환했을 때 배트의 좌우로
   투수, 타자 이렇게 버튼을 만들어줘. 지금처럼 하지 말고. 그리고 한번 변신하면
   못바꿔." `actions.js`의 `cycleBatmanMode()`를 `chooseBatmanMode(state,
   instanceId, mode)`로 교체 - 순환이 아니라 `mode`를 직접 지정하고,
   `instance.batmanMode`가 이미 있으면 무조건 거부한다(재선택 불가, 방어
   로직까지 함수 안에 넣어서 UI를 거치지 않는 직접 호출도 막음). `GameScreen.js`는
   `renderCellQuickActions()`의 반환 배열에 `batmanModeButtons`(캐릭터 좌우로
   각각 배치된 "투수"/"타자" 버튼 쌍, `instance.batmanMode`가 없을 때만 렌더)를
   추가했다 - 아래쪽 pill 스택이 아니라 칸 좌표 기준 절대좌표로 캐릭터 양옆에
   뜬다(`.batman-mode-buttons`/`.batman-mode-btn`, 채드 화살표 레이어와 같은
   위치 지정 패턴).
5. **채드 판매 화살표의 초록색 바운스 애니메이션 제거**: 사용자 요청 - "채드
   클릭했을 때 뜨는 초록색 화살표 애니메이션 빼". `.chad-sell-arrow`의
   `animation: chad-arrow-bounce...` 선언과 안 쓰게 된 `@keyframes
   chad-arrow-bounce` 블록을 삭제했다 - 화살표 자체(초록 원형 버튼)는 그대로
   남고 애니메이션만 없앤 정적인 상태. **참고**: 이 프로젝트에서 반복된
   "0.2초마다 DOM이 통째로 재생성되는데 `infinite` 애니메이션에 고정 딜레이를
   쓰면 매번 리셋되어 깜빡인다"는 함정과 같은 계열일 가능성이 높다(몬스터
   이동/캐릭터 공격모션/궁 링에서 전부 같은 원인으로 문제가 있었음) - 다만 이번엔
   "고쳐달라"가 아니라 "빼달라"는 명시적 요청이라 실제 경과시간 기반으로
   고치는 대신 완전히 제거하는 쪽을 택했다.

**검증 방법**: 이전 라운드들과 동일하게 `window.__debug` 훅을 임시로 추가해
Playwright로 검증했다 - (1) 배트맨 12강에서 `Math.random`을 강제로 고정해 실패를
재현시켜 레벨이 정확히 0으로 리셋되는지, 10강 미만에서는 실패 자체가 없는지,
(2) 배트맨/로카/밤바를 동시에 필드에 배치해 `.hero-head-badge` 3개가 각각
올바른 숫자로 뜨는지, (3) 에이스 배트맨을 선택했을 때 "투수"/"타자" 버튼이
각각 캐릭터 좌우에 뜨고(예전 순환 "변신" 버튼은 완전히 사라짐), 하나를 실제로
클릭하면 `batmanMode`가 설정되면서 두 버튼이 다시는 안 뜨는지, 함수를 직접
호출해도(`already-chosen`) 재선택이 막히는지, (4) 베인에게 `recordImmortalEvent`를
25회 반복 호출해서 `nextUltimateAt`이 궁 발동마다 갱신되는지와 실제 게이지
`fill` width가 계산식대로 나오는지(예: progress 25/start 15/next 31 →
62.5%), (5) 실제 CSS 규칙을 스캔해서 `chad-arrow-bounce` 키프레임과
`.chad-sell-arrow`의 `animation` 속성이 둘 다 사라졌는지 확인했다. 마지막으로
Playwright 스크린샷으로 좌우 버튼 배치와 세 배지가 실제로 캐릭터 근처에 잘
붙어 보이는지 시각적으로도 확인했다.

## 베인 게이지 위치 수정 + 임프 판매/합성 + 배트맨 확률 보정/2단계 상승 + 배지 가시성 + 채드 등급 제한 정정

스크린샷과 함께 4개를 한 번에 받았다.

1. **베인 궁 쿨타임 게이지가 캐릭터에서 너무 멀리 떨어져 있던 버그**: 직전
   라운드에 추가한 `renderBaneUltimateGauge`가 칸(rect) 하단 기준으로 그려졌는데,
   신화 등급은 토큰 박스가 칸보다 훨씬 커서(위로 크게 튀어나옴) 칸 하단과 실제
   캐릭터 발밑이 안 맞아 게이지가 멀리 떠 보였다(사용자 스크린샷 - "베인이랑
   바가 너무 멀어"). 로카/배트맨 머리 위 배지와 같은 방식으로 칸이 아니라
   토큰 자신의 좌표(`centerX`/`top+tokenHeight`)를 기준으로 다시 그리도록
   고쳤다 - 이제 신화 크기 배율이 바뀌어도 항상 캐릭터 발밑에 붙는다.
2. **임프 판매/합성 신규 허용**: 마마가 만드는 임프(`x_imp`)가 지금까지
   `renderCellQuickActions`의 `if (heroDef.tier === 'imp') return null;` 가드에
   막혀 어떤 조작도 불가능했다 - 사용자 요청대로(마리당 9코인 판매, 3마리 합성
   시 희귀 등급 랜덤 등장) 이 가드를 없앴다. 놀랍게도 기존 판매/합성 버튼
   블록이 이미 "mythic/immortal이 아니면 판매", "3마리면(전설이 아니면) 합성"
   같은 제네릭 조건이라 임프도 자동으로 걸려서, 실제로 고칠 건 (a) 가드 삭제
   (b) `synthesis.js`의 `SELL_GOLD_BY_TIER`에 `imp: 9` 추가 + `sellHero`/
   `sellPreview`가 `tier==='imp'`도 골드 보상 분기를 타도록(기존엔 normal만
   골드, 나머지는 행운석이라 imp가 잘못 행운석 분기로 빠졌음) (c)
   `synthesize()`가 `nextTierOf('imp')`(TIERS 배열에 없어서 null)를 못 쓰므로
   `heroId === IMP_HERO_ID`면 곧장 `'rare'`로 점프하는 특수 케이스 추가, 이
   3가지뿐이었다 - 나머지(판매 버튼 노출, 합성 버튼 노출, consolidateHeroStacks
   임프 스택 정리 등)는 전부 기존 제네릭 로직을 그대로 재사용했다. 임프는
   일반~전설 판매의 "6% 확률 행운석 보너스" 대상에서는 제외했다(별도 지정
   없었음 - 9코인 고정).
3. **배트맨 강화 - 숨겨진 40% 확률 보정 + 성공 시 10% 확률 2단계 상승**:
   사용자 지정 - "배트는 강화 성공확률 40% 증가(보이지 않는 곳에서) 그리고
   배트 강화가 성공하면 10% 확률로 2단계 상승해." `actions.js`에
   `BATMAN_HIDDEN_RATE_BOOST = 1.4`를 추가해서 **실제 판정에만** 곱한다(1.0
   초과분은 캡) - `nextEnhanceSuccessRate()`(버튼에 표시되는 확률)는 건드리지
   않아서 화면엔 기존 그대로(예: 11강 83%)만 보이고 실제 성공 확률만 몰래
   높다("보이지 않는 곳에서"). 성공 시 `BATMAN_DOUBLE_LEVEL_CHANCE = 0.1`로
   추가 판정해서 10% 확률로 `enhanceLevel`이 1이 아니라 2 오른다. 불멸 승급
   확률 공식(10강 1%, 11강 3.5%, 12강 6%... 강화 단계당 2.5%p씩 증가)은
   사용자가 "알고있지?"라고 재확인한 것뿐 - `heroes.js`의
   `IMMORTAL_CONDITIONS.m_batman`(`baseChance:0.01, perEnhanceBonus:0.025`)에
   이미 정확히 구현돼 있어서 코드 변경 없음.
4. **머리 위 배지(로카/배트맨/밤바) 가시성 강화**: 사용자 지적 - "강화 숫자
   배트맨 머리 위로 올려달라고... 글자도 좀 굵게 가시적으로 보이게. 가시성은
   밤바도 똑같아." `hero-head-badge`의 `font-size`를 9px→14px,
   `font-weight`를 800→900, 배경을 더 불투명하게(0.55→0.75 알파), 노란 테두리
   (`border:2px solid #ffe066`)를 추가해서 판매 버튼급으로 눈에 띄게 키웠다.
5. **채드 등급 제한 정정 - 일반 채드는 신화만, 기가채드는 신화+불멸**: 예전엔
   (PR #19) "신화불멸은 채드가 있을 때만 판매 가능"이라는 문구를 "일반 채드가
   둘 다 받는다"로 구현했었는데, 사용자가 명확히 정정 - "일반 채드는 불멸을
   못 팔아. 기가채드만 신화/불멸 다 팔 수 있어." 게다가 기가채드(`i_giga_chad`)는
   애초에 "먹이기"(다른 신화/불멸을 흡수) 기능 자체가 연결이 안 돼 있었다 -
   자기 자신을 파는 `sellGigaChad`(+6행운석) 버튼만 있었다. `synthesis.js`의
   `feedMythicToChad()`를 재작성해서 `m_chad`/`i_giga_chad` 둘 다 먹이는 주체가
   될 수 있게 하고, 대상 등급을 `chad.instance.heroId === 'i_giga_chad' ?
   ['mythic','immortal'] : ['mythic']`로 갈랐다. `GameScreen.js`의 "판매하기"
   (화살표 모드) 버튼도 `i_giga_chad`에서 뜨도록 확장했다 - **함정**: 여러
   채드가 동시에 필드에 있을 수 있어서(일반 채드 2마리 중 하나만 승급 등)
   `ui.chadSellMode`를 boolean에서 "지금 화살표 모드를 시작한 채드의
   instanceId"로 바꿔야 했다 - boolean이었다면 두 채드가 동시에 있을 때 어느
   쪽이 먹이는 주체인지 구분할 수 없었다(대상 등급 필터가 채드 종류에 따라
   달라지므로 이 구분이 필수). `renderChadArrowLayer`도 heroId가 아니라
   instanceId로 정확히 그 채드를 찾도록 고쳤다.

**검증 방법**: 이전 라운드들과 동일하게 `window.__debug` 훅을 임시로 추가해
Playwright로 검증했다 - (1) 베인 게이지와 토큰 하단 사이 실제 DOM 좌표 거리가
15px 이내인지, (2) 임프를 실제로 `sellHero`/`synthesize` 호출해서 9골드 지급과
희귀 등급 결과가 나오는지, UI에서도 판매/합성 버튼이 뜨는지, (3) 배트맨 12강
강화를 500회 반복 시행해서 실측 성공률이 보정된 값(기본 55%×1.4=77%) 근처에
오는지(통계적 검증), `Math.random`을 순차적으로 낮은 값 2번 고정 주입해서
"성공 판정"과 "2단계 상승 판정"을 각각 강제로 재현시켜 레벨이 정확히 +2
되는지(결정론적 검증), 버튼 표시 확률은 여전히 보정 전 값(83%)만 보이는지,
(4) 배지의 실제 `getComputedStyle` 폰트 크기/굵기가 커졌는지, (5) 일반 채드는
불멸 먹이기를 거부(`not-mythic` 사유)하고 기가채드는 신화/불멸 둘 다 받는지,
UI에서 기가채드 선택 시 "판매하기" 버튼과 불멸 대상 화살표가 정상적으로
뜨는지까지 전부 확인했다.

## 머리 위 배지가 캐릭터를 가리던 문제 재조정

PR #50에서 배지(로카/배트맨/밤바)를 "판매 버튼급으로 가시적으로" 키운 직후,
사용자가 스크린샷을 보내며 "지금 이렇게 캐릭터를 다 가려 캐릭터를 가리지
않게 네가 보고 적정히 조절해줘야지 다른 캐릭도 가려지지 않게"라고 지적했다 -
배지가 커진 만큼 캐릭터 얼굴을 덮어버리고 있었다.

**원인**: `transform: translate(-50%, calc(-100% - Npx))`는 배지를 자기 자신의
높이만큼 위로 올려서 배지의 "아래쪽 끝"이 항상 `anchor - Npx` 위치에 오게
만드는 방식이라, 배지 자체의 폰트/패딩/테두리가 커져도 anchor 지점(앵커) 자체는
전혀 안 바뀐다 - 즉 배지가 커져도 "가까이 붙는 정도"는 그대로다. 문제는 그
anchor 자체(`HEAD_BADGE_TOP_RATIO=0.86`, 이전 라운드에 작은 배지 기준으로 튜닝한
값)가 이미 캐릭터 머리에 거의 닿을 만큼 낮게 잡혀 있었는데, 배지가 커지면서
같은 anchor에서도 시각적으로 훨씬 크게 겹쳐 보이게 된 것 - 작은 배지일 땐
살짝 겹쳐도 티가 안 났지만 큰 배지에서는 명확히 얼굴을 가릴 만큼 두드러졌다.

**수정**: `HEAD_BADGE_TOP_RATIO`를 0.86 → 0.68(토큰 박스 안에서 더 위쪽,
캐릭터 머리 위로 더 멀리)로 낮추고, CSS의 고정 여백도 4px → 8px로 늘렸다.
Playwright 스크린샷으로 배트맨/로카/밤바 세 캐릭터를 한 화면에 동시에 배치해
직접 눈으로 확인하며 맞춘 값 - 이제 세 배지 모두 캐릭터 머리 위에 여유 있는
간격을 두고 뜬다. **교훈**: 배지/텍스트 요소의 크기(폰트, 패딩, 테두리)를
키우는 요청과 위치를 조정하는 요청은 서로 독립적으로 보이지만 실제로는
연동된다 - 요소를 키우면 같은 anchor 좌표에서도 겹침이 더 눈에 띄게 되므로,
크기를 바꿀 때마다 스크린샷으로 겹침 여부를 다시 확인해야 한다(이 프로젝트에서
캐릭터 크기/외곽선 오프셋을 바꿀 때마다 반복됐던 것과 같은 함정).

## 불멸 탭 진행도 표시 + 베인 스왑 이동 버그 + 밤바/배트맨 확률 판정 주기 정정

사용자가 두 가지를 지적한 뒤("1. 만약에 베인이 소환되었으면 신화 탭에 있는 베인
불멸이 해제되고 현재 불멸 진행 상태를 보여줘야지 2. 베인이 디버프걸린 애들이랑
서로 자리를 맞바꿔도 게이지가 차야해"), 작업 도중에 밤바/배트맨의 불멸 승급
확률/판정 주기에 대한 확인 메시지도 추가로 받았다.

1. **신화 팝업 "불멸" 탭에 진행도 숫자 표시**: 지금까지 불멸 탭 그리드는
   조건 충족 전이면 무조건 "잠김"만 보여줬다 - 사용자가 "베인이 필드에 있으면
   지금 진행 상태(N/132)를 보여달라"고 요청했다. `GameScreen.js`에
   `immortalProgressText(state, immortalDef)`를 추가해서, 원본(신화) 개체가
   필드에 있고 `target`이 있는(단순 progress/target 숫자형) 조건이면
   `"N/target"`을 보여주고, 원본이 필드에 없으면 여전히 `null`(→"잠김")을
   반환한다. `consumeCount`형(마마 임프 수, 닌자 마리 수 등 - 단일 progress
   숫자가 없는 조건)은 숫자로 표현할 수 없어 그대로 "잠김"으로 남는다.
   **주의**: 이건 PR #27에서 "마마뿐만 아니라 다른 신화들도 진행도 표시하지
   말라"고 정정했던 것과 정면으로 배치되는 것처럼 보이지만, 그건 **필드 칸을
   선택했을 때 뜨는 퀵액션 UI**(`renderCellQuickActions`) 얘기였고 이번 요청은
   **신화 팝업의 별도 "불멸" 탭 그리드**(`renderImmortalGrid`) 얘기라 완전히
   다른 화면이다 - 필드 칸 UI의 진행도 숨김 규칙은 그대로 유지되고, 이번 변경은
   불멸 탭에만 적용된다. 나중에 "진행도가 안 보인다"는 리포트가 오면 이 두
   화면을 헷갈리지 말 것.
2. **칸 맞바꿈(스왑) 시 "밀려난" 쪽의 이동 이벤트가 누락되던 버그**: 3전설 이후 이미
   교차 이동(자리 맞바꿈) 자체는 구현돼 있었지만, `moveHero()`의 스왑 분기가
   `movedInstanceIds`에 `fromSlot`(드래그를 시작한 쪽) 개체만 담고 있었다 - 반대편
   `toSlot`에 있던 개체(드래그로 밀려나 `fromSlot`으로 이동한 쪽)는 실제로 칸을
   옮겼는데도 "이동"으로 카운트되지 않았다. 그래서 디버프 걸린 캐릭터를 베인이
   있는 칸으로 드래그하면(베인이 반대로 밀려남), 베인은 실제로 이동했음에도
   이동 기반 불멸 조건(`IMMORTAL_CONDITIONS.m_bane`, `eventType:'move'`)의
   진행도가 전혀 안 쌓였다. `actions.js`의 `moveHero()` 스왑 분기 맨 앞에
   `movedInstanceIds.push(...toSlot.occupants.map((o) => o.instanceId));`를
   추가해서 스왑되는 양쪽 모두의 instanceId가 이동 이벤트 대상에 포함되도록
   고쳤다. Playwright로 (a) 디버프 대상(일반 헌터)을 베인 칸으로 드래그해서
   베인이 "밀려나는" 쪽이 되어도 progress가 증가하는지, (b) 반대로 베인을
   직접 드래그해서 헌터 칸으로 보내도(기존에 이미 되던 케이스, 회귀 확인)
   여전히 progress가 증가하는지 둘 다 확인했다.
3. **원시 밤바 - "30 도달 즉시 100% 승급 가능" 숨은 버그 발견 + 판정 주기
   재조정**: 사용자가 "밤바도 초마다 3번씩 기본공격 한다 치고 기본 공격마다
   0.1% 확률로 소환 가능한거 알고있지?"라고 확인해왔다 - 기존 코드는 이미
   `postCapChance: 0.001`(0.1%)까지는 맞았지만, 판정 주기가 `postCapIntervalSec:
   [1,5]`(1~5초 랜덤, 평균 3초에 1번 체크)였다. `heroes.js`에서
   `postCapIntervalSec`을 `1/3`(초당 3회 체크, "3번씩 기본공격"과 일치)로
   바꿨다. **이 검증 과정에서 별개의 진짜 버그를 발견했다**: `tickImmortalProgress()`가
   매 틱마다 범용 `isEligible()`을 호출해서 `progress >= target`이면
   무조건(RNG 판정 자체를 통과했는지와 무관하게) `instance.immortalEligible = true`로
   세팅하고 있었는데, `isEligible()`에 밤바 전용 예외가 없어서 `m_bamba`도 이
   범용 규칙을 그대로 탔다 - 즉 스택이 정확히 30(target)에 도달하는 그 틱에서
   `postCapChance` RNG와 완전히 무관하게 **100% 확률로** 즉시 승급 자격이
   생겨버렸다("30 도달 후엔 낮은 확률로만 승급 가능"이라던 기존 주석의 의도가
   실제로는 전혀 지켜지지 않고 있었던 것). `isEligible()`에
   `if (instance.heroId === 'm_bamba') return instance.immortalEligible === true;`
   예외를 추가해서(m_hailey/m_ray와 같은 패턴), 범용 target 도달 체크를 완전히
   건너뛰고 `tickOverrides.m_bamba`가 postCapChance RNG를 통과했을 때만 직접
   세팅하는 `instance.immortalEligible` 값만 신뢰하도록 고쳤다. Playwright
   통계 검증(300회 시행, 스택 30에서 10초 시뮬레이션)으로 수정 전엔 300/300(100%)
   승급 가능 판정이 나오다가, 수정 후엔 10/300(≈3.3%, 이론값 1-0.999^30≈2.96%와
   근접)로 스펙에 맞게 나오는 것을 확인했다.
4. **에이스 배트맨 - 궁극기 판정 주기를 10초로 늦춤**: 사용자가 "불멸 배트가
   너무 빨리 변신되는거같은데 [10강부터 궁극기 사용 시 1% 확률로 소환할 수
   있다. 강화 단계가 올라갈수록 소환 확률이 2.5%씩 증가한다] 이거 조건
   알고있는거 맞지?"라고 물어왔다 - 실제로 코드의 `baseChance`(1%)/
   `perEnhanceBonus`(2.5%p)/`minEnhance`(10강)는 이미 정확히 일치했지만, "궁극기
   사용 시"를 실제 궁극기 발동 이벤트(이 게임엔 그런 전투 시스템이 없음) 없이
   그냥 `tickIntervalSec: 5`(5초마다 무조건 판정)로 근사하고 있었던 게 문제였다
   - 실제 "궁극기 사용" 빈도와 무관하게 너무 자주 굴러가서 체감상 빨랐던 것.
   숫자를 추측하지 않고(아이언미야옹 라운드에서 겪은 교훈 - CLAUDE.md 위쪽
   "아이언미야옹 3단계 진행 재구현" 섹션 참고) AskUserQuestion으로 판정 주기를
   직접 확인받아 "10초에 1회(권장)"로 결정했다 - `heroes.js`의
   `tickIntervalSec: 5` → `10`. 이전 라운드에서 이미 추가한
   `BATMAN_HIDDEN_RATE_BOOST`(강화 성공확률 숨은 1.4배)/`BATMAN_DOUBLE_LEVEL_CHANCE`
   (강화 성공 시 10% 확률로 2단계 상승)는 "강화 레벨이 오르는 속도"에 관한
   것이라 그대로 뒀다 - 이번 수정은 "레벨이 오른 뒤 궁극기 판정이 얼마나 자주
   도는지"만 바꾼 것으로 별개 레버다. Playwright 통계 검증(300회 시행, 15강
   상태에서 30초=3틱 시뮬레이션)으로 104/300(≈34.7%, 이론값
   1-0.865³≈35.3%와 근접)이 나와 새 주기가 정확히 반영됨을 확인했다.

**검증 방법**: 이전 라운드들과 동일하게 `window.__debug` 훅(이번엔 `moveHero`,
`tickImmortalProgress`, `IMMORTAL_CONDITIONS`를 노출)을 `main.js`에 임시로
추가해 Playwright로 검증하고 끝난 뒤 다시 제거했다(디버그 전용, 커밋에 남기지
않음 - 제거 후 재빌드한 JS 번들이 디버그 훅 추가 전과 동일한 구조인지 grep으로도
재확인). 불멸 탭 진행도는 실제 신화 팝업을 열고 "불멸" 탭으로 전환해 DOM에서
`.mythic-progress` 텍스트를 직접 읽는 방식으로, 베인 스왑은 `moveHero()`를
양방향(밀려나는 쪽/드래그하는 쪽)으로 직접 호출해 `progress` 값을 비교하는
방식으로, 밤바/배트맨 확률은 300회 반복 시행 통계로 검증했다.

## 이동불능 회피 버그 수정 + 임프 생성 조건 강화 + 배트맨 20초 + 베인 불멸 조건 재정의

한 메시지 안에서 여러 건을 연달아 받았다.

1. **이동불능(게이지형) 회피 무력화 버그 수정**: "5초동안 원 생기고 속박 하잖아.
   근데 내가 그 원을 다 피했으면 캐릭터가 없는 자리는 속박이 안되는게 맞아.
   지금은 칸에 그냥 속박이 남아있어서 내가 피한애를 다시 그 칸에 들여다놓으면
   피했는데도 속박되어버려"라는 지적을 받았다. 원인: `isImmobilized(state, slot)`이
   `ev.phase==='active'`일 때 그 칸 좌표가 `ev.targetSlots`(필링 단계에서 미리
   뽑아둔 6칸 좌표)에 포함되는지만 확인하고 있었다 - "5초 게이지가 다 찼을 때
   실제로 그 칸에 누가 있었는지"는 전혀 보지 않고 순수하게 **좌표**만 기억하고
   있었으므로, 필링 단계 중에 그 칸을 벗어나 회피에 성공해도 칸 자체는 계속
   "속박 대상"으로 낙인찍힌 채 남아있었다 - 그 칸이 나중에 비었다가 다시 그
   캐릭터(또는 다른 캐릭터)가 들어오면 회피 여부와 무관하게 무조건 걸렸다.
   **수정**: `waveEvents.js`의 `handleImmobilizeEvent()`가 `phase`가 `filling`→
   `active`로 전환되는(게이지형) 또는 `idle`→`active`로 전환되는(즉시형) 바로 그
   순간에 대상 칸들에 **지금 실제로 있는 개체의 instanceId**를 스냅샷 떠서
   `e.targetInstanceIds`에 저장하는 `snapshotOccupantInstanceIds()`를 추가했다.
   `GameScreen.js`의 `isImmobilized(state, slot)`도 좌표 매칭 대신
   `slot.occupants.some((o) => ev.targetInstanceIds.includes(o.instanceId))`로
   바꿔서, 지금 그 칸에 있는 개체가 스냅샷 당시 거기 있던 개체와 겹치는지로
   판정하도록 고쳤다 - 필링 단계 중 도망친 개체는 스냅샷에 없으니 영원히 안
   걸리고, active 이후 그 칸에 새로 들어온 개체(도망쳤다가 돌아온 원래 개체
   포함)도 스냅샷 instanceId와 다르므로 안 걸린다. **처음에 데이터 모델(스냅샷
   저장)만 고치고 그걸 실제로 소비하는 `isImmobilized()`를 빠뜨렸다가
   Playwright DOM 검증에서 도망쳤다 돌아온 칸에 여전히 `immobilize-active`
   클래스가 남아있는 걸 보고 뒤늦게 알아챈 실수** - 데이터를 바꿨으면 그 데이터를
   실제로 읽는 모든 소비처를 같이 확인해야 한다는 교훈. Playwright로 (a) 도망친
   개체가 스냅샷에 안 잡히는지, (b) 그대로 있던 개체는 잡히는지, (c) 도망쳤다가
   원래 칸으로 돌아와도 여전히 안 잡히는지(핵심 버그 시나리오), (d) DOM에서
   실제로 도망친 칸엔 `immobilize-active` 클래스가 없고 남아있던 칸에만 있는지
   전부 확인했다.
2. **마마 임프 생성 중단 라운드를 9로**: "임프는 9라운드 이후로 생성하지 마라했어"
   - 기존 `IMMORTAL_MAMA_STOP_ROUND`가 10이라 10라운드까지도 생성되고 있었다
   (`wave > 10`에서 멈추는 조건이라 10라운드는 여전히 통과함). `9`로 낮춰서
   `wave > 9`(즉 10라운드부터 중단)로 정정했다 - "9라운드 이후로 하지 마라"는
   "9라운드까지만 생성, 10라운드부터 중단"으로 해석. `heroes.js`의 죽은 데이터
   `cond.extra.stopRound`(실제로는 아무 데서도 안 읽히고 `immortal.js`의 로컬
   상수만 쓰이고 있었음)도 값을 맞춰 같이 9로 갱신해서 나중에 헷갈리지 않게 했다.
3. **맵에 몬스터가 없을 때는 임프도 생성되면 안 됨**: "맵에 몬스터 없을때도
   생성하면 안돼" - `tickMamaImps()` 맨 앞에 `if (newState.monsterCount <= 0)
   return newState;` 가드를 추가했다. 그냥 건너뛰기만 하고 개체별
   `immortalTick.elapsed`는 건드리지 않으므로(원래 흐르던 시간을 몰수하지
   않음), 몬스터가 다시 나타나면 그 시점부터 이어서 카운트되어 재개된다(0으로
   리셋해서 처음부터 다시 세게 하지 않음). Playwright로 (a) 9라운드+몬스터
   있으면 생성됨, (b) 10라운드는 몬스터 유무와 무관하게 생성 안 됨, (c) 몬스터
   0마리면 라운드와 무관하게 생성 안 됨, (d) 몬스터가 없다가 다시 나타나면
   생성이 재개되는지 전부 확인했다.
4. **배트맨 궁극기 판정 주기를 10초 → 20초로**: 지난 라운드에 5초→10초로
   늦췄었는데, 사용자가 "배트 20초로 늘리자"고 한 번 더 늦춰달라고 해서
   `heroes.js`의 `m_batman.tickIntervalSec`을 20으로 갱신했다(확률 공식
   자체는 그대로).
5. **탑 베인 불멸 조건을 "이동 132회"에서 "궁극기(필살기) 12회 사용"으로 재정의**:
   "베인 불멸 기준은 필살기 12번 사용하는거야. 불멸 조건 바꿔"라는 명시적
   정정을 받았다 - 예전엔 원본 이동 누적치(`progress`)가 132에 도달하면
   승급 자격이 생기는 구조였는데(궁극기 이펙트는 그 진행 중에 15~20회
   왕복마다 한 번씩 곁다리로 터지는 연출일 뿐 승급과 무관했음), 이제는
   "궁극기가 실제로 터진 횟수" 자체가 승급 기준이 됐다. `heroes.js`의
   `m_bane.target`을 132 → 12로 바꾸고, `immortal.js`의
   `recordImmortalEvent()`를 재구성했다 - 원본 이동 누적치는
   `instance.moveProgress`라는 새 필드로 분리해서 궁극기 발동 임계치(15~20
   랜덤) 계산에만 쓰고, 승급 판정에 쓰이는 `instance.progress`는 궁극기가
   실제로 터질 때만 +1 되도록 바꿨다(그 외엔 손대지 않음 - `else` 분기로
   나머지 제네릭 이동 조건과 분리). 궁극기 쿨타임 게이지(`renderBaneUltimateGauge`,
   PR #49에서 추가)도 이제 `moveProgress`/`ultimateWindowStart`/`nextUltimateAt`
   기준으로 다시 계산하도록 고쳐야 했다 - `progress`를 그대로 썼다면 궁극기
   횟수(0~12라는 작은 범위)를 이동 임계치(15~20 단위)와 비교하는 꼴이 되어
   게이지가 완전히 깨졌을 것. 신화 팝업 "불멸" 탭의 진행도 표시(직전 라운드에
   추가한 `immortalProgressText`)는 `cond.target`을 그대로 읽는 범용 로직이라
   코드 변경 없이 자동으로 "N/12"로 바뀐다. Playwright로 `recordImmortalEvent`를
   반복 호출해 (a) `progress`(승급 판정용)가 정확히 12에서 멈추는지, (b) 원본
   `moveProgress`는 12회×15~20회 범위(180~240) 안에 들어오는지, (c) 실제 호출
   횟수와 `moveProgress`가 일치하는지(매 호출마다 amount=1씩 정확히 누적되는지)
   확인했다.

**검증 방법**: 이전 라운드들과 동일하게 `window.__debug` 훅(이번엔 `moveHero`,
`recordImmortalEvent`, `handleImmobilizeEvent`, `tickImmobilizeTimer`,
`tickMamaImps`를 노출)을 `main.js`에 임시로 추가해 Playwright로 검증하고 끝난
뒤 다시 제거했다(디버그 전용, 커밋에 남기지 않음 - 제거 후 재빌드 결과 debug
훅 참조가 완전히 사라졌는지 grep으로 재확인).

## 머리 위 배지 z-index 누수 + 인디 게이지/보물 마커 정정 + 보물 발굴 전 쿨타임 정지

이전 라운드(임프/베인/이동불능) 직후 여러 건을 연달아 받았다.

1. **머리 위 배지(로카/배트맨/밤바)와 이동불능 사슬이 팝업 위로 새어나오던
   버그**: "로카나 밤바나 배트 머리위에있는 숫자 있잖아 이거 팝업 위에까지
   올라와. 레이어 위계를 다시 잡아"라는 지적을 받았다. 원인: `.hero-head-badge`/
   `.stage-immobilize-mark`가 행(row) 기반 인라인 z-index(`20~33`)를 쓰는데,
   이걸 담는 `.stage-hero-layer`는 `position:absolute`만 있고 z-index가 없어서
   새 스태킹 컨텍스트를 만들지 못했다 - CSS2.1 스펙상 `position` + `z-index:auto`
   조합은 스태킹 컨텍스트를 생성하지 않으므로, 내부 요소들의 큰 z-index 값이
   그대로 형제 요소(`.game-popup`/`.mythic-popup`, z-index:10)와 직접 비교돼서
   30~33짜리 배지가 10짜리 팝업 위로 뚫고 올라왔다. `.stage-hero-layer` 자체에
   `z-index:4`(팝업류 10보다 낮고, `.hole-effects`(2)보다는 높은 값)를 줘서 새
   스태킹 컨텍스트를 만들었다 - 이러면 내부의 큰 z-index 값들은 이 레이어
   "안에서만" 서로 비교되고, 밖에서 볼 땐 레이어 전체가 z-index:4 하나로만
   취급되어 팝업보다 항상 아래에 있게 된다. Playwright로 신화 팝업을 연 상태에서
   배지 좌표에 `document.elementFromPoint`를 쏴서 최상단 엘리먼트가 배지가
   아니라 팝업인지 확인해 검증했다.
2. **인디 발굴 쿨타임 게이지가 실제 캐릭터에서 너무 멀리 떨어져 보이던 문제**:
   "인디도 바가 지금 너무 멀어. 좀 가깝게 해줘" - `renderIndyTreasureGauge`가
   베인 궁 게이지를 고치기 전과 똑같은 함정에 걸려 있었다(칸(rect) 하단 기준으로
   그렸는데, 인디는 신화 등급이라 토큰 박스가 칸보다 훨씬 커서 실제 발밑과 칸
   하단이 안 맞음). 베인 게이지와 동일한 방식으로 시그니처를
   `(state, rect)`에서 `(state, centerX, footY, width)`로 바꾸고, 호출부
   (`renderHeroTokenLayer`)에서 칸 좌표 대신 그 슬롯에서 이미 계산해둔 토큰
   좌표(`cellCenterX`/`baseTop+tokenHeight+1`/`tokenWidth`)를 넘기도록 고쳤다.
3. **보물 위치 마커가 상시 노출이 아니라 1초짜리 플래시여야 함**: "인디 보물
   위치는 쿨타임 찼을때 한번 1초 보여주고 사라졌다가 인디 다시 클릭하면 한
   1초 보여줬다 사라지는거야. 지금처럼 계속 보이는 게 아니라" - 기존
   `isTreasureSlot()`은 `indyTreasure.slot`이 설정돼 있는 한(발굴 전까지) 계속
   `true`라 글로우가 상시 켜져 있었다. 두 트리거의 1초 플래시로 바꿨다:
   (a) `indyTreasure.completedAt`(게이지가 다 차서 새 보물이 등장한 시점 -
   기존에 "완료" 텍스트 플래시용으로 이미 있던 타임스탬프를 재사용)로부터
   1초 이내, (b) 새로 추가한 `ui.indyMarkerFlashAt`(인디가 있는 칸을 클릭한
   시점)으로부터 1초 이내 - 둘 중 하나라도 해당하면 보인다. `onSlotClick()`에서
   선택된 칸에 `m_indy`가 있으면 `ui.indyMarkerFlashAt = Date.now()`를 세팅한다.
   **주의**: 마커는 인디가 서 있는 칸이 아니라 보물이 실제로 있는 칸
   (`indyTreasure.slot`, 서로 다른 칸일 수 있음 - 인디가 아직 그 칸까지 이동
   전이므로)에 뜬다 - 클릭은 "인디를 찾았다"는 트리거일 뿐, 마커 자체의 위치는
   여전히 보물 위치 기준이다. 다른 플래시 이펙트들과 같은 이유로(0.2초마다 DOM
   재생성) 고정 delay가 아니라 매 렌더 실제 경과 시간을 다시 계산한다.
   Playwright로 (a) 등장 직후(1초 이내) 보이는지, (b) 1초 지나면 사라지는지,
   (c) 인디 칸을 클릭하면 다시 보이되 보물이 있는 다른 칸에 뜨는지, (d) 그
   후로도 1초 지나면 다시 사라지는지 확인했다.
4. **보물을 발굴하기 전엔 다음 보물의 쿨타임이 흐르면 안 됨**: "인디 보물 찾기
   전이면 쿨타임 멈춰야해" - `tickIndyTreasure()`가 발굴 중(`digging`)일 때만
   타이머를 멈추고, 보물이 이미 등장했지만 아직 발굴 전(`slot`은 있고 `digging`은
   없는 상태)인 동안은 계속 타이머가 흘러서, 플레이어가 30초 안에 못 찾으면
   발굴도 안 됐는데 자동으로 다른 칸으로 자리가 옮겨가버렸다. 가드 조건을
   `state.indyTreasure.digging || state.indyTreasure.slot`로 넓혀서, 보물이
   등장한 순간부터 실제로 발굴에 성공(`tickIndyDig`가 `slot`을 비우고 타이머를
   리셋)할 때까지는 쿨타임이 완전히 멈추도록 고쳤다. Playwright로 30초 경과 시
   보물이 등장하고, 그 뒤 100초를 더 흘려도 자리/타이머가 그대로인지(멈춰
   있는지), 실제로 발굴에 성공한 뒤에야 타이머가 다시 줄어들기 시작하는지
   확인했다.
5. **마마 임프 9라운드 이후 중단 조건 재확인 - 이미 정확히 그렇게 구현돼
   있었음**: 사용자가 "임프 생성은 9라운드 이후로 하지 말라고. 이해했지
   이렇게?"라고 확인을 요청했다 - 직전 라운드에 `IMMORTAL_MAMA_STOP_ROUND`를
   `wave > 9`(9라운드까지는 생성, 10라운드부터 중단)로 이미 정정해뒀던 걸
   다시 코드로 확인하고 그대로 맞다고 답했다(코드 변경 없음, 확인 차원의
   요청이었음).

**검증 방법**: 이전 라운드들과 동일하게 `window.__debug` 훅(이번엔
`tickIndyTreasure`, `tickIndyDig`, `digTreasure`를 노출)을 `main.js`에 임시로
추가해 Playwright로 검증하고 끝난 뒤 다시 제거했다(디버그 전용, 커밋에 남기지
않음 - 제거 후 재빌드한 JS 번들 해시가 디버그 훅 추가 전과 정확히 일치하는
것까지 확인). 인디 클릭 플래시는 실제 필드 칸에 `pointerdown`/`pointerup`을
디스패치해서 `onSlotClick()` 경로를 그대로 태워 검증했다(내부 `ui` 상태를
직접 조작하지 않음 - `ui`는 GameScreen 클로저 안에 있어서 디버그 훅으로 노출된
적이 없다).

## 칸 선택 해제 ✕ 버튼 전체 제거

배트맨 강화 스크린샷을 보내며 "배트 강화랑 x랑 가리는데 이거 x표시 모든
캐릭터에서 일괄적으로 다 빼도 됨"이라고 지적했다 - 선택된 칸 위에 항상 뜨던
✕(선택 해제) 버튼이 배트맨의 강화 배지("+15", "강화 240G 성공 28%")와 겹쳐서
가리고 있었는데, 특정 영웅만 예외 처리하는 대신 이 버튼 자체를 모든 캐릭터에서
일괄 제거해달라는 요청이었다. `renderCellQuickActions()`에서 `above` 스택 맨
앞에 항상 push하던 `.cell-quick-close`(✕) 버튼을 삭제하고, 안 쓰게 된 CSS
규칙도 같이 지웠다 - 선택 해제는 이미 PR #40("칸 선택 후 다른 곳을 눌러도 해제가
안되던 문제" 수정)부터 빈 칸이나 다른 칸을 클릭해도 되도록 구현돼 있어서, 이
버튼이 없어져도 기능 손실은 없다(다른 버튼을 누르지 않고 선택만 취소하고 싶을
땐 빈 칸을 탭하면 된다). Playwright로 캐릭터를 선택해도 `.cell-quick-close`/✕
텍스트 버튼이 DOM에 전혀 없는 것, 선택 표시(원형 링)는 여전히 뜨는 것, 빈 칸을
탭하면 여전히 정상적으로 해제되는 것을 확인했다.

## 렉 피드백 원인 프로파일링 - sizeStageToFit()의 강제 동기 레이아웃 스래싱 제거

"이거 렉이 걸린다는 피드백이 많은데 어떻게 해야할까?"라는 질문을 받았다. 이미
같은 세션에서 두 차례(tick/render 주기 분리, window 리스너 누수 수정) 성능 점검을
했었기 때문에 이번엔 감으로 또 추측성 수정을 하는 대신, 사용자 동의를 받고 CDP
Profiler로 실제 병목을 먼저 찾았다(CLAUDE.md의 "클릭이 느리다" 진단 라운드에서
썼던 것과 같은 방법론 - `page.context().newCDPSession(page)`로 `Profiler.start()`/
`stop()`을 걸고 실제 8초간 게임을 돌린 뒤 샘플을 함수별 self-time으로 집계).
`window.__debug` 훅으로 필드를 최악의 경우(30마리, 신화/불멸 특수 캐릭터
6종(로카/배트맨/밤바/베인/인디/마마) + 나머지 칸을 일반~전설 3마리 스택으로 채우고
디버프/이동불능 이벤트까지 활성화)로 채운 상태에서 프로파일링했다.

**발견**: `(program)`/`(idle)`(브라우저 내부 렌더링/GC/대기 작업이라 특정 JS 함수로
못 잡히는 값들)를 빼면, **`get clientWidth`** 단일 항목이 8초 샘플 중 327ms(약
4%)를 먹고 있었다 - 순수 프로퍼티 읽기 치고는 비정상적으로 크다. 원인은
`sizeStageToFit()`(스테이지 크기를 뷰포트에 맞춰 픽셀로 못박는 함수)가
`render()`에서 매 렌더(초당 5회, `RENDER_INTERVAL_SEC=0.2`)마다 `root.innerHTML=''`로
DOM을 통째로 갈아엎은 직후 곧바로 `wrap.clientWidth`/`clientHeight`를 읽고
있었던 것 - DOM을 막 변경한 직후의 레이아웃 관련 프로퍼티 읽기는 브라우저가 그
변경사항을 동기적으로 강제 계산(forced synchronous reflow)해야만 정확한 값을
낼 수 있어서, 사실상 매 렌더마다 "DOM 파괴 → 재생성 → 강제 레이아웃 계산"이
반복되는 레이아웃 스래싱 패턴이었다.

**수정**: 뷰포트(스테이지를 감싸는 `.game-stage-wrap`)의 가용 크기는 실제
창 크기가 바뀔 때만 바뀌지, 0.2초마다 바뀌는 게 아니다 - 계산 결과를
`cachedStageSize`에 캐시해두고, `needsStageRemeasure` 플래그가 true일 때만
(최초 마운트 시, 그리고 `window resize` 이벤트가 실제로 발생했을 때만) 다시
측정하도록 바꿨다. 매 렌더마다 새로 생성되는 `stage` 노드에는 캐시된
width/height를 그대로 인라인 style로 적용만 하면 되므로 시각적으로는 완전히
동일하게 동작한다. 재측정 전(최초 마운트 시 아직 DOM에 안 붙어 `clientWidth`가
0으로 읽히는 경우)에는 기존처럼 `needsStageRemeasure`가 true로 남아 다음 렌더
때 자동으로 재시도한다.

**검증**: 수정 전/후 각각 CDP 프로파일러로 다시 떠서 대조했다 - 수정 전 상위
25개 self-time 함수 목록에 있던 `get clientWidth`(327ms/1655 samples)가 수정
후엔 목록에서 완전히 사라졌다(더 이상 상위 25개 안에 들지 않을 만큼 줄었다는
뜻). Playwright로 (a) 초기 마운트 시 스테이지 크기가 정상적으로 잡히고 688:1508
비율이 유지되는지, (b) 실제 뷰포트 크기를 바꾸면(`page.setViewportSize`) 여전히
새 크기에 맞게 재계산되고 비율이 유지되는지, (c) 원래 크기로 되돌리면 다시
정확히 원래 크기로 돌아오는지(캐시가 낡은 값을 붙잡고 있지 않는지) 확인해서
리사이즈 대응 자체는 전혀 깨지지 않았음을 검증했다.

**남은 것 - 더 큰 구조적 비용은 그대로 있음**: 이 수정은 발견된 구체적인 병목
하나(강제 레이아웃 읽기)만 제거한 것이고, `(program)`/`setAttribute` 항목들이
여전히 상위를 차지하는 걸 보면 "0.2초마다 전체 DOM을 파괴하고 처음부터
다시 만드는" 근본 아키텍처 자체의 비용은 그대로 남아있다 - 이건 이 프로젝트의
수많은 애니메이션/클릭 버그가 바로 이 패턴 때문에 생겼고(CLAUDE.md 전체에 반복
등장), 동시에 그 버그들을 고칠 때마다 "실제 경과 시간 기준으로 매 렌더 다시
계산"하는 식으로 이 패턴에 의존해서 해결해왔다는 이중적인 상황이라, 이걸
증분 렌더링(변경된 부분만 갱신)으로 바꾸는 건 훨씬 크고 위험한 리팩터링이다 -
지금은 그 리팩터링에 착수하지 않고, 이번에 찾은 작은 병목만 우선 고쳐서
배포하기로 했다. 이 수정 이후에도 렉 리포트가 계속 오면 그 다음 후보가 바로
이 "전체 DOM 재생성" 구조 자체라는 걸 기억할 것 - 다음에 다시 조사할 땐 이번과
같은 방식(CDP 프로파일러로 실제 병목 먼저 확인, 추측성 수정 금지)을 그대로
따를 것.

## 몬스터 미표시/임프 미생성 재조사 - 킬레이트가 스폰레이트를 항상 앞지르는 문제

직전 라운드에서 "임프 생성이 몬스터 0마리 게이트에 막힌다"는 가설을 세우고 사용자에게
확인을 요청했는데, 사용자가 "재확인해봐"(추측 말고 실제로 검증하라)라고 요구해서
실제 게임 코드(`tickWave`/`tickMamaImps`)를 그대로 호출하는 시뮬레이션으로 다시
확인했다. **첫 번째 가정(필드가 5~6라운드 만에 서서히 30마리로 찬다)으로 돌려보니
몬스터가 거의 0이 되지 않아서 가설 자체가 틀린 것처럼 보였다** - 그런데 이 프로젝트
곳곳에서 반복 사용된 "5~6라운드 만에 서서히 찬다"는 가정 자체가 실제 플레이 패턴과
다를 수 있다고 의심하고, **1라운드부터 즉시 30마리를 채우는(초반에 골드를 몰아써서
급하게 채우는) 시나리오**로 다시 돌려보니 완전히 다른 결과가 나왔다:

- **화면에 보이는 몬스터 수(`Math.floor(monsterCount)`)는 1라운드부터 9라운드까지
  거의 100%의 시간 동안 0이었다** - 필드 영웅 30마리 기준 처치 속도(`30×0.06=1.8/초`)가
  일반 라운드(30초)의 스폰 속도(`40/30≈1.33/초`)를 항상 앞지르기 때문에, 몬스터가
  쌓일 새도 없이 계속 0에 수렴한다. 이건 몇 라운드 전 "10라운드 시작 4초 후 전멸"
  요구사항에 맞추려고 처치 속도를 올렸던 결정(`MONSTER_KILL_RATE_PER_HERO_PER_SEC`
  0.04→0.06)의 의도된 부작용이었는데("정상 라운드의 몬스터 누적을 거의 0으로 만들
  것"이라고 그 라운드 기록에도 이미 적어뒀었다), 실제로 필드를 빨리 채우는
  플레이어에게는 라운드 극초반부터 몬스터가 거의 안 보이는 문제로 나타난다는 걸
  이번에 처음으로 정량 확인했다.
- **하지만 임프 생성 자체는 완전히 막히지 않았다** - `Math.floor()`로 반올림된
  화면 표시값은 계속 0이어도, `tickMamaImps()`가 쓰는 원시(raw) `monsterCount` 값은
  스폰↔처치가 같은 `tickWave()` 호출 안에서 순차 처리되면서(스폰이 몬스터를
  살짝 밀어올렸다가 바로 그 틱에서 처치가 다시 깎는 구조) 자주 0보다 살짝 큰
  상태(`>0`)를 거쳐간다 - 시뮬레이션 결과 원시값 기준으로는 시간의 약 65%가
  `>0`이었다(화면엔 항상 0으로 보이는데도). 그래서 "임프가 아예 생성 안 된다"는
  리포트는 게이트가 100% 막아서가 아니라, 그 좁은 확률 윈도우(35%의 시간만
  통과 가능)에 랜덤 생성 판정 타이밍이 자주 안 맞아서 벌어지는 일이었다.
- **임프 개수 상한(9마리)도 같이 제거**: 사용자가 "9마리 이상 되어도 돼"라고
  확정해서, `immortal.js`의 `MAX_IMP_STOCK` 상수와 그 체크를 통째로 제거했다 -
  이제 생성을 막는 자연스러운 조건은 `findAutoPlaceSlot`이 반환하는 빈 칸이
  없을 때뿐이다(승급 판정은 `countHeroOnField(...) >= target`으로 이미 "이상"
  비교라 9를 넘겨도 정상 작동). 시뮬레이션으로 실제 30마리까지 생성되는 것까지
  확인했다(예전엔 9에서 멈췄음).

**코드는 이번엔 변경하지 않은 부분**: 몬스터 킬레이트/스폰레이트 밸런스 자체(화면에
몬스터가 거의 안 보이는 근본 원인)는 이번 조사에서 건드리지 않았다 - "10라운드
전멸" 요구사항과 충돌하는 트레이드오프라 사용자 판단이 필요한 영역이라고 보고
확인만 요청해둔 상태다. 몬스터 없을 때 임프 생성을 막는 게이트
(`newState.monsterCount <= 0`) 자체도 이번엔 그대로 뒀다 - 위 시뮬레이션이
보여주듯 실제로는 이 게이트가 임프 생성을 100% 막고 있는 게 아니라서, 성급하게
없애기보다는 밸런스 재조정 여부가 먼저 결정된 뒤에 다시 볼 문제로 남겨뒀다.

**검증 방법**: 이전 라운드들과 동일하게 `window.__debug` 훅(`tickWave`,
`tickMamaImps`, `fieldOccupantCount`, `findAutoPlaceSlot`, `placeInstanceAtSlot`,
`createHeroInstance`)을 `main.js`에 임시로 추가해, 실제 게임 로직 함수를 0.5초
간격으로 반복 호출하는 시뮬레이션(자체 재구현이 아니라 진짜 `tickWave`/
`tickMamaImps` 그대로)으로 라운드 1~9 전체를 두 가지 영웅 성장 시나리오(서서히
vs 즉시 30마리)로 각각 돌려서 몬스터 표시값(floored)과 실제 판정값(raw)을
동시에 추적했다. 끝난 뒤 훅은 다시 제거했다(디버그 전용, 커밋에 남기지 않음 -
제거 후 재빌드해서 확인).

## 몬스터 미표시 - 밸런스 대신 표시 반올림 방식 수정

"화면에 몬스터가 아예 안나와 이거 수정해야하는데 어떻게해?"라는 요청을 받았다.
직전 라운드 시뮬레이션으로 이미 근본 원인은 확인해뒀다 - 필드 영웅 수가 많으면
처치 속도(`heroCount×0.06/초`)가 스폰 속도(`40/라운드길이`)를 항상 앞질러서
`state.monsterCount`(실제 판정에 쓰는 원시값)가 계속 0에 가까운 소수로 맴돈다.
이 처치 속도 자체는 몇 라운드 전 "10라운드 보스가 시작 4초 안에 전멸해야 한다"는
요구사항을 만족시키려고 올린 값이라(그 라운드 기록에 "정상 라운드의 몬스터 누적을
거의 0으로 만들 것"이라는 트레이드오프까지 이미 적혀 있었음), 이번에 그 수치
자체를 다시 낮추면 그 요구사항이 깨질 위험이 있어 손대지 않기로 했다.

대신 화면 표시 쪽에서 더 저렴하고 안전한 원인을 찾았다: 몬스터 카운트 표시
(`renderMonsterRow`의 "N / 110" 텍스트, `updateMonsterAnimation`의 장식용 몬스터
스프라이트 개수)가 둘 다 `Math.floor(state.monsterCount)`를 쓰고 있었다 -
`monsterCount`가 0.3이나 0.7처럼 정수 미만인 상태(직전 라운드 시뮬레이션에서 확인한
"화면엔 0으로 보이지만 판정용 원시값은 시간의 65%가 0보다 큰" 그 상태)를 전부
"몬스터가 없다"로 뭉개서 보여주고 있었던 것 - 실제로는 몬스터가 존재하는데(다음
정수 단위를 향해 처치 진행 중인 상태) 화면에서만 안 보이는 순수 표시 버그였다.
`displayMonsterCount(state)` 헬퍼를 새로 만들어 `Math.ceil`로 바꿨다 - 0보다 큰
소수는 최소 1마리로 올려서 보여주고, 정확히 0일 때만(실제로 다 처치했을 때만)
0으로 표시한다. 승패 판정(`monsterCount >= monsterMax`)은 여전히 원시값을 그대로
쓰므로 이 변경과 무관하게 그대로 동작한다 - 순수하게 "존재하는 걸 안 보여주던"
반올림 문제만 고친 것이라 밸런스/난이도에는 영향이 없다.

Playwright로 (a) `monsterCount=0.4`일 때 카운트 텍스트가 "0 / 110"이 아니라
"1 / 110"으로, 장식용 스프라이트도 최소 1개 뜨는지, (b) `monsterCount=0`(정확히
0)일 때는 여전히 "0 / 110"·스프라이트 0개로 표시되는지(회귀 없음), (c) 30마리
꽉 찬 필드를 실제 `tickWave()`로 라운드 1~9 시뮬레이션했을 때 "화면에 몬스터가
보이는(ceil 기준 >0) 비율"이 65%(직전 라운드에 확인한 원시값 기준 65%와 정확히
일치 - 이제 화면 표시가 원시값의 실제 상태를 정직하게 반영한다는 뜻)로 나오는지
확인했다. **주의**: 65%는 "항상 보인다"는 아니다 - 나머지 35%는 실제로
`monsterCount`가 정확히 0인 순간이라 여전히 화면에 안 보이는 게 맞다(존재하지
않는 몬스터를 억지로 보여줄 순 없으므로). 이걸로도 "몬스터가 너무 자주 안
보인다"는 리포트가 다시 오면, 이번엔 표시 문제가 아니라 진짜 밸런스(처치/스폰
속도) 문제라는 뜻이니 그때는 "10라운드 4초 전멸" 요구사항과 다시 맞춰가며 처치
속도 수치 자체를 사용자와 상의해서 조정해야 한다.

## 몬스터 이동 애니메이션 + 좌우 굴 보라색 소용돌이 완전 제거 (렉 개선)

"이거 몬스터 이동이랑 보라색 모션 빼면 렉 좀 줄어드려나? 리펙토링 전에도?"라는
질문에, 추측 대신 먼저 실측했다 - CDP `Emulation.setCPUThrottlingRate(4)`로
중저가 모바일 기기를 근사하고, 30마리 풀 필드+몬스터 60마리+디버프 활성 상태로
`requestAnimationFrame` 프레임 시간을 6초씩 측정해서 "몬스터 이동 애니메이션
(`.stage-monster`, `@keyframes monster-travel`)"과 "좌우 굴 보라색 소용돌이
(`.hole-vortex`, `hole-vortex-pulse`/`hole-vortex-spin`)"만 CSS `!important`
오버라이드로 무력화한 전/후를 대조했다 - 평균 프레임 시간이 169.8ms → 80.2ms로
52.8% 줄었다(체감 프레임레이트 약 2배). 이 두 효과는 게임플레이에 영향이 전혀
없는 순수 장식(몬스터 존재 자체는 상단 카운트 바 "N / 110"으로 이미 확인
가능하고, 좌우 굴 위치는 카운트다운 배지로 이미 표시됨)이라 "완전히 없애"라는
사용자 지시를 받아 실제로 제거했다.

**원인**: 몬스터 이동은 화면에 표시되는 몬스터 수(`Math.ceil(monsterCount)`,
최근 라운드에 floor→ceil로 바꾸면서 표시 개수 자체가 늘어난 참이었음)만큼
스프라이트 `<div>`가 각각 무한 반복 애니메이션을 돌리는데, 0.2초마다 전체 DOM이
재생성되면서 매번 `animation-delay`를 실제 경과 시간 기준으로 다시 계산해 넣어야
했다(끊김 방지 - CLAUDE.md 반복 패턴). 좌우 굴 소용돌이는 `conic-gradient` 회전 +
`blur` 필터가 걸린 애니메이션 2개가 게임 진행 중 항상 켜져 있었는데, conic-gradient
회전은 브라우저가 매 프레임 다시 페인트해야 하는 costly한 효과다. 이 두 비용은
전부 페인트/컴포지트 스레드에서 발생해서, 이전 라운드에 CDP JS Profiler로 잡았던
`(program)`/`setAttribute` 등 순수 JS 실행 시간 프로파일에는 전혀 안 잡혔다 -
`requestAnimationFrame` 프레임 시간(JS+스타일+레이아웃+페인트+컴포지트 전체를
포함하는 종단간 지표)으로 측정해야만 보이는 종류의 비용이었다는 게 이번에 새로
확인된 교훈.

**구현**: `GameScreen.js`에서 `updateMonsterAnimation()`/`ui.monsters` 상태/
`MONSTER_TRAVEL_MS`/`renderStage()`의 몬스터 스프라이트 루프를 전부 삭제하고,
`renderHoleEffects()`에서 `.hole-vortex` 두 개를 제거하되 별개 기능인 라운드 종료
카운트다운 배지(`.hole-countdown`)는 그대로 남겼다(같은 `nodes` 배열 안에 같이
있었어서 헷갈리지 않게 분리). CSS도 `.stage-monster`/`@keyframes monster-travel`/
`.hole-vortex`(및 `::before`/`::after`)/`@keyframes hole-vortex-pulse`/
`hole-vortex-spin`을 전부 삭제(`.hole-effects`/`.hole-countdown`은 유지 - 카운트다운
배지가 여전히 그 컨테이너를 씀). 죽은 `ui.monsterSpawnWave`/`monsterSpawnedCount`
필드(이미 예전부터 어디서도 안 읽히던 죽은 코드)도 같이 정리했다.

**검증**: 실제 제거 후(CSS 오버라이드가 아니라 진짜 코드/CSS 삭제) 같은 4배
스로틀 조건으로 재측정하니 평균 프레임 시간이 52.8ms까지 더 내려갔다(오버라이드
테스트의 80.2ms보다도 빠름 - DOM 노드 자체가 줄어든 효과까지 더해진 것으로
추정). Playwright로 `.stage-monster`/`.hole-vortex`가 DOM에 전혀 없는 것, 몬스터
카운트 텍스트는 여전히 정상 표시되는 것(존재 정보 손실 없음), 라운드 종료
카운트다운 배지는 회귀 없이 그대로 동작하는 것까지 확인했다.

## 캐릭터 공격 모션(위아래 스쿼시-스트레치) 완전 제거 (렉 추가 개선)

몬스터 이동/보라색 소용돌이를 없앤 직후 "이거 보니까 위아래로 움직이는 모션도
빼면 렉 덜걸릴듯"이라는 관찰을 받았다 - `.stage-hero-image`에 걸려있던
`stage-hero-attack`(위아래로 살짝 늘어났다 줄어드는 스쿼시-스트레치, 900ms
무한 반복) 얘기다. 이번에도 추측 대신 같은 방식(CDP 4배 CPU 스로틀 + 30마리
풀 필드, `requestAnimationFrame` 프레임 시간 실측)으로 먼저 확인했다 - CSS
`!important` 오버라이드로 이 애니메이션만 무력화하니 평균 프레임 시간이
57.0ms → 21.0ms(63.2% 감소)로 나왔다. 몬스터/소용돌이 제거보다도 비율상 더 큰
효과였는데, 이 애니메이션은 필드에 있는 **모든** 캐릭터 토큰(최대 30마리)마다
각각 걸려 있고, 몬스터/소용돌이와 마찬가지로 0.2초마다 DOM이 재생성되면서
`animation-delay`를 실제 경과 시간 기준으로 매번 다시 계산해 넣어야 했기
때문으로 보인다 - 개수가 몬스터(카운트에 따라 가변)나 소용돌이(고정 2개)보다
훨씬 많고 항상 최대치에 가깝다(필드가 꽉 찰수록 정확히 그만큼 늘어남).

순수 연출용(데미지 계산 등 실질 효과 없음)이라 이전 두 효과와 같은 기준으로
완전히 제거하기로 했다. `GameScreen.js`에서 `ATTACK_CYCLE_MS`/`hashPhase()`
(이 애니메이션에만 쓰이던 위상 분산 헬퍼, 다른 곳에서 안 쓰여서 같이 삭제)와
`imgStyle`의 `animation-delay` 계산 부분을 제거하고, `main.css`에서
`.stage-hero-image`의 `animation: stage-hero-attack ...` 선언과
`@keyframes stage-hero-attack`을 삭제했다. 실제 제거 후(CSS 오버라이드가 아니라
진짜 코드 삭제) 같은 조건으로 재측정하니 27.6ms까지 더 내려갔다(오버라이드
테스트의 21.0ms보다 살짝 높게 나왔는데, 이번엔 필드에 몬스터 20마리도 같이 떠
있는 상태로 쟀기 때문 - 몬스터/소용돌이 제거 검증 때와 동일한 패턴). Playwright로
`.stage-hero-image`의 `getComputedStyle().animationName`이 `'none'`인지(완전히
제거됐는지), 이미지 자체는 여전히 정상 렌더링되는지(회귀 없음) 확인했다.

**이 세 라운드(몬스터 이동/보라색 소용돌이/공격 모션)를 관통하는 패턴**: 전부
"필드/카운트에 비례해 개수가 늘어나는 무한 반복 CSS 애니메이션 + 0.2초마다
DOM 재생성"이라는 조합이었다 - 이 프로젝트에서 반복 등장하는 "고정 delay를 쓰면
안 되고 실제 경과 시간을 매번 재계산해야 한다"는 패턴 자체가, 사실은 그 계산과
DOM 재생성 비용이 누적되는 원인이기도 했다는 뜻이다. 앞으로 비슷한 순수 장식용
무한 반복 애니메이션을 추가할 일이 생기면(또는 기존에 놓친 게 더 있는지 찾을
때), 이 조합(요소 개수가 필드 상태에 비례 + infinite animation)에 해당하는지
먼저 확인하고, 필요하면 이번처럼 CDP 프레임 시간 실측으로 비용을 정량화한 뒤
결정할 것.

## 베인 궁극기 발동 간격 고정(17회) + 합성 시 필드 캐릭터 상한 체크 누락 수정

"굳. 이거 베인 지금 11번 왕복했을 때 1번 궁 쓰잖아. 그러지 말고 17번으로 하자"와
"오류 하나 찾았어. 임프 하나를 합성하니까 30마리 꽉 찼는데 합성이 되더라. 합성될
때도 마리수 보고 하게 해줘" 두 가지를 받았다.

1. **베인 궁극기 발동 간격을 랜덤(15~20)에서 고정 17회로**: 기존
   `IMMORTAL_CONDITIONS.m_bane.extra`가 `ultimateThresholdMin:15,
   ultimateThresholdMax:20`(왕복 이동 15~20회마다 매번 랜덤)이었는데, 둘 다
   17로 맞춰서(`min===max`) `immortal.js`의 기존 랜덤 로직(`min +
   Math.floor(Math.random()*(max-min+1))`)이 항상 17만 반환하도록 만들었다 -
   별도 분기 없이 데이터만 바꿔서 고정값으로 만드는 방식(다른 곳에서도 쓰는
   패턴). Playwright로 40회 이동 시뮬레이션(정확히 2번 발동, 각 구간이
   정확히 17칸), 20회 독립 시행 전부 동일한 다음 임계치(34)가 나오는지(랜덤성이
   실제로 없어졌는지) 확인했다.
2. **합성(3마리→상위 등급) 시 필드 캐릭터 상한(30) 체크 누락 수정**: `synthesize()`가
   지금까지 필드 인원 상한을 전혀 확인하지 않고 있었다 - 일반~전설 체인(3마리
   소모→1마리 생성, 항상 -2)은 카운트가 절대 늘어나지 않아 문제가 없었지만,
   **임프 3마리→희귀 1마리** 합성은 다르다: 임프는 `fieldOccupantCount`(인원 30
   상한 판정)에 안 잡히는데 합성 결과인 희귀 등급은 잡히므로, 필드가 이미
   30/30으로 꽉 찬 상태에서 임프를 합성하면 카운트가 31로 넘어가버리는 버그였다
   (사용자가 직접 재현해서 리포트). `synthesize()`에 "합성 후 카운트가 상한을
   넘기면 거부"하는 범용 체크(`consumedCount = heroId===IMP_HERO_ID ? 0 : 3`,
   `fieldOccupantCount - consumedCount + 1 > fieldMaxCapacity`면 `reason:
   'field-full'`로 거부하고 원본 state 그대로 반환 - 재료 소모 없음)를 추가했다.
   Playwright로 (a) 일반 영웅 30마리+임프 3마리인 상태에서 임프 합성 시도 →
   거부되고(`field-full`) 캐릭터 수/임프 모두 그대로 유지되는지, (b) 필드가
   비어있으면 임프 합성이 정상적으로 성공하는지(회귀 없음) 확인했다.

**검증 방법**: 이전 라운드들과 동일하게 `window.__debug` 훅(`recordImmortalEvent`,
`synthesize`)을 `main.js`에 임시로 추가해 Playwright로 검증하고 끝난 뒤 다시
제거했다(디버그 전용, 커밋에 남기지 않음).

## 13라운드 삭제 공격 직전 게이지형 이동불능 강제 발생

"13라운드 삭제 직전에 동그라미로 그 속박 공격하는거 한번 있어야해"라는 요청을
받았다. "직전에"가 정확히 어떤 타이밍/성격을 뜻하는지 애매해서(기존 이동불능은
게임당 정확히 2회 랜덤 스케줄인데 이것과 별개인지, "직전"이 삭제 게이지가 다
차기 전인지 후인지) AskUserQuestion으로 먼저 확인받았다 - (1) 기존 2회 랜덤
스케줄과 별개로 13라운드 전용 강제 발생, (2) 속박이 완전히 끝난 뒤에 삭제가
시작(권장안 그대로 선택됨).

**수학적으로 완전히는 불가능함을 확인**: 게이지형 이동불능은 채움 5초+잠금
20초=총 25초가 걸리는데, 13라운드(삭제 있는 버전에서만 삭제가 발동하는 라운드)
자체가 30초짜리 일반 라운드이고 삭제 공격은 waveTimeLeft<=15부터 게이지가 차서
<=6에 발동한다. 이동불능을 라운드 시작(waveTimeLeft=30) 즉시 발동시켜도 끝나는
시점은 waveTimeLeft≈5로, 삭제가 이미 발동(6)한 뒤에야 끝난다 - 약 1초의 겹침은
30초 라운드에 25초짜리 이벤트를 욱여넣는 이상 수학적으로 피할 수 없다. 이 사실을
파악하고 "완전히 안 겹치게 하려면 불가능하다, 최대한 이른 시점에 트리거해서
겹침을 ~1초로 최소화하는 절충안으로 가겠다"고 판단해 구현했다(사용자에게 사후
보고).

**구현**: `waveEvents.js`의 `onWaveStart()`에서 `state.gameType === 'delete' &&
state.wave === 13`일 때 게이지형(`type:'gauge'`) 이동불능을
`triggerAtTimeLeft: waveDuration(13)`(=30, 즉 라운드 시작 즉시)으로 강제 설정하는
분기를 기존 "게임당 2회 랜덤 스케줄" 체크보다 먼저 두었다 - 13라운드가 그 랜덤
스케줄에 우연히 뽑혀 있었어도 이 강제 버전이 우선 적용된다(같은 슬롯이라 결과적
으로 하나만 남음). "삭제 없는 버전"(`gameType !== 'delete'`)에서는 애초에 13라운드
삭제 자체가 없으므로 이 강제 로직을 적용하지 않고 기존 랜덤 스케줄 로직 그대로
둔다. Playwright로 (a) 12→13라운드 전환 시 게이지형 이동불능이 즉시 `filling`
단계로 강제 설정되는지, (b) 실제로 `active`(잠금)→`done`까지 정상 진행되는지,
(c) 삭제 게이지가 waveTimeLeft≈15에서 정상적으로 차기 시작하는지, (d) 이동불능-
삭제 겹침이 계산대로 약 1초 안팎인지, (e) "삭제 없는 버전"에서는 13라운드에
이 강제 로직이 전혀 적용되지 않는지(회귀 없음) 확인했다.

## 시작 화면 불멸 기본 체크 + 돌파 체크박스 신규(13명) + 베인 궁극기 20회로 재조정

"이거 시작할때 불멸도 미리 다 체크하고 시작하자"(+ 28명 명단)와 "돌파만 별도로
체크하는 걸로 하자"(+ 13명 명단)를 받았다. 시작 화면의 "불멸" 체크박스가 지금은
저장만 되고 어느 게임 로직에도 안 쓰인다는 걸 먼저 확인했고, "돌파"는 마마
(m_mama) 하나만 실제 효과(임프 9→7마리 조건)가 있다는 것도 확인해서, 정확히
무엇을 구현해야 하는지 AskUserQuestion으로 확인받았다 - (1) 불멸 체크는 설정
기본값만 바꾸기(권장 - 게임 로직에 아직 안 씀), (2) 돌파는 13명 카드에 체크박스
UI만 먼저 추가, 효과는 나중에(권장). 작업 도중 "베인 궁극기 20번 굴려야 한 번
쓰게 해달라"는 정정도 받아 직전 라운드에 고정한 17을 20으로 다시 바꿨다.

1. **신화 카드 31개 중 불멸 승급 경로가 있는 28명만 기본 체크**: 신화 등급은
   총 31명인데(`MYTHIC_RAW`), 그중 인디(`m_indy`)/와트(`m_watt`)/로켓츄
   (`m_rocketchu`) 3명은 애초에 `IMMORTAL_CONDITIONS`에 항목이 없어 불멸 승급
   경로 자체가 없다 - 사용자가 준 28명 명단과 정확히 일치했다. `HeroSelectScreen.js`의
   `defaultHeroSettingsMap()`이 `IMMORTAL_CONDITIONS[heroId]` 존재 여부로 나눠서
   28명은 `immortal:true`, 나머지 3명은 기존처럼 `immortal:false`로 초기화한다.
   **처음에 `heroesByTier('mythic')` 31명 전부를 무조건 체크된 상태로 만들었다가
   Playwright 검증에서 "31개 전부 체크됨"이 나와서 사용자가 준 28이라는 숫자와
   안 맞는 걸 알아챘다** - 정확한 명단 개수 대조가 실제로 버그를 잡아준 사례.
2. **"돌파" 체크박스 신규 - 13명 한정, UI/저장만**: `BREAKTHROUGH_ELIGIBLE_IDS`
   (13개 heroId) 집합을 새로 두고, 그 안에 있는 신화 카드에만 "불멸" 체크박스
   아래에 "돌파" 체크박스를 하나 더 그린다(`instance.breakthrough`가 아니라
   시작 설정 `heroSettings[].breakthrough` - 완전히 별개 필드, 기본값은 체크
   안 됨). 지금은 저장만 되고 마마를 포함해 13명 전부 이 체크박스 자체는 아직
   아무 게임 로직도 안 읽는다(마마의 실제 돌파 효과는 여전히 필드에서 개별
   버튼(`cell-quick-extra`, "돌파")으로 켜는 기존 방식 그대로 - 이번 시작 화면
   체크박스와는 독립적인 별개 스위치라는 것을 잊지 말 것, 나중에 헷갈리기 쉽다).
3. **불멸 기본값 변경에 맞춰 프리셋 저장 로직도 같이 고쳐야 했음**: 기존
   `collectHeroSettings()`는 `v.immortal || v.favorite`가 참인 항목만 저장했는데,
   불멸 기본값이 true로 바뀌면서 이 필터를 그대로 두면 "사용자가 명시적으로 체크
   해제한" 항목도 "다른 필드도 다 거짓이니 저장 안 해도 됨"으로 오판해서 저장을
   건너뛰고, 나중에 프리셋을 다시 불러오면 새 기본값(체크됨)으로 되돌아가버리는
   버그가 생길 뻔했다 - 필터를 없애고 28+3명(31명) 전부 항상 저장하도록 바꿨다.
   `applyPreset`/기본 프리셋 자동 로드 로직도 먼저 새 기본값 맵을 만들고 그 위에
   프리셋에 저장된 값을 덮어쓰는 순서로 고쳐서, breakthrough 필드가 없는 예전
   프리셋을 불러와도 자연스럽게 새 기본값(불멸 체크/돌파 미체크)으로 채워지게
   했다. Playwright로 (a) 한 카드의 불멸을 체크 해제 → 프리셋 저장 → 로컬에서만
   다시 체크(저장 안 함) → 프리셋 재로드 시 저장 시점의 체크 해제 상태로 정확히
   복원되는지(기본값으로 안 돌아가는지) 확인해서 이 문제를 검증했다.
4. **베인 궁극기 발동 간격을 17 → 20으로 재조정**: 직전 라운드에 15~20 랜덤을
   고정 17로 확정했었는데, 사용자가 "20번 굴려야 궁 한번 쓰게" 해달라고 다시
   조정을 요청해서 `heroes.js`의 `ultimateThresholdMin/Max`를 20/20으로
   갱신했다(불멸 승급 target=12, "궁극기 사용 횟수"라는 의미 자체는 그대로).

**검증 방법**: `window.__debug` 훅(`createHeroInstance`, `recordImmortalEvent`)을
`main.js`에 임시로 추가해 Playwright로 검증하고 끝난 뒤 다시 제거했다(디버그
전용, 커밋에 남기지 않음). 시작 화면 검증은 실제 체크박스 클릭/프리셋 저장·로드
UI를 Playwright로 그대로 조작해서 확인했다(내부 상태를 직접 주입하지 않음 -
`local`은 HeroSelectScreen 클로저 안에 있어서 디버그 훅으로 노출된 적이 없다).
베인은 이전 라운드와 동일하게 40회 이동 시뮬레이션으로 정확히 20칸 간격인지
확인했다.

## 예전 프리셋이 새 불멸 기본값을 조용히 덮어쓰던 버그 (PR #60 배포 후)

바로 위 라운드에서 "불멸 체크박스 기본값을 전부 켬"으로 바꾼 뒤, 사용자에게
"돌파 대상 13명도 불멸이 체크돼 있는지" 재확인을 요청받아 정적 분석 + 새 브라우저
컨텍스트로 라이브 Playwright 검증까지 해서 "이미 그렇게 되어 있다"고 답했었다.
그런데 사용자가 실제 라이브 화면 스크린샷을 보내며 "선택 안된게 있는데 무슨말이야"
라고 정정했다 - 스크린샷엔 불멸 경로가 아예 없는 3명(인디/와트/로켓츄)뿐 아니라
9명(오크주술사/밤바/마마/개구리왕자/배트맨/베인/아토/로카/채드)이 추가로 안
체크돼 있었다.

**내 이전 검증이 놓친 이유**: 그때 검증은 매번 **완전히 새 브라우저 컨텍스트**
(localStorage 비어있음)로 했다 - 코드 자체의 기본값 로직은 실제로 맞았다(정적
분석도, 빈 상태에서의 라이브 확인도 전부 통과). 하지만 사용자의 실제 브라우저에는
**이 세션에서 "불멸 기본값 켬" 기능이 생기기 전에 저장해둔 "기본값" 프리셋**이
남아있었다 - 그 프리셋을 저장할 당시(즐겨찾기만 켜고 저장했을 가능성이 높음) 그
9명은 `immortal:false`(그때는 그게 기본값이었음)로 같이 저장돼 있었다.

**원인**: `HeroSelectScreen.js`의 `applyPreset()`과 "화면을 열 때 기본값 프리셋
자동 로드" 블록 둘 다, 새 기본값 맵(`defaultHeroSettingsMap()`, 불멸 경로 있는
28명은 `immortal:true`)을 만든 뒤 프리셋에 저장된 `heroSettings`로 각 필드를
덮어쓰는 구조였는데, 이때 `immortal: h.immortal ?? true`처럼 **프리셋에 저장된
immortal 값을 그대로 신뢰**하고 있었다 - 그 예전 프리셋에 박제된 `false`가 매번
화면을 열 때마다(또는 그 프리셋을 다시 고를 때마다) 새로 계산한 기본값 `true`를
도로 덮어써버린 것이다. 코드 로직 자체(기본값 계산)는 틀리지 않았지만, "이미
저장된 오래된 데이터가 새 기본값을 이긴다"는 상호작용을 놓쳤다.

**수정**: `applyPreset()`과 기본값 프리셋 자동 로드 블록 둘 다에서 `immortal`
필드는 프리셋에서 절대 읽지 않도록 바꿨다 - 항상 그 시점에 새로 계산한
`defaultHeroSettingsMap()`의 값(`base.immortal`)만 쓴다. `favorite`/`breakthrough`는
기존처럼 프리셋 값을 그대로 따른다. 이건 몇 라운드 전("즐겨찾기 무료 재소환 폐지"
근처)에 "프리셋에 저장된 명시적 체크 해제가 재로드 후에도 유지돼야 한다"고
일부러 만들어뒀던 설계를 **이 필드 하나에 한해 의도적으로 뒤집는 것**이다 -
안전한 이유는 (1) 불멸 체크박스 값 자체가 지금 어느 게임 로직에도 전혀 쓰이지
않는 순수 저장용 값이라 데이터 손실 리스크가 없고, (2) 사용자가 "선택 안된게
있는데 무슨말이야"까지 반복해서 명확히 지적한 현재 우선순위가 "예외 없이 전부
기본 체크"이기 때문이다. 사용자가 이번 세션에서 직접 체크를 해제하면 그 값은
여전히 정상적으로 저장되고(`collectHeroSettings()`는 필터 없이 항상 전부
저장), 그렇게 새로 저장하는 프리셋부터는 그 해제 상태가 다시 반영된다 - 이번에
막은 건 "프리셋의 immortal 값이 화면을 열 때마다 자동으로 새 기본값을 덮어쓰는"
경로 하나뿐이다.

**교훈**: "기본값을 바꿨다"는 변경은 새 유저/빈 상태 기준으로는 맞아도, 이미
그 기능 이전부터 데이터를 쌓아온 실사용자에게는 옛 데이터가 새 기본값 계산
로직을 우회해서 계속 이기는 경로가 있을 수 있다 - 이번처럼 "프리셋/로컬스토리지
같은 영속 저장소를 거쳐 read-modify-write 하는 코드가 있는지"를 기본값 변경
직후에 반드시 같이 점검할 것. 빈 브라우저 컨텍스트로만 검증하면 이런 종류의
버그는 절대 못 잡는다.

**검증 방법**: Playwright로 예전 프리셋을 정확히 재현하는 시나리오(9명의
heroId에 `immortal:false, favorite:true`가 저장된, `breakthrough` 필드가 아예
없는 구버전 형태의 프리셋을 `localStorage`에 직접 심고 "기본값" 프리셋으로
지정한 뒤 페이지를 새로고침)로 검증했다 - (a) 그 9명이 이제 전부 불멸 체크된
상태로 뜨는지, (b) 프리셋에 저장돼 있던 `favorite:true`는 여전히 정상 반영되는지
(불멸만 무시하고 다른 필드는 그대로 따르는지), (c) 불멸 경로 자체가 없는
3명(인디/와트/로켓츄)은 여전히 미체크인지, (d) 전체 28명이 다 체크되는지 4가지를
전부 확인했다. **디버깅 중 삽질**: 처음 두 번은 테스트 스크립트 자체의 실수로
false-fail이 났다 - `presetStore.js`의 실제 기본값 프리셋 키가
`guildRaidPractice.presets.default.v1`인데 `defaultPreset.v1`으로 잘못 짐작해서
썼던 것(1차), `setDefaultPreset()`이 id를 **raw string 그대로**
`localStorage`에 저장하는데(`JSON.stringify` 없음) 테스트에서
`JSON.stringify('preset_stale_test')`로 따옴표를 씌워 저장해서 `loadPreset()`의
`find(o=>o.id===id)`가 절대 매치 안 되고 조용히 `null`로 빠졌던 것(2차, 이땐 결과가
"기본값(불멸 true, favorite false)"과 우연히 비슷하게 나와서 더 헷갈렸다). 둘 다
실제 소스(`presetStore.js`)를 다시 읽어 정확한 저장 형식을 확인하고서야 잡았다 -
프리셋 스토리지처럼 이미 구현된 직렬화 형식을 테스트에서 흉내낼 땐 추측하지
말고 반드시 실제 구현 코드를 먼저 확인할 것.

## 채드 판매(먹이기)가 대상 없이 시도되면 영구 먹통 - ui.chadSellMode 미해제 버그

"채드한테 먹이를 먹이니까 그 다음에 채드 다시 클릭하면 클릭이 안돼. 판매를 할 수가
없어"라는 리포트를 받았다. 처음엔 드래그 임계치/더블탭 방지 JS/터치 이벤트 타이밍
쪽을 의심해서 `window.__debug` 훅으로 실제 터치 시퀀스를 재현해봤는데(채드+신화
여러 마리를 필드에 두고 판매하기→화살표→먹이기→재선택→재판매까지 `page.touchscreen.tap()`
으로 전부 실제 터치로 재현), 이 경로는 문제없이 잘 작동했다 - 재현이 안 되니 다른
조건을 찾아야 했다.

**원인**: `renderChadArrowLayer()`(`GameScreen.js`)는 두 가지 이유로 `null`을 반환할
수 있다 - (1) `ui.chadSellMode`가 가리키는 채드 개체 자체가 필드에서 사라진 경우
(`!chad`), (2) 먹일 수 있는 신화/불멸 대상이 필드에 하나도 없는 경우
(`targets.length === 0`). 그런데 (1)은 `ui.chadSellMode = null`로 명시적으로 리셋하는
반면, (2)는 그냥 `return null`만 하고 리셋을 안 했다. 한편 `renderCellQuickActions()`는
`ui.chadSellMode`가 켜져 있는 동안 무조건 `null`을 반환해서 퀵액션 패널 전체(판매/합성/
모든 캐릭터별 특수 버튼 포함, 채드 하나에 국한되지 않음)를 숨긴다("화살표 선택 모드
중엔 다른 칸 클릭을 방해하지 않게" 하려는 의도). 그래서 "필드에 마지막 남은 신화 하나를
채드에게 먹인 뒤, 다시 "판매하기"를 눌렀는데 이제 먹일 대상이 하나도 안 남은" 상황이
되면 - 화살표도 안 뜨고(대상 없음), `ui.chadSellMode`는 계속 켜진 채로 남고, 그 결과
`renderCellQuickActions()`가 영구히 `null`만 반환해서 **어떤 칸을 클릭해도 퀵액션
패널이 다시는 뜨지 않는 먹통 상태**가 됐다 - 취소 버튼조차 이 가드에 걸려 같이
사라지므로 스스로 빠져나올 방법이 없다. 사용자가 겪은 정확한 시나리오: 채드 근처에
신화가 정확히 1마리만 있어서 그걸 먹인 뒤, 습관적으로 "판매하기"를 한 번 더 눌렀다가
이 상태에 걸린 것으로 보인다.

**진단 과정에서 배운 것**: 처음 재현 시나리오(신화 2마리를 채드 옆에 두고 하나만
먹인 뒤 재선택)는 통과했는데, 이건 정확히 "타겟이 아직 남아있는" 케이스라 이 버그를
비켜갔다 - 재현 실패를 "터치 타이밍 문제가 아니었다"는 결론으로만 받아들이지 않고,
"먹이는 대상의 개수가 시나리오에 어떤 영향을 주는지"까지 좁혀서 케이스를 다시
설계한 게 실제 버그를 찾은 결정타였다. 사용자의 한 줄짜리 리포트만으로 재현 조건을
단정하지 말고, 코드에서 "이 상태에 들어가는 경로가 여러 개인데 그중 하나만 예외
처리가 빠져 있는" 패턴(이번처럼 두 개의 조기 반환 중 하나만 리셋 로직이 있는 경우)이
있는지 먼저 훑어보는 게 빨랐을 것 - 다음에 비슷한 "특정 재현 조건에서만 먹통"
리포트를 받으면, 관련 상태 플래그를 리셋하는 모든 경로(guard clause/early return)를
나열해서 그중 리셋을 빠뜨린 게 없는지부터 대조해볼 것.

**수정**: `renderChadArrowLayer()`의 `targets.length === 0` 분기에도 `!chad` 케이스와
동일하게 `ui.chadSellMode = null`을 추가해서, 대상이 없는 상태로 진입하면 즉시
정상 상태로 돌아오도록 고쳤다.

**검증 방법**: `window.__debug` 훅(`createHeroInstance`, `findAutoPlaceSlot`,
`placeInstanceAtSlot`, `getState`/`setState`)을 `main.js`에 임시로 추가해 Playwright
`page.touchscreen.tap()`(실제 터치 시뮬레이션)으로 검증했다 - (1) 신화가 정확히
1마리뿐인 채드를 만들어 먹인 뒤 "판매하기"를 다시 눌러(대상 0개) 이 정확한 시나리오를
재현했더니 수정 전엔 그 뒤로 어떤 칸을 클릭해도 퀵액션 패널이 영원히 안 뜨는 것을
확인했고, 수정 후엔 같은 시퀀스에서 "판매하기"를 다시 눌러도 정상적으로 원래 상태로
돌아와 채드를 재선택하면 "판매하기" 버튼이 다시 뜨는 것을 확인했다. (2) 신화가
여러 마리 있는 정상 케이스(회귀 확인)도 다시 돌려서, 먹이기→재선택→재판매하기(남은
대상으로 화살표 다시 뜨는 것)까지 문제없이 이어지는지 확인했다. 검증이 끝난 뒤
`window.__debug` 훅은 `main.js`에서 다시 제거했다(디버그 전용, 커밋에 남기지 않음 -
제거 후 재빌드해서 `__debug` 참조가 번들에 전혀 없는 것까지 확인).

## 채드가 채드를 못 먹던 버그 + 화살표 모드 취소 경로 신규 추가

바로 위 버그를 고친 직후 "이번엔 채드가 채드를 못먹는 문제가 생겼는데?"라는 리포트를
받았다 - 확인해보니 이건 방금 고친 버그와는 무관한, 훨씬 예전("채드 판매 흐름 전면
재설계" 라운드)부터 있던 별개의 버그였다.

1. **채드가 채드(다른 개체)를 못 먹던 버그**: `renderChadArrowLayer()`의 대상 필터가
   `occ.heroId !== 'm_chad' && occ.heroId !== 'i_giga_chad'`로, **heroId 기준**으로
   모든 채드/기가채드 개체를 통째로 제외하고 있었다. 그 라운드의 원래 지시("필드의
   다른 신화/불멸(채드/기가채드 **본인** 제외) 머리 위에 초록 화살표")는 "지금 판매를
   시작한 그 채드 개체 자기 자신"만 빼라는 뜻이었는데, heroId로 걸러버리는 바람에
   필드에 채드가 2마리 이상 있으면 서로를 영원히 못 먹는 상태가 됐다(m_chad는 신화
   등급이라 원래는 유효한 먹이 대상이어야 했음 - `feedMythicToChad()` 자체는 tier만
   보고 heroId 예외가 없어서, UI 필터만 걷어내면 바로 정상 동작했다). `occ.instanceId
   !== chad.instanceId`(자기 자신의 개체 ID만 제외)로 바꿨다. Playwright로 채드
   2마리+신화 1마리를 필드에 두고 (a) 신화를 직접 클릭해도 일반 "판매" 버튼은 안 뜨는지
   (아래 2번 확인사항과 동일선상 - 이 필터 변경이 그 규칙을 깨지 않는지), (b) 채드A가
   판매하기를 누르면 화살표가 정확히 2개(채드B + 신화) 뜨는지, (c) 그중 하나를 실제로
   눌렀을 때 채드B 또는 신화 중 하나가 정확히 1마리만 줄어드는지(진짜로 판매/먹이기가
   되는지) 확인했다.
2. **(재확인, 코드 변경 없음) 신화/불멸을 직접 클릭해도 판매되면 안 됨**: 사용자가
   "신화 클릭했을때 판매되면 안되는거 알지? 채드 판매하기 눌렀을 때만 초록 화살표로
   팔 수 있는거야"라고 재확인을 요청했다 - `renderCellQuickActions()`의 일반 "판매"
   버튼은 `heroDef.tier !== 'mythic' && heroDef.tier !== 'immortal'`일 때만 뜨므로
   신화/불멸은 애초에 대상이 아니었다(1번 수정과도 무관 - 그건 화살표 "대상 목록"
   필터고, 이건 "직접 클릭했을 때 뜨는 판매 버튼" 필터라 서로 다른 코드 경로).
   위 1번 검증의 (a)에서도 실측으로 재확인했다 - 이미 정확히 그렇게 구현돼 있었다.
3. **화살표 모드에서 아무것도 안 팔고 취소하는 경로 신규 추가**: "채드로 판매하기
   눌렀을 때 아무것도 안파는 경우도 있거든 그럴때 밖에 아무거나 클릭했을 때 초록
   화살표 없어지게 해줘"라는 요청을 받고 코드를 다시 보니, `renderCellQuickActions()`
   가 `ui.chadSellMode`가 켜져 있는 동안 통째로 `null`을 반환하는 가드(바로 위 버그
   수정 라운드에서 다룬 것과 같은 가드) 때문에, 화살표 모드에 들어가면 "취소" 버튼
   자체가 안 보여서 **화살표를 강제로 눌러 뭔가 먹이는 것 말고는 이 모드를 빠져나갈
   방법이 아예 없었다**(사용자가 지적하기 전까진 이 UX 사각지대를 놓치고 있었음).
   루트 `pointerdown` 핸들러 맨 앞(팝업 바깥 클릭 처리보다도 먼저)에 "`chadSellMode`가
   켜져 있고 클릭 대상이 `.chad-sell-arrow`가 아니면 그 탭으로 다른 동작(칸 선택 등)을
   같이 수행하지 않고 화살표 모드만 취소하고 끝낸다"는 가드를 추가했다 - 팝업 바깥
   클릭 시 팝업만 닫고 끝내는 기존 패턴과 동일한 구조. Playwright로 (a) 화살표가 뜬
   상태에서 화살표가 아닌 곳(상단 배지)을 탭하면 화살표가 사라지는지, (b) 그 뒤 채드를
   다시 선택하면 정상적으로 "판매하기" 버튼이 다시 뜨는지(먹통 아님, 바로 위 라운드의
   수정과 상호작용 확인), (c) 취소 후 다시 "판매하기"→화살표를 실제로 누르면 여전히
   정상적으로 먹이기가 실행되는지(회귀 확인) 확인했다.

**검증 방법**: 이전 라운드와 동일하게 `window.__debug` 훅을 `main.js`에 임시로
추가해 Playwright `page.touchscreen.tap()`(실제 터치 시뮬레이션)으로 검증하고
끝난 뒤 다시 제거했다(디버그 전용, 커밋에 남기지 않음 - 제거 후 재빌드해서
`__debug` 참조가 번들에 전혀 없는 것까지 확인).

## 개구리왕자 승급을 "변신 먼저, 그 다음 불멸" 2단계로 재구현 - 예전 확인이 틀렸음

"내가 개구리는 수동으로 변신 한번 눌르고 변신 성공하면 그다음 바로 불멸 될 수 있다고
말했는데 왜자꾸 그냥 불멸 시켜?"라는 지적을 받았다. 예전 라운드("사신개구리 2단계
변신 메커니즘 재확인" 섹션 참고)에서 코드를 다시 읽고 "설명과 정확히 일치한다"고
확인했던 게 실제로는 **틀린 확인이었다** - 그때 확인한 건 "35% 확률 판정이 존재하고
실패하면 소멸한다"는 큰 틀만 맞았을 뿐, 정확히는 그 판정이 **왼쪽 즐겨찾기 바의
"승급 가능!" 아이콘 클릭 한 번**에 다 같이 일어나고 있었다 - `IMMORTAL_CONDITIONS.
m_frog_prince.target`이 `null`이라 `isImmortalPromotionReady()`가 개구리왕자
소환 즉시(아무 선행 조건 없이) "승급 가능!"으로 띄웠고, `promotionHandlers.
m_frog_prince`가 그 클릭 시점에 35% 판정+소멸까지 한 번에 처리했다. `FROG_TRANSFORM_
IMAGES`(개구리왕자변신.webp)라는 전용 "변신" 이미지 자산도 있었지만 코드 어디에도
연결이 안 된 죽은 데이터였다 - 즉 "변신"이라는 별도 단계 자체가 아예 존재하지
않았다. AskUserQuestion으로 정확한 구조를 재확인받았다: "개구리왕자가 있어. 변신
버튼을 눌러. 그러면 개구리왕자변신 상태가 돼. 이건 35% 확률이고 실패하면 없어져.
만약 변신에 성공하면 불멸 소환이 가능해져. 그러면 사신개구리를 필드에 소환 가능해."
즉 위험한 RNG(35%, 실패 시 소멸)는 **"변신"이라는 별도의 선행 단계**에서 끝나야
하고, 그 변신에 성공한 뒤에야 비로소 "승급 가능!" 상태가 되며, 그 이후의 실제
사신개구리 전환은 **더 이상 확률 없이 확정**이어야 한다(사용자 설명에 그 단계의
실패 확률이 언급되지 않음 - 위험은 이미 변신 단계에서 다 해소된 것으로 해석).

**구현**: `immortal.js`에 `attemptFrogTransform(state, instanceId)`를 신규
추가했다(`i_death_frog`의 기존 "2차 변신"(`attemptSecondStageEvolution`)과 같은
패턴 - 수동 버튼, 즉시 35% 판정, 실패 시 개체 소멸) - 성공하면 개체를 다른
heroId로 교체하는 게 아니라 같은 `m_frog_prince` 개체에 `instance.frogTransformed
= true` 플래그만 세운다(외형만 바뀌고 등급은 아직 신화 그대로). `promotionHandlers.
m_frog_prince`는 이제 확률 판정을 하지 않고 `instance.frogTransformed`가 true인지만
확인한다(false면 거부) - 위험한 RNG는 이미 변신 단계에서 끝났으므로 여기 도달하면
항상 확정 성공. `isImmortalPromotionReady()`의 스위치에 `m_frog_prince` 전용
case를 추가해(예전엔 `default: return true`로 폴백해서 무조건 준비된 것으로
취급했음) `instance.frogTransformed === true`일 때만 왼쪽 바에 "승급 가능!"
아이콘이 뜨게 했다. `GameScreen.js`에는 `heroDef.id === 'm_frog_prince' &&
!instance.frogTransformed`일 때만 뜨는 전용 "변신(성공 35%)" 버튼을 추가했다(
`i_death_frog`의 "2차 변신" 버튼과 같은 위치/패턴 - `below` 스택). `heroVisual.js`의
`resolveHeroImage()`에도 `m_frog_prince` + `frogTransformed`일 때 `FROG_TRANSFORM_
IMAGES.m_frog_prince`를 보여주는 분기를 추가해서 죽어있던 이미지 자산을 마침내
연결했다(배트맨 모드/아이언미야옹 변신과 같은 기존 패턴 재사용).

**교훈**: "코드를 다시 읽어서 사용자 설명과 일치하는지 확인했다"는 검증이라도,
그 확인이 실제로는 표면적인 항목(확률 수치, 실패 시 소멸 여부)만 맞았을 뿐 **더
세부적인 구조(몇 단계로 나뉘는지, 어느 클릭에 어느 판정이 묶여 있는지)까지는
검증하지 못했을 수 있다** - 이번처럼 나중에 "왜 자꾸 그냥 불멸 시켜?"라는 재지적을
받고 나서야, 그 확인이 "35% 확률과 소멸이라는 결과"만 맞고 "그 판정이 정확히
언제/어떤 버튼에서 일어나는지"는 어긋나 있었다는 걸 알았다. 다음에 비슷하게 "예전에
이미 확인했다"고 답하기 전에는, 언급된 단계 수(이번엔 "변신"과 "불멸" 두 단계)가
실제 코드의 클릭 지점 수와 정확히 일치하는지까지 대조할 것 - 죽어있는 에셋
(`FROG_TRANSFORM_IMAGES`처럼 매핑만 되고 어디서도 안 쓰이는 것)이 있다면 그 자체가
"이 기능이 아직 완성되지 않았을 수 있다"는 강한 신호로 받아들일 것.

**검증 방법**: `window.__debug` 훅(`createHeroInstance`, `findAutoPlaceSlot`,
`placeInstanceAtSlot`, `checkImmortalPromotion`, `isImmortalPromotionReady`,
`attemptFrogTransform`, `getState`/`setState`)을 `main.js`에 임시로 추가해
Playwright로 검증했다 - (1) 변신 전에는 왼쪽 바에 "승급 가능!" 아이콘이 안 뜨고
선택 시 "변신(성공 35%)" 버튼만 뜨는지, (2) `Math.random`을 0으로 고정해 변신을
강제 성공시켜 `instance.frogTransformed=true`가 되고, 개체는 필드에 그대로
남아있고, 이미지가 `m_frog_prince_transform.webp`로 바뀌고, "변신" 버튼은 사라지고
"승급 가능!" 아이콘이 새로 뜨는지, (3) 그 아이콘을 `Math.random`을 0.999(실패
쪽)로 고정한 채로 눌러도 확정 승급되는지(더 이상 RNG가 없다는 걸 반증하는 검증 -
만약 아직도 판정이 남아있었다면 이 값에서 실패했을 것), (4) 원본 개구리왕자는
사라지고 `i_death_frog`로 교체됐는지, (5) 변신 실패 케이스(`Math.random`을 0.99로
고정)에서 개체가 필드에서 완전히 소멸하는지, (6) 회귀 확인으로 `i_death_frog`의
기존 "2차 변신"(50%, `attemptSecondStageEvolution`)이 이번 변경과 무관하게 그대로
동작하는지까지 총 10+2개 항목을 전부 확인했다. 검증이 끝난 뒤 `window.__debug`
훅은 `main.js`에서 다시 제거했다(디버그 전용, 커밋에 남기지 않음 - 제거 후 재빌드
해서 `__debug` 참조가 번들에 전혀 없는 것까지 확인).

## 시작 화면 "돌파" 체크박스가 실제 마마 개체 생성에 반영되지 않던 버그

"지금 돌파를 클릭하고 필드에 들어왔는데 돌파 7마리 적용이 안돼"라는 리포트를 받았다.
"시작 화면 불멸 기본 체크 + 돌파 체크박스 신규(13명)" 라운드 기록에 이미 이 함정이
예고돼 있었다 - 그때 "지금은 저장만 되고... 아무 게임 로직도 안 읽는다(마마의 실제
돌파 효과는 여전히 필드에서 개별 버튼으로 켜는 기존 방식 그대로 - 이번 시작 화면
체크박스와는 독립적인 별개 스위치)"라고 명시적으로 적어뒀었는데, 사용자는 당연히
시작 화면에서 체크한 게 그대로 적용될 거라 기대했다 - 예고된 함정이 실제로 발생한
사례.

**확인**: `state.heroSettings`(시작 화면에서 넘어온 설정)를 실제로 읽는 코드가
어디에도 없다는 걸 grep으로 재확인했다 - `gameState.js`가 `config.heroSettings`를
그대로 저장만 할 뿐, 필드에 영웅 개체를 생성하는 어떤 경로(`craftMythic`,
summon.js의 소환 경로들)도 이 값을 조회하지 않았다. 마마의 `instance.breakthrough`는
오직 필드에서 마마를 선택했을 때 뜨는 "돌파" 버튼(`actions.js`의
`toggleBreakthrough`)으로만 켜지는 완전히 별개의 인스턴스별 필드였다.

**수정**: 신화 등급은 오직 `synthesis.js`의 `craftMythic()`으로만 새로 생성된다
(`synthesize()`는 legendary가 3마리 합성 대상에서 제외돼 있어 - `GameScreen.js`의
"합성" 버튼 조건 `heroDef.tier !== 'legendary'` - mythic까지 올라갈 수 없다) - 이
한 곳만 고치면 신화 생성 경로 전부를 커버한다. `craftMythic()`이 개체를 생성하는
시점에 `newState.heroSettings`에서 해당 heroId의 `breakthrough` 값을 찾아
`createHeroInstance()`의 초기값으로 넘기도록 고쳤다 - 시작 화면에서 마마 돌파를
체크하고 시작했다면, 그 뒤로 조합되는 모든 마마 개체가 처음부터
`instance.breakthrough = true`로 생성되어 임프 9마리가 아니라 7마리부터 승급 후보로
뜬다. Playwright로 (1) 시작 화면에서 마마 돌파 체크박스를 켜고 게임을 시작한 뒤
실제로 재료(사냥꾼/나무/전기로봇)를 채워 `craftMythic('m_mama')`을 호출하면 결과
개체가 `breakthrough:true`로 생성되는지, 그 상태에서 임프를 정확히 7마리만
채워도 `isImmortalPromotionReady`가 true를 반환하는지, (2) 회귀 확인으로
`heroSettings`의 마마 돌파를 꺼둔 채 조합하면 `breakthrough:false`로 생성되고
임프 7마리로는 아직 승급 후보가 아닌지(9마리가 그대로 필요한지) 확인했다.

## 용사 레이 "검" 시스템 신규 구현 (검 부르기/검 제작/초기화 + 확률표)

사용자가 실제 게임 스크린샷과 함께 레이(m_ray)의 전체 메커니즘을 상세히 설명했다 -
신화 단계에선 "검 부르기" 버튼(마나가 찰 때만 무료로 시도 가능)을 눌러 등급별
확률표(일반/희귀/영웅/전설, 등급마다 18~19종 효과)에서 검을 하나 뽑고, 전설 등급을
뽑으면 불멸(용사 레이) 승급 자격이 생긴다. 불멸이 된 뒤에는 "검 제작"(희귀 등급
영웅 1마리 소모)으로 검을 최대 3개까지 누적할 수 있고, "초기화"로 전부 지우고
처음부터 다시 모을 수도 있다. 예전엔 `heroes.js`의 `IMMORTAL_CONDITIONS.m_ray`에
"마나 차면 버튼 클릭 → 전설 1회 획득 시 승급"이라는 큰 틀과 `tickOverrides.m_ray`
(마나 타이머)만 있고, 실제 확률표/버튼/검 보유 상태는 전부 미구현이었다.

**모호했던 두 지점은 AskUserQuestion으로 먼저 확인**: (1) 신화 단계 "검 부르기"도
검이 누적되는지, 매번 교체되는지 → **매번 교체**("최대 3개까지 제작 가능"이라는
문구가 불멸 이후 "검 제작"에만 붙어 있어서 신화 단계는 누적 없이 마지막 결과만
남는 구조로 해석). (2) 뽑힌 검을 어디에 표시할지 → **화면 상단 고정 배너, 단
그 레이를 선택했을 때만 노출**(상시 노출이 아니라 선택 시에만).

**구현**:
- `src/data/raySwords.js`(신규) - 일반/희귀/영웅 3개 등급은 효과 이름 목록이
  동일해서(전설만 "기본 공격 추가 타격"이 하나 더 있음) `buildTier()` 헬퍼로
  공유 이름 배열에 등급별 확률/효과 문구만 매핑, 전설만 19개 전체를 따로 나열했다.
  총 73개 항목을 `RAY_SWORD_TABLE`(하나의 평평한 배열)로 합쳐서, 신화의 "검
  부르기"와 불멸의 "검 제작"이 이 표 하나를 그대로 공유한다(등급 구분 없이
  가중치 그대로 굴림 - `summon.js`의 기존 `weightedRandom()` 재사용). 데미지 계산
  등 실질 전투 효과는 이 게임에 없으므로(다른 영웅들의 강화/버프도 전부 연출용)
  검의 effect 텍스트는 표시 전용이다.
- `src/logic/immortal.js`에 `callRaySword()`(신화, `instance.manaReady`가 true일
  때만 시도 가능 - 기존 마나 타이머가 이미 세팅해주던 값을 그대로 소비, 검을
  `instance.raySwords = [뽑은 검]`으로 교체하고 `manaReady`를 다시 false로 되돌림,
  전설이면 `instance.progress`를 target까지 채워 승급 자격을 준다), `craftRaySword()`
  (불멸, 필드에서 희귀 등급 아무 영웅이나 1마리를 자동으로 찾아 소모하고
  `instance.raySwords`에 push, `RAY_SWORD_CRAFT_MAX`(3) 도달 시 거부), `resetRaySwords()`
  (`instance.raySwords = []`로 초기화, 비용 없음)를 신규 추가했다. m_ray는
  `promotionHandlers`에 별도 항목이 없어 승급 자체는 기존 제네릭 경로(`{ok:true}`)를
  그대로 타므로 손댈 필요가 없었다.
- `GameScreen.js`의 `renderCellQuickActions()`에 신화용 "검 부르기"(마나 없으면
  비활성, 승급 가능 상태가 되면 마마/아이언미야옹과 같은 패턴으로 버튼 자체를
  숨김) / 불멸용 "검 제작(희귀 1) N/3"(3개 도달 또는 희귀 재료 없으면 비활성) +
  "초기화"(검이 0개면 비활성) 버튼을 `below` 스택에 추가했다.
- 화면 상단 고정 배너(`renderRaySwordBanner()`, `.ray-sword-banner`)는
  `selectedInstance(state)`로 지금 선택된 칸이 m_ray/i_hero_ray일 때만 렌더링되고,
  검이 하나도 없으면(신화 단계 첫 소환 직후 등) 아예 안 뜬다. 그림 없이 등급명
  태그 + `[검 이름]` + 효과 문구만 표시하고, 카드 테두리/글자 색을
  `RAY_SWORD_TIER_COLOR`(일반 흰색/희귀 파랑/영웅 보라/전설 노랑, 사용자 지정
  색상)로 등급마다 다르게 칠했다.

**검증 방법**: `window.__debug` 훅(`createHeroInstance`, `findAutoPlaceSlot`,
`placeInstanceAtSlot`, `checkImmortalPromotion`, `isImmortalPromotionReady`,
`callRaySword`, `craftRaySword`, `resetRaySwords`, `getState`/`setState`)을
`main.js`에 임시로 추가해 Playwright `page.touchscreen.tap()`(실제 터치
시뮬레이션)으로 전체 흐름을 20개 항목으로 검증했다 - 마나 없을 때 "검 부르기"
비활성, 검이 없으면 배너가 안 뜸, `Math.random`을 낮은 값으로 고정해 일반 등급을
강제 재현(raySwords가 정확히 1개로 교체, manaReady가 다시 false로 소모, 배너
색상이 흰색), 레이 선택을 해제하면 배너가 사라짐(처음엔 선택 해제 테스트에 쓴
빈 칸이 선택된 칸 바로 아래(below 액션 버튼 스택이 겹쳐 보이는 자리)라 오탐이
났었다 - `document.elementFromPoint`로 실제 클릭이 버튼에 맞고 있었다는 걸 확인하고
멀리 떨어진 빈 칸으로 바꿔서 재검증), `Math.random`을 높은 값으로 고정해 전설
등급을 강제 재현(즉시 승급 자격 발생, "검 부르기" 버튼이 숨겨지고 왼쪽 바에
"승급 가능!" 아이콘 노출), 승급 실행 시 `i_hero_ray`로 교체되고 원본 `m_ray`는
사라짐, 희귀 영웅을 5마리 준비해두고 "검 제작"을 3번 눌러 정확히 3개 누적 +
희귀 영웅 3마리 소모 + 배너에 3개 전부 표시 + 4번째부터 버튼 비활성, "초기화"로
0개로 되돌아가는 것까지 전부 확인했다. 검증이 끝난 뒤 `window.__debug` 훅은
`main.js`에서 다시 제거했다(디버그 전용, 커밋에 남기지 않음 - 제거 후 재빌드해서
`__debug` 참조가 번들에 전혀 없는 것까지 확인).

**남은 트레이드오프**: 배너가 화면 최상단(`top:2%`)에 고정돼 있어서, 검을 3개
다 채운 상태로 레이를 선택하면 배너 높이가 상단 배지(WAVE)/몬스터 카운트 바와
살짝 겹친다 - 이 두 요소는 원래도 화면 최상단에 있어서 배너를 "화면 상단"에
고정하라는 요구와 근본적으로 공간이 겹칠 수밖에 없었다. 레이를 선택했을 때만
잠깐 뜨는 화면이라(다른 팝업들도 화면 일부를 가리는 게 이미 이 프로젝트의 기존
패턴) 일단 이대로 배포하고, 사용자가 문제 삼으면 그때 위치를 다시 조정할 것.

## 용사 레이 쿨타임 고정 40초 + 쿨타임 게이지 추가

바로 위 라운드 직후 두 가지를 추가로 받았다 - "신화 단계일 때는 쿨타임 차면 검부르기
할 수 있고, 쿨타임 시간은 40초로 설정해줘"(예전엔 `IMMORTAL_CONDITIONS.m_ray.
tickIntervalSec`가 `[1, 10]` 랜덤이었다 - 마나가 정확히 언제 찰지 예측 불가능했음),
그리고 진행 중에 끼어든 "쿨타임 바도 보여줘"(캐릭터 위에서 눈으로 확인할 방법이
없었음 - `instance.manaReady` boolean만 있고 시각화가 전혀 안 됨).

**고정 40초**: `tickIntervalSec: [1, 10]` → `tickIntervalSec: 40`으로 데이터만
바꿨다 - `rollValue()`가 배열이 아닌 값은 그대로 반환하는 기존 헬퍼라 코드 변경
없이 데이터 수정만으로 충분했다(다른 여러 영웅의 "고정 간격" 조건들과 동일한
패턴 - 예: 베인 궁극기 발동 간격을 랜덤→고정으로 바꿨을 때도 같은 방식이었음).

**쿨타임 게이지**: 베인의 궁극기 쿨타임 게이지(`renderBaneUltimateGauge`)와 완전히
같은 패턴으로 `renderRayCooldownGauge(occ, centerX, footY, width)`를 추가해
`renderHeroTokenLayer()`의 `m_ray` 개체 렌더링 직후 항상(선택 여부와 무관, 인디
발굴 게이지·베인 게이지와 동일한 상시 표시 원칙) 캐릭터 발밑에 붙인다. 채움
비율은 `instance.immortalTick.elapsed / .nextTick`로 계산하되, **다 찬 뒤
(`manaReady===true`)에는 이 비율 대신 무조건 100%로 고정 표시**해야 한다는 걸
미리 알아챘다 - `tickOverrides.m_ray`의 while 루프가 `manaReady`를 true로 세운
뒤에도 `elapsed -= nextTick; nextTick = rollValue(...)`로 계속 다음 40초 주기를
새로 돌리기 때문에(플레이어가 바로 안 누르고 오래 방치하면), 원시 비율을 그대로
쓰면 이미 다 찬 상태에서도 게이지가 "다시 채워지는 중"처럼 보이는 오해를 낳을
뻔했다(실제로 구현 전에 로직을 읽으며 미리 예상하고 대응함 - 이번엔 검증에서
걸린 게 아니라 설계 단계에서 피함). 승급 후(`i_hero_ray`)는 heroId 자체가
달라지므로 이 게이지 조건에 자연히 안 걸려 사라진다.

**검증 방법**: `window.__debug` 훅(`createHeroInstance`, `findAutoPlaceSlot`,
`placeInstanceAtSlot`, `callRaySword`, `tickImmortalProgress`, `getState`/`setState`)을
`main.js`에 임시로 추가해 Playwright로 검증했다 - (1) 레이를 선택하지 않아도
게이지가 항상 보이는지, (2) `tickImmortalProgress`를 실제로 10초 흘려서
`nextTick`이 정확히 40(더 이상 1~10 랜덤이 아님)이고 게이지가 대략 25%
(10/40)로 차 있는지, (3) 총 40초를 흘리면 `manaReady=true`가 되고 게이지가
정확히 100%로 표시되는지(다음 주기로 넘어가며 원시 비율을 그대로 썼다면
실패했을 검증), (4) 실제로 "검 부르기" 버튼을 눌러 검을 뽑으면 `manaReady`가
다시 false로 소모되고 게이지도 거의 0%로 리셋되는지까지 총 9개 항목을 확인했다.
검증이 끝난 뒤 `window.__debug` 훅은 `main.js`에서 다시 제거했다(디버그 전용,
커밋에 남기지 않음 - 제거 후 재빌드해서 `__debug` 참조가 번들에 전혀 없는 것까지
확인).

## 이동불능(빨간 원) 라운드 고정 스케줄 + 10라운드 보스 즉시 광역 속박

"빨간색 원 그거 수정좀 해줘. 랜덤으로 총 2번 말고, 2,4,7,9에 나오게 하고
10라운드에는 빨간색 없이 보스가 바로 속박 공격 한번 하고(대여섯칸), 11,
13(이미 한거), 17 에 나오게"라는 요청을 받았다. "빨간색 원"은 게이지형
이동불능의 5초짜리 예열 시각효과(`.immobilize-filling`, `--fill-sec` conic-gradient)를
가리킨다 - 즉시형은 예열 단계 자체가 없어서 이 시각효과가 안 뜬다.

**기존 구조**: `createGameState()`가 게임 시작 시 `rollImmobilizeRounds()`로
1~9 중 1회 + 11~19 중 1회를 랜덤으로 뽑아 `bossAttackSchedule.immobilizeRounds`에
저장해두고, 해당 라운드가 되면 즉시형/게이지형을 다시 50:50으로 랜덤 결정하는
구조였다. 13라운드는 별도로 "삭제 공격 직전 게이지형 강제 발생"이라는 완전히
독립된 규칙이 이미 있었다(바로 위 섹션 참고).

**수정**: 랜덤 스케줄을 통째로 없애고 고정 라운드 목록으로 교체했다.
`waveEvents.js`에 `IMMOBILIZE_GAUGE_ROUNDS = [2, 4, 7, 9, 11, 17]`(게이지형이
뜨는 라운드 - 13은 이미 있는 강제 규칙이 처리하므로 목록에서 제외)과
`IMMOBILIZE_BOSS_INSTANT_ROUND = 10`을 새로 두고, `onWaveStart()`의 분기 순서를
13라운드 강제 규칙 → 10라운드 보스 즉시 공격 → 고정 게이지 목록 → 없음, 순으로
정리했다. `gameState.js`의 `rollImmobilizeRounds()`와 `bossAttackSchedule.
immobilizeRounds` 필드는 더 이상 쓰이지 않아 완전히 제거했다(이제 라운드가
고정값이라 게임 시작 시 미리 뽑아둘 무작위 요소 자체가 없어짐).

**10라운드 - 즉시형이지만 대상 칸 수가 다름**: "보스가 바로 속박 공격 한번
하고(대여섯칸)"을 기존 즉시형(원래 1칸만 대상)의 대상 칸 수를 5~6칸으로 늘린
변형으로 구현했다 - `handleImmobilizeEvent()`의 즉시형 분기가 `pickRandomSlots(state,
e.instantCellCount ?? 1)`로 이벤트 객체에 실려온 `instantCellCount`를 우선
쓰도록 고치고(기본값 1이라 다른 즉시형 호출부는 영향 없음 - 애초에 10라운드
전용 강제 이벤트 외엔 즉시형이 나올 라운드 자체가 없어졌으므로 사실상 이 필드는
10라운드에서만 채워짐), `onWaveStart()`가 10라운드 이벤트를 만들 때
`instantCellCount: 5 + Math.floor(Math.random()*2)`(5 또는 6)를 같이 세팅한다.
발동 시점(`triggerAtTimeLeft`)은 게이지형과 같은 방식으로 라운드 내 임의
시점(150초 보스 라운드 전체 범위)에서 랜덤하게 고른다 - "바로"라는 표현은
예열 없이 즉시 잠긴다는 뜻으로 해석했고, 라운드 내 정확한 발동 시각까지
지정된 건 아니라고 판단했다.

**검증 방법**: `window.__debug` 훅(`tick`, `getState`/`setState`)을 `main.js`에
임시로 추가해 Playwright로 검증했다 - 실제 `tick()`을 `waveTimeLeft`만큼씩만
정확히 흘려서(한 번에 정확히 한 라운드씩만 전환되도록) 1~20라운드를 전부
순회하며 매 라운드의 `eventLog.immobilizeEvent`를 기록: (a) 2/4/7/9/11/13/17
라운드는 전부 `type:'gauge'`인지, (b) 10라운드는 `type:'instant'`이면서
`instantCellCount`가 5 또는 6인지, (c) 그 외 모든 라운드(1/3/5/6/8/12/14~16/
18~20)는 이벤트가 전혀 없는지 21개 항목을 확인했고, 추가로 10라운드를 실제
`active`(잠금) 단계까지 진행시켜 DOM에서 `.immobilize-filling`(빨간 원) 요소가
단 하나도 렌더링되지 않는 것도 확인했다. 검증이 끝난 뒤 `window.__debug` 훅은
`main.js`에서 다시 제거했다(디버그 전용, 커밋에 남기지 않음 - 제거 후 재빌드해서
`__debug` 참조가 번들에 전혀 없는 것까지 확인).

## CSS/레이아웃에서 배운 것

1. **배경 이미지 비율 유지는 JS로 실측해서 픽셀로 박아라.** `.game-stage`를
   `aspect-ratio + width:auto + max-width`(또는 `height:100%`) 조합만으로
   맞추려 했더니, 뷰포트 가로세로 비율에 따라 한쪽만 클램프되고 반대쪽은
   그대로 남아 배경이 눌려 보이는 경우가 있었다. 지금은
   `GameScreen.js`의 `sizeStageToFit()`이 `game-stage-wrap`의 실제 크기를
   재서 688:1508 비율을 유지하는 width/height를 인라인 px로 직접 지정하고,
   `window resize`에도 재계산한다. **이 방식을 다시 순수 CSS로 되돌리지 말 것**
   — 같은 버그가 재발한다.
2. **flex row에서 `height:100%; aspect-ratio:X`로 고정폭 형제가 여럿 있으면
   `flex:1` 형제가 폭 0으로 찌그러질 수 있다** (고정폭들의 높이 기준 폭 합이
   행의 실제 너비를 넘으면). 신화/룰렛 버튼을 `width:%` 기준으로 바꿔서 해결함
   (`.mythic-btn-img`, `.roulette-btn-img`).
3. 게임 화면 전체(`#app`의 padding 포함)는 `100dvh`에 맞춰 스크롤 없이 한
   화면에 들어오게 되어 있다 (`.game-screen { height: calc(100dvh - 24px); }`).
4. 재화 바/신화·소환·룰렛·강화 버튼/즐겨찾기 즉시소환 아이콘은 전부 별도
   패널이 아니라 **`.game-stage` 안의 절대좌표 오버레이**로 배경 art 위에
   직접 얹혀 있다. 선택된 영웅 패널(`selected-panel`)도 마찬가지로, 영웅을
   선택했을 때만 보스 위에 카드로 떠서 보이는 오버레이다(상시 노출 아님,
   의도된 설계).
5. (2차 이미지 매핑 때 변경) 목업 HTML이 "라운드/시간 배지"와 "몬스터 카운트 바"까지
   포함한 배경 이미지 위에 전부 절대좌표로 얹는 구조라, `top-bar`/`monster-row`를
   `.game-stage` 바깥의 별도 flex 형제로 두던 걸 버리고 **`.game-stage` 안의
   오버레이**로 합쳤다(`GameScreen.js`의 `renderTopBadge`/`renderMonsterRow`).
   `.game-screen`은 이제 `.game-stage-wrap` 하나만 감싸는 얇은 래퍼다. 룰렛/강화
   팝업도 `position:fixed` 풀스크린 모달이 아니라 `.game-stage` 안에 절대좌표로
   붙는 하단 시트(`.game-popup`)로 바뀌었다 - 미션 팝업/결과 화면만 예전 방식
   (`.popup-overlay`, 풀스크린 고정)을 그대로 씀(목업에 해당 화면이 없어서 굳이
   안 바꿈).

## 기획서를 다시 읽고서야 발견한 버그

**즐겨찾기 "즉시소환" 아이콘**: 처음엔 사전 선택한 즐겨찾기 영웅을 전부
보여주고, 클릭하면 필드에 있는 개체를 (0,0)으로 옮기는 걸로 구현했었다.
기획서 원문("카드 내 별표 클릭 → 즐겨찾기로 표시... **조합 완료 시** 좌측
최상단에 소환 버튼 노출")을 다시 읽고서야, 이게 "즐겨찾기 등록 + 이번 판에서
**실제로 조합을 완료한 적 있는** 신화 영웅에만 나타나는, 새로 무료로
즉시소환하는 전용 버튼"이라는 걸 알았다. `state.unlockedInstantSummons`
(craftMythic 성공 시에만 등록) + `instantSummonFavorite()`로 고침. **UI
동작을 짤 때 기획서 문장을 표면적으로만 읽지 말고, 괄호 안 부연설명까지
정확히 볼 것.**

## 아직 안 끝난 것 / 다음에 손볼 만한 것

- **간헐적 튕김(크래시) - 원인 미상, 보류 중**(PR #28 배포 후 리포트): 사용자가
  "게임 하다보면 튕긴다"고 했는데 재현 조건/에러 로그가 없어서 이번 세션에서는
  조사하지 않았다. 다음에 이 얘기가 다시 나오면 재현 스텝이나 브라우저 콘솔
  에러/스크린샷을 먼저 요청할 것 - 막연히 코드만 보고 추측성 수정을 하지 말 것.
- ~~`public/heroes/extra/`의 여분 이미지들~~ — 2차 이미지 매핑 때 해결됨: 새 zip에서
  정식 한글 파일명(에이스배트맨변신투수/타자모드, 아이언미야옹변신1/2, 로켓츄변신)으로
  다시 받아서 정식 반영했고, 정체불명이던 예전 `extra/` 폴더는 삭제했다.
- 인디(`m_indy`)의 "보물 발굴"(5-4) 보물 자체의 전용 아이콘, 초나의 "나아무" 소환
  유닛 이미지는 이번 zip에도 없어서 텍스트로만 표시 중.
- ~~강화 팝업의 epic/legend 카드가 "준비 중" 잠금 카드~~ — "버그 리포트 라운드"
  섹션에서 해결됨: 4칸 전부 `GameState.globalEnhance` 전역 4트랙 진짜 버튼으로
  교체했다.
- `src/logic/immortal.js`: 27개 불멸 승급 조건 중 고유 소모/미니게임 로직이
  실제로 구현된 건 `m_frog_prince`, `m_mama`, `m_ninja`, `m_chona`, `m_gigi`,
  `m_tar`, `m_lancelot` 정도고, 나머지는 "진행도만 목표 도달하면 승급"하는
  제네릭 폴백이다. `IMMORTAL_CONDITIONS`(`heroes.js`)에 데이터는 다 있으니
  필요하면 `promotionHandlers`에 채워 넣으면 됨.
- ~~판매 보상 골드/보석 수치~~ — 아래 "판매 칸 위/아래 버튼" 섹션에서 확정 수치로
  교체됨(일반 30코인/희귀 1행운석/영웅 2행운석/전설 4행운석). 몬스터 누적 곡선,
  강화 비용은 여전히 정식 밸런스 수치가 없어서 플레이스홀더 값 그대로다(코드 내
  주석에 표시돼 있음).

## 베인 궁 30회로/불멸 진행도 표시 누락/강화 비용 재정의/로카 80% 맥스장전/오크 30% 지연 + 랜슬롯 승급 자체가 죽어있던 버그 발견

한 메시지에서 여러 건을 받았다: (0) 베인 궁극기 발동 간격을 20 → 30회 왕복으로,
(1) "필드에 베인이 있으면 불멸 창에 진행 상태가 보여야 하는데 다른 영웅들 중에
안보이는 것들도 있다"는 지적, (2) 강화 비용 재정의(소환 확률 100코인/12맥스,
전설~불멸은 "다음 레벨만큼" 행운석), (3) 로카 장전 80% 확률로 최대치, (4) 오크
주술사 스킬 발동 간격 30% 증가.

1. **베인 궁극기 간격 20 → 30**: `IMMORTAL_CONDITIONS.m_bane.extra.ultimateThresholdMin/Max`를
   30/30으로(이전에도 15~20 랜덤 → 17 고정 → 20 고정을 거쳤던 값 - 이번이 네 번째
   조정, `min===max`로 두면 `recordImmortalEvent`의 기존 랜덤 로직이 항상 그 값만
   반환하는 패턴 재사용).
2. **불멸 탭 진행도 "안 보이는" 영웅들 조사** - `immortalProgressText`(GameScreen.js)를
   다시 훑어서, `cond.target`이 있고 어딘가에서 `instance.progress`가 실제로
   증가하는 영웅(베인 포함 대부분)은 이미 정상 표시되고 있음을 먼저 확인했다.
   진짜 문제였던 셋:
   - **마마/개구리왕자**: `cond.target`이 애초에 `null`(진행을 별도 상태로 관리)이라
     `immortalProgressText`가 무조건 `null`을 반환해 항상 "잠김"만 떴다. 마마는
     `countHeroOnField(state, IMP_HERO_ID).count`를 목표(돌파 여부에 따라 9/7)와
     비교해 "임프 N/M"으로, 개구리왕자는 `instance.frogTransformed` 여부로
     "미변신"/"변신 완료"를 보여주도록 특수 분기를 추가했다.
   - **랜슬롯**: `cond.target=3`이 있지만 `instance.progress`를 어디서도 증가시키지
     않는 구조(승급 판정 자체가 "10강 달성 개체 수"를 필드에서 그때그때 세는
     방식)라 항상 "0/3"만 뜨고 실제 진행을 전혀 반영 못 했다 - 판정과 같은 계산식
     (`enhanceLevel >= 10인 개체 수`)을 그대로 재사용해서 보여주게 했다.
   - 같은 김에 `immortalUnlockStatus`(잠김/해금 배지)도 재검사했다 - `cond.target
     == null`이면 무조건 "해금"으로 잘못 취급하고 있어서, 마마/개구리왕자는 진행
     상태와 무관하게(임프 0마리든 변신 전이든) 필드에 올라오자마자 항상 "해금"이
     떴었다. 이미 정확한 판정 로직을 가진 `isImmortalPromotionReady()`를 그대로
     재사용하도록 통째로 바꿨다(자체 재구현 대신 기존 진실 소스에 위임 - 로직이
     두 곳에 따로 있으면 이번처럼 한쪽만 고쳐지고 다른 쪽은 계속 틀리는 문제가
     반복된다).
   - **조사 중 발견한 별개의 심각한 버그**: `isEligible()`(immortal.js)이 랜슬롯도
     제네릭 `progress >= target` 분기를 타고 있었는데, 랜슬롯은 progress가 절대
     안 올라가니 **승급 자격 자체가 영원히 안 생기는** 상태였다(왼쪽 바에는
     "승급 가능!" 아이콘이 이미 떠 있었는데 - 그건 `isImmortalPromotionReady`의
     별도 case로 정확히 판정하고 있었으므로 - 정작 눌러서 `checkImmortalPromotion`을
     타면 이 `isEligible()` 게이트에 막혀 매번 조용히 실패했다). m_bamba/m_ray와
     같은 패턴으로 m_lancelot 전용 분기를 추가해서 고쳤다.
   - **그 수정으로 드러난 두 번째 버그(연쇄 발견)**: `isEligible()`을 고치고 나니
     `promotionHandlers.m_lancelot`/`isImmortalPromotionReady`의 `case
     'm_lancelot'` 둘 다 `cond.extra.maxEnhance`(옵셔널 체이닝 없이)를 읽고
     있었는데, `IMMORTAL_CONDITIONS.m_lancelot`에는 애초에 `extra` 필드 자체가
     없어서(`undefined.maxEnhance`) **즉시 크래시**했다 - 이 코드는 `isEligible`이
     항상 false를 반환하던 예전엔 도달할 일이 없던 죽은 경로라 지금까지 한 번도
     발견되지 않았다. `cond.extra?.maxEnhance ?? 10`으로 옵셔널 체이닝을 추가해
     고쳤다.
   - **세 번째 버그(가장 심각): 랜슬롯은 승급해도 필드에서 그냥 사라졌다** -
     `promotionHandlers.m_lancelot`이 "10강 달성 3마리"를 소모할 때 **자기 자신을
     제외하지 않고** target(3)마리를 전부(자기 자신 포함) 필드에서 지우고 있었다.
     `checkImmortalPromotion`은 이 핸들러가 끝난 뒤 원래 자신의 슬롯에서 자신의
     instanceId를 찾아 불멸 개체로 바꿔치기하는데, 이미 그 슬롯에서 자기 자신이
     지워진 뒤라 `findIndex`가 -1을 반환해 아무 일도 안 일어난다 - `promoted:
     true`는 반환되지만 실제로는 재료(3마리)만 사라지고 불멸은 생성되지 않는
     조용한 실패였다. m_ninja/m_gigi와 같은 패턴(`if (ref.instance.instanceId ===
     instance.instanceId) continue;`)으로 자기 자신은 소모 대상에서 빼고
     target-1(2)마리만 제거하도록 고쳤다. **이 세 버그는 전부 "isEligible이 항상
     false라 랜슬롯 승급 경로 자체가 한 번도 실행된 적 없었다"는 같은 뿌리에서
     나온 연쇄였다** - 진입 조건 하나를 고쳤더니 그 안쪽에 숨어있던 두 개의 별개
     버그가 순서대로 드러난 사례.
3. **강화 비용 재정의**: `GLOBAL_ENHANCE_MAX_LEVEL.rate`를 11 → 12(사용자 지정 -
   "12가 맥스였어. 12되면 맥스라고 표시하고 비활성화"). 소환 확률 트랙 자체의
   골드 비용(100코인)은 이미 일치해서 변경 없음. 전설~불멸(legendary) 트랙은
   고정 2행운석이던 걸 "다음 레벨만큼" 행운석이 드는 가변 비용으로 바꿨다(사용자
   지정 - "1→2는 2행운석, 7→8은 8행운석"). `actions.js`에
   `nextGlobalEnhanceCost(state, track)`를 새로 추가해서(배트맨 전용
   `nextEnhanceGoldCost`와 같은 패턴) `state.globalEnhance.legendary + 2`(현재
   표시 레벨+1이 다음 도달 레벨)를 반환하고, `upgradeGlobalEnhance()`와
   `GameScreen.js`의 강화 팝업 렌더 둘 다 이 함수로 통일했다.
4. **로카 장전 80% 확률로 최대치**: 매 장전 틱(10초 간격)마다 80% 확률로 그냥
   최대(incrementPerTick 범위 상한, 5)로 채우고 나머지 20%만 기존처럼 1~5 랜덤을
   굴리도록 `immortal.js`의 `tickOverrides.m_roka`를 고쳤다(`ROKA_MAX_CHARGE_CHANCE
   = 0.8`, 사용자 지정 - "장전이 너무 늦어. 80% 확률로 폭발탄 최대 수로 장전을
   해줘").
5. **오크 주술사 스킬 발동 간격 30% 증가**: `IMMORTAL_CONDITIONS.m_orc_shaman.tickIntervalSec`를
   7 → 9.1(7×1.3)로(사용자 지정 - "오크도 스킬 발동 시간을 좀더 늘려주면 좋겠는데
   지금에서 30% 더 늘려줘").

**검증 방법**: 이전 라운드들과 동일하게 `window.__debug` 훅(`tickImmortalProgress`,
`checkImmortalPromotion`, `isImmortalPromotionReady`, `recordImmortalEvent`,
`upgradeGlobalEnhance`, `nextGlobalEnhanceCost`, `countHeroOnField`,
`createHeroInstance`, `findAutoPlaceSlot`, `placeInstanceAtSlot`, `HEROES_BY_ID`,
`IMMORTAL_CONDITIONS`, `IMP_HERO_ID`)을 `main.js`에 임시로 추가해 Playwright로
검증하고 끝난 뒤 다시 제거했다(디버그 전용, 커밋에 남기지 않음 - 제거 후
재빌드해서 `__debug` 참조가 번들에 전혀 없는 것까지 확인). 실제 게임 로직 함수를
그대로 호출해서: 베인은 29회 이동까지 progress=0, 30회째에 정확히 1이 되는지;
마마 임프 7/9 상태에서 진행도 텍스트가 "임프 7/9"로 뜨는지, 개구리왕자 변신 전/후
텍스트가 "미변신"/"해금"으로 바뀌는지(신화 팝업 불멸 탭을 실제로 열어 DOM
텍스트까지 확인); 10강 랜슬롯 2마리(2/3, 미달)에서는 승급 후보가 아니다가 3마리
(3/3)를 채우면 후보가 되고, 실제로 `checkImmortalPromotion`을 호출하면 크래시 없이
`promoted:true`가 반환되며 필드에서 재료 2마리만 사라지고(자기 자신은 그대로
남아 불멸로 교체) 승급이 실제로 일어나는지; 소환 확률 트랙에 11번 연속 성공시켜
Lv.12에서 정확히 막히는지(`reason:'max-level'`), 전설~불멸 트랙이 레벨 0/6일 때
각각 2/8행운석을 요구하는지; 로카를 10.05초(정확히 1회 장전)씩 400회 독립
시행했을 때 progress 증가폭이 5(최대치)로 나오는 비율이 83%(80% ±3%p, 통계적
오차 범위)로 나오는지; 오크 주술사가 9.0초에는 progress=0이다가 9.2초에는 1이
되는지(9.1초 경계 확인)까지 전부 확인했다.

## 더블탭 확대 근본 원인 발견(좌표가 아니라 노드 정체성 비교였음) + 팝업 중 필드/즐겨찾기바 조작 확장 + 임프로 필드가 꽉 찼을 때 소환 차단

"더블탭했을때 확대되는거 진짜 어떻게 못해?"(이 세션에서만 벌써 네 번째 재발 리포트),
"팝업 떠있을 때 필드 조작 가능하게 해달라 했잖아... 왼쪽에 신화/불멸 조합 뜨는거
클릭도 가능하게 해줘", 그리고 별도로 "임프 포함 필드가 꽉차면 마리수가 남아도
소환이 안되어야해" 세 건을 받았다.

1. **더블탭 확대 - 이번엔 진짜 근본 원인을 찾음**: 지금까지 이 문제를 뷰포트 meta,
   `touch-action` CSS(`body`→`*` 확장), JS `touchend` 방지(전체→배경 전용) 순으로
   여러 번 고쳤다고 기록했었는데 그때마다 "같은 지점(target)을 300ms 안에 두 번
   건드렸는지"를 **DOM 노드 자체(`e.target`)의 동일성**으로 판정하고 있었다 - 이
   게임은 0.2초마다 `root.innerHTML=''`로 전체 DOM을 다시 그리는 구조라(CLAUDE.md에
   반복 등장하는 함정), 필드 위 캐릭터/칸/보스 같은 요소는 화면상 같은 위치라도
   두 번째 탭 시점엔 이미 다른 노드로 교체돼 있는 경우가 흔하다 - 그러면
   `lastBgTouchEnd.target === e.target`이 항상 false가 되어 더블탭 판정 자체가
   무력화된다. "배경(비클릭 영역)" 항목처럼 재렌더가 드문 곳에서만 우연히 통과됐을
   뿐, 정작 캐릭터/칸이 밀집한 필드 위에서는 사실상 거의 항상 실패하고 있었다 -
   사용자가 반복해서 "여전히 확대된다"고 리포트했던 이유였다. **수정**: "같은
   지점"을 DOM 노드가 아니라 **터치 좌표**(`clientX`/`clientY`, ±24px 오차 허용)로
   판정하도록 `main.js`를 고쳤다 - 좌표는 노드가 재생성돼도 그대로라 이 재렌더
   구조와 완전히 무관해진다(사용자가 대안으로 제시한 "플레이 화면을 감싸는 컨테이너
   박스" 아이디어는 검토했지만, 컨테이너를 새로 만들어도 그 안 노드의 정체성 문제
   자체는 그대로 남아 근본 해결이 안 된다고 판단해 채택하지 않았다 - 좌표 비교가
   더 직접적인 해법). Playwright로 (a) 같은 좌표를 250ms 간격(자연 렌더 루프가 최소
   1회 DOM을 갈아엎을 시간)으로 두 번 두드리면 두 번째가 `defaultPrevented:true`가
   되는지(DOM이 바뀌어도 좌표 비교라 안 깨짐 - 핵심 검증), (b) 300ms를 넘긴 세
   번째 탭은 안 막히는지(정상 개별 탭 보존), (c) 버튼은 여전히 이 로직에서
   제외되어 빠른 연타가 안 막히는지(PR #43 회귀 재확인) 전부 확인했다.
2. **팝업 떠있을 때 필드 조작 가능 범위를 룰렛에서 룰렛/강화/신화로 확장**: 예전엔
   "룰렛 팝업이 떠있을 때에도 필드 조작 가능하게"라는 요청을 룰렛에만 좁게
   적용했었다(그때 기록에도 "나중에 강화 팝업도 같은 요청이 오면 조건에 'enhance'만
   추가하면 됨"이라고 미리 적어뒀었음). 이번에 재요청을 받아 `BOTTOM_SHEET_POPUPS
   = ['roulette', 'enhance', 'mythic']`로 넓혔다(미션 팝업은 전체화면 모달이라
   대상 아님 - 필드가 애초에 안 보임). **왼쪽 즉시소환/승급 바(`.favorite-bar`)도
   같은 예외 목록에 추가**했다("왼쪽에 신화/불멸 조합 뜨는거 클릭도 가능하게") -
   안 그러면 그 아이콘을 누르는 순간 루트 `pointerdown` 핸들러가 먼저 팝업을 닫고
   `render()`로 DOM을 통째로 다시 그려버려서, 뒤이어 오는 click 이벤트가 이미 고아가
   된 노드를 향하게 되어 아이콘의 onclick이 실행되지 않는 문제가 있었다(CLAUDE.md의
   "신화 팝업 클릭이 잘 안 먹힘" 섹션과 같은 계열 - pointerdown에서 DOM을 바꾸면 그
   제스처의 나머지 이벤트가 고아 노드를 향한다). Playwright로 강화 팝업을 연 상태에서
   필드 칸을 탭해도 팝업이 안 닫히고, 왼쪽 바의 조합 가능 아이콘을 눌러 실제로
   `craftMythic`이 실행되는지(재료 소모+신화 등장까지 실측), 팝업 바깥(상단 배지)을
   누르면 여전히 정상적으로 닫히는지(회귀 확인) 확인했다.
3. **임프가 물리적으로 필드를 다 채우면 소환/룰렛을 막음**: `fieldOccupantCount()`
   (인원 30 상한 판정)는 마마 임프를 의도적으로 제외하는데(예전 "마마 임프가
   캐릭터 카운트에 잡히던 버그" 수정), 그 부작용으로 임프가 24칸을 전부 3마리씩
   채워버려도(인원수 표시는 여유가 있어 보임) 소환/룰렛 버튼은 계속 활성 상태였다
   - `summonNormal()`이 `findAutoPlaceSlot`에서 자리를 못 찾으면 그냥 아무 것도
   안 하고 조용히 골드만 날리는 버그로 이어졌다(사용자 지적 - "임프 포함 필드가
   꽉차면 마리수가 남아도 소환이 안되어야해"). `state/gameState.js`에
   `isFieldPhysicallyFull(state)`(모든 칸이 더 이상 어떤 영웅도 못 받는 상태인지 -
   신화/불멸 점유 칸이거나 3마리 꽉 찬 칸뿐인지)를 새로 추가해서
   `fieldOccupantCount>=상한` 체크와 나란히 `summonNormal`/`summonRoulette`
   (둘 다 `logic/summon.js`)과 소환/룰렛 버튼의 `disabled`, 룰렛 클릭 핸들러의
   사전 가드에 전부 추가했다. **일반 플레이에서 오탐이 안 나는 이유**: 24칸×3마리
   =72자리인데 인원 상한(30)을 절대 못 넘으니 "모든 칸이 3마리로 꽉 참" 상태는
   30명 이하로는 도달 불가능하다 - 임프처럼 상한 밖에서 한도 없이 계속 쌓이는
   경우에만 실제로 걸린다. Playwright로 (a) 24칸을 임프 72마리로 가득 채운 상태에서
   `summonNormal()`을 호출하면 `reason:'field-full'`로 거부되고 골드가 그대로인지,
   (b) 필드가 비어있으면 정상적으로 소환되는지(회귀 확인) 확인했다.

**검증 방법**: 이전 라운드들과 동일하게 `window.__debug` 훅을 `main.js`에 임시로
추가해 Playwright(`page.touchscreen`/직접 `TouchEvent`/`PointerEvent` 디스패치)로
검증하고 끝난 뒤 다시 제거했다(디버그 전용, 커밋에 남기지 않음 - 제거 후 재빌드해서
`__debug` 참조가 번들에 전혀 없는 것까지 확인). **주의**: 이번엔 `main.js`
자체에 실제 배포될 더블탭 좌표비교 수정이 같이 들어있었어서, 검증이 끝난 뒤
`git checkout -- src/main.js`로 통째로 되돌리면 그 수정까지 같이 날아간다 - 디버그
훅 블록만 정확히 골라 수동으로 제거하고 실제 수정은 그대로 남겼다(앞으로도 같은
파일에 실제 수정과 디버그 훅이 동시에 들어가면 `git checkout`을 쓰지 말고 훅
부분만 수동으로 잘라낼 것).

## 몬스터 처치 골드 30.4로 확정 + 룰렛 실패 위로 보상(환불/하위등급 지급) 신규

"몬스터 처치를 하면 30.4코인을 받는다고 설정해줘. 그리고 룰렛 소환 실패 시 20%
확률로 비용(행운석)을 되돌려주고, 룰렛 실패 시 20% 확률로 해당 등급의 하위
단계 영웅을 지급해줘"를 받았다.

1. **몬스터 처치 골드**: `constants.js`의 `MONSTER_KILL_GOLD`를 30 → 30.4로(사용자
   지정 확정 수치, 소수도 그대로 허용 - 골드 표시는 어차피 `Math.floor()`로 화면에
   찍히므로 누적 연산 자체에는 문제 없음).
2. **룰렛 실패 위로 보상 2종 신규**: "20% 확률로 환불", "20% 확률로 하위 단계
   영웅 지급"을 서로 독립된 별개의 굴림으로 해석했다(문장이 "그리고"로 따로
   나열돼 있고 하나가 다른 하나를 배타적으로 대체한다는 언급이 없었음 - 즉 둘 다
   터질 수도, 둘 다 안 터질 수도 있음). `logic/summon.js`의 `summonRoulette()`
   실패 분기에 `ROULETTE_FAIL_REFUND_CHANCE`/`ROULETTE_FAIL_CONSOLATION_CHANCE`
   (둘 다 0.2)를 추가 - 환불은 그 시도에 쓴 비용(`cost`)을 그대로 돌려주고, 하위
   등급 지급은 `heroes.js`에 새로 추가한 `prevTierOf(tier)`(`nextTierOf`의 역방향
   버전 - `TIERS` 배열에서 한 칸 아래)로 지급할 등급을 정하고 `pickRandomHeroOfTier`
   +`findAutoPlaceSlot`(자리 없으면 성공 경로와 동일하게 `pendingPlacementQueue`에
   보관)으로 실제 영웅을 필드에 놓는다 - 희귀 룰렛(하위가 일반)까지 전부 커버되고
   전설 룰렛 실패 시엔 영웅 등급이 위로로 나온다. `GameScreen.js`의 룰렛 결과
   연출도 손봤다 - 위로로 실제 영웅이 나온 경우엔 해골(`rouletteFailTier`) 대신
   그 영웅 그림을 잠깐 보여주도록(성공 때와 같은 표시 패턴, `rouletteSuccessHero`
   재사용) 분기를 나눴다 - 안 그러면 "실패해서 해골이 떴는데 동시에 캐릭터가
   필드에 나타나는" 앞뒤가 안 맞는 연출이 됐을 것.

**검증 방법**: 이전 라운드들과 동일하게 `window.__debug` 훅(`tickWave`,
`summonRoulette`, `createHeroInstance`, `findAutoPlaceSlot`, `placeInstanceAtSlot`,
`HEROES_BY_ID`)을 `main.js`에 임시로 추가해 Playwright로 검증하고 끝난 뒤 다시
제거했다(디버그 전용, 커밋에 남기지 않음 - 제거 후 재빌드한 번들 해시가 훅 추가
전과 정확히 일치하는 것까지 확인, `main.js`엔 이번에도 실제 배포 코드가 없어
`git checkout`이 아니라 훅 블록만 수동으로 잘라냈다). 몬스터 골드는 처음엔
`wave`만 강제로 1로 세팅하고 `waveTimeLeft`를 그대로 둬서 의도치 않게 라운드
종료 보너스(+6010)와 몬스터 트리클 스폰이 같이 섞여 들어가는 테스트 버그를 먼저
겪었다 - `waveTimeLeft`를 라운드 duration 그대로(elapsed=0, 스폰 기여 0) 맞추고
아주 짧은 deltaSec만 흘려서 라운드 전환 없이 정확히 몬스터 1마리만 죽는 조건을
만든 뒤 골드 증가분이 정확히 30.4인지 확인했다(테스트 자체의 사이드이펙트를
가정 없이 직접 원인 추적한 사례 - 아래 "빌드/확인 방법" 위 다른 라운드들과
같은 패턴). 룰렛은 hero 등급(실패율 70%)으로 500회 반복 시행해서 실패한 350건
중 환불 비율(20.0%)과 위로 영웅 지급 비율(22.0%, 500회 표본 오차 범위)이 각각
20% 근처로 나오는지, 위로로 나온 영웅이 전부 정확히 한 단계 아래(hero→rare)
등급인지 전부 확인했다.

## "이동불능"/"속박" 용어를 "기절"로 전면 교체 + 기절 중 자동 진행(스킬) 정지 신규

"내가 2, 4, 7 뭐 하면서 속박이라 한거 기절로 바꿔줘. 그러면 스킬도 못쓰고
제자리에 그대로 있어야하는거야"를 받았다. 두 가지가 모호해서 AskUserQuestion으로
먼저 확인받았다 - (1) "스킬을 못 쓴다"가 27개 신화 각각의 자동 진행(로카 장전,
오크주술사 저주, 마마 임프 생성 등)과 수동 버튼(검 부르기/발굴/강화 등) 중 어디까지
막는 것인지 → **자동 진행만 멈춤**(수동 버튼은 그대로 유지, 권장안). (2) 용어
교체 범위가 화면에 노출되는 알파벳/텍스트뿐인지 코드 주석/변수명 전반인지 →
**코드 전체(주석/아이콘 alt 텍스트) 다 기절로**(권장안).

1. **용어 교체**: `src/ui/GameScreen.js`, `src/logic/waveEvents.js`,
   `src/styles/main.css`에서 "이동불능"/"속박"이 등장하는 모든 한글 주석과 실제
   화면에 노출되는 사슬 아이콘의 `alt` 텍스트('이동불능' → '기절')를 일괄 교체했다
   (`이동불능(속박)`처럼 두 단어가 겹쳐 쓰인 자리는 "기절(기절)"처럼 중복되지
   않게 먼저 개별 처리한 뒤 나머지를 치환). **CLAUDE.md의 과거 기록(이 문서
   자체)은 건드리지 않았다** - 그건 당시 실제로 오간 대화/작업 내역을 보존하는
   일지라, 지금 용어가 바뀌었다고 소급해서 다시 쓰면 그 시점 기록의 정확성이
   깨진다(세션 시작 지침에도 "실제로 이전에 반영한 버전과 diff해서 확인" 같은
   식으로 이 문서의 사실성을 신뢰하는 전제가 깔려 있음). `src/data/assets.js`의
   `immobilizeIcon: ... // "속박.png"` 주석도 그대로 뒀다 - 이건 게임 개념 설명이
   아니라 원본 zip 파일의 실제 파일명을 기록한 것이라 사실 자체가 바뀌지 않는다.
   함수/변수/CSS 클래스명(`isImmobilized`, `.immobilize-active`,
   `.stage-immobilize-mark` 등, 전부 영어)은 그대로 남겨뒀다 - 질문에서 확인받은
   범위가 "한글 용어(주석/alt 텍스트)"였고, 내부 식별자까지 바꾸는 건 회귀
   리스크만 키우고 사용자에게 보이는 이득이 없다고 판단했다.
2. **기절 중 자동 진행 정지 신규 구현**: `waveEvents.js`에 `isInstanceStunned(state,
   instanceId)`를 새로 추가했다 - 기존 `GameScreen.js`의 `isImmobilized(state, slot)`
   (칸 단위 판정, `eventLog.immobilizeEvent.targetInstanceIds` 기반)에서 개체 단위
   판정 로직만 뽑아내 로직 레이어(waveEvents.js)로 옮기고, `GameScreen.js`는 이제
   각 occupant에 대해 이 함수를 호출하는 방식으로 재구현했다(칸 안의 개체 중
   하나라도 기절 중이면 그 칸이 기절 상태로 보이는 기존 동작은 동일). 이렇게 UI와
   로직이 같은 판정 함수를 공유해야, 나중에 판정 조건이 바뀌어도 두 곳이 따로
   어긋나지 않는다. `immortal.js`의 `tickImmortalProgress()`(27개 신화의 자동
   진행 전체를 갱신하는 함수)가 각 개체를 처리하기 직전에
   `isInstanceStunned(newState, instance.instanceId)`를 확인해서, 기절 중이면
   `tickOverrides`/`applyGenericTick` 호출 자체를 건너뛴다(진행도가 그대로
   멈춤 - 시간을 몰아서 나중에 따라잡지 않도록 elapsed 누적도 같이 멈춘다).
   `tickMamaImps()`(마마의 임프 생성 - 이것도 마마 자신의 "자동 진행"이라 같은
   원칙 적용)에도 마마 본인이 기절 중이면 그 틱을 건너뛰는 가드를 추가했다(몬스터가
   0마리일 때 멈추는 기존 가드와 같은 패턴 - `return`으로 그냥 건너뛰어 elapsed도
   같이 정지시킨다). **수동 버튼은 이 정지 대상이 아니다** - `enhanceHero`/
   `advanceIronMeyaong`/`callRaySword`/`digTreasure` 등 클릭으로 직접 호출되는
   함수들은 손대지 않았다(사용자 지정 - "자동 진행만 멈춤"). 이동 자체는 이미
   예전부터 드래그 차단으로 막혀 있었으니(`endDrag()`의 `immobilized` 체크) 이번엔
   건드릴 필요가 없었다 - "제자리에 그대로 있어야" 요구사항은 이미 충족된 상태였다.

**검증 방법**: 이전 라운드들과 동일하게 `window.__debug` 훅(`tickImmortalProgress`,
`tickMamaImps`, `isInstanceStunned`, `enhanceHero`, `createHeroInstance`,
`findAutoPlaceSlot`, `placeInstanceAtSlot`)을 `main.js`에 임시로 추가해 Playwright로
검증하고 끝난 뒤 다시 제거했다(디버그 전용, 커밋에 남기지 않음 - 제거 후 재빌드한
번들 해시가 훅 추가 전과 정확히 일치하는 것까지 확인). `state.eventLog.immobilizeEvent`를
`{phase:'active', targetInstanceIds:[...]}`로 직접 세팅해 특정 개체를 기절시킨 뒤 -
(1) 로카를 기절시키고 `tickImmortalProgress`를 15초 흘리면 progress/ammo가 그대로
0으로 남는지, 기절 안 시킨 대조군은 정상적으로 5까지 쌓이는지(장전 자체는 정상),
(2) 마마를 기절시키고 `tickMamaImps`를 30초 흘리면 임프가 0마리인지, 대조군은
정상적으로 여러 마리 생성되는지, (3) 기절 중인 배트맨에게 `enhanceHero`를 직접
호출해도 정상적으로 강화가 성공하는지(수동 버튼은 안 막힘을 확인), (4) 기절시킨
칸의 사슬 아이콘 `alt` 텍스트가 실제로 '기절'로 렌더링되는지까지 전부 확인했다.

## 더블탭 확대 재차 재발 - touchend에서 touchstart로 전환

PR #70 배포 직후 "더블탭 안고쳐지네"라는 재지적을 받았고, 사용자가 `html,body {
touch-action:manipulation}`과 `#game {touch-action:manipulation}` 두 CSS
스니펫을 제시하며 "둘 중에 적절해보이는걸로 적용좀 잘해봐"라고 요청했다.

**두 스니펫 다 이미 효과가 없다는 걸 먼저 확인**: `main.css`에 이미 `* {
touch-action:manipulation; }`(모든 요소, PR #38 즈음부터)가 걸려 있어서 `html,body`나
`#game`(이 프로젝트엔 애초에 `#game`이라는 id 자체가 없다 - 실제 루트는
`#app`)처럼 더 좁은 범위로 다시 걸어봐야 이미 걸려있는 전역 규칙보다 넓어질 수
없다 - 아무 효과가 없는 헛수고였을 것이므로 둘 다 적용하지 않았다.

**진짜 원인 후보 - touchend는 너무 늦을 수 있음**: CSS/meta로는 이미 최대한
넓게 막아뒀는데도 재현된다는 건, 이번엔 JS 방어선(`main.js`의 `touchend` 기반
좌표 비교, PR #70에서 노드 동일성 버그를 고친 바로 그 코드) 쪽을 다시 의심해야
했다 - `touchend`는 "손을 뗄 때" 발생하는 이벤트인데, 일부 브라우저는 그보다
이른 시점인 **두 번째 터치가 시작되는 순간(`touchstart`)**에 이미 더블탭-확대
제스처 여부를 확정 짓고 넘어가버려서, `touchend`에서 뒤늦게 `preventDefault()`를
불러도 이미 늦어 취소가 안 될 수 있다. 방어선을 `touchstart`로 옮겨서 최대한
이른 시점에 막도록 고쳤다(좌표 비교/300ms 임계값/버튼·팝업 제외 로직은 그대로,
이벤트 종류와 좌표 소스(`e.changedTouches`→`e.touches`)만 바뀜).

**touchstart로 옮겨도 드래그/선택이 안 깨지는 이유**: Pointer Events 스펙상
`pointerdown`/`pointerup`은 근원이 된 터치 이벤트의 `preventDefault()` 여부와
무관하게 항상 독립적으로 발생한다 - 이 프로젝트의 필드 칸 선택/드래그가 전부
`pointerdown`/`pointerup` 기반이라(`click`이 아님), `touchstart`에서
`preventDefault()`를 불러도 그 로직에는 전혀 영향이 없다. (반면 브라우저가
`touchstart`의 기본 동작이 취소되면 그로부터 합성하는 레거시 `mousedown`→`click`
체인은 취소하므로, 여전히 버튼은 이 방어선에서 제외해야 한다 - 기존 로직 그대로
유지.)

**검증 방법**: `window.__debug` 훅(`createHeroInstance`, `getState`/`setState`)을
`main.js`에 임시로 추가해 Playwright로 검증하고 끝난 뒤 다시 제거했다(디버그
전용, 커밋에 남기지 않음 - 제거 후 재빌드한 번들 해시가 훅 추가 전과 정확히
일치하는 것까지 확인). (1) 합성 `TouchEvent('touchstart')`를 같은 좌표로 250ms
간격(자연 렌더 루프가 DOM을 최소 1회 갈아엎을 시간)을 두고 두 번 쏘면 두 번째가
`defaultPrevented:true`가 되는지, 400ms 넘겨서 쏘면 안 막히는지 확인. (2)
`page.touchscreen.tap()`(실제 터치 시뮬레이션)으로 소환 버튼을 100ms 간격 5연타
했을 때 골드가 정확히 120(20+22+24+26+28, 5회 전부 정상 소환된 값) 깎이는지 -
버튼 제외 로직과 연타 반응성이 이번 전환으로 안 깨졌는지 회귀 확인. (3) 실제
터치로 필드 칸을 한 번 탭했을 때 `.cell-quick-actions`(선택 UI)가 정상적으로
뜨는지 - `touchstart`에서 `preventDefault()`를 호출해도 `pointerdown` 기반 선택
로직이 영향받지 않는다는 것을 실측으로 확인. 전부 기대대로 나왔다. **다만 이번
수정도 실기기 확대 재현이 실제로 사라졌는지까지 보장하지는 못한다** - 헤드리스
Chromium은 실제 모바일 브라우저의 네이티브 더블탭-확대 제스처 자체를 재현하지
않으므로, 이 검증은 JS 로직이 설계한 대로(더 이른 시점에 정확히 판정해서 막는다)
동작함을 확인한 것이지 실기기 재확인을 대체하지 않는다 - 다시 리포트가 오면
사용자에게 어떤 기기/브라우저인지 먼저 물어볼 것.

## 빌드/확인 방법

```bash
npm install
npm run build
npx vite preview --port <아무포트> --strictPort
```
레이아웃 변경할 때마다 Playwright로 최소 2~3개 뷰포트 비율(좁은 폰/넓은
화면/정사각형에 가까운 화면)에서 스크린샷 찍어 직접 눈으로 확인할 것 —
이 프로젝트에서 실제로 뷰포트별로 다르게 깨진 버그가 있었다.
