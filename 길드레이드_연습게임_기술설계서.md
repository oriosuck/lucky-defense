# 기술 설계 문서

기획서(길드레이드_연습게임_기획서.md) 기준. 웹 배포를 전제로 작성.

## 1. 시스템 아키텍처

### 전체 구조

서버, 데이터베이스, 로그인 없이 브라우저에서만 동작하는 정적 단일 페이지 애플리케이션(SPA)이다. 사용자가 링크에 접속하면 정적 파일(HTML/CSS/JS)이 그대로 브라우저에 로드되고, 이후 모든 로직(소환, 배치, 웨이브 타이머, 불멸 승급 진행도 계산 등)이 브라우저 내 자바스크립트로 처리된다. 유일하게 남는 상태는 "프리셋 저장" 데이터이며, 이는 각자의 브라우저 localStorage에만 저장된다(길드원 간 공유 저장소 없음, 기획서 확정 사항).

```
[사용자 브라우저]
  ┌───────────────────────────────────────┐
  │  UI 레이어 (화면 렌더링, 입력 처리)        │
  │    - 영웅 선택 화면 컴포넌트               │
  │    - 게임 화면 컴포넌트                   │
  ├───────────────────────────────────────┤
  │  게임 상태 관리자 (Game State Manager)   │
  │    - 웨이브/타이머/재화/필드 상태          │
  │    - 초당 tick 루프                     │
  ├───────────────────────────────────────┤
  │  로직 모듈                               │
  │    - 소환/룰렛/합성/판매/이동 처리          │
  │    - 웨이브 특수 이벤트(삭제/행동불능) 처리  │
  │    - 불멸 승급 진행도 계산 엔진             │
  │    - 미션 진행 체크                      │
  ├───────────────────────────────────────┤
  │  정적 데이터 모듈 (영웅/유물/미션 마스터)    │
  ├───────────────────────────────────────┤
  │  저장소 어댑터 (localStorage 래퍼)        │
  └───────────────────────────────────────┘
           │
           ▼
   [정적 호스팅 서버] — 최초 1회 파일 로드만 담당, 이후 통신 없음
```

컴포넌트 간 관계: UI 레이어는 게임 상태 관리자의 상태를 구독해 화면을 그리고, 사용자 입력을 로직 모듈로 전달한다. 로직 모듈은 정적 데이터 모듈(영웅 확률표, 조합 재료, 미션 조건 등)을 참조해 계산하고 게임 상태를 갱신한다. 저장소 어댑터는 프리셋 저장/불러오기 요청이 있을 때만 게임 상태 관리자와 상호작용한다.

### 기술 스택

- 언어: JavaScript (ES6+)
- 프레임워크: 없음(Vanilla JS) — 화면이 2개뿐이고 서버 통신이 없어 프레임워크 없이도 상태 관리 부담이 크지 않음. 다만 상태 변경 지점이 많아(웨이브 tick, 다수의 불멸 진행도 타이머 등) 컴포넌트 재렌더링을 최소화하려면 UI 레이어를 상태-뷰 분리 구조로 작성
- 빌드 도구: Vite (개발 서버 + 정적 빌드 산출물 생성)
- 스타일: CSS (별도 프레임워크 불필요)
- 데이터베이스: 없음
- 저장소: 브라우저 localStorage (프리셋 저장용)

## 2. 데이터 설계

### 데이터 모델

관계형 DB가 없으므로 ERD 대신 JS 객체 구조로 정의한다.

**HeroDefinition (정적 마스터 데이터)**
| 필드 | 타입 | 설명 |
|---|---|---|
| id | string | 고유 식별자 |
| name | string | 영웅 이름 |
| tier | enum | 일반/희귀/영웅/전설/신화/불멸 |
| summonSource | string[] | 소환 경로(일반소환/희귀룰렛/영웅룰렛/전설룰렛/합성) |
| synthMaterials | {heroId, count}[] | 신화 등급 조합 재료 (신화 등급에만 존재) |
| immortalCondition | ImmortalCondition | 불멸 승급 조건 (신화 등급에만 존재) |
| baseHeroId | string \| null | 불멸 등급의 원본 신화 영웅 id |

**ImmortalCondition**
| 필드 | 타입 | 설명 |
|---|---|---|
| type | enum | `real-event` \| `time-based` \| `hybrid` |
| target | number | 목표 수치 |
| tickIntervalSec | number \| [min,max] | 시간 기반 증가 주기(고정 또는 랜덤 범위) |
| incrementPerTick | number \| [min,max] | 틱당 증가량 |
| eventType | string \| null | 실제 이벤트 연동 시 어떤 액션(판매/소환/이동/강화 등)을 카운트할지 |
| extra | object | 영웅별 특수 조건(예: 승급 시 재료 소모 개수, 확률 등) |

**FieldSlot**
| 필드 | 타입 | 설명 |
|---|---|---|
| row | number | 0~3 |
| col | number | 0~5 |
| occupants | HeroInstance[] | 일반~전설은 최대 3개(동일 종류), 신화~불멸은 최대 1개 |

**HeroInstance**
| 필드 | 타입 | 설명 |
|---|---|---|
| instanceId | string | 개체 고유 id |
| heroId | string | HeroDefinition 참조 |
| isImmortalPath | boolean | 불멸 진행 추적 대상 여부(신화 등급만 true 가능) |
| progress | number | 불멸 승급 진행도 현재값 |
| enhanceLevel | number | 강화 단계 |
| breakthrough | boolean | "돌파" 여부(현재는 마마 전용) |
| favorite | boolean | 즐겨찾기 여부(영웅 선택 화면에서 전달됨) |

**GameState**
| 필드 | 타입 | 설명 |
|---|---|---|
| wave | number | 0~20 |
| waveTimeLeft | number | 초 |
| monsterCount / monsterMax | number | 누적/최대 |
| gold / luckstone | number | 재화 |
| fieldMaxCapacity | number | 고기 유물 레벨 기준, 상한 30 |
| field | FieldSlot[24] | 전장 그리드 |
| speed | 1 \| 2 | 배속 |
| paused | boolean | 일시정지 여부 |
| gameType | "no-delete" \| "delete" | 게임 종류 |
| relics | {vault, moneygun, luckstone, meat, wallet: number} | 유물 레벨(1~11) |
| immortalPet | boolean | |
| missions | MissionProgress[] | |
| eventLog | WaveEventState | 13/20라운드 삭제, 2/4/5/7/9라운드 행동불능 진행 상태 |

**Preset (localStorage 저장 단위)**
| 필드 | 타입 | 설명 |
|---|---|---|
| id | string | |
| name | string | 사용자 입력 |
| gameType | string | |
| immortalPet | boolean | |
| relics | object | |
| ownedHeroes | {heroId, immortal, favorite}[] | |

### 입출력 데이터 (유효성 검사)

- 유물 레벨: 정수, 1~11 범위 외 값 입력 불가
- 게임 시작 조건: 게임 종류(필수 단일 선택), 불멸 펫 유무(필수 단일 선택), 유물 5종 전부 선택, 보유 영웅 1개 이상 — 하나라도 미충족 시 시작 버튼 비활성화
- 프리셋 이름: 빈 문자열 불가, 최대 길이 제한(예: 20자) 필요
- 필드 배치: 동일 칸에 서로 다른 종류의 영웅을 함께 놓을 수 없음(일반~전설 규칙), 신화~불멸은 칸당 1개 초과 배치 불가
- 저장 데이터 포맷: JSON, localStorage에 문자열로 직렬화하여 저장

## 3. 인터페이스 및 API 명세

### 엔드포인트

서버 통신이 없으므로 해당 없음. 정적 파일(HTML/JS/CSS)을 최초 1회 로드하는 것 외에 네트워크 요청이 발생하지 않는다.

### 함수 정의 (핵심 모듈)

| 함수 | 시그니처(개념) | 반환값 |
|---|---|---|
| summonNormal | `summonNormal(state): {hero, newState}` | 확정된 확률표(일반50/희귀30/영웅15/전설5, 강화 반영)로 뽑은 영웅과 갱신된 상태 |
| summonRoulette | `summonRoulette(state, tier): {success, hero?, newState}` | 룰렛 등급(희귀60%/영웅30%/전설15%)에 따른 성공 여부와 결과 |
| synthesize | `synthesize(state, slotId): {resultHero, newState}` | 동일 영웅 3마리 → 랜덤 상위 등급 1마리 |
| craftMythic | `craftMythic(state, mythicHeroId): {success, newState}` | 조합 재료 보유 확인 후 신화 영웅 생성 |
| sellHero | `sellHero(state, instanceId): {reward, newState}` | 판매 처리, 재화 지급 |
| moveHero | `moveHero(state, fromSlot, toSlot): newState` | 슬롯 간 이동, 탑베인 등 이동 카운트 연동 |
| enhanceHero | `enhanceHero(state, instanceId): newState` | 강화 단계 증가, 관련 미션/불멸 조건 갱신 |
| toggleBreakthrough | `toggleBreakthrough(state, instanceId): newState` | "돌파" 상태 On/Off (현재 마마 전용) |
| tickImmortalProgress | `tickImmortalProgress(state, deltaSec): newState` | 시간 기반 불멸 진행도 전체 갱신(각 영웅 조건별 로직 분기) |
| tickWave | `tickWave(state, deltaSec): newState` | 웨이브 타이머 감소, 라운드 전환, 특수 이벤트 트리거 체크 |
| checkImmortalPromotion | `checkImmortalPromotion(state, instanceId): {eligible, newState}` | 진행도가 목표 도달 시 승급 처리 |
| savePreset / loadPreset / deletePreset | `(state, presetName)` | localStorage 읽기/쓰기 |

## 4. 워크플로우 및 로직

### 시퀀스 (게임 화면 기준, 매초 반복)

1. 게임 루프 tick 발생 (배속에 따라 1초 또는 0.5초 간격)
2. `paused === true`면 아무 것도 하지 않고 종료
3. `tickWave` 호출 — 웨이브 타이머 감소, 0 도달 시 다음 웨이브 전환 및 몬스터 누적 갱신
4. 현재 라운드가 2/4/5/7/9/13/20이면 해당 라운드의 특수 이벤트 상태 갱신(행동불능 게이지, 삭제 게이지 등) 및 발동 조건 체크
5. `tickImmortalProgress` 호출 — 필드에 있는 신화 등급 영웅 전원에 대해 각자의 조건 타입(시간 기반/혼합)에 따라 진행도 갱신
6. 진행도가 목표에 도달한 개체가 있으면 `checkImmortalPromotion` 호출해 승급 처리(재료 소모 필요한 경우 재료 존재 여부 확인 후 처리, 조건 미충족이면 대기)
7. 몬스터 누적 수가 최대치 도달했는지 확인 → 도달 시 게임 실패 처리, 아니면 웨이브 20 완료 여부 확인 → 완료 시 게임 성공 처리
8. 상태 변경분을 UI 레이어에 반영(재렌더링)

버튼 클릭(소환/룰렛/합성/판매/이동/강화/미션 확인)은 위 tick 루프와 별개로 이벤트 발생 즉시 처리되고, 처리 직후 관련 미션 진행 상태와 실제 이벤트 기반 불멸 진행도(예: 판매 카운트, 이동 카운트)를 함께 갱신한다.

### 핵심 알고리즘 (의사코드)

**가중치 랜덤 뽑기 (소환/룰렛 공통)**
```
FUNCTION weightedRandom(항목별 확률 목록):
  누적확률 = 0
  랜덤값 = 0~1 사이 난수
  FOR EACH (항목, 확률) IN 목록:
    누적확률 += 확률
    IF 랜덤값 <= 누적확률:
      RETURN 항목
  RETURN 목록의 마지막 항목  // 부동소수점 오차 대비
```

**합성 (동일 영웅 3마리 → 상위 등급 1마리)**
```
FUNCTION synthesize(슬롯):
  IF 슬롯 내 동일 영웅 개수 < 3:
    실패 반환
  상위등급 후보군 = (현재 등급 바로 위 등급에 속하는 영웅 전체)
  결과영웅 = 상위등급 후보군 중 균등 랜덤 1개 선택
  슬롯에서 재료 3마리 제거, 결과영웅 1마리 추가
  RETURN 결과영웅
```

**불멸 진행도 tick (시간 기반 항목 공통 처리)**
```
FUNCTION tickImmortalProgress(state, deltaSec):
  FOR EACH 신화등급 개체 IN 필드:
    조건 = 개체.heroId의 immortalCondition
    IF 조건.type == "time-based" OR "hybrid":
      경과누적시간 += deltaSec
      WHILE 경과누적시간 >= 다음틱까지시간:
        경과누적시간 -= 다음틱까지시간
        증가량 = 조건.incrementPerTick (고정값 또는 범위 내 랜덤)
        개체.progress += 증가량
        다음틱까지시간 = 조건.tickIntervalSec (고정 또는 범위 내 랜덤 재추첨)
    IF 개체.progress >= 조건.target:
      승급 대기열에 추가
  RETURN 갱신된 state
```

**웨이브 삭제 이벤트 (13/20라운드)**
```
FUNCTION handleDeleteEvent(state, deltaSec):
  IF 현재라운드 == 20 AND 오크주술사 보유수 >= 5:
    RETURN state  // 무효화
  IF 웨이브남은시간 == 15초 시점 도달 AND 이벤트 미시작:
    이벤트 시작, 랜덤 행 또는 열 1개 선택, 대상 6칸 지정, 게이지시작시간 = 현재
  IF 이벤트 진행 중:
    경과 = 게이지시작시간 - 웨이브남은시간
    IF 경과 >= 9초 (즉 웨이브남은시간 <= 6초):
      대상 6칸의 영웅 전원 제거
      이벤트 종료
  RETURN state
```

## 5. 예외 처리

| 시나리오 | 대응 |
|---|---|
| 소환/룰렛 클릭 시 재화 부족 | 버튼 비활성화 또는 클릭 시 "재화 부족" 안내, 상태 변경 없음 |
| 소환/합성 결과가 나왔는데 필드(24칸/인원상한)가 가득 찬 경우 | 일반 소환은 막고 안내 표시(비활성화). 룰렛 소환은 대기 상태로 보관했다가 필드에 자리가 나면 자동으로 배치 |
| 신화 조합 시 재료 부족 | 조합 버튼 비활성화, 부족한 재료 안내 |
| 프리셋 저장 시 이름 미입력 또는 중복 이름 | 저장 막고 안내 문구 표시 |
| localStorage 사용 불가(시크릿 모드, 저장공간 초과 등) | 저장 시도 실패를 감지해 "이 브라우저에서는 저장이 안 됩니다" 안내, 게임 진행 자체는 계속 가능하게 처리 |
| 불멸 승급 조건은 도달했으나 소모 재료(동종 개체) 부족 | 승급 보류, 조건 충족 상태만 표시하고 재료 확보 시 재시도 |
| 게임 진행 중 브라우저 새로고침/이탈 | 진행 중이던 게임 상태는 저장하지 않음(연습용, 세션 종료 시 소멸) — 프리셋(시작 설정)만 유지됨 |
| 타이머 오차 누적(브라우저 탭 비활성화 시 setInterval 지연) | tick 함수에 실제 경과시간(deltaSec)을 매번 계산해 보정, 고정 1초 가정하지 않음 |

## 6. 디렉토리 및 인프라

### 디렉토리 구조

```
guild-raid-practice/
├── index.html
├── src/
│   ├── main.js                 # 앱 진입점
│   ├── state/
│   │   ├── gameState.js        # GameState 정의 및 갱신 함수
│   │   └── presetStore.js      # localStorage 어댑터
│   ├── data/
│   │   ├── heroes.js           # HeroDefinition 전체 목록
│   │   ├── relics.js
│   │   └── missions.js
│   ├── logic/
│   │   ├── summon.js           # 소환/룰렛
│   │   ├── synthesis.js        # 합성/조합
│   │   ├── immortal.js         # 불멸 승급 진행도 엔진
│   │   ├── waveEvents.js       # 13/20라운드 삭제, 행동불능 이벤트
│   │   └── missions.js
│   ├── ui/
│   │   ├── HeroSelectScreen.js
│   │   ├── GameScreen.js
│   │   └── components/
│   └── styles/
│       └── main.css
├── public/
│   └── (아이콘 등 정적 자산)
├── vite.config.js
├── package.json
└── README.md
```

### 배포 환경

- 호스팅: 정적 호스팅 서비스(예: Vercel, Netlify, GitHub Pages 중 택1) — 별도 서버/DB 운영 비용 없이 `vite build` 산출물(`dist/`)을 그대로 배포
- CI/CD: 저장소에 push하면 호스팅 플랫폼이 자동으로 빌드·배포(플랫폼 기본 제공 기능 사용, 별도 파이프라인 구축 불필요)
- 환경 변수: 없음 (서버 통신/API 키가 없으므로 별도 환경 변수 불필요)
- 접근 방식: 배포된 URL을 길드원에게 공유 → 각자 접속해 개별 브라우저에서 플레이 (계정/로그인 없음)

## 7. 확정 완료 (참고)

- 필드가 가득 찬 상태 처리: 일반 소환은 막음, 룰렛 소환 결과는 대기 후 자리 나면 자동 배치
- 불멸 승급 재료 소모 개수(귀신닌자 5마리/만년초나 3마리/마도학자지지 2마리 등): 기획서 명시 수치 그대로 확정
- 진행 중 게임 상태 저장 여부: 저장하지 않음, 매번 새로 시작(프리셋만 유지)
